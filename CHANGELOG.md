# Changelog

Personal fork of super-agent-party, customized for Korean-language desktop use.
Patch notes go here — newest on top.

## [Unreleased]

### Changed (💾 백업·복원을 채팅앱 홈 대시보드로 이동)
- 월드 데이터 백업/복원을 월드게임 채팅창 하단에서 **채팅앱 홈 대시보드**로 이동 — '🌏 월드 펫
  성격·시스템 지시' 카드 옆에 '💾 월드 데이터 백업'·'📥 월드 데이터 복원' 카드 2개 신설.
- world.js: 대화 기록 패널의 백업/복원 푸터 전면 제거(패널은 헤더+본문만).
- vue_methods.js: worldBackup(다운로드)·worldRestore(zip 업로드→복원, $message 알림) 추가.
- 로케일 ko/en/zh에 worldBackup·worldRestore 키 추가.
- 검증: 대시보드 카드 3개 나란히, 백업 클릭→zip 다운로드 확인, 월드 채팅창 푸터 제거, world-smoke 15/15.


### Removed (🧹 채팅창 기억 초기화 버튼 제거)
- 대화 기록 패널 하단의 '기억 초기화'(병아리·강아지) 버튼 2개 제거 + 이제 안 쓰이는 memResetBtn 함수 삭제.
  백업/복원(💾·📥)은 그대로 유지, 푸터 라벨은 '데이터 백업 · 복원'으로 정리.
  (서버 /api/world_chat reset 엔드포인트는 그대로 — UI만 제거.)


### Changed (🧠 계층형 메모리 — 원문 전체 보존 + 비파괴 요약)
- 프롬프트 전송 창 16 → **30개**로 확대(WORLD_CHAT_SEND).
- **비파괴 요약 복원**: 최근 창(30개)을 벗어난 오래된 턴만 요약(summary)에 '덧쌓되', **원문(history)은
  절대 삭제하지 않는다.** summary_upto 지표로 이미 요약한 구간은 재요약 안 함(비용 절감). 미요약 원문이
  KEEP(48)를 넘으면 백그라운드로 접는다. 프롬프트 = 요약 + 요약이 커버 못 하는 원문(갭 없음).
- 결과: 파일엔 모든 턴이 타임스탬프와 함께 무제한 보존 + 펫은 요약으로 오래된 관계도 기억(일기도 재활용).
- 검증: 원문 50개 시드 → 1턴 → history 52 전부 보존·요약 275자 생성·summary_upto 22 전진 확인, world-smoke 15/15.


### Changed (🕰️ 대화 기록 전체 보존 + 타임스탬프)
- 펫 대화 기록(world_chat/{pet}.json)의 각 메시지에 타임스탬프(`t`, ISO 로컬시각) 추가 — 날짜로 훑기 편하게.
  (기존 메시지는 원래 시각을 알 수 없어 새 메시지부터 기록됨.)
- **전체 보존으로 전환**: 120개 초과 시 잘라내던 로직 제거 — 이제 모든 턴을 원문 그대로 무제한 보관.
- **압축(요약) 전면 제거**: 오래된 턴을 롤링 요약으로 접고 원문을 삭제하던 `_world_chat_summarize` 제거.
  프롬프트에는 여전히 최근 WORLD_CHAT_SEND(16)개만 슬라이딩 윈도우로 전송(비용·컨텍스트 한도) — 파일엔 전부 쌓인다.
- LLM 전송 시 `t` 등 부가 키는 빼고 role/content만 보내도록 정리(호환).
- 검증: 실제 채팅 왕복 정상(부가키 오류 없음)·새 메시지에 `t` 기록 확인, world-smoke 15/15.


### Added (🗣️ 곁에서 엿듣기 — 펫끼리 리빙월드식 대화 공유)
- 사적 대화는 여전히 펫별 비공개(world_chat/{pet}.json)지만, 대화할 때 다른 펫이 **근처(≤3.5)** 에
  있으면 그 대화 요지가 친구 기억에 40분간 잠깐 남아, 다음 대화에서 자연스럽게 안다는 듯 언급할 수 있다.
  프롬프트의 `[방금 곁에서 들은 것]` 블록으로 전달(멀리 있으면 못 들으니 모름 — Generative Agents/Inworld 방식).
- 기존 절친 거들기(maybeFriendChime)를 **근접 게이트** — 이름을 콕 집어 부른 게 아니면 곁에 있을 때만
  35%로 반응(달·정거장 등 멀리 있어도 "옆에서 들은 듯" 거들던 비현실 수리).
- 검증: 근접(dist=2) 기록·주입 확인, 원거리(dist=8) 미기록 확인, world-smoke 15/15.


### Changed (💬 독 버튼 제거 + 채팅 화면 로그 영속화)
- 우측 독 📖 서랍에서 💬(대화 기록) 버튼 제거 — 채팅바 ▲ 토글과 중복이라 삭제(서랍 제목도 정리).
  기억 초기화·백업/복원은 대화 기록 패널 하단 푸터에 그대로 있어 ▲로 접근 가능.
- 화면 채팅 로그를 localStorage에 저장(최근 60줄) — 앱을 껐다 켜도 이전 대화가 이어서 보인다.
  기존엔 DOM 메모리뿐이라 새로고침하면 화면 로그가 비었음(펫의 '기억'은 서버 세션 world_chat/{pet}.json에
  따로 유지돼 재실행해도 문맥은 이어짐 — 이번 변경은 '눈에 보이는 스크롤백'만 영속화).
- 검증: 독/서랍에 💬 없음, 전송→localStorage 저장→리로드 복원 3줄 확인, world-smoke 15/15.


### Added (💬 채팅 기록 ▲ 지름길 + 스크롤)
- 채팅 입력창 앞(왼쪽)에 ▲ 삼각형 버튼 추가 — 누르면 게임식 채팅창처럼 대화 기록이 채팅바
  바로 위로 펼쳐진다(▼로 접힘). 기존엔 독 서랍 → 💬를 거쳐야 봤는데 한 번에 열림.
- 대화 기록 패널을 채팅바 위 하단 중앙으로 재배치(입력창 폭에 맞춤). 💬 독 버튼도 같은 패널을
  토글하며 삼각형 방향이 동기화된다(단일 진입점 setChatLog/toggleChatLog).
- 기록 본문에 `min-height:0` 추가 — flex 컬럼 안에서 실제로 스크롤되게(작은 창·긴 대화 대비).
- 검증: 데스크톱·모바일 ▲→펼침(채팅바 위·중앙)·스크롤·글리프 전환·빈 상태 92px 확인, world-smoke 15/15.


### Fixed (🏊 물놀이 갈 때 안 걷고 미끄러지듯 이동하던 문제)
- 물놀이(dip) 접근·입수 중 걷기 모션 없이 한 방향으로 끌려가듯 이동하던 버그 수리. 원인 2가지:
  - **잔여 1회성 모션**: 도착 인사(happy/think)·소환 wave가 재생 중일 때 dip을 시작하면, 엔티티가
    action 브랜치로 빠져 발을 rest로 고정 → 이동해도 걷기 애니가 안 나옴. `startDip`에서 잔여
    `pet.action`을 끊어 해결(→ 물놀이 가는 순간 자연스럽게 걷기 시작).
  - **업데이트 순서**: `updateWander`가 busy 펫의 `walking=false`를 애니메이터 직전에 리셋하는데,
    정작 물놀이 이동(`dipSteer`)은 그 뒤 `updateDips`에서 `walking=true`로 몰아, 애니메이터가 늘
    stale한 false를 봐 walkAmt가 0에 고정됐음. busy 분기에서 `p.dip`인 펫은 walking을 안 끄도록 수정.
- 검증: 헤드리스로 approach·enter 전 구간 이동 프레임 글라이드 0/66, walkAmt 정상 이징 확인.


### Fixed (🏰 모래놀이가 안 끝나던 문제 — 타이머 단축 + 안전망)
- 자율 모래놀이 지속시간 90~180초 → **40~75초**로 단축. 다른 활동(물놀이·과일 등 30~90초)보다
  길어 한 펫이 모래놀이만 독차지하는 것처럼 보이던 문제 해소. leisure dur·balance-sim 동기.
- **안전망**: 조종 중이 아닌 펫이 타이머 없이(`_sandUntil=0`) 모래스팟에 앉아 있으면 스스로
  타이머를 부여받아 일어난다 — 조종으로 앉힌 뒤 조종을 풀면 영영 안 일어나던 엣지 케이스 수리.
- 검증: E2E 짧은 타이머로 exit 정상, updateSandPlay 매 프레임 구동 확인.


### Added (👀 관전 시점 — 조종 없이 펫 따라가기, 자율행동 유지)
- 우측 dock에 **👀 버튼**: 누를 때마다 병아리 → 강아지 → 해제 순환. 카메라만 그 펫을 따라가고
  **ai.state는 안 건드려 자율행동이 그대로 이어진다** (조종 버튼은 ai.state='player'로 만들어
  자율행동을 멈추는 것과 대비 — 사용자 리포트 "조종하면 자율행동 끊김"의 대안).
- 게임 표준 패턴(스펙테이터/팔로우 캠 ↔ 플레이어 컨트롤 분리): 조종은 입력을 뺏고 AI를 멈추고,
  관전은 카메라만 붙는다. possessed와 상호배타(조종 중 👀 누르면 조종 해제 후 관전, possess 시 관전 해제).
- 팔로우캠은 기존 로직 재사용(대상만 possessed→spectating 확장). Esc/✕로도 관전 해제.
- 검증: 헤드리스 순환(병아리·강아지·해제) 확인, 문법 OK.


### Added (🙋 월드 펫 다이얼로그 맨 위에 사용자 이름 칸 — {{user}} 값 직접 입력)
- 🐾 성격 다이얼로그 최상단에 **사용자 이름 입력칸**(memorySettings.userName 바인딩) — 설정 깊은
  곳의 '사용자 이름'을 못 찾겠다는 피드백 반영. 여기 적으면 시스템 지시의 {{user}}가 그 이름으로
  치환된다(현재 Mini). 헤드리스 검증: 칸 최상단 렌더·Mini 프리필, world_chat "알지! Mini잖아. 멍!".
- worldConfig는 비운 상태(현재 상수 = 🐤🐶 고정)로 정리. ⚠️ "이모지 자꾸 바뀜"의 원인은 실행 중인
  구버전 데스크톱 앱이 메모리의 옛 오버라이드를 DB에 덮어쓰는 것 — 앱을 한 번 재시작하면 멈춘다.


### Fixed (🐾 월드 펫 설정 — 다이얼로그 열기만 해도 기본값이 오버라이드로 굳던 버그)
- 증상: {{user}} 토큰화 이후에도 펫이 계속 "주인"이라 부름. 원인은 이전 프리필+자동저장 설계가
  **다이얼로그를 여는 순간 그 시점 기본 텍스트("주인" 하드코딩)를 worldConfig 오버라이드로 저장** →
  이후 서버 상수(토큰) 개선을 가림.
- 편집 버퍼 분리: `worldEdit`(화면 표시·편집) ↔ `worldConfig`(실제 저장되는 오버라이드)를 나누고,
  저장 시 **기본값과 같으면 오버라이드를 비운다**(서버 상수를 계속 추종 → {{user}} 치환 유지).
  GET /api/world_persona는 effective + defaults 둘 다 반환(정규화 비교용).
- 굳어 있던 기존 오버라이드 6종은 비워서 기본값(토큰) 복귀. 검증: 다이얼로그 열고 편집 없이
  저장 트리거 → worldConfig 계속 빈 상태(안 굳음), 프리필 6칸, world_chat에서 userName=Mini →
  "Mini! 삐약!" 이름 호칭.


### Changed (🙋 월드 펫이 사용자를 이름으로 부름 — {{user}} 토큰 규약, 메인 채팅과 통일)
- 월드 펫 프롬프트의 하드코딩 "주인"을 **{{user}} 토큰**으로 교체(세계관·우편 페르소나·행동
  태그·그림일기·요약). 런타임에 [설정 → 사용자 이름](memorySettings.userName)으로 치환 —
  메인 채팅이 이미 쓰는 캐릭터-카드 규약과 동일.
- 이름 미설정(기본 'user'/'User'/공란)이면 "주인"으로 폴백해 기존 톤 유지. 이름이 설정되면
  system_prompt에 "'주인' 대신 '<이름>'(이)라고 이름으로 부른다" 지시를 얹어 자연스러운 호칭.
- 🐾 다이얼로그 편집 박스에서 {{user}}를 직접 써도 치환됨(힌트에 안내 추가, 3개 로케일).
  요약·일기 라벨도 이름 반영("Mini: ...", "[Mini와의 기억]").
- 검증: 이름 해석 유닛('user'/공란→주인, 'Mini'→Mini, 공백 트림), 라이브 world_chat —
  userName=Mini에서 "응! Mini야! 삐약!"로 이름 호칭 확인. 문법 4파일 OK.


### Added (🌏 월드 펫 성격·시스템 지시 편집 UI — 펫 설정 탭 접이식 섹션 + 🐾 바로가기)
- **🐾 홈 대시보드 바로가기 카드**: 설정 탭 깊숙한 접이식이 안 보인다는 피드백 반영 — 홈 화면 빠른 실행 그리드(대화 시작·캐릭터 생성 …)에 🐾 카드를 추가, 클릭하면 전용 다이얼로그(텍스트박스 6종, 열 때 유효 텍스트 프리필)를 연다. (처음엔 우상단 툴바 아이콘으로 넣었다가, 홈 카드로 이동 요청 반영.) 헤드리스 검증(카드 렌더·다이얼로그·프리필 6칸·무에러) 통과.
- 데스크톱 펫 설정 탭에 **"🌏 월드 펫 성격·시스템 지시" 접이식 섹션**(기본 접힘 — 거의 안 바뀌는
  값이라). 편집 텍스트박스 6종: 병아리/강아지 성격 · 세계관 · 대답 규칙 · 행동 태그 스펙 · 우편 답장 성격.
- **단일 소스 유지**: server.py 상수가 기본값 정본. UI는 `settings.worldConfig` 오버라이드에만
  저장하고, 칸을 비우면 상수로 폴백(= 기본값 리셋). world_chat/그림일기/우편이 `_world_persona_*`
  헬퍼로 "오버라이드 or 기본값"을 읽는다.
- 프리필: 섹션을 펼치면 `GET /api/world_persona`가 현재 유효 텍스트를 반환해 빈 박스만 채운다
  (시작 비용 0, 저장된 오버라이드는 안 건드림). autoSaveSettings에 worldConfig 편입, i18n 3개 로케일.
- 검증: 페르소나 로직 유닛 테스트(오버라이드·빈칸리셋·puppy 분기), `GET /api/world_persona`
  read 경로(SENTINEL), Bedrock world_chat end-to-end 실응답 — 전부 통과. 문법 6파일 OK.

### Added (☁️ Amazon Bedrock 연동 — customAnthropic 공급자로 Claude 사용)
- `ClaudeAsOpenAI` 어댑터가 litellm 프로바이더 프리픽스를 존중: 모델명이
  `bedrock/apac.anthropic.claude-…` 형식이면 Bedrock으로 라우팅 (bare 모델명만 Anthropic 기본).
- **키 입력은 기존 UI 그대로**: 모델 공급자(customAnthropic)의 API Key 칸 = Bedrock API 키
  (bearer — litellm이 직접 사용 + AWS_BEARER_TOKEN_BEDROCK env 폴백, IAM 액세스 키 사용자는
  안 건드림), **URL 칸 = AWS 리전**(`ap-northeast-2` 또는 bedrock-runtime 전체 URL 인식).
- Bedrock 모델엔 base_url을 절대 전달하지 않음(SigV4 라우팅 보호 — world_chat의
  api.anthropic.com 기본값이 새지 않게).
- 검증: 리전 파싱(문자열/URL/오탐 없음)·litellm 라우팅 오프라인 테스트 통과.

### Added (🛒 잔교 주문 게시판 — 장터에 없는 날, 내일 아침 페리가 사다 놓는다)
- (피드백 반영) 품목당 상한 3 → **10개** · 패널 제목 줄에 **✕ 닫기 버튼**(주문 후 상태
  화면에서도 닫힌다 — 종전엔 게시판 재클릭만 가능했음).
- 본섬 북 잔교 곁에 **주문 게시판** 신설(기둥+판+차양+핀 종이, 베이크 병합 — draws 무증가).
  클릭 = 주문 패널: **재료별 수량 −/＋ 조절(품목당 3개) → 주문하기** → 다음날 아침 배달.
  하루 1주문. "페리가 아침 운항에서 봐 온다"는 기존 장터 설정의 물리적 완성.
- **배달 알림은 오브젝트 상태**: 도착하면 게시판 곁에 📦 상자가 놓여 있고(호버 문구도 전환),
  게시판을 열면 원터치 수령 — 토스트로 조르지 않는다(고요 패치 기조).
- **배달분(pierStock)은 상하지 않는 별도 재고**: 재료 카운트·요리 차감·레시피 팁·주방
  스트립(📦 표기)에 자동 편입, 차감은 "오늘이 지나면 사라지는" 장터 일일 재고부터.
- 검증: world-layout-sweep 통과(첫 좌표가 아이스박스와 겹쳐 1회 이동 — 스크립트가 잡음),
  world-smoke 15/15 · draws 236.

### Changed (🍳 레시피 팁 — 호버/길게누름에 재료 전부, 있는 것/없는 것 구분)
- 타일 툴팁이 요리명+"부족한 것"만 말하던 걸 **전용 팁**으로: 필요한 재료 전부를
  ✓ 초록(있음 — 개수/찬장 표기) / ✗ 붉게(없음) 구분해 보여준다. 데탑 = 마우스 호버,
  모바일 = 길게 터치(2.6초 자동 닫힘). 요리 창 자체는 그대로 — 카드 UI 대신 팁 한 장.
- **💗 마음 요리 잠금 표현 분리**: ❓ 회색(재료부족 회색과 혼동)이 아니라 분홍 하트 +
  "서로 다른 요리 N종 더 만들면 떠올라요" 힌트(HEART_UNLOCKS 마일스톤에서 계산).
- world-smoke 15/15. (장터/고기 로테이션은 다음 논의로 보류.)

### Changed (🎣 낚시 챔질을 동숲식으로 — 반응속도 게임 → 신호 인지 게임)
- **문제**(실플레이 리포트 "타이밍 잡기 힘들다"): 니블과 진짜 입질이 찌 딥 -0.035/-0.1과 소리
  크기라는 "정도 차이"뿐이었고, 펫 알럿 포즈는 둘 다 동일 발동 — 판별이 안 되는데 성급 클릭은
  캐스트 전체 취소(재대기 ~10초)라는 최대 벌. 동숲의 답: 신호는 종류로, 벌은 싸게, 윈도우는 관대하게.
- **신호 3채널 분리**: 진짜 입질 = 찌가 물속으로 **완전히 사라짐**(-0.26) + ❗ 말풍선(숨바꼭질
  문법 재사용) + 거리 감쇠 없는 낮은 "꼬록"(귀로만도 낚시 가능). 니블은 까딱+퐁만 — 알럿
  포즈(몸 숙임·눈 커짐)는 입질 전용으로 이동. 절친(AI) 입질 소리는 종전 감쇠 유지(자율 낚시 소음 방지).
- **챔질 윈도우 0.65 → 1.0초** — 신호가 명확해진 만큼 반응은 관대하게 (전 어종 균일 유지:
  희귀함은 등장 확률이 담당, 스킬 난이도가 아니라는 기존 철학 그대로).
- **실수 비용 인하(동숲식)**: 성급 챔질 = 물고기가 물보라와 함께 도망(벌은 유지)하되, 성급·놓침
  직후의 다음 캐스트는 그림자가 빨리 돌아온다(1.6~4.2초 → 0.7~1.5초). ❗는 훅/미스/정리 전
  경로에서 안전 정리.
- world-smoke 15/15. 어종 분포·밸런스는 무변경(UX만).

### Changed (🕊️ 고요 패치 1차 — 조과 자동 보관 + 배지 전면 제거)
- **물고기는 무조건 아이스박스로**: 잡을 때마다 뜨던 보관/놓아주기 2버튼(사실상 의미 없는
  결정 요구) 제거 — 진짜 물고기(꽝 제외)는 조용히 보관되고 조과 토스트에 `· 🧊`만 붙는다.
  **절친(AI) 조과도 동일하게 보관** — 자율 낚시가 요리 재료를 채워준다. 오프라인 정산 낚시도
  라이브 규칙과 정합(아이스박스행, 도감은 종전대로 주인 조과만).
- **독 배지(빨간점/초록점) 전면 제거**: "새 어종" 알림 점은 게임이 숙제를 조르는 문법 —
  피로도 진단의 1순위 요구 표면. 모드 상태는 버튼 자체가 이미 말한다(🔨 주황 배경 · 🌙 반투명).
  dexSeenN·world-dex-seen-n 추적 장부와 10초 폴링도 함께 제거.
- world-smoke 15/15 (정산 시나리오 토스트도 새 문구 확인).

### Changed (🎣 낚시 미세 조정 — 비≠눈 + 도감 미등록 ×1.5)
- **비 조건이 강수 종류를 본다**: 조건 판정이 강도(wxF)만 봐서 겨울 눈에도 개구리·연어가
  잡혔다 — 이제 진짜 비/뇌우만 ("비 오는 연못" 힌트와 일치). 눈 오는 날 빙어는 그대로.
- **어종 추첨에 중복 보호**: 도감 미등록 종 ×1.5 (해산물 ×2.5와 같은 문법 — 낚시는 활성 풀이
  3~5종으로 작아 배수는 온건하게). 등록 순간 꺼져서 장기 분포 불변, 첫 만남만 앞당긴다.
  부수 효과: 초반 붕어 연속 체감 완화. 밸런스 리뷰(캐스트당 확률표·완주 2~3세션 추정)는
  대화 기록 기준 — 구조 자체는 실무 체크리스트(조건 잠금+힌트+밸브+이중 희귀 결) 통과 판정.
- world-smoke 15/15.

### Changed (🤿 해저 채집 다양성 — 서식지·중복 보호·뭉침 방지·도감 힌트)
- **문제**(실플레이 리포트): 심해 산책에서 성게·해삼만 보임. 원인은 확률값이 아니라 **존 배타
  풀 × 동선** — near 4종(미역·불가사리·굴·멍게)은 섬 반경+5m 띠 전용이라 심해에선 0%, far에선
  성게+해삼이 69% 독식. 확률 튜닝 대신 접근성(서식지)으로 푼다.
- **서식지 스폰** (열수구=새우 문법 재사용): 🌿 켈프 숲 곁 = 미역(w50)·멍게(w25), 🦪 난파선
  곁 = 굴(w45)이 심해에도 모여 산다 — 탐험이 곧 도감. 불가사리만 얕은 바다 전용으로 남는다
  (도감 힌트가 안내).
- **중복 보호**: 도감 미등록 종 가중 ×2.5 (스폰·잠수 롤 공용) — 같은 시간에 첫 만남이 더 자주.
- **뭉침 방지**: 화면 내 동일종 스폰 ≤2 (4슬롯) — "성게만 깔린" 인상 제거.
- **도감 힌트**: 미발견 칸이 존 두루뭉술 대신 서식지를 짚어준다 ("서식지: 먼바다 · 열수구 온천
  곁") — 잠금은 콘텐츠지만 비밀 잠금은 버그로 체감되므로.
- **게이트 신설** `scripts/seafood-sim.mjs` (승인 기준 4항목, 400런): 첫 20분 조우 5.0→**6.8종**
  (기준 ≥6) · 도감 완주 4.6→**3.1세션**(≤5) · 동일종 3연속 9.5→**4.1%**(≤5) · 서식지 개성
  켈프 1위 성게→**미역**·열수구 새우 유지 — 4/4 PASS. 동선 가정은 실리포트로 캘리브레이션
  (근해 링 노출 2%). world-smoke 15/15.

### Reverted (🪼 해파리 — 기본은 시안 A로 되돌리고, 리모델은 시안 B로 보존)
사용자 판단으로 **기본 조형을 원래대로(시안 A) 롤백**했다. 직전 커밋의 리모델은 지우지 않고
`?jellyb=1`로 켜지는 **시안 B**로 남긴다 — 이 저장소가 이미 쓰는 플래그 문법(`?nomerge=1`·`?sealab=1`)이고,
해파리는 검수 랩에 없어서 **실제 해저 조명 아래에서 A/B를 나란히 보는 것 말고는 판단할 방법이 없다**.

- `makeJellyA`(기본, 원래 조형) / `makeJellyB`(리모델) / `makeJelly`가 플래그로 고른다
- 재질도 갈라 뒀다: `jellyBellMats`·`jellyTentMats`(A) vs `jellyBellMatsB`·`jellyTentMatsB`(B)
- 맥동 애니는 `userData.bell` 유무로 갈린다 — A는 CPU가 벨을 스케일하고, B는 정점 셰이더가 한다
- 둘 중 하나를 고르면 **나머지를 지운다**(블록 머리에 그렇게 적어 뒀다)

실측 대비 — A: 3드로우 · 684 tri / B: 2드로우 · 4,080 tri. 둘 다 맥동 정상(변화 23% / 25%).
`npm run test:world` 15/15 PASS · draws 239 · 해저 로드 무에러(양쪽 다).

### Changed (🪼 해파리 시안 B — 빨대 꽂은 반투명 그릇에서 메두사로 · 개체당 3→2드로우)
구 조형의 문제: 벨이 `LatheGeometry`라 **물결 가장자리를 만들 수 없었고**(φ가 균일), 촉수는 곧은
테이퍼 원통 11개라 **빨대 다발**로 읽혔고, 구강완은 굵기만 다른 같은 막대라 촉수와 구분이 안 됐고,
심지는 눌린 구 하나(그릇에 든 공), 알파는 균일 0.42라 **판유리** 같았다.

- **맥동을 정점 셰이더로** — CPU가 `bell.scale`을 만지던 방식은 ① 벨 전체가 균일 스케일이라 '풍선
  부풀리기'였고 ② 심지가 같이 못 움직여 별개 메시로 갈라져 있었다(개체당 3드로우).
  `aBend`(꼭대기 0 → 립 1)로 가중하니 **립이 먼저 오므라드는** 진짜 메두사 수축이 되고, 심지도 같은
  지오에 합쳐 약하게(0.34) 같이 맥동한다(사용자 지정) → **개체당 2드로우**. CPU는 부력만 잇는다.
- **물결 가장자리** — Lathe를 버리고 극좌표 격자로. 립에서만 `sin(8φ)`가 살아나게 `t^2.2`로 가중.
  ⚠️ NP는 물결 한 갈래에 5세그먼트 이상 필요(30 → 40) — 낮으면 물결이 각진다.
- **알파 그라디언트** — 정점색 4성분(꼭대기 0.95 → 립 0.33). 균일 opacity가 판유리로 보이던 원인.
- **4엽 생식소** — 우산 너머로 비치는 게 해파리의 시그니처다.
- **촉수** — 곧은 원통 → 끝에서 반지름 0인 스윕 튜브(문어 다리 골격 재사용).
  ⚠️ 뻗기만 하면 **고드름/창**이 된다 → 길이를 따라 좌우로 흔들리는 항(`sin(t·wob)`)을 넣어야
  물속의 실로 읽힌다. 길이·굽기·위상을 개체마다 흔든다.
- **구강완을 촉수와 다른 물건으로** — 주름진 넓은 리본(미역 잎 문법)이 안팎으로 말린다.

⚠️ 리팩터 중 `kelpBladeGeo`를 통째로 날렸다 — 교체 범위(`jellyBellMats`~`seaChunkInfo`)가 그 함수를
품고 있었다. 해저 청크 로드가 `ReferenceError`로 죽어서 발견했고 HEAD에서 복원했다. **최상위 함수
목록을 HEAD와 diff해 다른 손실이 없음을 확인**했다. 큰 블록을 좌표로 잘라낼 땐 그 안에 뭐가 있는지
먼저 본다.

비용: 개체당 3드로우 → **2드로우** · 재질 2(전 개체 공유) · 삼각형 ~800 → 4,080.
해파리는 해저 청크 18%에 2~3마리 무리라 삼각형 여유는 크다(월드 190k).
검증: 맥동 픽셀차 평균 8.7·변화 25%(1.2초 간격) · 해저 로드 무에러 · `npm run test:world` 15/15 PASS · draws 239.
(연출·무리 병합은 사용자 판단으로 보류 — 무리 병합은 맥동 위상을 이미 어트리뷰트로 옮겨 뒀으니
나중에 벨을 통째로 병합하기만 하면 된다.)

### Changed (🌿 미역 — 튜브 매듭에서 진짜 엽체로 · 아틀라스 셀 28 · 미세 스웨이)
구 조형: `TubeGeometry`를 **만든 뒤 월드 공간에서 눌렀다**(`scale(1.6,1,0.42)`). 납작해지는 방향이
잎을 따라가지 않아 비틀리고, 경로가 코르크스크류라 세 가닥이 서로 꼬여 **매듭**이 됐다. 미역 해부학은
하나도 없었다.

**켈프와의 관계**: 완전히 다른 물건이다 — 켈프는 배경 드레싱(0.9~2.3, `seaKelpMat` 스웨이, 청크 시드
자동 배치), 미역은 채집 아이템(0.11, `gradMat` 정적). 그런데 **해조류 언어는 켈프 쪽이 맞았다**(납작한
테이퍼 잎). 그래서 켈프의 잎 언어 + 미역 해부학으로 다시 짰다:
줄기(stipe) → 중륵(midrib) 융기 → **깃꼴 결각**이 들어간 잎 3장 → 밑동을 감는 주름진 **미역귀**(포자엽).
잎은 튜브가 아니라 **띠 메시**로 직접 짠다(중륵 곡선 + 좌우 폭) — 그래야 잎면이 항상 평평하다.
크기도 0.11 → 0.215로(다른 해산물 0.15~0.48 대비 너무 작았다).

**광택 + 미세 스웨이**(사용자 지정): `fcMat`을 클론한 전용 재질 — roughness 0.32로 낮춰
`scene.environment`가 은은한 반사광을 주고, 켈프와 같은 스웨이 셰이더(`aBend`/`aPh` + `wxTime`)를
**아주 약한 진폭**으로(켈프 0.16/0.10 → **0.030/0.018**). 배경 켈프처럼 크게 나부끼면 주워서 손에 든
순간 어색하다. ⚠️ 스웨이를 `fcMat` 본체에 넣으면 조개·물고기까지 흔들리므로 반드시 클론.
검증: 같은 각도 1.4초 간격 두 프레임의 픽셀차 — 평균 7.3 · 변화 픽셀 26%(눈에 띄되 과하지 않은 폭).

⚠️ **얇은 잎에 법선 기반 음영은 못 쓴다.** 한 장짜리 면은 DoubleSide에서 앞뒤 법선이 반대라, 같은
다발 안에서 어떤 잎은 새까맣고 어떤 잎은 거의 흰색으로 나왔다(해저 실측 — 검수 랩은 백색 앰비언트라
멀쩡해 보인다). 이 월드가 잎사귀에 원래 쓰는 문법 = **높이 그라디언트** → `fcLeafShade` 추가.
팔레트도 두 단계 올렸다: 진 녹색 × 톱라이트 × 해저 조명이 겹치면 그냥 검정이 된다.
⚠️ `lean`을 월드 z로만 주면 잎 3장이 같은 쪽으로 누워 **옥수수 껍질**로 뭉친다 → 각 잎의 바깥
방향(`ca`,`sa`)으로 밀어야 사방으로 벌어진다.

**정정**: 직전 커밋에서 "아틀라스 예약분을 다 썼다"고 적었는데 그건 `20~27` 예약 범위 얘기였다.
실제로는 **28~31 네 칸이 비어 있다**(참조 0건 확인) — 미역이 28을 쓰고 **셋 남는다**.
비용: 360 → **2,632** tri · 1드로우(자기 메시라 재질이 달라도 드로우는 그대로) · 재질 +1.
`npm run test:world` 15/15 PASS · draws 238. **해산물 8종 카툰 이관 완료.**

### Fixed (🐙 문어 — 머리·다리가 딴 동물처럼 갈렸고, 실루엣이 그냥 공이었다)
사용자 리포트 "실제 문어랑 너무 다르다 · 머리랑 다리색도 분리된 것 같다". 넷 다 맞았다.

- **색 분리**: 외투막을 진한 자주(`#8e2340`), 다리를 밝은 분홍(`#f0a0b4`)으로 칠해 두 토막으로 보였다.
  실물 문어는 온몸이 **같은 적갈색 계열**이고 옅은 건 **다리 안쪽(빨판 면)뿐**이다 → 팔레트를 하나로
  (`#8a3826`/`#a94e35`/`#c4674a`), 대역 경계(v 0.30~0.36)를 겹쳐 잇고, **얼룩을 몸통·다리 양쪽에 같은
  밀도로** 뿌렸다(한쪽만 얼룩지면 거기서 또 경계가 보인다). 옅은 건 φ≈1.5π 빨판 줄 주변만.
- **실루엣**: 단조 증가하다 닫히는 프로파일은 그냥 공이다. 실물은 아래에서 위로
  [다리 관 → **머리 불룩** → **목 잘록** → 외투막 자루 → 둥근 꼭대기]이고, 이 잘록함이 없으면
  문어로 안 읽힌다 → 키프레임 10점 + smoothstep.
- **눈 위치**: 외투막(s 0.6)에 붙어 있어 정수리에 눈이 달린 꼴이었다 → **머리 불룩(s 0.28)**으로 내렸다.
- **물갈퀴(interbrachial web)**: 없어서 소시지 8개가 따로 놀았다 → 다리 밑동을 잇는 막을 추가.
  자유변은 다리 방위에서 멀리 나가고 사이에서 처진다 → `|cos(4φ)|`가 8봉우리를 만든다.

⚠️ 프로파일 키프레임을 두 번 고쳤다: 간격이 크고 낙차가 심하면 smoothstep이 각 구간을 **접시**로
만들어 비행접시를 쌓은 **탑**이 됐고(0.88→0.80→0.60→0.80→1.00), 낙차를 줄이자 이번엔 **통(barrel)**이
됐다 → 위쪽을 3점(0.94→0.72→0.40→0)으로 굴려 알 모양 자루로. 분할도 30→42×30(낮으면 굴곡이 각진 턱).

비용: 5,200 → **6,808** tri(프로파일 분할 + 물갈퀴) · 1드로우 · 재질 1. 해산물 중 가장 크고 가장 무겁지만
해저 동시 표시는 최대 4개다. `npm run test:world` 15/15 PASS · draws 238.

### Changed (🦐🐙 새우·문어 — 예약 셀 26·27로 해산물 카툰 이관 완료)
**새우** 구 조형: 구체 5개를 호로 늘어놓아 서로 안 섞이는 **애벌레**, z로 0.3 눌린 원뿔이 꼬리(종이
삼각형), 더듬이는 붙을 머리도 없이 공중에 떠 있었다.
→ 몸은 **한 개의 스윕 튜브**. 마디는 붙이지 않고 반지름 리플로 — 뒤로 갈수록 부풀다 뚝 떨어지는
톱니(`pow(frac, 2.6)`)가 겹친 갑각 판으로 읽힌다. 로스트럼→갑각→복부 테이퍼, C컬 경로, 꼬리 부채
4장(얇은 판), 머리에서 실제로 뻗는 더듬이, 눈 구슬.

**문어** 구 조형: 머리가 그냥 구체, 다리는 **부분 토러스 8개를 바닥에 눕힌 것**(핑크 크루아상 링)이라
머리에 붙지도 않고 끝이 뭉툭하게 잘려 있었다. 눈은 흰자·동공이 별개 구체라 각도에 따라 어긋났다.
→ 외투막은 자루 로프트, 다리는 **각각 스윕 튜브**(끝에서 반지름 0 = 뾰족, 뻗다가 끝만 말려 올라감),
빨판은 다리 안쪽 두 줄 `disp` 융기, 눈은 외투막 `disp` 융기 + 텍스처 슬릿 동공(별개 구체 폐기 =
어긋남 원천 제거). **다리 뿌리 좌표는 외투막 프로파일에서 파생**시켰다 — 두 군데 하드코딩하면 목이
벌어진다(굴 힌지에서 배운 것). 사용자 지정대로 **가장 크게**: 폭 0.47(멍게 0.15 · 해삼 0.32 · 성게 0.30).

`fcTubeGeo`에 **스윕 프레임**(`o.path`)을 추가했다 — 중심선이 크게 휠 때 고정 (x,y) 평면으로 훑으면
튜브가 납작하게 찌그러진다. path가 없으면 기존 동작 그대로라 해삼·멍게는 무변경.
부속(다리·더듬이·꼬리)은 `fcRemapV`로 **몸통과 다른 v 대역**에 둔다 — 같은 대역을 쓰면 한쪽 색을 바꿀 때
다른 쪽이 딸려 변한다(멍게 사이펀에서 실측한 함정을 이번엔 처음부터 피했다).

⚠️ 문어 눈 φ는 두 번 틀렸다: ±0.95는 정면에서 하나만 크게 보여 가로 슬릿이 **'입'**으로 읽혔고,
반대로 ±1.55(178° 벌림)는 정확히 반대편이라 어느 각도에서도 **한쪽만** 보였다 → ±0.62(71°)로 앞옆에 나란히.
⚠️ 새우 더듬이는 실물처럼 길게 뒀더니 폭 0.49로 **문어(0.47)를 넘어 크기 서열이 뒤집혔다** → 0.072로 축소.

비용: 새우 440 → **2,432** tri · 문어 1,408 → **5,200** tri · 둘 다 1드로우 · 재질 1(`fcMat` 공유).
**아틀라스 예약분(20~27)을 정확히 다 썼고 확장은 없다.** 남은 미역은 `gradMat` 정점색만으로 두어
해산물 8종 중 7종 이관 완료.
`npm run test:world` 15/15 PASS · draws 237 · 조개 4종 회귀 없음.

### Fixed (🍊 멍게 세로 그라디언트 — 위가 진하고 밑동이 연하게)
사용자 지정. 기존 그라디언트는 위 `#c0341a` → 가운데 밝음 → **밑동도 `#a8280f`로 어두운** 산 모양이라
실물과 반대였다. 위→아래 단조 감소로 바꿨다(`#8a2210` → `#f8c8a8`).

⚠️ 그런데 바꾸자마자 **사이펀이 살구빛으로 떴다**. 사이펀 uv가 몸통과 같은 v 대역(0.02~0.30)에
있었기 때문이다 — 밑동을 연하게 만들면 사이펀이 그 색을 훔쳐 간다. 반대로 사이펀을 어둡게 칠하려고
깔았던 오버레이(v<0.13)는 **몸통 밑동까지 어둡게** 만들고 있었다(셀 픽셀 샘플로 확인: v 0.25에서
(225,123,74)인데 v 0.05에서 (165,95,69)로 되레 어두워졌다). 즉 두 물건이 한 v 대역을 두고 싸우고 있었다.
→ 사이펀 uv를 **꼭대기 색 한 점(0.5, 0.96)으로 고정**해 대역을 비웠다. 분화구의 깊이는 텍스처가 아니라
정점색 톱라이트(아래 향한 면 = 그늘)가 만든다. 오버레이는 제거.
돌기 하이라이트도 `0.35 + 0.65·s`로 눌렀다 — 이미 연한 밑동에 황토를 세게 얹으면 얼룩이 된다.
텍스처 밑동은 정점색이 아래 향한 면을 0.62로 누르는 걸 감안해 한 단계 더 연하게 잡았다.

`npm run test:world` 15/15 PASS · draws 240 · 삼각형·드로우 불변.

### Fixed (🍊 멍게 — 물결치는 양파가 아니라 **바다의 파인애플**)
사용자 리포트 "실제 멍게랑 다르다 — 윗부분이 오돌토돌한 그거". 맞다. 정수 계수 사인 3개를 합친
저주파 필드는 **물결치는 양파**를 만든다. 실물 멍게(Halocynthia roretzi)는 매끈한 굴곡이 아니라
**뾰족한 원뿔 돌기가 개별로 박혀** 있고, **위로 갈수록 크고 촘촘**하며 밑동은 매끈하다.

- 저주파 필드 → **개별 돌기 테이블**(`FC_SQ_KNOBS` 64개). φ는 황금각으로 고르게, s는 `pow(t,0.58)`로
  위 편중, 크기는 `pow(s,1.8)`로 위로 갈수록 크게. 밑동(s<0.20)은 비워 매끈하게 남긴다.
- 봉우리 모양: 가우시안은 둥글어 물결로 읽힌다 → 거리 1차 감쇠 `f²(0.34+0.66f)`로 뾰족하게.
  σφ를 `rad(s)`로 나눠야 좁은 위·아래에서 돌기가 옆으로 늘어나지 않는다.
- ⚠️ **분할이 모자라면 뾰족해지지 않는다**: ns/np 30이면 돌기 하나가 세그먼트 1.4개에 걸쳐 법선
  보간이 둥근 돔으로 뭉갠다(실측 — 라즈베리처럼 보였다) → 40×46.
- 텍스처도 **같은 테이블**을 그린다(따로 그리면 조형과 엇갈려 스티커가 된다). 픽셀 직접 쓰기는
  64돌기 × 256² = 300만 회라 굽기가 느려진다 → 돌기마다 방사 그라디언트 타원 하나.
  등방 보정 `Δu = Δv·0.432/rad`, u 0/1 이음선에 걸친 돌기는 반대쪽에도 찍는다.
- 밑동은 옅은 살구빛으로 — 실물의 부착부.

비용: 1,996 → **3,876** tri(분할 상향분) · 1드로우 · 재질 1. 굴(5,752)보다 아래.
`npm run test:world` 15/15 PASS · draws 236.

### Changed (🥒🍊 해삼·멍게 — 매끈한 프리미티브 + 붙인 구체를 관형 로프트로 · 아틀라스 셀 24·25)
둘 다 같은 안티패턴이었다(사용자 리포트 "형태부터 질감 싹 다"): 매끈한 프리미티브 하나 + 돌기를
**구체로 붙임**. 해삼은 지름이 일정한 캡슐 + 구체 9개 = 초콜릿 롤에 조약돌, 멍게는 면수 낮은 구체 +
납작한 원통 굴뚝 2개 + 여드름 7개였고, **멍게의 정체성인 파인애플 같은 울퉁불퉁함이 아예 없었다**.

공용 `fcTubeGeo` — 축을 따라 단면을 훑는 닫힌 곡면. `rad(s)`·`sec(φ,s)`(단면 형태)·`disp(φ,s)`(변위)·
`axis(s)`(아치)를 호출부가 준다. 조개 `fcValveGeo`, 별 `fcStarGeo`에 이어 세 번째 골격 — 남은 새우
몸통·문어 머리도 이걸 쓴다. 규율은 그대로: **돌기·혹은 붙이지 말고 반지름 변위로** → 실루엣 자체가
울퉁불퉁해지고 드로우는 1.
⚠️ 이름 주의: 기존 `fcLoftGeo`는 **가오리용 평면 로프트**다(위 아웃라인 + 상하 높이 필드). 골격이
달라서 합치지 않고 `fcTubeGeo`로 분리했다 — 처음에 같은 이름을 써서 `already been declared`로 페이지가
통째로 죽었다(검수 랩이 안 뜨는 증상으로 나타난다).

**해삼**: 양끝 테이퍼(`sin(πs)^0.42`) + 머리쪽 비대칭 · 단면 D자로 **평평한 발바닥** · 등쪽 종렬 4줄
돌기를 변위로(줄마다 s 위상·개수·크기를 흔들어 기계적 반복을 깬다) · 아치 + 완만한 S 곡선(곧은 막대는
죽은 것처럼 보인다) · 길이/지름 2.9 → **3.7**.
⚠️ 돌기 지수 7·진폭 0.34는 **톱니(가시)**가 됐다 → 지수 4·진폭 0.23~0.33으로 완만한 융기.

**멍게**: 서양배 프로파일(`pow(s,1.35)`로 봉우리를 s≈0.60까지 밀어올린다) · 파인애플 혹을 정수 계수
사인 3개 합으로(φ 계수가 정수여야 φ=0/2π에서 이어진다) · 사이펀 2개 · 아틀라스 셀 25.
⚠️ 혹 진폭 0.15는 실루엣이 매끈해 **텍스처가 무늬를 다 짊어졌다**(칠한 위장색처럼) → 0.30으로 올려
조형이 짊어지게 하고 텍스처 강도를 낮췄다.
⚠️ 사이펀은 **세 번** 고쳤다: ① 원통 = 굴뚝(구 조형) ② 얕은 깔때기 + 1.45배 치마 = **찢어진 판지**,
게다가 그 높이의 몸통 반지름(≈0.43R)보다 작아 통째로 파묻혔다 ③ 정답은 작은 **화산(분화구)** —
밑동이 몸통에 잠기고 위로 좁아지다 테두리에서 안으로 말려 오목해진다(소라 개구부와 같은 원리).
색도 v 0~0.30을 통째로 어둡게 했더니 새까만 판지가 돼서, 분화구 안쪽(v<0.12)만 어둡게 바꿨다.
⚠️ 혹 무늬를 따로 그리면 조형과 엇갈려 스티커가 된다 → **조형과 같은 함수를 샘플링**해 골은 어둡게·
정수리는 밝게. 그리고 `fillRect` 격자(64² = 4px 셀)로 칠했더니 **모아레 그물**이 됐다 →
`getImageData`/`putImageData`로 픽셀 직접 쓰기(256² 루프가 fillRect 65,536번보다 싸다).

곁들여: 굴에서 정한 정점색 문법(월드 톱라이트 + 아래를 향할수록 따뜻하게)을 `fcSeaShade`로 공용화했다.
그리고 `fcSeaBuild`가 가로채 **도달 불가가 된 구 빌더 브랜치 4개**(굴·불가사리·해삼·멍게)를 제거했다 —
물고기 리모델에서 세운 관례("도달 불가가 된 구 빌더 본문은 제거한다")를 따른다.

비용: 해삼 450 → **1,560** tri · 멍게 398 → **1,996** tri · 둘 다 1드로우 · 재질 1(`fcMat` 공유) ·
아틀라스 확장 없음(예약 20~27 중 24·25 사용, 26·27 남음).
`npm run test:world` 15/15 PASS · draws 239 · 조개 4종·굴·불가사리·성게 회귀 없음.

### Changed (⭐🟣 불가사리 질감·색·팔끝 · 성게 가시 밑동 — 사용자 지정 4건)
- **색 진하게**: 파스텔 주황(`#e0722c→#ffc97e`)이 물빠져 보였다 → 붉은 벽돌빛(`#8f2f10→#efa257`).
- **팔끝 뾰족하게**: `armE` 2.3 → 3.0, p 계수 0.95 → 1.10, `armMin` 0.28 → 0.26.
- **성게 가시 밑동 굵게**: 0.0055/0.0048 → **0.0078/0.0068**. 사용자 예상대로 가시가 서로 닿아
  꽉 찬 실루엣이 된다(몸통이 거의 안 보이는 게 실물에 맞다).
- **실제 불가사리 질감** — 여기서 uv 함정을 세 번 밟았다:
  1. 과립을 u 전역에 랜덤으로 흩었더니 대부분이 **팔 사이 골**(lobe가 작아 표면이 실오라기뿐)에
     떨어져 화면에 안 나왔다 → 팔 축 주변 밴드로 몰았다.
  2. 등방 보정에서 **lobe를 빼먹었다**. 팔 축은 lobe≈1이지만 팔 가장자리는 ≈0.31이라 필요한 폭이
     3배 차이 난다 → 과립이 팔을 따라 흐르는 줄무늬가 됐다.
  3. 그래도 안 됐다. **근본 원인은 uv 배분**이었다: u에 둘레 전체(2π)를 담으면 셀 256px을 팔 5개가
     나눠 가져, 팔끝에서 등방한 돌기를 그리려면 텍스처에 **0.4px 폭** 타원을 찍어야 한다 — 물리적으로
     불가능하다(그래서 과립이 팔끝에서 사라졌다).
     → 팔 5개는 서로 같으니 **한 팔 섹터를 접어** u에 담는다: `u=0` 팔 축 · `u=1` 팔 사이 골.
       해상도 10배, 비등방 6.3배 → **1.6배**로 떨어져 그때부터 과립이 팔끝까지 또렷하다.
       대가: 무늬가 팔마다·좌우로 반복돼 **천공판(madreporite)처럼 '하나만' 있어야 하는 건 못 넣는다**
       → 뺐다. 팔의 좌우 대칭 무늬는 실물에서도 자연스러워 손해가 아니다.
  4. 마지막으로 과립을 크고 진하게 깔았더니 **바탕색을 다 덮어 튀김옷**이 됐다 → 요청한 '진한 색'이
     보이려면 과립은 결만 만들고 물러나야 한다. 크기·알파를 절반으로(큰 과립 46개 + 잔 과립 110개).

비용 불변: 불가사리 2,520 tri · 성게 1,132 tri · 둘 다 1드로우.
`npm run test:world` 15/15 PASS · draws 238 · 조개 4종·굴 회귀 없음.

### Changed (⭐ 불가사리 — 팔은 붙이는 게 아니라 반지름 함수에서 돋아난다 · 아틀라스 셀 23)
구 조형: `ConeGeometry(0.026, 0.085, 5)`를 `scale(1, 1, 0.42)`로 눌러 만든 **납작한 칼날 5개**를 눌린
구체에 꽂았다(사용자 리포트 "심각하다"). 팔 두께가 0에 가깝고, 중심 돔이 팔보다 높아 **햄버거에 칼
꽂은** 실루엣이었고, 팔과 중심이 전혀 이어지지 않았다. 삼각형 130개.

불가사리는 극좌표 함수로 딱 떨어진다 — `|cos(2.5θ)|`는 2π에 봉우리가 정확히 5개라 반지름을 그걸로
흔들면 팔이 **저절로** 돋아나 디스크에 녹아든다. 윗면 → 림 → 밑면을 한 지오로 닫아 두께를 만든다.
- ⚠️ 지수를 p(중심→림)와 무관하게 두면 팔의 **각폭이 일정**해서 선형 폭이 반지름과 함께 커진다 →
  끝이 뭉툭하게 벌어진 **플루메리아 꽃잎**이 됐다(실측). 지수를 p와 함께 키워(`armE·(0.42+0.95p)`)
  뿌리는 넓고 끝은 좁은 진짜 팔로.
- ⚠️ p=1(림)에서 위·아래가 **같은 y**여야 닫힌다 — `(1−p²)` 항이 거기서 0이 되게 둔다(조개 렌즈와 같은 불변식).
- 팔끝을 살짝 들어올린다(`p³·arm²`) — 실물 불가사리의 특징.
- 아틀라스 **셀 23**(비어 있던 자리): 중심 진주황 → 팔끝 살구 그라디언트 · 팔 사이 골 · 팔 축 알갱이
  2줄 + 중앙 능선 · 천공판(madreporite).
  ⚠️ **uv가 등방이 아니다**: u는 둘레(2π·rad) 전체를, v는 반지름 R만 256px에 담는다 → 림에서 u가
  v보다 ~6배 늘어난다. 동그란 점을 찍으면 3D에서 **가로로 늘어난 잎맥**이 됐다 → 텍스처에선 세로로
  길쭉하게(폭 0.22배) 찍어야 3D에서 둥근 돌기가 된다.
  ⚠️ u 0/1 이음선에 걸친 팔이 반쪽만 그려진다 → 무늬 루프를 `k <= 5`로 돌려 u=1.0까지 한 번 더 찍는다.

해산물 디스패치는 **맵 상수가 아니라 함수**(`fcSeaBuild`)로 뒀다 — 선언 호이스팅 덕에 빌더가 파일
뒤쪽에 있어도 TDZ가 없다. 새 종을 카툰 조형으로 옮길 때 여기 한 줄만 추가한다.

### Changed (🟣 성게 — 개수가 아니라 배치와 단면이 문제였다)
사용자 예상("가시만 많이 붙이면 됨")이 맞았지만 개수만은 아니었다:
- 배치가 `((i*7)%11)/11·π − π/2`로 **위도가 11단계뿐이고 경도와 상관관계**가 있어 가시가 뭉치고
  대머리 구역이 생겼다 → **피보나치 구면**(황금각). 몇 개를 뽑아도 균일하다. 22 → **65개**.
- `ConeGeometry(…, 4)` 4각 원뿔은 각도에 따라 **납작한 판**으로 보였다(탑뷰의 사각형들이 그 증거) → 6각.
- 실물처럼 긴 주가시(2/3) + 짧은 보조가시(1/3)를 섞고 길이도 흔든다.
- ⚠️ 그라디언트는 **회전 전에** 구워야 침 길이 방향으로 흐른다 — `bakeGrad`는 지오의 y 범위를 쓰므로
  회전 후에 구우면 방향이 엉킨다. 끝을 어둡게, 뿌리를 밝게.
- 몸통은 세그먼트 12×10 → 16×12, 색은 가시 뿌리와 갈리게 살짝 올렸다. 아틀라스는 안 쓴다 —
  가시가 표면을 가려서 텍스처 이득이 적다(`gradMat` 유지).

비용: 불가사리 130 → **2,520** tri(1드로우 · `fcMat` 공유) · 성게 392 → **1,132** tri(1드로우 ·
`gradMat` 유지). 해저 동시 표시 최대 4개라 여유가 크다.
`npm run test:world` 15/15 PASS · draws 236 · 조개 4종·굴 회귀 없음.

### Fixed (🦪 굴 — 부채꼴 모서리 제거 + 검은 띠 축소 + "살 뚫림"의 진짜 원인)
사용자 리포트 3건. 앞의 두 커밋에서 잘못 짚은 것을 바로잡는다.

**"껍데기 위아래 부채꼴 모서리 다 없애줘 — 굴 껍데기 형태 알지?"**
`fcValveGeo`의 패치는 (r, q) 사각형이라 평면 윤곽이 **사다리꼴**이다 — 힌지 끝과 자유변 끝에 각진
모서리가 4개 생긴다. 조개는 그게 부채라서 맞지만 굴은 불규칙한 **타원 덩어리**다. 폭 프로파일을
호출부가 직접 주도록 `wprofile`을 열고(기본식은 조개 4종이 쓰는 그대로 남는다), 굴은
`0.10 + 0.90·sin(½π·t^0.62)^1.35`을 쓴다 — 힌지 폭이 0.32 → 0.10으로 좁아져 모서리 2개가 **둥근
각정(umbo) 하나**로 합쳐지고, `edge` 코너 컷을 0.14 → 0.34로 키워 앞끝을 둥글렸다. arc 2.05 → 2.25.

**"내용물 검은 띠 영역이 너무 커, 특히 하단"** → 띠 폭 0.14 → 0.10.
⚠️ 처음엔 아래짝만 v 하단을 잘라내 띠를 제외했는데(vLo), 적도에서 알파가 뚝 끊겨 **하드한 분리선**이
생겨 오히려 구멍 테두리로 보였다 → 되돌리고 위·아래 같은 uv로 둔다. 밑면 볼륨을 살린 뒤로는
v 0~0.10이 얇은 고리라 자를 필요가 없고, 검은 테가 적도를 **끊김 없이** 감는다.

**"옆에서 볼 때 여전히 살 뚫려있어"** — 이번엔 구멍이 아니었다. 두 가지가 겹쳐 있었다:
- 아래 볼륨 0.10이라 밑면이 거의 **평평**했다 → 눈높이에서 속살이 검은 띠에서 끝나고 그 아래로
  컵 안쪽면이 드러난다. 0.22로 부풀려 통통한 덩어리로(컵 바닥은 lateral 0.58R에서 h≈−0.35R,
  속살 밑면은 −0.248R이라 여유가 있다). 렌즈 지수도 0.66 → 0.82로 올려 위가 더 둥글다.
- 초록 바운스를 잡으려고 넣은 **따뜻한 기울기가 속살에도 걸려** 밑면만 분홍으로 떴다 → 위·아래가
  딴 물건처럼 갈리고 그 경계가 구멍 테두리로 읽혔다. `push(..., warm=false)`로 속살은 제외한다 —
  속살은 컵 안에 있어 애초에 초록 바운스를 안 받는다.

비용 불변: 1드로우 · 재질 1 · 삼각형 5,752. `npm run test:world` 15/15 PASS · draws 240 ·
조개 4종 회귀 없음(`wprofile` 미지정 시 기존 식과 동일).

### Fixed (🦪 굴 — 속살 봉합 + 검은 외투막 테두리 + 껍데기 짙은 무늬)
사용자 리포트 3건.
- **"속살 안쪽 뚫린 것처럼 보여"** — 실제로 뚫려 있었다. 속살은 렌즈 두 짝(위 봉긋·아래 얕게)인데
  `sin(πr)^0.66`이 첫 행 r=0.055에서 이미 0.31이라 **위·아래가 0.19R 떨어져** 있었고, DoubleSide로
  그 틈이 들여다보였다 → `(r−0.055)/0.18` 램프로 첫 행을 정확히 0으로 눌러 봉합.
- **"속살의 검은 테두리"** — 굴 속살을 알아보는 표식이라 셀 22에 넣었다. 자유변(셀 아래쪽)이 3D에서
  덩어리의 둘레고, 위·아래 렌즈가 같은 uv를 쓰므로 띠 하나가 적도를 한 바퀴 감는다. 프릴(물결 경계)
  + 바로 위 밝은 받침선까지 한 세트.
  ⚠️ **uv 폭 ≠ 3D 폭**: 렌즈에서 v 0~0.26은 얇은 테가 아니라 **넓은 고리**(반지름 0.74~1.0)다.
  0.26 폭에 숯빛을 깔았더니 속살 절반이 검은 치마가 되어 오히려 구멍처럼 보였다. 반지름 비가
  (0.34 + 0.66·r^1.10)이므로 **v 0~0.08 ≈ 반지름 0.94~1.0**이 진짜 테두리 폭 — 0.14로 좁혔다.
- **"껍데기의 짙은색 무늬"** — 셀 20에 자갈색·회자색 얼룩 7개 + 검은 반점 4개. 알파 0.10~0.22로는
  백악질에 묻혀 안 보였으므로 진하게·성기게(만화식 규율).
  ⚠️ 처음엔 방사 줄무늬(힌지→자유변)를 넣었는데 가로 박편과 교차해 **타탄체크**가 됐다 → 무늬 축을
  층 결 하나로 통일한다. 검은 반점도 원형이면 뚫린 구멍처럼 보여 가로로 늘였다.

그리고 속살이 컵 벽을 뚫고 나와 옆에서 **날카로운 V 홈과 검은 틈**으로 보이던 것도 같이 잡았다:
컵 안쪽은 반구가 아니라 **골(trough)**이다 — 가장 깊은 곳이 lateral 0.61R이고 힌지 쪽(0.30R)과
림 쪽(1.0R)에서 둘 다 h=0으로 올라온다. 큰 덩어리를 가운데 놓으면 힌지 쪽 벽을 뚫는다 →
0.70R·−0.28R → **0.58R·−0.12R**(골에서 가장 넓고 얕은 구간에 얹는다) + 아래 볼륨 0.15 → 0.10.

비용 불변: 1드로우 · 재질 1 · 삼각형 5,752. `npm run test:world` 15/15 PASS · draws 236.

### Changed (🦪 굴 — 이매패류니까 조개 골격을 그대로 물려받는다 · 아틀라스 셀 20~22)
사용자 리포트 "형태부터 질감까지 싹 다". 진단 7건이 전부 **조개 리모델에서 이미 폐기한 안티패턴**이었다:
뚜껑이 아래짝과 접점 없이 공중에 떠 있었고(rotateX 후 translate), 반구 껍질 하나라 두께 0 블레이드였고,
성장 골이 토러스 4개 스택(= 등나무 바구니 테두리)이었고, 속살은 완벽한 타원체(= 아이스크림 한 스쿱),
껍데기 안쪽면이 없어 '그릇'이 아니었고, 위에서 보면 표적/시나몬롤이었다. 그리고 해산물 8종은 전부
`gradMat`(정점색만, map 없음)이라 **질감이 나올 수가 없는 구조**였다.

`fcValveGeo`를 그대로 재사용한다 — 벽 두께·경첩선 폭(wmin)·부리·자유변 리브 감쇠·**벌림 = 힌지축
회전**(떠 있는 문제가 구조적으로 사라진다)이 전부 딸려온다. 굴만의 규율 3가지:
- **비대칭이 정체성**이다 → 두 짝의 rough 위상·깊이를 다르게(아래=컵, 위=얕은 뚜껑)
- **겉면과 안쪽면이 다른 셀**을 쓴다 → `faceAttr`로 정점마다 셀을 고른다(겉=백악질 박편, 안=자개+근육 자국).
  ⚠️ 병합 전에 `deleteAttribute` 필수 — mergeGeometries는 속성 집합이 같아야 붙는다
- **속살도 fcValveGeo로** 만든다 → 같은 힌지·같은 arc라 컵에 저절로 맞는다(구 조형은 별개 구체라 안 맞았다)

아틀라스는 예약돼 있던 셀 20~22를 쓴다 — **확장 없음**(2048×1024 그대로). 재질은 `fcMat` 공유 → 추가 0.
겉면 박편은 "얹힌 선"이 아니라 "겹친 판"으로 읽히도록 **위 모서리 밝게 + 아래 모서리 어둡게** 한 쌍으로
그린다. 96px 도감 아이콘 때문에 성기게(조개에서 배운 규율).

검수가 잡은 것들 — **넷 다 검수 랩에서는 안 보인다**:
- 컵 depth 0.88·arc 1.85는 폭보다 높이가 커서 세로로 선 **헬멧**이 됐다 → 폭 > 길이 > 높이 순서를 지킨다
- 속살 렌즈는 y=0(림 높이) 대칭으로 나오므로 그대로 두면 **컵 위로 솟는다**(컵에 얹힌 달걀) → −0.28R 안착
- 속살 rough 위상이 껍데기와 달라 크기가 컵에 가까우면 옆으로 삐져나온다(**흰 날개**) → 0.80·0.94 → 0.70·0.88
- 벽 두께 0.075R은 조개와 똑같이 **흰 개스킷 띠** → 0.045R. 림은 안쪽 셀 대신 **겉면 셀**로 돌린다
  (껍데기 단면은 실제로도 백악질이고, 자개 크림색이 겉면 회색과 부딪히면 플라스틱으로 읽힌다)

⚠️ **해저에서 껍데기가 진초록으로 떴다.** 정점 프로브로 원인을 특정했다(uv·텍셀 #d4cfc2·셀 20 전부 정상,
옆에 놓은 성게는 보라 그대로, 순백 기준구는 위가 하늘색) → 조형도 텍스처도 아니고 **아래를 향한 면적**이
원인이었다. 아래를 향한 면이 받는 빛은 hemiLight `groundColor 0x8fca62`뿐이고, 굴은 이 월드에서 유일하게
**중간 톤 무채색 반구를 아래로 향한 채** 놓이는 물건이었다. 세 갈래로 줄였다:
① 조개의 `0.84 + 0.18·dn` 규칙은 **아래짝의 아래 향한 면을 가장 밝게(1.02)** 만든다 — 얕은 조개에선
   안 보이지만 깊은 컵에선 치명적이다 → 월드 기준 톱라이트로 교체
② 컵 depth 0.52 → 0.36 + 굴만 0.10R 파묻기(실물 굴도 해저에 가라앉아 있다) — 아래를 향한 면적 자체를 줄인다
③ 남은 색조는 밝기를 눌러도 안 빠진다(조명이 곱해지므로) → 아래를 향할수록 정점색을 **따뜻하게** 기울여
   초록을 올리브·황갈로 끌어온다. 전역 조명은 건드리지 않았다 — 같은 모래섬 모래성 벽도 세이지색이다.

비용: **1드로우**(구 조형과 동일) · 재질 1(fcMat 공유) · 삼각형 816 → **5,752**(조개 6,032~6,740보다 아래).
해저 동시 표시는 최대 4개(`seaSpawns.length < 4`)라 여유가 크다. 검수 훅 `__worldDev.seaMake(id, scale, x, z)`
추가(fishMake와 같은 문법 — 좌표를 주면 실제 해저 조명 아래 놓는다).
`npm run test:world` 15/15 PASS · draws 236 · 조개 4종 회귀 없음.

### Fixed (🐚 힌지 쐐기 — 벌림이 아니라 **폭이 깊이에 묶인 것**이 원인이었다)
직전 커밋에서 "진주조개를 눈높이에서 보면 힌지 쐐기가 어둡게 보인다 → 벌림을 0.32로 줄이면 가려진다"고
남겼는데, 사용자 지적이 둘 다 맞았다:
- **벌림을 줄이면 진주가 가려진다.** 진주 지름 0.50R vs 앞쪽 틈 ≈ `open × 1.245R` → 0.40에서 딱 맞는다.
  0.32면 0.40R로 좁아져 진주를 먹는다. 진주는 이 종의 정체성이라 줄이면 안 된다.
- **진짜 원인은 힌지 쪽이 좁은 것**이었다. `x = rad·sin(ph)`로 **폭이 깊이(rad)에 묶여** 있어 부채가
  파이 조각이 되고 힌지에서 한 점으로 수렴했다. 그 수렴점이 벌어진 틈으로 검은 쐐기로 보인 것이다.

폭 프로파일을 깊이와 분리했다(`wmin`·`wpow`): 힌지에서도 최대폭의 30%를 유지한다. 힌지 행은 **넓고
z가 눌린 직선**이 되어 두 짝이 그 선 전체에서 맞물린다 = 진짜 경첩선. **벌림은 0.40 그대로 두고**
쐐기만 사라졌고, 진주는 8방위 전부에서 보인다.
⚠️ 튜닝 폭이 좁다: wmin 0.42·wpow 0.62는 중간 깊이에서 이미 최대폭 80%라 실루엣이 **드럼통**이
됐다(실측). 0.30·1.15가 경첩선을 주면서 타원을 유지하는 값. 기본값 `wmin 0.055·wpow 1`은 옛 파이
형태와 정확히 같아서 가리비 귀 등 다른 호출부는 그대로다.
`npm run test:world` 15/15 PASS · draws 236.

### Changed (🐚 조개 4종 — "어설프다" 7건 + 검수가 잡아낸 실물 버그 4건)
표면 디테일은 이미 충분했다. 어설프게 보인 진짜 이유는 **부채 3종이 실루엣 계보가 같아서**였다:
`arc`가 1.16·1.34·1.28 rad(66°·77°·73°)로 사실상 동일해 세로로 길쭉한 사다리꼴이 되고, 세로 리브가
얹히면서 셋 다 **주름 종이컵**으로 읽혔다. 종을 가르던 건 리브 개수와 색뿐이었다.

- ① **arc를 벌렸다** — 2.15 / 1.95 / 1.80 (실물 가리비 부채는 120~140°). 코너 컷 지수도 3.4 → 2.2로
  낮춰야 넓어진 양끝이 각지지 않는다. 이 한 줄이 가장 크게 바꿨다.
- ② **부리(umbo)를 만들었다** — 이매패류 식별의 핵심인데 힌지가 한 점으로 수렴하고 있었다.
  ⚠️ h로 들면 두 짝이 뒤에서 벌어지고, r 시작값을 키우면 렌즈 h가 0에서 멀어져 같은 문제가 된다 →
  **z로만** 민다(안쪽 절반을 −z로 넓게). 이음면 불변식을 안 깨는 유일한 축이다.
- ③ **소라 첨탑 복원** — 축 상승 1.02는 "스프링" 리포트의 **과교정**이었다. 층은 붙었지만 평평하게
  감긴 달팽이가 되어 옆에서 강낭콩이었다 → 1.90. 층 이탈은 봉합 클램프의 y 항(0.25→0.45)이 잡는다.
  입술은 t>0.93·1.7 → t>0.87·1.0(0.80·2.3은 체층을 삼키는 나팔이 됐다).
- ④ **벌림 축소** — 0.34/0.26/0.88 → 0.20/0.13/0.40. 진주조개 0.88 rad(50°)은 열린 게 아니라 **반으로
  쪼갠** 것으로 보였다.
- ⑤ **자유변에서 리브 감쇠** — `r^1.15`로 가장자리에서 최대였던 걸 `×(1−r^5)`로 죽였다(가리비만
  지수 12로 약하게 = 파상 변 유지). 옆에서 **곰덫**처럼 보이던 톱날이 사라졌다.
- ⑥ **진주조개 마감** — rough 진폭 최대 10% → 3%(찢어진 천), 자개 띠 8줄×선폭26 → **3줄×18**
  (해변 크기에서 진흙 얼룩이 됐다), 진주를 원점 → **아래짝 안쪽 바닥**(y −0.24R·z +0.20R)에 앉혔다.
- ⑦ **색 대비** — 가리비=주황 크림 / 분홍조개=진분홍 / 소라=명암 폭 확대. 4종이 다 파스텔이라
  해변 원경에서 구분이 안 됐다.

그리고 검수 렌더가 **실물 버그 4건**을 잡았다 — 넷 다 검수 랩에서는 안 보이는 것들이다:
- **소라가 거꾸로 서 있었다.** `cy`가 th와 함께 커져서 큰 체층이 위, 좁은 첨탑이 바닥에 깔렸다.
  축 상승을 1.02로 눌러 뒀을 땐 수직 폭이 작아 안 보였고, ③으로 되올리자 드러났다 → 부호 반전
  (`YTOP − R0·RISE·gr`). 이전 층이 이제 **위**에 있으므로 봉합 클램프의 `Math.max` 방향이 맞다.
- **첨탑이 머핀 위 작은 왕관**이었다. K=0.115는 층당 2.06배·총 10배라 마지막 한 바퀴가 부피를 다
  먹는다 → **층당 1.60배(K=0.075)·3.6바퀴**. 융합 조건은 `exp(2πK) < 2.70`, RISE 상한은 ≈2.15
  (그 위로는 층 간격이 단면 지름보다 벌어져 다시 스프링).
- **힌지에 검은 송곳** — 지수 0.72가 r=0 근처에서 너무 빨리 솟아 면이 한 점으로 모이며 원뿔이 됐고,
  크게 벌어지는 진주조개 안쪽에서 드러났다 → `min(1, r/0.18)` 램프로 안쪽 18%를 이음면에 눌러
  붙였다. 그 납작한 구간이 그대로 **경첩**으로 읽힌다.
- **아래짝이 통째로 모래에 묻혀 있었다.** 검수 랩은 조개를 공중에 띄우지만 월드는 origin을 지면에
  놓는다(`terrainHeight + 0.005`) → 렌즈가 y=0 대칭이라 위짝만 지느러미처럼 남았다. 소라도 같은
  이유로 파묻혀 있었다 → 병합 후 bbox로 **바닥을 y=0에 정렬**(진주 발광구도 같은 양 이동).
  ⚠️ 구 조형(눌린 구체)은 반쯤 묻혀도 자연스러웠기 때문에 이 버그가 여태 숨어 있었다.

부수 수정: 소라 적도의 **이끼 낀 타이어 같은 어두운 띠** — 두 짝짜리 판용 자개 밝기 규칙(수평 법선
→ 0.84)을 통짜에 적용한 탓이다 → `solid` 분기로 톱라이트만. 가리비 귀는 `arc*0.5+0.30`이라 부채를
넓힐수록 안으로 파묻혔다 → 회전을 부채 폭과 분리해 1.50 rad 고정. 벽 두께는 0.030R(깨진 달걀
껍데기) ↔ 0.085R(고무 개스킷 띠) 사이 **0.055R**.

비용은 그대로다: **종당 1드로우**(진주만 2) · 재질 1개 · 삼각형 6,032~6,740(소라만 +8%, 층 세그먼트
증가분). 밑면의 초록 색조는 조개 문제가 아니라 월드 공통 `hemiLight` groundColor(`0x8fca62`)다 —
같은 모래섬 모래성 벽도 세이지색이다. 검수 랩은 백색 앰비언트라 안 보인다. 손대지 않았다.
`npm run test:world` 15/15 PASS · draws 236.

### Changed (🐚 조개 4종 — 카툰 패턴 + 벽 두께 + 나선 융합)
사용자 검수 3건을 반영했다:
- **"패턴이 너무 촘촘하다"**: 이 게임 스타일 대비 과했다 → 리브 11·16·9 → **8·10·6**, 성장 아치 9→5,
  성장띠 13→6, 소라 화염 30→12·층경계 7→4, 자개 간섭색 22줄→**8줄(선폭 9→26)**. 성기게 갈수록
  골은 **깊게**(리브 능선 0.052→0.070) 해야 카툰식으로 읽힌다.
- **"조형이 종잇장 같다"**: 판이 두께 0인 면 하나라 자유변이 무한히 얇았다 → 겉면 → r=1에서 림 →
  안쪽면(역순)을 **한 지오메트리로** 짜서 닫힌 껍데기로. ⚠️ 이때 자개 밝기 판정을 지오 단위로 두면
  위짝 전체가 밝아지고 **림이 흰 개스킷 띠**로 보인다 → **면 방향(normal·outward)으로** 판정한다.
- **"소라는 틈 때문에 스프링 같다"**: 층이 떨어져 있었다 → 안쪽면을 이전 층 겉면에 **클램프**해
  봉합선을 만들고, 축 상승을 1.62→1.02로 낮춰 세로로도 붙게 했다.
- **"리브가 아코디언 같다"**: 리브를 **순수 사인**으로 두면 골이 V자로 패여 주름이 된다 →
  양(능선)은 넓게·음(골)은 좁고 얕게(`v>0 ? v^0.5 : −(−v)^1.75×0.55`) = 둥근 리브 + 가는 골.
- **"조개 이어지는 부분이 떨어져 있다 — 앞만 벌리면 되잖아"**: 원인은 벌림이 아니라 **h가 힌지에서
  0이 아닌 것**이었다(r=0.055에서 이미 0.16R 부풀어 있었다). `sin(πr)^0.72`로 바꿔 힌지·자유변
  모두 0인 렌즈로 만들고, 벌리는 건 **힌지축(x축, z=−0.245R) 회전으로만** 한다 — 평행이동을 섞으면
  뒤가 뜬다. 리브도 `r^1.15`로 힌지에서 모이고 자유변에서 강해진다.
- 림의 정점 법선은 겉·안 사이로 스무딩되므로 자개 판정 임계가 느슨하면 **림이 밝은 파이핑**이 된다.

### Changed (🐚 조개 4종 1차 — 종별 시그니처를 실루엣에 넣었다 + 아틀라스 2048×1024)
구 조형은 눌린 구체 + 붙인 토러스·구슬이라 리브·성장융기·나선이 **표면에 얹힌 장식**으로 읽혔다.
조개는 상시 표시물(해변 3 + 무인도 1~3)이라 **드로우·재질이 예산이고 삼각형은 여유가 크다** —
실측 씬 515드로우 중 조개 몫은 2~3개(0.5%)다. 그래서 삼각형은 528~910 → 4,864~6,260으로 올리고
**종당 1드로우를 지켰다**(진주만 2: 발광 진주).

- **아틀라스 1024² → 2048×1024**(8MB, 셀 256² × 8열 × 4행 = 32칸). 16~19 조개, 20~27 해산물 예약.
  ⚠️ 비정사각이라 **u·v 스케일이 다르다**(`FA_KU`/`FA_KV`) — 하나로 쓰면 세로가 늘어난다.
- **🐚 가리비**: 부채꼴 극좌표 판 + 힌지 귀. 리브를 **반지름과 높이에 같은 주기로** 변조해 파상 변과
  리브 음영이 일치한다(따로 넣으면 무늬 스티커로 보인다). 위짝 16° 젖혀 자개 속이 보인다.
- **🐚 소라**: 구슬 스택 → **연속 로그 나선 튜브**. 단면은 물방울(내측 눌림) + 층 어깨 돌기 열.
- **🐚 분홍조개**: 하트형 새조개. 성장 융기를 토러스 링 대신 **높이 필드**로(`grow(r)`), 얕은 방사 골 16.
- **🫧 진주조개**: 자개 간섭색 띠(청록→분홍→금)가 정체성. 굴처럼 거친 비대칭 변, 위짝 62° 젖힘.
  발광 진주는 **아귀 초롱 재질을 재사용**해 재질 총량을 유지한다(전용 재질 하나 제거).

**함정 4개**:
- **리브당 각도 샘플이 6개 미만이면 리브가 패싯이 된다**(52샘플 ÷ 14리브 = 3.7 → 아코디언 천).
- **아웃라인 리브 변조는 아주 얕게**(0.048 → 0.013). 크면 자유변이 톱니가 된다.
- **리브 능선을 자유변까지 살리면 리브 끝마다 뾰족한 삼각**이 된다 → `sin(πr)`로 양끝에서 죽인다.
- 거친 변을 **해시 스텝**으로 만들면 불연속이라 찢긴 천이 된다 → 저주파 사인 합(연속·불규칙).
- 소라 성장률 K가 크면 1.5바퀴만 보여 바나나가 된다(0.36 → 0.115 = 3.2바퀴·총 10배).
  개구부 플레어도 3.2배면 매끈한 덩어리가 붙은 것처럼 보인다(→ 1.7배).

### Fixed (🐟 잡은 물고기 지오메트리 누수 + 죽은 구 빌더 제거)
사용자 질문("물고기는 잡힐 때만 뜨는데 그래도 발열에 영향 있나?")을 계측해서 답을 확인했다 —
**거의 없다.** 그리고 그 계측이 진짜 버그를 찾아냈다.

계측: 잡은 물고기는 화면에 **한 마리, 약 2초**만 존재한다(절친이 같이 낚으면 최대 2마리).
생성 비용은 종당 **0.3~2.3ms**(개구리 2.34가 최악 — 발가락 20개), 아틀라스 최초 굽기 **6.7ms**로
전부 한 프레임(16.7ms) 안이라 히치가 없다. 도감 아이콘은 오프스크린 렌더 후 dataURL이라 씬에
들어오지도 않는다. **즉 "드로우 7~19 → 1"은 사실이지만 2초 창에만 적용되는 값이라 발열 근거로는
과장이었다. 어종 리모델의 실제 정당성은 조형 품질이다.**

- **지오메트리 누수 수정**: `scene.remove(f.fishMesh)`가 4곳에 있는데 `geometry.dispose()`가
  없었다(이 저장소는 다른 곳에서 dispose를 31번 쓴다 — 관례가 아니라 누락이다). 낚을 때마다
  지오메트리가 VRAM에 남고, 카툰 리모델로 개체당 크기가 2~4배 커져 누수가 커졌다.
  `disposeTempMesh()` 추가 — **재질은 공용(fcMat·fcGlassMat·fcGlowMat)이라 절대 dispose하지 않는다.**
- **구 빌더 본문 170행 제거**: 13종 전부 카툰으로 라우팅돼 도달 불가 코드가 됐다. 등록 누락을
  조용히 삼키지 않도록 함수는 throw로 남긴다.

**교훈**: 노출 패턴을 먼저 확인해야 한다. 상시 표시물(조개·지형)은 드로우가 중요하고,
순간 표시물(잡은 물고기)은 **생성 시간과 정리**가 중요하다. 같은 지표로 둘을 평가하면 틀린다.

### Changed (🐸🥾🩻🐟 남은 4종 — 골격을 갈아엎었다)
9종은 진짜로 "축 하나를 따라가는 방추형"이라 튜브 빌더가 맞았지만, 남은 4종은 **하나도 그게 아니었다.**
그런데 방금 튜브 빌더로 성공했으니 그걸 계속 집었다 — 초반에 진단한 "최근에 통한 도구를 계속 집는"
실패가 그대로 재발한 것이다. 4종 다 골격부터 다시 만들었다.

| 종 | 실제 골격 | 잘못 쓴 것 | 결과였던 것 |
|---|---|---|---|
| 헌 장화 | 공업 제품 | 유기물 튜브 + L자 척추 | 엘보 파이프 |
| 가오리 | 납작 원반 | 로프트(맞음) + 틀린 수식 | 종이 다트 |
| 개구리 | 사지 있는 네발짐승 | 회전체 튜브 | 잎사귀 + 말뚝 4개 |
| 아귀 | 입이 정체성인 물고기 | 튜브 + **칠한** 입 | 올챙이 + 립스틱 |

- **🥾 헌 장화 = 기계 문법**: 통(정점 변위 튜브)·발·밑창을 직접 로프트. 단면은 슈퍼타원(둥근 사각).
  구김은 **실루엣이 흔들려야** 구겨져 보이므로 정점 변위 — 발목 아코디언 접힘(아래에서 강하고 위로
  감쇠)·세로 구김 3·5주기·앞으로 처짐·입구가 타원으로 눌림. 텍스처 구김 음영은 **같은 주기**로 맞춰
  그림자가 실루엣과 붙는다. 흙·물때·이끼·긁힘·트레드에 낀 흙은 텍스처. 통은 뒤꿈치 위에 선다.
- **🩻 가오리 = 공용 로프트 빌더 신설**(`fcLoftGeo` — 위 아웃라인 w(t) + 상하 높이 필드). 개구리와
  같은 골격("위에서 본 실루엣이 정체성")이라 빌더를 공유한다. 앞전 볼록·뒷전 오목·날개끝은 진짜 점,
  정지 자세의 날개끝 들림, 눈두덩·분수공은 **몸에서 이어진 융기**(fcBump)에 칠하기, 배지느러미 1쌍,
  꼬리 기부 0.052L→0.006L 테이퍼 + 가시 5.
- **🐸 개구리 = 로프트 + 관절 사지**: 위 아웃라인이 둥근 삼각형(최대폭이 눈·어깨 t=0.30). 뒷다리는
  **무릎이 등선 위로** 접히고(수직 말뚝 금지), 발은 납작 패드 + 부챗살 발가락 + 물갈퀴 막.
  눈은 융기 위에 칠한다(금색 홍채 + 가로 동공). 셀 9 하나를 상하로 나눠 등/배를 담는다.
- **🐟 아귀 = 턱을 지오메트리로**: 머리 폭 0.68L(체고의 3배)·s 0.32에서 급격히 좁힘, 아래턱을 별도
  얕은 보울로 매달아 그 사이가 실루엣에 걸리는 구강이 된다. 림에 원뿔 이빨 2열, 팔처럼 큰 가슴 패들,
  피부 프릴 5.
- **어종 13종 전부 실게임 적용 완료** (드로우 1~2). 종별 크기는 `0.44 × (실측cm/25)^0.45`.

**새 함정 5개** (전부 실측):
- `RoundedBoxGeometry`는 **non-indexed** — 인덱스 있는 파츠와 `mergeGeometries`가 에러 없이 **null을
  반환**하고, 렌더 시점에 `morphAttributes of null`로 터진다. 직접 로프트로 해결(형태도 좋아졌다).
- 아틀라스 셀을 **밴드별로 칠하면 안 칠한 밴드가 흰색으로 남는다**(초기값이 흰 캔버스) — 셀 전체를
  바탕색으로 먼저 채운다.
- 로프트 높이 필드는 **|u|=1에서 0으로 떨어져야** 한다. 안 그러면 수직 테두리를 가진 슬래브가 되고
  옆에서 보트로 읽힌다(`fcUf`).
- 로프트 **두께는 중앙에 질량**이 있어야 한다. 선형항 곱만 쓰면 몸통까지 얇아져 종이가 된다.
- 국소 융기가 **등 정점보다 높으면** 정면에서 쌍봉 + 가운데 골이 된다(개구리 눈두덩).
- 밑창을 상자로 두면 모서리가 튀어나와 스케이트보드가 되고, 아래턱 처짐이 크면 펠리컨 주머니가 된다.

### Changed (🐟 어종 조형 카툰 리모델 — 지오는 실루엣만, 색·무늬는 텍스처)
구 어종 13종은 프리미티브 합성(구체 몸통 + 원뿔 꼬리 + 구체 눈)이라 **13종이 전부 좌우가 위아래보다
넓었다**(체고÷체폭 0.73~0.85). 실물 어류는 1.5~2.5다. 원인은 "한 번도 렌더해 본 적이 없어서"였고,
증거로 복어 가시 9개가 전부 `z=-0.01`에 몰려 허리 링이 되어 있었다(옆에서 보면 가시가 안 보인다).
동숲류가 저폴리인데 자연스러운 이유를 따라간다 — **지오메트리는 실루엣만, 나머지는 전부 그림.**

- **아틀라스 1장(1024² = 256² 셀 16개)**: `u`=둘레(이음선은 배 용골 — 가장 안 보이는 자리) ·
  `v`=체장(머리 우선 워프). 눈·아가미선·비늘·무늬가 전부 캔버스 2D 그림이라 실루엣을 안 건드린다.
  재질은 `vertexColors × map` 하나 — 정점색은 **형태 음영만**, 색·무늬는 텍스처 몫.
- **지느러미는 회색조 공용 1셀** + 종별 색은 정점색으로 착색 → 셀 12개 절약.
- **드로우**: 종당 7~19 → **1**(유리병만 2). 붕어 삼각형 9,726(정점색안) → 1,140.
- **적용 9종**: 붕어·금붕어·잉어(홍백)·고등어·연어·빙어·메기·복어·유리병.
  개구리·헌 장화·가오리·아귀는 구 빌더 유지 — 조형 재작업 대기.
- **종별 크기 도입**: 구 빌더는 종에 무관하게 z 0.45~0.55였다. 실측 cm를 쓰되
  `0.44 × (cm/25)^0.45`로 완화 — 빙어(11cm) 0.30 ~ 연어(62cm) 0.67, 2.3배 폭.
  그대로 쓰면 빙어가 점이 되고 연어가 펫보다 커진다.
- **복어 가시 버그 수정**: 허리 링 → 피보나치 구면 분포 56개(배쪽이 더 길다).

**규약 5개** (종 추가 시 이것만 채우면 된다):
① 프로파일 표는 체장 1 기준 정규화 — `hw` 열이 곧 단면 종횡비 조절기다(메기 머리 0.73:1 납작 넓음 →
꼬리 3:1 장어형이 이 열 하나로 나온다. 별도 aspect 함수 불필요) ② 아틀라스 셀 + 머리 우선 v 워프
③ 무늬는 만화식 — 굵고 성긴 초승달 비늘(촘촘하면 96px에서 노이즈, 원경에 모아레)
④ 지느러미는 공용 회색조 셀 + 정점색 착색 ⑤ 정점색은 형태 음영·지느러미 착색만.

**함정 6개** (전부 실측으로 물린 것):
- **눈 반지름은 체장 비율로 authoring**. 월드 단위로 쓰면 체장이 작은 종에서 폭발한다(빙어 눈이 셀 폭의
  59% → 머리가 검은 덩어리).
- **정점색 셰이딩은 uv를 아틀라스 좌표로 덮어쓰기 전에** 해야 한다. 뒤에 하면 엉뚱한 값을 읽는다.
- **측면에서 보이는 건 등이 아니라 옆구리다.** 단면 상단이 뾰족하면(`edge` 큼) 등날 근처 u 0.31~0.5가
  화면에선 얇은 띠로 눌린다 → 등이 어두운 종은 램프 전환점을 옆줄 위치(u 0.18~0.24)까지 내려야 한다.
- **물결 무늬 진폭이 줄 간격보다 크면** 이웃과 교차해 X자 낙서가 된다(고등어 23줄: 진폭 0.0072 / 간격 0.0311).
- **유리는 알파를 텍스처가 쥐어야 한다.** 재질 `opacity`를 낮추면 반사광까지 같이 눌려 젖빛 플라스틱이
  된다 → 맵 알파로 몸통 0.17 / 하이라이트·림 0.96. `transmission`은 별도 렌더패스라 금지(발열).
  `envMapIntensity`도 2.2면 곡면 전체가 균일하게 밝아져 우윳빛이 된다 → 0.75.
- **척추를 꺾을 때 단면 링도 접선에 수직으로 회전**시켜야 한다. 위치만 옮기면 통이 납작한 리본이 된다(장화).

`?sealab` 격자에서 4각도 + 96px 아이콘으로 검수한다(`?sealab=c-koi` 식으로 접사). 검수 게이트는
`npm run test:world`(draws ≤ 250) — **한 번도 안 본 종은 반드시 틀린다.**

### Changed (🧊 아이스박스 마감 — 진짜 힌지 + 속이 보이는 얼음)
- **뚜껑**: 주석은 "뒤 힌지"였지만 실제로는 뚜껑 판을 제 중심에서 −0.3rad 기울인 것뿐이라
  열린 게 아니라 **떠 있는 판**으로 읽혔다 → 뒤 모서리를 축으로 회전 + 힌지 금구 2. 얼음·생선도
  몸통 윗면보다 위에 떠 있던 것을 안쪽으로 내려 **담긴 것**으로 보이게 했다.
- **사용자 검수 2건**:
  - 옆면 `🐟 FRESH` 스티커 제거 — 클릭 판정도 상태 표시도 없는 **순수 장식**인데 캔버스
    텍스처 + 전용 재질로 드로우를 하나 먹고 있었다.
  - 얼음이 **불투명 하늘색**(`M(0xe8f8ff)`)이라 조각이 뭉쳐 젤리 한 덩어리로 읽혔다 →
    **반투명**(opacity 0.66 · roughness 0.05)으로 바꾸고 조각을 5 → **7알**, 크기·각도·간격을
    흩어 개체가 갈라져 보이게. 뒤의 생선이 얼음 너머로 비친다.
- ⚠️ 프로브 교훈: 밤에는 **낮 라이트가 `scene.remove` 돼 있어** 기존 조명 intensity를 올려도
  소용없다 — `_prop-shot.mjs`에 `lit` 모드(프로브가 직접 라이트를 단다)를 넣어 형태 검수.
- world-smoke 15/15.

### Changed (🚀 별똥호 조형 업그레이드 — 표면 위계·엔진·핀·그리블)
- **A 표면 위계**: 민무늬 Lathe 하나라 "크림색 병"으로 읽혔다 → **패널 이음매 링 3 + 리벳 54**
  (18×3) + 세로 패널 분할 6 + 엔진 슈라우드. 정점색·기존 버킷이라 드로우 0.
- **B 엔진**(가장 허전했던 곳): 벨 바깥 **냉각 채널 16줄** + 벨 립 2 + **목(throat)** + 짐벌 조인트 +
  터보펌프 2 + 배관.
- **C 핀**: 베벨 없는 압출(두께 0.045)이라 골판지로 읽혔다 → **베벨 에어포일 단면**(중앙 두껍고
  앞뒤 얇게) + 보강 리브 2 + 뿌리 필렛 + 팁 캡. 발은 **쇼크 업소버 2단 + 접지판**.
  ⚠️ 핀이 착륙 다리를 겸하는 틴틴 문법은 캐릭터의 핵심이라 유지(별도 랜딩 레그 추가 안 함).
- **D 재질 대비**: 전부 매트라 평평했다 → 노즈콘 **클리어코트 광택** + 상단 탱크 **단열 밴드**
  (주름 리브 24). ⚠️ **`gradMatFoil`(metalness 0.92)을 넓은 원통에 쓰면 어두운 RoomEnvironment만
  반사해 "검은 띠"가 된다**(실측) — 작고 구겨진 간식 파우치엔 맞지만 로켓엔 못 쓴다. metalness 0인
  광택 버킷으로 옮겨 밝은 은백으로 해결. 버킷은 +1개(gloss)만 늘렸다.
- **E 그리블**: **RCS 스러스터 4**(십자, 노즐 포함) + **접힌 그리드 핀 3**(프레임 + 격자 살 + 힌지).
- **G 캐빈**: 통유리는 스케일이 안 읽혀 **창틀 멀리언 6** + 외부 핸드레일 2 + 유리를 클리어코트로
  (roughness 0.15→0.05, envMap).
- F(마킹)·H(그을음)는 사용자 지정으로 이번 라운드 제외.
- 3,040 → **8,436 tris** · 메시 10 → 11(라이브 오브젝트라 재질 1개 = 드로우 1개) · **섬 239 draws** ·
  world-smoke 15/15.

### Changed (🚀 발사 플랫폼 리뉴얼 — 반경 2.05·갑판 -0.18 고정, 그 위를 전부 다시)
- **P0 발사 인프라**(가장 큰 결손 — 로켓 엔진 벨이 **나무 갑판 위에 그냥 앉아 있었다**):
  갑판 중앙 **화염 편향판**(개구부 + 강철 커브 + 그레이팅 5) + **홀드다운 클램프 3**
  (⚠️ 4개가 아니라 3개 — 로켓 발이 반경 0.47에 120° 간격이고 `heading`까지 같은 식으로 계산해
  정확히 물린다) + **선체 측면 배기 벤트 2**(화염 트렌치가 옆으로 빠진다는 신호) + 위험 빗금 12
  + `PAD 01` 명판. **엄빌리컬 배관 2줄 + 커넥터**는 갠트리 암의 자식이라 카운트다운에 **같이 젖혀진다**.
- **P1 갠트리**: 다리 2 → **4다리 정사각 트러스**(옆에서 보면 납작한 2D 사다리였다). 6단 가로대
  ×4면 · X브레이스 8 · **2단 정비 플랫폼**(캐빈 높이 2.32 + 상단 3.06) + 토레일 · 사다리(레일 2 +
  발판 13) · 케이블 트레이 · 베이스 슈 4.
- **P2 바지선다운 선체**: **흘수선 스트라이프** · 선체 리브 8 · **로프 방현재 8** · **불워크**
  (섬 쪽 승선 구간만 비움) + 레드 캡레일 · **볼라드 4**(기존 클리트는 너무 작아 스케일 단서가 안 됐다).
- **P3 계류·접근**: 계류 체인 3(늘어짐 4토막) + 부표 2 · **승선 사다리**(펫은 헤엄쳐 오므로 실제로
  필요) + 그랩레일 · 구명부표(불워크에 세워 건다) · 소화 캐비닛.
- **P4 조명**: **갑판 유도등 8 = 4위상 러닝** — 달·정거장 패드와 `padLightMats`를 **공유**해 점멸
  코드 0줄로 통일(⚠️ 발사대가 이 파일에서 가장 먼저 쓰므로 선언을 그 위로 올렸다) + 갠트리 꼭대기
  **항공장애등 점멸**.
- ⚠️ 실측 함정: 불워크 판자를 `rotateY(-a)`로 놓으면 **방사 방향으로 서서 말뚝 울타리**가 된다
  (접선은 `-a - π/2`). 체인은 `lookAt` 행렬 대신 쿼터니언 정렬이라야 허공의 점선이 안 된다.
- 드로우: 정적부는 월드 베이크에 흡수(다크 버킷도 gradMat 정점색으로 접어 버킷 0 증가), 라이브는
  유도등 4 + 비콘 1 → **섬 232 → 235 draws**(상한 250). world-smoke 15/15.
- **2차: "가장 실제 발사대처럼" 라운드** — 표면 디테일(패널·기계류)만으로는 발사대가 되지 않는다.
  진짜 발사대의 시그니처는 **로켓이 갑판에 붙어 있지 않다는 것**과 **피뢰침**이다.
  - 🏗️ **발사 마운트 승강(P0, 핵심)**: `ROCKET_PAD_DECK_Y` 하나가 "펫이 걷는 갑판"과 "로켓이 앉는
    높이"를 겸하고 있었다 → **`ROCKET_MOUNT_Y = DECK_Y + 0.42` 신설로 분리**(겸하면 로켓을 띄우는
    순간 펫이 공중을 걷는다). 로켓 초기 y·착륙 클램프·점화/증기 임계값·갠트리 암(캐빈 바닥)·착지
    먼지 링을 마운트 기준으로 옮기고, **펫 보행·하선 좌표는 갑판 기준 그대로** 뒀다. 그 위에
    강철 마운트(다리 4 + 상판 고리 + 크로스 빔 4 + 접근 계단 4단)를 세우고 홀드다운 클램프 3을
    갑판에서 **마운트 상판**으로 올렸다. 화염 덕트는 엔진 지름의 **3.6배**로 넓히고 유도판을 옆으로 텄다.
  - ⚡ **피뢰침 3주 + 카테너리(P1)**: 높이 4.6 마스트 3개 + 늘어진 와이어 3줄 + 침. 실루엣만으로
    "발사장"이 읽힌다. 꼭대기 점멸등은 갠트리 비콘과 **한 메시로 병합**해 드로우 증가 0.
  - 💧 **음향 억제 물분사(P2)**: 마운트 둘레 노즐 링 10 + 급수 탱크 + 굵은 배관 2구간.
  - ⛽ **추진제 구형 탱크 2(P3)** + 크래들 4 + 마운트로 가는 배관.
  - 🔧 **갑판 패널·기계류(P4)**: 방사 이음매 16 · 동심 링 2 · 점검 해치 2 · 매니폴드 · 밸브 스탠드 ·
    케이블 릴. 갠트리는 로켓이 0.42 올라간 만큼 다리 3.35→3.85, 플랫폼·사다리·케이블 트레이 동기.
  - P5(2단 갑판·화염 피트)는 **의도적으로 생략** — 마운트 승강이 이미 단차를 만들었고 갑판이
    기계류로 찼다. 보행 훅을 반경별로 쪼개는 위험만 남아 이득이 없다.
  - 검증: `rocketState().y = 0.24`(= MOUNT_Y) · 갑판 보행 -0.18 불변 · **섬 238 draws** · smoke 15/15.

### Changed (🌕 꼬마 달 지형화 + 착륙 패드 신설 — R7만 고정)
- **P0 지형**: 크레이터가 표면에 붙인 **원반 스티커**(CircleGeometry + Torus 림)라 깊이가 0이었다 →
  구를 30×22에서 **96×64로 올리고 정점을 반경 변위**해 진짜 그릇으로 판다(대형 3 R1.7~2.5 ·
  중형 9 · 소형 18, 그릇 −R·0.2 + 림 +R·0.095). **미세 기복 2옥타브**(사인 노이즈 ±0.068)로
  민무늬 반죽 표면에 자잘한 요철을 깔았다 — 실루엣(림)까지 울퉁불퉁해진다.
  ⚠️ **조형과 보행 표면이 같은 함수(`moonSurfOffset`)를 공유**한다(화물 상자 교훈). 안 그러면
  펫이 크레이터 위를 떠서 지나간다. **패드 발밑(dot≥0.955)은 기복을 0으로 감쇠** — 데크가
  평면이라 지형이 울렁이면 펫이 데크를 뚫는다.
- **P1 착륙 패드 신설**(조형이 **0개**였다 — `MOON_PAD_N`은 크레이터를 비우고 로켓 좌표를 계산하는
  데만 쓰였고 로켓이 맨 언덕에 앉았다): 8각 데크(윗면이 정확히 R이라 `padPos` 불변) + 그레이팅 +
  ⊕ 타깃 + 데크 마킹 14 + 앵커 다리·클램프 4 + **유도등 8×4위상**(정거장과 같은 `padLightMats` →
  도킹 중 가속도 공유) + 관제 스탠드(쿨 스크린) + 보급 컨테이너 2 + MOON-1 명판.
  ⚠️ **데크는 평면, 그 밖은 구면** — 곡률 보정(`pDrop`) 없이 놓은 소품은 0.19~0.31 떠 있었다.
- **P2 지질 정점색**: y축 그라디언트 하나 → **고지대/마레(3곳, 첫 곳은 앞면 고정 — 랜덤이면 셋 다
  뒷면에 몰려 앞은 민무늬 회색 공이 된다) + 크레이터 바닥 어둡게·림 밝게 + 대형 크레이터 광조(ray)**.
- **P3 생활감**: 레골리스 30(밝으면 진주알로 읽혀 어둡고 납작하게) · **아폴로 문법 깃발**(달엔 바람이
  없어 **가로 지지대**로 편다 + 주름 3단 + 베이스 플레이트) · **패드→깃발 발자국 13쌍**(대원호 보간) ·
  보물 마커에 삽 추가 + 지형 변위 위로 재배치.
- **P4 지구조**: 전용 재질 `poiMoon`(roughness 1 · emissive 0x11151f) — 그림자면이 새까맣게 죽지 않고
  마레가 드러난다.
- **후속 2건(사용자 리포트 "가까이서 저화질 그림 같다 · 깃발은 흩날리는 삼각 페넌트로")**:
  - 🪨 **저폴리 플랫 셰이딩으로 전환** — 뭉개짐의 원인은 스무스 셰이딩 + 고주파 노이즈 조합이었다.
    가까이 가면 정점 보간이 형태를 죽여 저해상 그림처럼 보인다. 구를 96×64 → **72×46 + `toNonIndexed`**로
    바꿔 **면마다 각진 법선**을 만들고 **색도 면 단위**(삼각형 3정점에 같은 값)로 넣어 보간을 끊었다.
    고주파 옥타브 삭제 + 기복 0.055→0.034, 면 색 지터 0.26→0.12(플랫에선 얼룩으로 읽힌다).
    크레이터 단면은 포물선 그릇 → **평평한 바닥 + 가파른 벽 + 좁고 높은 림**(R·0.22 / R·0.115),
    소형도 0.5~0.95로 키워 면 단위에서도 형태가 읽히게. 15k → **9.3k tris**로 오히려 가벼워졌다.
  - 🌗 **다시: 폴리곤으로 파던 크레이터를 텍스처로 이관**(3차 리포트 "삼각형이 너무 많다 ·
    실제 게임 3D 달 에셋은 어떤 식이냐"). 플랫 셰이딩도 정답이 아니었다 — **실제 게임 달 에셋은
    크레이터를 지오메트리로 파지 않는다**: 매끈한 스무스 구 + 알베도 + **노멀맵**이 표준이고,
    아주 큰 지형만 변위로 실루엣을 깨뜨린다. 그대로 옮겼다.
    - 지오메트리: 96×64 **스무스** 구, 변위는 **대형 크레이터 3 + 마레 3 + 완만한 기복(0.022)**만.
      보행 표면(`moonSurfOffset`)도 같은 대형 지형만 보므로 조형·보행 1:1 유지.
    - `makeMoonMaps()`: 등장방형 캔버스에 **알베도 2048×1024 + 높이 1024×512**를 절차 생성 →
      높이맵을 **소벨로 미분해 노멀맵(DataTexture)** 으로. 크레이터 **280개**(대형 3 + 텍스처 전용
      150 + 패드 코앞 잔 크레이터 130), 마레, 광조, 레골리스 잔입자 9000 — **전부 폴리곤 0**.
    - ⚠️ 알베도엔 **빛을 굽지 않는다**(해가 움직이면 틀려진다) — 음영은 노멀맵이 만든다.
      등장방형은 극으로 갈수록 가로가 늘어나므로 스탬프 가로 반지름을 1/sinθ로 보정 + 좌우 wrap 3회.
    - 결과: 실루엣이 매끈해지고 크레이터 밀도는 오히려 크게 올랐다. 생성 비용은 1회 100ms 미만.
    - **검수 라운드(4차 리포트 "크레이터가 무분별하게 찍힌 느낌 · 화질이 각자 다르다")** — 둘 다 실측 원인이 있었다:
      - *화질 편차*: ① 노멀맵이 알베도의 **절반 해상도**(1024×512)라 작은 크레이터만 3~5px로 뭉갰다
        → **2048×1024로 동급화**. ② `DataTexture`는 **밉맵이 기본 OFF**라 거리에 따라 노멀맵만
        지글거렸다 → `generateMipmaps` + `LinearMipmapLinear` + anisotropy 8(알베도도 4→8).
        ③ R 0.07짜리는 애초에 텍셀이 모자라 못 그리는 크기였다 → **최소 반지름 0.16**(높이맵 ~7px).
      - *도장 반복*: 모든 크레이터가 같은 그라디언트였다 → 개체마다 **나이(신선↔낡음)**를 주고
        림 높이·바닥 대비·**에젝타 분출물 범위**를 나이로 좌우, **타원율·회전·중앙봉(18%)** 추가.
        배치도 30%는 기존 크레이터 곁에 붙여 **군집/중첩**을 만들고, **큰 것부터 그려** 나중 것이
        먼저 것을 덮게 했다(균일 난수 + 동일 프로파일 = 도장을 흩뿌린 느낌의 정체).
  - 🚩 **삼각 페넌트 + 애니메이션** — 사각 깃발 + 가로 지지대 → **끝으로 갈수록 좁아지는 뾰족한
    페넌트**(14세그 스트립). 끝으로 갈수록 진폭이 커지는 2주파 웨이브 + 리프트를 매 프레임 갱신
    (`moonFlagWave`, 30정점 · POI 보일 때만). 초기 포즈도 접어둬야 첫 프레임이 뻣뻣하지 않다.
- 검증: 격리 렌더 5컷(전체·패드·그림자면·근접 2) · `__worldDev.poiSurf(x,z,0)`로 보행 표면
  실측(패드 중심 = 정확히 구면 6.859 · 평지 −0.03 지형 반영) · world-smoke 15/15 · 달 9.3k tris.

### Changed (🛰️ 별빛 정거장 조형 리뉴얼 — 갑판 지름만 고정, 그 위를 전부 다시)
- **P0 구조**: 통짜 원통 파일런 2 → **격자 트러스**(기둥 4 + 가로 링 6단 + 지그재그 브레이스 +
  베이스 플랜지·볼트·상단 캡). 텅 빈 크림 원반이던 갑판에 **방사 이음매 16 · 동심 링 2 ·
  케이블 트레이 + 서포트 12 · 킥플레이트 · 해치 2(림·손잡이·볼트) · 중앙 에어록 해치**(평평 0.03 —
  산책 간섭 0) · **패드 그레이팅**(레드 링 r1.32 안쪽으로 원형 클립) · **위험 빗금 14 · 통행 셰브론**.
- **P1 코어**: 링 프레임 5 + 세로 이음매 4 + **MLI 은박 엔드캡/밴드**(poiFoil) + **포트홀 2**
  (금속 림 + 진청 유리 + 안쪽 웜 글로우 — 유리·글로우는 껍질 **바깥**에 둬야 보인다) + 핸드레일 4 +
  명판. **⚠️ 기존 창문 스트립 2장은 코어 표면에서 0.22 떠 있던 판때기**였다 → 십자 모듈 **측면
  스트립 2 + 갑판을 내려다보는 끝면 원창**으로 옮겨 붙였다. 호버 문구에만 있던 "클램프 정렬"을
  실물로: **패드 클램프 4 + 엄빌리컬 마스트·접이 암·은박 호스**.
- **P2 태양전지판**: 민무늬 남색 판 → **셀 격자(가로 5 × 세로 2) + 프레임 4변 + 스파**, 위=셀 블루
  /아래=캡톤 금색으로 앞뒤 분리(정점색 1머지). **요크 축·포크**를 붙이고, 주석만 있고 실제로는
  무한 회전이던 것을 **해의 동서 이동에 연동**(±0.62rad 감쇠). ⚠️ 해는 xy 평면(z=-7 고정)을
  지나는데 요크는 붐(x축) 회전뿐이라 방향을 그대로 겨누면 판이 뒤집혀 셀이 안 보인다(정거장이
  해보다 30m 높다) — 그래서 아지무스를 기울기로 매핑.
- **P3 생활감**: 난간 2단(기둥 20 · 사선 브레이스 · **게이트 3곳 = 노란 체인 + 발판**) · 화물
  컨테이너 2 + 팔레트 + 고박 스트랩 · **파라볼라 안테나(스윕)** · 라디에이터 2 · 조명 위계
  (포트홀·창 = 웜 / 작업등·에어록 링·갑판 가장자리 = 쿨 / **패드 유도등 8 = 4위상 러닝, 도킹
  시퀀스 중엔 2.2→7로 가속**).
- ⚠️ 배치 함정: 화물을 좌현에 놓았더니 **망원경(-3.05, 1.8) 위에 얹혔다** → 우현 후방으로. 코어
  중앙 포트홀(x=0)은 **십자 모듈 속에 파묻혀** 보이지 않아 제외.
- 검증: 격리 렌더 3컷(정면·3/4·top-down, `scripts/_vend-shot.mjs stn`) 전후 대조 · world-smoke
  15/15 · 섬 draws 232(정거장은 우주 전용이라 지상 예산 무영향) · 정거장 조형 ~11k tris.
- **후속 2건(사용자 리포트)**:
  - ☀️ **태양전지 셀에 각도 반사** — 판을 통짜 박스에서 **본체 + 셀 유리(별도 메시)**로 쪼개고
    유리에 전용 재질 `poiCell`(MeshPhysical · roughness .07 · clearcoat 1/.03 · ior 1.7 ·
    envMap = `scene.userData.envTex` · intensity 1.8). 매트 `poiGrad`(roughness .85)로는 남색
    종이판으로 읽혔다. 요크가 기울면 하이라이트가 흐르고 각도가 벗어나면 짙은 남색으로 죽는다
    (`scripts/_panel-probe.mjs`로 −0.62/−0.1/+0.45rad 3컷 실측). 버스바는 갈색 이음매색 →
    은색 리본(`BUS`)으로 — 갈색이면 녹슨 선으로 읽힌다.
  - 🎚️ **셀 색 심화 + 자세 미세 조정**(2차 리포트 "빛 때문에 색이 연해진다 · 움직임을 자연스럽게"):
    색이 뜨는 주범은 넓은 하늘 반사였다 → `envMapIntensity` 1.8→1.05로 낮추고 metalness 0,
    좁고 밝은 하이라이트는 `clearcoatRoughness` .025로 유지 — 번쩍임은 그대로 두고 바탕만 진하게.
    정점색 0x4a72d8→**0x1e3a85**(실제 PV의 검푸른 유리), 캡톤 뒷면도 한 단 낮춤 → 은색 버스바·
    흰 프레임과 대비 강화. 움직임은 해 추적만 걸면 하루 종일 멈춰 보여서 **좌우 위상차 0.9rad의
    미세 요동(±0.045rad) + 아주 느린 플렉스(z축 0.014rad)**를 얹었다.
  - 📦 **화물 상자 위에 올라설 수 있게** — 조형만 있고 보행 표면 훅에 없어 점프하면 그대로
    통과했다. `STATION_CRATES`(로컬 좌표·반치수·회전·윗면) + `stationCrateTop()`을 만들어
    **`poiSurfaceY` 한 곳**에 물렸다(EVA 보행·점프 착지·AI 배치가 전부 이 함수를 지난다 —
    지상의 groundHeightAt/playerSupportY 2훅 문법과 다르다). 0.56 상자를 한 번에 오르지 않게
    **스텝 크레이트(0.26)** 추가. 실측 훅 `__worldDev.poiSurf(x,z)`: 상자 중심/모서리 0.56 ·
    바로 밖 0 · 스텝 0.26 · 패드·망원경·자판기 자리 0.

### Changed (🔥🍢 구이 3종 완성도 라운드 — 사용자 반복 검수)
- **🐟생선**: 원통 몸통(애벌레·불쾌한 골짜기 리포트) → **좌우 납작(ZK 0.42) 방추형** + 최대 굵기를
  머리에서 40% 지점. **눈은 raw·cooked 모두 유지**(감은 눈은 "눈이 없다"로 읽혀 오히려 징그럽다 —
  사용자 확정): 구운 뒤엔 흰자가 뽀얘지고 반짝이 하이라이트. 무늬는 **겹친 아치(세이가이하) 비늘
  문양**(raw)·**굽이치는 시어링 밴드**(cooked) — 브릭 격자 음영만으론 "비늘 같지 않다"였고, 밴드를
  둘레각(th)으로 흔들면 실루엣 가장자리에 몰려 어색했다. **아가미뚜껑은 정점색 선 대신 눈 뒤 띠를
  부풀린 실제 단차**(⚠️머리 전체를 부풀리면 눈이 파묻힌다 — 띠 폭 8.8mm로 한정, 눈은 주둥이 쪽
  0.0972로 이동). 뒷지느러미 밑변은 몸통 속(rAt≈0.0073)까지 물려야 붙어 보인다.
- **🦪조개**: ExtrudeGeometry → **극좌표 격자 셸**(겉·속 두 시트 + 마진 스트립) — 셰이프 삼각분할이
  면 내부에 정점을 안 만들어 리브·성장륜·돔이 아예 렌더되지 않던 것이 "웨이퍼" 원인(실측).
  기복은 얕게(사용자 확정) + 결은 정점색 격자(리브 12 × 성장륜 5)가 전담. **정체 모를 갈색 원
  = 움보 구슬 → 직선 인대선 + 좌우 귀(auricle)로 교체**, 이음선 토러스는 꼬치를 감는 철사 고리로
  읽혀 삭제. 닫힘은 **돔 0.42R 두 짝의 림 맞물림**(위짝을 들면 틈으로 자개가 보여 "벌어진 조개"가
  된다) + **닫힘 상태의 속면은 껍데기 톤으로 위장**(경첩 쪽에서 자개 흰빛이 새던 문제). 벌어짐은
  110°→41°, 부채각 ±1.34로 확장.
- **🍡마시멜로**: 통통하게(R 0.0135·높이 0.019) 3알·틈 3mm로 좁힘 + 파우더 스펙클·블리스터·
  카라멜 광택 밴드·처짐. world-smoke 15/15 · draws 228.


### Changed (🍡 마시멜로 리모델 + 🐟 생선 마감 2건 — 사용자 검수)
- **🍡 마시멜로**: 반지름 +30%·높이 +22%로 **통통하게**, 슬롯 간격을 좁혀 **틈 11mm→3mm**
  (꼬치가 빈약해 보이던 리포트). 질감은 아이디어대로 — **분 묻은 무광**(저대비 흰색-on-흰색
  스펙클 + 위아래 설탕 절단면, raw만)·**방향성 토스팅**(불에 가까운 위·바깥 테부터 매끈한
  금빛 그라데이션)·**물집 3개**(슬롯마다 위치 분산, 릴리프+그을린 정수리)·**녹아 처짐**(스쿼시
  0.16→0.22 + 아랫배 불룩)·가장 오래 구운 것엔 **카라멜 드립**.
  ⚠️교훈 2: ①**해시 균열 금지** — 흰 바탕에 검은 점이 흩뿌려져 흙·곰팡이 얼룩으로 읽힌다
  ②**둘레 광택 토러스 밴드 금지** — 옆에서 플라스틱 링이 된다 → 월드 카메라가 내려다보는
  **윗면 캐러멜 디스크**(광택 자식)로 대체하니 "불에 닿아 녹은 설탕"이 됐다.
- **🐟 생선**: ①구운 뒤에도 **눈 유지**(감은 눈은 "눈이 없다"로 읽혀 오히려 징그러움 — 사용자
  확정): 익은 뽀얀 흰자 + 진한 눈동자 + 반짝이. ②꼬리 쪽 **뒷지느러미 부착** — 밑변이 몸통
  반지름 밖에 있어 삼각형만 떠 있었다(밑변을 몸통 속까지 물린다).
- world-smoke 15/15.


### Changed (🐟 생선꼬치 전면 재조형 — "불쾌한 골짜기" 해소 + 바삭한 구움 질감)
- **원인 3가지**(사용자 리포트 "징그럽다"): ①단면이 원통이라 애벌레/털 뽑힌 새로 읽힘 ②매끈한
  몸에 검은 점 눈 + 어두운 입 슬릿 = 시체 눈 ③시어링이 흰 반점과 섞여 곰팡이처럼 보임.
- **실루엣**: 좌우 납작(ZK 0.42) + 최대 굵기를 머리에서 ~40%로 옮긴 **방추형** + 가는 꼬리자루,
  굽이는 0.0075→0.0032 (사용자 확정: 약간만). 스파이크 3개 등지느러미·뒷지느러미·가슴지느러미
  2장·크게 벌어진 V 갈래꼬리. **지느러미 밑변은 몸통 안쪽까지** — 바깥이면 떠 보인다.
- **raw = 겹친 비늘**: 행마다 반 칸 엇갈린 브릭 배열의 낱장을 **셀 중심 거리로 볼록**하게(중앙
  밝고 경계 골). ⚠️행 이음선만 강조하면 골판지 주름으로 읽힌다(실측). + 등 얼룩·측선 은빛·
  미묘한 무지개빛 색조.
- **cooked = 바삭함**: 부풀어 터진 **물집**(릴리프+그을린 정수리)·굵은 사선 시어링·**터진 껍질로
  드러난 흰 속살**(배쪽만 조금)·굵은소금 알갱이 3개(노이즈 반점 대신 실물)·**기름 윤기 광택
  자식 2개**(맛있어 보이는 결정타). ⚠️시어링 띠의 둘레각 계수가 크면 사탕처럼 몸을 감는다.
- **😊 귀여운 눈**(사용자 확정): raw=반짝이 하이라이트 붙은 큰 동그란 눈, cooked=**감은 웃는 눈**.
  ⚠️입꼬리 호·아가미 토러스는 옆면에서 **사선 흠집(찡그림)** 으로 읽혀 삭제 — 아가미 선은
  정점 페인트 밴드로 대체.
- **🌊 물결 무늬 밴드 5줄**(사용자 리포트 "비늘이 비늘 같지 않다"): 등을 가로지르며 굽이치는
  고등어식 무늬 — raw=심해 청록, cooked=진한 호박갈색(상태 색에 맞춤). 비늘 격자는 대비를
  절반으로 낮춰 **바탕 결**로만 남기고 무늬가 실루엣 다음의 읽기를 맡는다. ⚠️굽이 폭이
  작으면(0.0055) 얼룩으로 보인다 → 0.0092 + 2차 하모닉, 밴드 경계는 smoothstep.
  구운 쪽은 물집 그을림이 무늬를 삼켜 그을림 색만 0.5→0.34로 낮췄다(릴리프는 유지).
- 1입=옆구리 크레이터(납작한 몸이라 중심을 배 바깥에 두고 실루엣을 깎는다)+드러난 갈비,
  2입=머리·등뼈·갈비·꼬리. world-smoke 15/15 · draws 230.


### Fixed (📞 우주 정거장 친구 부르기 — 소환 안 되던 버그 + 빈 별똥호 자동 귀항)
- 리포트: 모래섬에서 친구 부르기를 했는데 정거장의 친구가 안 옴. 원인 = 통화 소환의
  `yankFriendFree`가 침대·물놀이·탈것은 다 해제하면서 **우주 산책(poiWalk)은 몰랐다** —
  곁으로 순간이동시켜도 다음 프레임 EVA 울타리 클램프가 도로 정거장에 붙잡았다.
- 수리: 소환 시 우주 상태 청산(구면 자세 포함, 보드 문법) + 비행 좌석에서도 빼냄(남는 승객
  없으면 빈 여정 완주). **마지막 산책자가 소환되면 빈 별똥호도 알아서 언도킹해 발사대로
  귀항** — POI에 로켓이 좌초되지 않는다. 소환 서사 분기("별똥호 편으로 부리나케 돌아와…").
- 픽션 가드 2종(우주는 별똥호로만): 우주 갑판에서 발신 금지("전화가 안 터져요"), 📍 친구한테
  가기는 친구가 우주면 거절("별똥호를 타고 가요!") — 기존엔 정거장 좌표의 해수면으로 순간이동
  하는 잠복 붕괴였다. 가드는 발신자 본인 탑승만 검사(빈 로켓 귀항 중 지상 통화는 안 막음).
- 부수리: `callFriend` 개발 훅 중복 정의 제거(나중 키가 불리언 버전을 그림자로 가림).
- 검증: 소환 E2E 8항목(정거장→모래섬 곁 d0.4·상태 청산 out 0·빈 로켓 귀환길→발사대 완주·
  갑판 발신 가드·지상 통화 회귀) + 우주 해제 회귀 + smoke 15/15.


### Fixed (🎣 낚시 2건 — "낚았는데 물고기가 없다" + 월척 자축이 쏘아올린 초고도 점프)
- (코드는 82a9e1f에 동승 — 병행 세션 꼬치 수리 커밋이 미커밋 작업을 함께 실음)
- **아이스박스 기본 = 보관**: 조과 제안 바가 "6초 방치 = 놓아주기(종전 유지)"여서, 팡파레
  구경하는 사이 물고기가 조용히 방생됐다 — 그래서 구이도 재료도 없던 것. 이제 방치·새 조과에
  밀려남 모두 **보관**이 기본(잡은 건 내 것 — 스타듀 기대값), 놓아주기만 명시 클릭.
  6초 안에 연속으로 잡아도 이전 조과가 사라지지 않는다.
- **월척 자축 폴짝 적분 수리**: 자축 폴짝(hopY 0.09m)을 조종 펫에 `+=`로 얹었는데, 'land'
  연출 중엔 updatePlayer가 입력 잠금으로 早반환이라 "매 프레임 기준선 복원" 가정이 거짓 —
  폴짝×2(~84프레임) 동안 y가 수 미터 적분돼 "완료 직후 초고도 더블점프"로 보였다. AI 펫은
  같은 병을 이미 절대값으로 고쳤던 자리의 가정 구멍. 교체식(+이번 −지난)으로 —
  배 위 낚시(서포트 밖 기준선)도 안전.
- 검증: 낚시 E2E 7항목(월척 강제 훅 fishBigNext — 폴짝 max +0.09m·드리프트 0.000·방치
  보관·밀어내기 보관·놓아주기 명시) + smoke 15/15 ×2회.


### Fixed (🍢 굽는 꼬치가 펫 몸통을 관통하던 자세 — 사용자 리포트)
- 꼬치 손잡이를 **펫 중심에서 불 방향 0.17m**에 두고 있었는데, 몸통 반지름(≈0.22)보다 짧아
  손잡이가 몸 속에서 시작 → 막대가 가슴·머리를 뚫고 나오는 자세가 됐다. **든 음식과 같은
  문법으로 교체**: `flankX` 레이캐스트로 실제 털 표면을 찍고 그 바깥 3cm, 좌표는 든 음식·음료와
  동일한 손(dims.y·0.34 / dims.z·-0.12) — 이제 옆구리 손에서 불 쪽으로 뻗는다.
- ⚠️ 손잡이는 **몸 앞쪽(-z)** 이어야 한다: 뒤로 빼면 불을 겨눈 막대선이 몸통을 스쳐 다시 관통한다.
- 검증: 굽기 E2E 측면 스크린샷 2위상(딥 상·하) — 관통 없음, 손잡이가 부리와 겹치지 않음.

### Fixed (🍢 다 구운 꼬치를 손에 들 때 크기가 2.6배 작아지던 버그 — 사용자 리포트)
- 굽는 프롭은 `mesh.scale 2.6`인데 완성품(makeFoodMesh)은 1이라, 굽기가 끝나 손에 옮기는 순간
  꼬치가 확 쪼그라들었다. **SKEWER_FOODS/SKEWER_SCALE 공용 상수**로 두 경로를 묶어 해소.
- 같이 딸려온 정렬 문제도 수리: 먹기 포즈는 `topH/2`만큼 메시를 입 아래로 내리는데 ①topH가
  월드 단위로 안 쓰여 스케일이 무시됐고(→ `topH × mesh.scale`) ②꼬치의 topH가 *스틱 전체 길이*라
  입이 막대 중간에 닿았다 → **topH = 먹이 중심 × 2**(생선=몸통 중심, 조개=껍데기 2개 중심,
  마시멜로=남은 개수 평균)로 바꿔 먹이가 부리에 온다. 물기 지오 교체 시에도 topH 재동기 —
  마시멜로가 3→2→1로 줄며 아래로 내려와도 계속 부리에 맞는다.
- 검증: 모닥불 E2E(빙의→tp→grillTry→완성→fruitEat ×2) 스크린샷 — 굽기/들기 크기 동일,
  1입·2입에서 남은 마시멜로가 부리에 정확히 닿음. world-smoke 15/15 · draws 228.


### Changed (🥤 우주 자판기 — 조종 펫은 받기만, 자동 냠냠 제거)
- 리포트: 자판기 간식을 받으면 곧장 1입 먹기 시작. 원인 = giveSnack이 음식 객체에 먹기
  시퀀스(seq)를 심은 채 생성("받자마자 냠냠" 설계였음).
- 수리: **조종 펫은 seq 없이 받기만**(푸드 부스 문법 — 먹기는 펫 우클릭 메뉴의 "먹기"),
  절친의 자율 따라 뽑기(15%)만 기존처럼 그 자리에서 냠냠 유지.
- 검증: vend E2E에 "받고 5초 뒤에도 bites 0" 어서션 추가 — 7항목 통과 + smoke 15/15.


### Added (🚜 농장 섬 — 텃밭 8칸 확장 이전, 여섯 번째 섬)
- 텃밭이 본섬 북서 구석(림까지 1.0m, 이웃 3개 밀집)에 갇혀 확장 불가 → **서쪽 신규 섬**
  (-12.2, 3.9, r 3.0)으로 통째 이전. 간격은 표준(2.6m)보다 한 발 물러난 **본섬 3.6m**(서쪽
  수평선 숨통 — 모래섬 3.4m 선례), 다리(스팬 4.6m)로 연결해 AI 배회·자율 동선 무료.
- **텃밭 2×4 = 8칸**: 앞 4칸은 옛 저장 인덱스 위치 그대로(호환 — 구세이브는 앞 4칸에 꽂힘),
  프레임 2.78m + 가운데 칸막이 레일 + FLAT_SPOTS 평탄 패드(follow:'garden-1' — 구릉 위 기울음
  실측 수리). **허수아비**(밀짚모자·빨간 셔츠·파랑새 — 머리는 크라운보다 가늘게, 굵으면 모자를
  뚫는다 실측)·**퇴비함**(슬랫 상자·새싹·삽) 신규 조형, 둘 다 병합 정적.
- 시스템 연쇄: BRIDGES에 **모래섬 자리 null 홀**([3] — 인덱스=섬 순서 규약 유지) + 순회 4곳
  `if (!br) continue` 가드 · 페리 서쪽 부표 -16,2 → -18.3,3.5(섬 여유 4.4m·수평선 18.6<19.6)
  · W 림 울타리는 다리 상륙지에서 남쪽으로(MOVED_DEFAULTS 추종) · ISLAND_SIG 변경이 위성섬
  프롭 저장 좌표를 자동 폐기 → 사용자 저장본도 새 농장 위치로 스냅.
- 검증: layout-sweep 전수 통과(본섬↔농장 3.6m·다리5 4.61m) + 농장 E2E 9항목(섬 인식·다리
  경로·8칸 심기/수확·팬트리 정산·데크 보행·구세이브 매핑) + 보트 회귀 + smoke 15/15
  (**draws 228 불변** — 크로스 병합이 섬·다리·프롭 흡수).


### Changed (🔭 전망 망원경 리모델 — 관광 쌍안경에서 정거장 계측기로 + 요크가 실제로 겨눈다)
- 진단(격리 렌더 계측): 부품이 7개뿐이었다 — 맨 막대 기둥·12각 와셔 받침·경통 원통 2개·
  테두리뿐인 토러스(렌즈 없음)·경통 사이에 끼어 안 보이는 레드 밴드·접안 글로우 구슬 1개.
  두 경통을 잇는 하우징이 없어 파이프 두 개가 공중에 떠 있었고, **회전 기구가 조형에 없어서**
  `teleGroup`은 레이캐스트에만 쓰이고 보는 중에도 엉뚱한 방향으로 굳어 있었다.
- **요크 마운트**: 기둥 위 베어링(회전 틈을 어두운 링으로 노출) → `teleYawG`(좌우) →
  `teleTiltG`(상하) 2축 분리. 두 팔이 하우징 옆구리를 잡고 피벗 허브·볼트 캡이 축을 보여준다.
- **하우징**: 브릿지로 두 경통을 한 몸으로 묶고 8각 대물 벨 + 방열핀 3 + 은박 단열 랩 +
  접안 테이퍼 + **러버 아이컵**(펫이 얼굴 대는 자리) + 손잡이 2 + 포커스 노브. 렌즈는 실제
  유리(어두운 정점색 + **좁은 코팅 사선 스트릭 1줄**)를 넣고 레드는 정면에서 보이는 대물 림으로.
  ⚠️ 대물 림을 **원판**으로 만들면 렌즈를 덮어 "빨간 눈"이 된다(실측) — 반드시 링(Torus).
- **정거장다움**: ★ 뱃지·상태 LED(자판기 스크린과 같은 `vendCoolMat` 색온도)·명판을 등 리지와
  뒷면(펫이 서는 쪽)에 배치, 볼트 4개 박힌 앵커 플레이트 + **미끄럼 방지 발판**(접안부가 펫 키
  보다 높아 까치발이던 것) + 6각 기둥 + 케이블 컨듀잇. 접안 글로우는 자판기 펄스 공유를 끊고
  전용 `teleGlowMat`으로 — 보는 중엔 밝고 평소엔 은은하다.
- **요크 조준 연동**(`updateTeleAim`/`teleAimAt`): ←→로 풍경을 바꾸면 **밖에서 보이는 망원경도
  그쪽을 겨눈다**. 각도는 항상 짧은 쪽으로 감고(wrapPi), 정거장이 섬보다 35m 위라 실제 각도
  그대로면 거의 수직으로 꺾여 부러져 보이므로 틸트는 **-0.62~+0.5로 클램프**. 아무도 안 보면
  난간 바깥을 향해 살짝 숙인 파킹 자세로 감쇠 복귀.
- 비용: 4메시 864tri → 10메시 1,924tri(고정부 3 · 요크 1 · 하우징 6). 월드 베이크 draws 228 불변.
- 검증: 조준 각도 단위검사(달·우리 집·놀이터 3목표 **요 오차 0.00°**, 훅 `__worldDev.teleAim`)
  + 격리 렌더 4컷 전후 대조 + world-smoke 15/15.
- ⚠️ 병행 세션이 같은 `world.js`·`world-layout.js`에서 허수아비·8칸 텃밭 작업 중이라, 그 미완성
  코드(`PROP_BUILDERS[p.type] is not a function`)를 피해 **내 훅만 스테이징한 사본으로 검수**했다
  (`scripts/_stage-mine.sh` + 프로브의 파일 오버라이드). 이 커밋에는 그쪽 변경이 들어있지 않다.

### Changed (🥤 우주 자판기 조형 리모델 — 통짜 박스에서 진짜 자판기로)
- 진단(격리 렌더 계측): 몸통이 **단일 BoxGeometry 1개**, 진열창은 불투명 네이비 판, 진열
  상품 4개는 유리 안이 아니라 **몸통 앞면(z 0.2)보다 앞(z 0.216)에 붙어** 있었고 선반도 같은
  평면 — 즉 창·선반·상품이 전부 데칼이라 **내부 깊이 0**. 배출구는 구멍이 아닌 회색 판,
  간식 5종 중 4종만 그것도 추상 블록, 발광은 마퀴·선반·스크린이 **한 재질 한 위상**.
- **진짜 함몰 진열창**: 개구부(x -0.23~0.14 · y 0.5~1.08)를 실제로 비우려면 통짜 박스로는
  안 된다 — 몸통을 **9분절**(하단 문턱·배출구 좌/우/뒤·인방·창 뒤·좌기둥·우기둥·상단)로 쪼개
  깊이 0.14 캐비티를 만들고 어두운 내벽 5면 + 선반 2단 + 언더라이트 + 천장등 + 유리 1장.
  ⚠️ 캐비티를 통짜 박스 **안에 얹으면** 앞면과 코플래너가 되어 z-fight — 반드시 쪼개서 비운다.
- **진열 상품 = 간식 5종 실물 미니**(0.95배, `makeFoodGeo` 재사용 — 신규 조형 0): 🌟골드빵은
  레어 받침등 위 단독 칸, 🧃파우치·🍦아이스크림, 🥫튜브·🍪별쿠키 2단. 은박/글레이즈 계보가
  그대로 살아 진열창만 봐도 뭘 파는지 읽힌다. ⚠️ **간식마다 속성 구성이 달라(uv 유무·색 유무)
  그대로 합치면 mergeGeometries가 null → 그 계보가 통째로 증발**(실측: 5종 중 2종이 안 보임)
  — position/normal/color로 정규화 후 계보별 1드로우 병합.
- **배출구도 실제 구멍**: 어두운 캐비티 5면 + 금속 립(아래 립은 손이 들어가는 턱으로 두껍게)
  + 스모크 플랩. 실루엣 분절: 안으로 물린 플린스(접지) · 앞면 수직 필러 2 · 0.07 돌출 캐노피
  + 아래 그림자 홈. 조작부는 리세스 패널에 3×2 키패드 + 스크린 + 카드/코인 슬롯으로 묶고,
  옆판은 통짜 네이비 → 리브 3 + 통풍 슬롯 5 + ★ 뱃지, 안전매트는 두꺼운 판 → 얇은 갑판 데칼.
- **발광 2계보 분리**: 따뜻한 진열등(vendGlowMat)과 차가운 스크린(vendCoolMat)을 다른 색온도·
  다른 위상으로 — 전부 같이 깜빡이면 정보 위계가 사라진다.
- 비용: 5메시 264tri → 10메시 10,074tri(대부분 간식 미니). 우주 POI는 섬 씬이 꺼진 고도에서만
  보이므로 월드 베이크 draws 228 불변. 크기·좌표계(0.55×1.15×0.4·VEND_A·VEND_TRAY)는 그대로 —
  스케일(펫 키의 2.5배, 키패드가 머리 위)은 별도 카드로 남김.
- 검수 1건(사용자 리포트 "정면에서 버튼·스크린이 우측 회색바와 겹침"): 조작부 가용 폭은
  창 기둥 안쪽(0.14)~**앞면 필러 안쪽(0.254)**뿐인데 중심을 0.2075/폭 0.105~0.115로 잡아
  우측 버튼 열(0.221~0.259)·스크린 베젤(0.155~0.26)·리세스 패널(0.15~0.265)이 필러를
  5~11mm씩 파고들었다 → 중심 0.1975로 당기고 폭 축소(패널 0.098·버튼 0.034·열간 0.046·
  베젤 0.092·슬롯 0.072), 필러까지 7~16mm 여백 확보.
- 검증: 격리 렌더 3컷(정면·3/4·측면, `scripts/_vend-shot.mjs` 임시 프로브) 전후 대조 +
  world-smoke 15/15.

### Fixed (🌙 우주 밤샘 정산 — 끄고 자도 아침엔 펫들이 집에)
- 리포트: 밤에 정거장 산책 상태로 앱을 껐더니 아침에도 그대로 정거장에 있음. 원인 = 밤 자동
  귀항은 **켜져 있을 때만 도는 라이브 로직**이고, 부팅 복원(applySpaceRestore)은 "그 자리
  이어짐" 설계라 **부재 중 밤이 지났다는 사실을 안 봤다**. (3차 수리의 90초 유예 중 종료도
  같은 구멍 — 유예만 남고 귀항 기회 소멸.)
- 수리 ①: `gapTouchedSleep` — 부재 구간 [마지막 하트비트, 지금]이 취침대(22–06시)에
  걸쳤으면(재접속이 밤이어도 — "그 시간엔 자야 하니까") 그 자리 복원 대신 **밤사이 귀환
  정산**(settleSpaceHome): 그곳의 펫 전원 본토 물가 + 로켓 발사대 + 그날 밤 22:04 시각의
  이벤트 로그("밤이 깊어 …별똥호를 타고 집으로 돌아왔다 🌙🚀" — 일기 소재, 토스트 금지 원칙).
  1분 미만 재시작은 연속 취급. ⚠️ lastAliveMs는 매 프레임 하트비트로 덮여서 부팅 스냅샷
  (`bootAliveMs`)을 따로 남겨 판정(실측 — 오프라인 정산과 같은 함정).
- 수리 ②(미아 방지·잠복 버그): 저장 토큰에 **누가 갔는지(who)** 기록 — 기존엔 POI id만
  저장해 복원이 무조건 두 마리 다 소환(솔로 여행 후 재접속 시 집에 있던 펫까지 정거장에
  나타남). 이제 복원·정산 모두 "그곳에 있던 펫만" 단위로 — 1마리든 2마리든 함께, 미아 불가.
- 검증: 밤샘 E2E 11항목(26h 부재=귀환 정산·로그 시각·낮 10분=이어짐·솔로 who 필터·판정
  단위검사 5조합) + 라이브 밤 귀항 회귀 6항목 + world-smoke 15/15.


### Fixed (🔥 모닥불 ⌘ 과대 반경 — 곁의 조개를 못 줍던 버그)
- 리포트: 모닥불 근처에서 조개를 주우려 ⌘를 누르면 구이 패널이 열림. 원인 둘 —
  ① doInteract 사슬에서 모닥불 분기가 조개 줍기(0.85m)보다 **앞**, ② 반경 1.5m가
  상호작용물 최대급(주방 1.3·나무 1.05·피아노 1.0)이라 불 주변 1.5m의 조개가 전부 먹통.
  야자수(불에서 1.43m)의 코코넛 흔들기까지 선점하고 있었다.
- 수리: 모닥불 분기를 **조개 뒤로 이동** + 반경 **1.5→1.05**(나무·피아노급). startGrill
  자체 검증은 1.6이라 굽기 흐름 무변. 발밑 줍기(낙과·조개)·수확이 항상 우선하는 문법 정착.
- 검증: 범위 E2E 3항목(1.3m 안 열림·0.9m 열림·야자 곁 야자 우선) + grill-test 7항목 재통과
  + world-smoke 15/15. (참고: 모래섬 남쪽 비행기 탑승 링 1.4는 원래부터 모닥불보다 우선 —
  E2E 좌표를 그 링 안에 찍었다 헛짚을 뻔, 모래섬 ⌘ 지형은 붐빈다.)


### Fixed (🍅 재료 별칭 — 바구니 토마토로 버거를 못 만들던 버그)
- 리포트: 토마토가 바구니에 있는데 버거가 "재료 없음". 원인 = 월드에 토마토가 **둘** —
  덩굴 티피 토마토(`fruit:tomato`, 과일 바구니)와 텃밭 토마토(`pantry:tomato`, 팬트리)가
  별개 아이템이고, 레시피 재료가 `{src,id}`로 **창고에 하드코딩**돼 버거·토마토수프가
  텃밭 토마토만 인정했다. 토마토잼 실험은 매칭이 id만 봐서 바구니 토마토를 받는데 해금 후
  원클릭 조리는 팬트리를 요구하는 자기모순까지.
- 수리(실무 원칙 "아이템 정체성 ≠ 보관 위치"의 최소 적용): `ING_ALIASES` 동치 테이블 —
  `pantry:tomato`는 `fruit:tomato`도 인정. **조회 = 창고 합산**(ingredientCount),
  **차감 = 캐논 창고 먼저 + 별칭 폴백**(consumeUnit — 바구니 과일은 '아무 과일' 레시피·선물
  몫이 있어 아껴둔다). 레시피 정의·실험 매칭·패널은 무수정.
- 검증: 별칭 E2E 7항목(팬트리1+바구니1 합산 조리 가능 → 수프 완주 → 팬트리 먼저·바구니
  폴백 차감 → 버거 토마토 인정 → 토마토잼 원클릭 해소 → 주스 와일드카드 회귀) +
  cook/exp E2E 재통과 + world-smoke 15/15.


### Fixed+Added (🚀 도킹 후 "펫 조종 해제 = 강제 송환" 최종 수리 + ⬇️ 문맥 하선 버튼, 리포트 3차)
- 리포트(모바일): 빙의한 채 도킹한 뒤 내리려고 펫 조종 버튼을 다시 누르면, 조종 해제와 함께
  펫이 로켓째 본토로 송환. 원인 = **밤 자동 귀항에 유예가 없음** — 해제하면 펫이 무빙의 산책자가
  되는데, 취침 시간(22시~)이면 **같은 프레임에** poiAutoReturn이 재탑승·귀환시켰다. 낮에 돌린
  E2E가 전부 통과했던 이유이자 밤에 플레이하는 실사용만 계속 당한 이유.
- 수리: `ROCKET.poiGraceT` 유예 90초 — 하선(disembarkPoi)·산책 해제·밤 부팅 복원(120초) 시
  장전, 유예가 남아 있으면 밤 귀항 보류. 유예 후 밤 자동 귀항은 그대로(고아 방지).
- **⬇️ 문맥 하선 버튼** (GTA류 '탈것 컨텍스트 액션' 실무 문법, 사용자 제안 그대로):
  착륙/도킹 완료 + 빙의 라이더일 때만 — 모바일 = ✋ 버튼이 잠시 **'하선'**으로 스왑(하늘색
  테두리), 데탑 = 우측(독 레일 왼쪽)에 **'⬇️ 하선하기'** 알약 버튼. 동작은 ⌘와 동일 경로
  (doInteract → disembarkPoi — **빙의 유지한 채** 갑판/달 산책).
- **힌트 오표기 정정**: 도킹 완료 힌트가 "✋/⌘ 이륙·언도킹"이라 적혀 있었는데 실제 동작은
  하선 — 이 오표기가 ✋를 피하고 펫 토글로 내리게 만든 원흉. "✋/⌘ 하선 (재탑승하면 이륙)"으로.
- 검증: 재현 E2E 12항목(밤 23시 해제=유예 산책·6초 무송환·유예 후 귀항 유지·데탑 버튼
  표시/클릭=빙의 유지 하선·모바일 ✋↔하선 스왑) + 회귀(1차 4·2차 7·해제 6·smoke 15/15).
  rocketState 훅 graceT·graceShort 추가.


### Fixed (🚀 도킹 "완료" 후에도 강제 귀환 — 자동 이륙 레이스, 리포트 2차)
- 리포트(모바일): 1차 수리 후에도 "도킹 완료" 토스트를 보고 펫 버튼을 눌렀는데 "갑판 산책!"
  메시지와 **동시에** 우주선이 본토로 귀환. 원인 = **자율 도킹의 자동 이륙 타이머(aiDwell 4초)**
  가 토스트를 읽는 사이 만료 → 언도킹(padDir −1)이 이미 시작된 뒤의 터치. 하이재킹이 조종권만
  가져오고 진행 중인 이륙은 되돌리지 않아 그대로 떠났다.
- 수리 ①: 하이재킹 시 이륙 이징이 진행 중이면(padK>0.1) **padDir을 +1로 뒤집어 재정박** —
  "주인이 잡으면 떠나던 로켓도 되돌아와 다시 붙는다". ②: aiDwell 4→10초(토스트 읽을 여유).
- 검증: 레이스 E2E 5항목(이륙 중간 padK 0.82 포착→펫 버튼→재정박 padK 1→하선→8초 도킹 유지)
  + 1차 수리 회귀 4항목 + 우주 해제 회귀 5항목 + world-smoke 15/15. rocketState 훅에
  padDir·aiDwell 계측 추가.


### Fixed (🚀 도킹 중 펫 조종 버튼 = 강제 본토 귀환 버그)
- 리포트(모바일): 우주에서 도킹 후 펫 조종 버튼을 누르면 표면 산책 대신 우주선째 본토로 귀환.
  원인 = **도킹 이징 중(padK<0.99) 빙의 해제/전환**이 "비행 중" 취급 → 오토파일럿 전환 → 도킹
  완료 4초 뒤 "AI 라이드는 곧장 귀환" 분기가 펫째 발사대로. 시각적으론 이미 붙어 보이는 마지막
  1~2초라 터치 흐름에서 특히 잘 눌린다(padK 0.16에서 재현).
- 수리: 이징 중 해제 = **"마저 도킹하고 내려주기" 예약**(dropAtDock) — 도킹을 끝내고 펫들을
  표면에 하선시키며 로켓은 도킹 유지(일기: "별똥호가 마저 도킹하고 펫들을 내려주기로 했다 🛰️").
  언도킹 시 예약 자동 소멸.
- 검증: 재현 E2E 4항목(이징 중간 포착→전환→귀환 금지·완료 하선·10초 도킹 유지·달 표면 y)
  + 우주 해제 회귀 5항목(발사 전 취소·비행 오토파일럿·밤 귀항) + world-smoke 15/15.


### Added (🔥🍢 모닥불 구이 3종 실물 조형 — raw/cooked/1입/2입 4형태, 클로슈 졸업)
- **공통**: makeFoodGeo 분기 하나가 `f.raw`+bites로 4형태 파생. 가는 대나무 꼬치(skStick — 그립
  마디·뾰족 끝·구운 뒤 끝 그을림). **buildSkewer = 실물 지오**(색 스왑 폐기 → cook()이 raw→cooked
  지오 통째 스왑: 조개가 벌어지고 마시멜로가 처진다), 완성품은 giveFood(r.id)로 **실물 꼬치를 들고
  먹기**(MODELED_DISHES 등록). endGrill은 지오만 dispose(재질은 전부 공유 — dispose 금지).
- **생선꼬치**: 통생선 S웨이브(축제 은어꼬치) — raw=등푸른→배은백+비늘 스펙클+측선, cooked=골든+
  사선 시어링+굵은소금+껍질 균열. 스파이크 3개 등지느러미·큰 V 갈래꼬리·꼬치 문 입 링·아가미.
  1입=옆구리 단면+드러난 갈비(크레이터 y범위 안 — 밖이면 멀쩡한 피부를 뚫는다), 2입=머리·꼬리·
  등뼈·갈비. ⚠️ 교훈 2: ①얇은 몸에 큰 덴트 구는 반대편까지 뚫는다 — 작은 구 2개 ②**라테 링이
  성기면 정점색 띠가 링 사이로 샌다** — 프로파일 2.2mm 조밀 보간이 시어링 띠의 전제.
- **조개구이**: 신규 밸브(리브 11·성장륜 릿지 변위·방사 크림 줄무늬·자개 안·가장자리 그을림 charK).
  raw=꽉 닫힌 조개 2(틈새 라인), cooked=경첩 활짝+탱글 조갯살(광택)+육즙 방울(반투명)+바닥 글리스닝.
  살 2→1→0: 빈 껍데기엔 잔여 살점+마른 육즙 자국.
- **마시멜로**: 동일 원통 3개(프로파일을 스틱 반지름까지 닫는다 — 안 닫으면 위아래 뚫려 C커프,
  실측) — cooked=위치별 토스팅 그라데이션(끝일수록 진하게·윗면 포함)+처짐 스쿼시+캐러멜 균열.
  3→2→1: 먹은 자리 스틱에 끈적 잔여 링+늘어난 실, 마지막 하나는 카라멜 드립.
- dishlab: 꼬치 3종은 raw 컬럼 포함 4컬럼·스케일 6(9면 윗행 관통), labGo 카메라 분기.
  world-smoke 15/15 · draws 228.


### Changed (타코야끼 가츠오부시 ×3)
- 수기 목록 → **알마다 해시 산포 루프**(perBall 7/5/9): 총 42→20→9장 — 반투명이라 얇게 보이던
  문제를 밀도로 해결. 크기·말림·기울기·높이 전부 해시 지터.

### Changed (특별 요리 검수 라운드 5)
- **자장면 면발 촘촘하게**: 가닥 주파수 340→520 + 흐름 위상 강화 — 골 선 문법 유지한 채 밀도 업.
- **타코야끼 가츠오부시 풍부하게**: 반투명 대팻밥 6→**14장**(알마다 2~3장, 크기·말림·기울기 지터로
  춤추는 느낌), 단계 감소 14→7→3.
- **돈까스 소스**: ①1입에도 **크기 유지**(물린 건 커틀릿 끝 — 소스가 줄면 부자연, 사용자 확정.
  살짝 +x 시프트만) ②색 재보정 — 96421a는 클리어코트 워시로 핑크빛 실측 → **진갈색 데미그라스**
  (6e3512→3f1c08, 드립·자국 동조).

### Changed (특별 요리 검수 라운드 4)
- **자장면 면발**: 사인 명암만으로는 "밀가루 덩어리" — **가닥 경계에만 서는 가는 골 선**
  (pow((1-sin)/2, 3.6) 프로파일: 릴리프 골 파임 + 진한 경계색 lerp) + 굽이치는 흐름 위상.
- **돈까스**: 소스 채도 업 — 데미그라스 레드브라운(96421a→561f08, 드립·자국 동조).
  양배추채 16→**24리본**(간격·층고 확대) + **당근채 3점 포인트**.
- **푸드랩 기억 시점**: `?dishlab=<id>`가 localStorage(world-dishlab-focus)에 저장 —
  맨 `?dishlab=1`로 열어도 **마지막 편집 요리 앞에서 시작**(기본 jajang). 콘솔
  `__foodlab.go('id')` 점프 헬퍼 추가.

### Changed (특별 요리 검수 라운드 3 + 푸드랩 시작 카메라)
- **자장면**: 춘장을 면 돔 프로파일을 따라 내려앉는 드레이프(R 0.0285)로 확장 — 면발을 넉넉히
  덮고 가장자리로 얇아진다. 흘러내리는 혀·윤기 스트릭 위치도 새 높이에 동기.
- **돈까스**: 소스를 "얹힌 띠" → **부어져 퍼진 드레이프 패치**(중앙 도톰·가장자리 얇음·물결
  테두리·럼피)로 — 커틀릿 윗면에 자연스럽게 밀착, 옆면 드립 유지.
- **타코야끼**: 구움 우둘 위에 **번철 그을림 블로치**(윗면 위주 브라운, lerp 상한 0.55 은은하게).
- **푸드랩 시작 시점**: `?dishlab=<id>`로 열면 해당 요리 3단계 앞에서 시작(드래그 불필요).
  ⚠️ 휠줌 글라이드(zoomTargetDist)가 매 프레임 카메라를 끌어당기므로 **동기 필수**(망원경 실측
  재확인) + 랩은 minDistance 1.2 접사 허용.

### Changed (🍔🍜🐙🍱 특별 요리 검수 라운드 2 — 사용자 4건)
- **버거**: 윗빵 글레이즈 광택 제거(플라스틱으로 읽힘) → 매트 빵 질감 — 크럼 스펙클·구움 얼룩·
  밀가루 더스팅 + 미세 요철.
- **자장면**: 면 돔을 진짜 중화면 노랑(f0d68c→d2a850)으로, 가닥 릴리프 2배 + **가닥 사이 골
  음영**(s<-0.35 어두운 골색) + 실물 면 가닥 3→7줄 — "밀가루 덩어리" 리포트 해소.
- **타코야끼**: ①먹은 자리 트레이 소스 자국 삭제(갈색 점=벌레로 읽힘, 금지 주석 박제)
  ②드리즐 지그재그를 알 정수리 위로 — ⚠️ 경로째 y스케일하면 볼 사이에 파묻힘(실측): 경로는
  y≈0에서 만들고 납작화 후 들어올린다 ③가츠오부시 = **반투명 대팻밥**(liquidMat 자식,
  makeFoodGeo clearParts→userData.clearGeo→syncShinyParts 3번째 키) 6→3→1로 감소.
- **돈까스**: 조각 캐스케이드 폐기(사용자 확정: 통으로) → **통 커틀릿** — 물결 가장자리 타원
  슬래브 + 윗면 바삭 요철 + 빵가루 2톤 스펙클, 물기=끝에서 크게 베어물어 고기 단면(2입=꽁다리
  +덴트 2개). 소스는 허리 가로 좁은 띠 유지. 양배추채 9→16리본 3층 수북.

### Added (🍔🍜🐙🍱 특별 요리 4종 조형 — 클로슈 졸업, 12/17 요리 실물화)
- **햄버거**: 참깨 글레이즈 윗빵(광택 자식)·처진 치즈·양상추 러플·패티 크러스트 얼룩. 물기 =
  위아래 두 구를 겹친 **수직 원기둥 덴트**(전 층 단면 정렬 — 빵크럼/속살/치즈/토마토 띠), 2입엔
  위층이 밀리며 기움 + 접시에 참깨 낙하.
- **자장면**: 파란 이중선 중식 그릇 + 면 가닥 릴리프 돔 + 실물 TubeGeometry 면 3줄 + 검은 춘장
  (물결 가장자리·흘러내리는 혀). 1입=비벼진 마블 + **림에 걸쳐 늘어진 면 한 가닥**, 2입=바닥
  소스 스월 자국+잔면. **교훈: 검정에 클리어코트 금지** — 하늘을 거울반사해 은박이 된다 →
  매트+페인트 윤기 스트릭. 잔반은 그릇 안바닥면(y≈0.015) 위에 얹어야 보인다.
- **타코야끼**: 물결 윗단 종이보트+빨간 띠, 6알(크기 지터·구움 우둘) 위로 **소스·마요 연속
  지그재그 드리즐**(남은 알 범위로 재생성), 아오노리·가츠오부시 컬·꼬치 픽. **6→4→1** — 먹은
  자리 소스 자국, 물린 알 단면에 **문어 조각**, 2입엔 픽이 쓰러져 있다.
- **돈까스 정식**: 타원 접시에 **도미노 캐스케이드 5조각**(빵가루 요철+스펙클, 절단면=크러스트
  테+분홍기 고기 심) + 중앙만 가로지르는 소스 띠(광택, 양옆 황금 튀김옷 노출) + 양배추채 리본
  더미 + 밥 돔(스펙클·깨·낱알)·토마토 반쪽·옥수수. 조각 5→3→1, 밥 크레이터·흩어진 밥알·
  튀김 부스러기. **교훈: 조각 세로 균일 열 + 떠 있는 소스 타원 = "테이블"로 읽힌다** — 같은
  각도로 겹쳐 기대는 캐스케이드 + 조각을 감싸는 좁은 띠가 정답.
- 4종 모두 MODELED_DISHES 등록 → 서빙 실물 + 주방 타일 스냅샷 자동 적용. dishlab 3열 확장.
  world-smoke 15/15 · draws 228.


### Changed (🍳 주방 패널 타일 — 이모지 → 실물 3D 스냅샷)
- 조형 완성 8종(MODELED_DISHES)은 타일 이모지 대신 **실물 조형 스냅샷**(도감 문법 그대로: 첫 열람 1회
  오프스크린 96px 렌더 → dataURL 캐시 → 컨텍스트 즉시 폐기, 발열 0). 유리컵·광택 자식까지 자동 포함
  (makeFoodMesh 경유), 접시류 위주라 카메라 앙각 ~36°. **흰 타일 배경 유지**(투명 PNG), 재료부족
  grayscale·잠금 ❓·미조형(특별·마음) 이모지 폴백은 종전 그대로.


### Changed (🚀 우주 자판기 간식 5종 고퀄 리모델 — 스펙 "보완" 컬럼 구현 + 각도 반응 광택)
- **광택 재질 2종 신설**: `gradMatFoil`(은박 — metalness 0.92·roughness 0.16)·`gradMatGloss`(클리어코트
  글레이즈). 정점색 틴트는 유지, **RoomEnvironment 텍스처를 재질 envMap에 직접 참조** — scene.environmentIntensity
  (0.15)에 묶이면 금속이 검게 죽는다(실측). 간식당 광택 자식 메시 +1 드로우(아이스컵 유리와 같은 관례).
  `syncShinyParts()`가 makeFoodMesh 생성·먹기 지오 스왑·스낵랩 3곳에서 자식을 부착/교체/제거.
- **🧃주스 파우치**: 몸통·크림프 은박화(각도 따라 반사가 흐른다) + 마실수록 허리 홀쭉 + 주름 골 음영.
  교훈: **변형은 카메라가 보는 축에** — 깊이(z)만 줄이면 정면에서 "밋밋한 축소"로 읽힌다.
- **🍦아이스크림 샌드**: 통짜 슬래브 → 웨하스 상·하판 차양 + 인셋 속층 패널. 캡 클램프 후퇴는 온전
  단계만(물린 단계에 적용하면 스캘럽 정점까지 끌려가 사선 덮개 아티팩트). **직선 변에 층 경계 경유점
  필수** — 없으면 캡 삼각분할이 층 정점색을 보간으로 지워버린다.
- **🥫튜브 우주죽**: 각진 테이퍼 → 둥근 어깨(반구 캡), 콘서티나 밴드 4개 밀착(케이크 층 교훈: 뜨면
  분리) + 말릴수록 옆(x)으로 퍼짐, 크림프 접힘단도 단계 따라 상승·확폭.
- **🍪별 쿠키**: 설탕 반짝 → 은박 재질(카메라 돌면 진짜 글린트).
- **🌟골드 문크림빵**: 돔 글레이즈(클리어코트 — 각도 바꾸면 윤기가 흐르는 레어템 광택), 반짝별 은박화
  +위로 기울여 밝은 천장 반사 수신, 물린 크림 = 튀어나온 공 → 단면에 드러난 납작 패치.
- 검증: `?snacklab=1` 격자 5종×3단계 반복 대조(스펙 카드와 1:1), 부팅 무에러. (코드는 25e3e09에 선반영)

### Changed (🚀 우주 간식 재작업 라운드 2 — 실물 검수 5건 "거의 안 달라졌어")
- **🧃파우치 = 카프리썬 문법**: 키 줄이기 폐기(어색 리포트) → **키·크림프 고정, 마실수록 얇아지고(dep
  0.016→0.0062) 가장자리 실루엣이 우글우글** + 주름 골 음영 + b2 몸통 좌우 굽이. 라벨 인쇄면은
  크럼플 30%만(라벨 뚫힘 방지).
- **🍦샌드 전면 재조형**: 통짜 슬래브 폐기 → **진짜 4단 조립**(라운드 웨하스 상·하판 + 인셋 바닐라·
  딸기층, 도트 딤플, 물기=층별 크레이터 정점 덴트). ⚠️ **벤더 RoundedBoxGeometry는 치수가 어긋난다
  (높이 1/3 실측)** — BoxGeometry 조밀 그리드 + SDF 라운딩 투영(rbox)으로 대체, 치수 정확 보장.
- **🥫튜브 전면 재조형**: 원통 조립 폐기 → **라테 프로파일**(납작 크림프→하단으로 갈수록 z납작·x확폭
  전이→둥근 어깨→리브 캡) + 물면 가로로 누운 말림 롤(굵어지며 상승). 몸통 글레이즈 광택(민트 채도
  확보 — 밝으면 흰 병으로 보임).
- **🍪별쿠키 전면 재조형**: 각진 별 폐기 → **팁·골이 둥근 부푼 도우**(스무스스텝 폴라 별) + 윗면
  돔·우둘 변위 + 중심 밝고 가장자리 토스트 + 구움 얼룩 + 물린 단면 크럼색. 초코칩 불규칙 회전
  반박힘, 아이싱 별은 돔 위 스펙 칩 색.
- **🌟문크림빵**: 바닥 라이너 링·크림 패치 제거(정체불명 고리·계란 리포트) — 물린 단면은 덴트
  정점색이 전담. **초승달 토러스도 제거**(음영으로 "어두운 고리"가 됨 — 사용자 확정: 대체 스탬프도
  넣지 않음, 민무늬 글레이즈 돔+반짝별만). ⚠️ 이로써 매트 파트가 0이 되는 첫 간식 — **빈 parts 병합은
  null을 던져 간식이 통째로 사라진다**(실측) → makeFoodGeo 병합 엔딩에 빈 BufferGeometry 폴백.
- **🧃빨대 접합**: 두 마디가 공중에서 끊겨 있던 것 → 관절 구 + 관절점에서 축방향·반길이로 정확 접합,
  꺾임은 원래대로 좌측(사용자 확정).

### Changed (🍽️ 요리 폴리시 라운드 3 — 실물 검수 2건)
- **생선구이 레몬**: 세워 기울인 각도가 접시를 뚫고 있던 것 → **접시에 평평하게 눕힘**(단면
  과육이 위, rotateY로만 방향). 세운 반달은 rotateZ 기울임이 반지름만큼 아래로 내려가
  접시를 관통한다 — 눕히는 게 정답.
- **잼 토스트**: 핑크 잼 → **거의 빨간 딸기잼**(e0263c→8f0f24) + 촉촉·반투명 읽기 —
  얇은 가장자리·윗면에 빛이 배어나는 웜레드(ff6146)를 정점색으로 페이크 서브서피스
  (단일 드로우 규율 유지 — 진짜 알파는 병합 밖 별도 메시가 필요해 먹기 단계 재빌드
  배관까지 건드림). 광택·과육·드립·웅덩이도 딸기 레드 계열로 동조. 물린 단면은 진한
  속색 유지 = 잼이 두껍게 발린 느낌.

### Changed (🍽️ 요리 8종 폴리시 라운드 2 — 사용자 실물 검수 6건 반영)
- **주스·스무디**: 컵 밖에 붙어 보이던 물방울 구슬 제거. **레몬 반달 재조립** — 껍질면·과육·부채살·
  테를 전부 같은 로컬 프레임(XY 반달·z 두께)에서 만들고 단일 변환(lodge) 하나로 림에 옮김
  (파트별 회전 어긋남의 근본 수리), 현(밑변)을 림에 1mm 파묻어 접촉이 명확.
- **토마토수프 내용물 전면 재작업**: 촘촘 링판(RingGeometry 40×9)에 정점색으로 그린 수프면 —
  가장자리 딥레드·과육 얼룩(도톰 릴리프), 크림은 진짜 나선(아르키메데스 스파이럴 튜브, rT
  비례라 수프 줄면 함께 축소), 바질 티어드롭 잎, 구운 크루통, 올리브오일 방울.
- **조개찜 질감**: 껍데기 겉=성장륜 4줄+석회질 스펙클+리브 명암 강화, 안=자개 무지개빛 힌트
  (서 있는 밸브가 보여주는 면이라 세게), 조갯살=살결 주름+윗등 관자빛 정점색(형태 불변).
- **생선구이**: b1 단면 타원 축 버그 수정 — CircleGeometry는 rotateY(-π/2) 후 x→z라
  **z축(scale x)을 0.6 압축**해야 몸과 맞물림(y 압축은 단면이 앞뒤로 삐져나오던 원인).
  그릴 자국을 박힌 말뚝 박스 → **몸 단면 타원을 감싸는 사선 시어링 띠**(토러스 호를 x축
  둘레로 돌리고 z 0.6 압축 후 사선 기울임)로 — 피부에 눌린 실제 그릴 자국처럼.
- **샐러드 전면 리모델**: 평면 스텐실 → **3층 수북 히프**(가장자리 말림 컵핑+물결 주름 잎
  10장, 아랫단 드리움·중간 기울임·꼭대기 작은 잎), 당근 리본 컬(납작 토러스 호)+julienne,
  통 토마토(윤광)+반쪽 토마토(단면 과육·씨), 오이는 눕히고·기대 세우고 — **토핑은 잎더미
  위 높이에**(파묻히면 초록 일색이 된다, 실측).

### Changed (🍳 수확 요리 조형 고퀄 리모델 4종 — 스펙 시트 준수)
- **🍞잼 토스트 전면 리모델**: 반투명 봉투처럼 보이던 세운 슬라이스 → **접시에 누운 토스트 +
  잼 스프레드**. 수퍼타원 둘레를 "물기 원"으로 갉아 빵·잼이 같은 원을 공유 — 물린 단면에
  뽀얀 크럼 + 붉은 잼 띠가 층으로 맞물린다(스펙 그대로). 옆·아랫면 시어링, 광택 하이라이트,
  과육 덩어리, 옆면 타고 흐르는 잼 줄기→접시 웅덩이, 물면 빵부스러기. **물기 원 중심은 반드시
  빵 밖** — 안쪽이면 투영이 뒤집혀 아웃라인이 접힌다(실측 버그).
- **🦪조개찜 껍데기 리모델**: 매끈한 크림 공(만두 같던 것) → **진짜 부채꼴 밸브** — 방사 리브
  9줄(끝 스캘럽과 위상 일치)·성장띠 2줄·자개 안쪽·움보, 경첩에서 나비처럼 벌어지고 아래
  밸브에 진한 주황 조갯살+윤기. 밸브는 수면 위로(림 기준 -1mm — 더 낮추면 국물 원판이 삼킴),
  마지막 빈 껍데기는 그릇에 비스듬히 세워 어두운 안쪽에 실루엣(누이면 국물색에 위장됨).
- **🥥스무디**: 평평하던 수면 → 봉긋한 돔(0.55) + **코코넛 채 컬 토핑**(진갈색 겉·토스티드 탠
  교대 토러스 호 + 부스러기 플레이크) — 수위 따라 함께 내려앉는다.
- **🍰과일 케이크**: 흰 통짜 → **분홍 시트·붉은 잼·크림 6층**(스펙 칩 f6c9d8 준수) + 잘린
  옆면에 스펀지 기공 점, 통딸기 라테 프로파일(넓은 어깨에 꼭지잎 5장·나선 씨 9)·씨 링 보이는
  키위 슬라이스를 앞쪽에 기대 세움·크림 돌롭, 물리면 뭉툭한 스캘럽 단면 + 접시에 잼 자국.
  **층 밀착 배치 버그 수정(사용자 리포트 "층 분리")**: 웨지가 rotateX(-π/2) 후 y∈[0,h]인데
  yb+h로 올려 층마다 한 층 높이만큼 떠 — 잼이 스펀지 속에 묻히고 층 사이 틈이 보이던 것 →
  yb 스택으로 밀착. 잼 0.4mm 인셋(코플래너 z-fight 번짐 방지), 바깥 호 프로스팅은 별도
  실린더(반경 불일치로 따로 서 보임) 대신 **정점 페인트** — 임계 R·0.44는 크림이 앞 단면
  가장자리를 살짝 감싸 잼 띠가 크림에 잠기는 룩(의도된 선택, 올리지 말 것).
- 검증: `?dishlab=1` 격자 8종×3단계 스크린샷 반복 대조(스펙 HTML 카드와 1:1) + world-smoke 15/15.
  dishlab 훅은 기존 3랩(snack/drink/food)과 같은 관례로 유지.

### Added (🔥 해변 모닥불 — 휴양지 모래섬 구이 스팟)
- **모닥불 조형**: 티피형 장작 5(껍질·밝은 단면·불 쪽 그을림)+돌 둘레 9+잿바닥·그을린 모래·
  속 숯덩이+꼬치 거치대(Y가지+가로대). **불꽃 = 분수 물방울 문법의 셰이더 Points 2겹**(겉 주황·
  속 노랑 — wxTime 위상 루프로 위로 흔들리며 솟다 줄어듦, CPU 0·드로우 2) + 헤일로·잉걸불
  플리커 + 타닥 소리(근접 감쇠). **해질녘(17.5시)~새벽 점화, 낮엔 꺼진 잿불** — 굽는 동안 강제 점화.
- **구이 3종**(⌘ 곁 메뉴): 생선 꼬치구이(어획 1)·조개구이(조개 2)·마시멜로 꼬치(찬장). 조종 펫
  전용 — **꼬치를 들고 불에 대는 모션**(꼬치가 딥 사이클로 담갔다 들렸다, 소품이 연기), 치익+
  불티, 반환점에 노릇한 익힘 색 전환, 7초 완성 → 클로슈·도감·첫 요리 팡파레. 해제하면 꼬치를
  모래에 떨어뜨린다.
- 배치: 모래섬 반경 2.6→2.9 확장(놀이터 확장 선례) + 모래성·야자 1그루 소폭 이동 + 페리 첫
  부표 동쪽 이동(커진 섬 스침) — world-layout-sweep 전수 통과. 스윕 VIS에 bonfire/kitchen/icebox
  정직 등록(주방은 부스 동급 0.62 — 데크는 걷는 바닥).
- 검증: E2E 7항목(밤 점화·거리 거절·마시멜로 익힘 전환 6.8초 완성·생선 꼬치 재고 정산·해제
  낙하) + 주·야 스샷 QA + world-smoke 15/15. 훅 grillTry/grillState/bonfireState/grillOpen.
- 참고: 모닥불 world.js 코드는 병행 세션 커밋(d56646b·2fc594d)에 함께 실렸고, 이 커밋의
  레이아웃 배치가 기능을 완성한다.


### Added (🍳 수확 요리 8종 조형 + 먹기 3단계)
- 요리가 전부 **임시 클로슈** 하나로만 나오던 것을, 수확 티어 8종에 실제 조형을 붙였다 —
  🥤과일주스 · 🥥코코넛 스무디 · 🍲토마토수프 · 🥗당근 샐러드 · 🍞잼 토스트 · 🦪조개찜 ·
  🐟생선구이 · 🍰과일 케이크. 과일·음식·음료와 같은 규율(bakeGrad 정점색 → 병합 1드로우).
- **먹는 방식을 요리마다 다르게**: 컵은 수위 하강(잔 벽이 음료색으로 비쳐 보인다) · 볼은 덜어내
  안쪽 그늘이 넓어짐 · 샐러드/조개찜은 개수로 감소 · 토스트는 모서리 스캘럽 베어물기 ·
  생선은 꼬리부터 발라 **마지막엔 가시만** · 케이크는 팁부터 잘려 **층 단면**이 드러남.
- **재료가 결과물에서 읽히게**: 수프=토마토+바질+크루통, 샐러드=잎맥 잎+당근 채, 조개찜=성장선
  조개+조갯살, 생선구이=그릴 자국+레몬, 케이크=크림/스펀지/잼 5층+딸기·키위.
- `dishDef(id, name)`로 조형이 있는 요리만 실제 id로 서빙 — 특별·마음 티어는 클로슈 유지(후속).
- 그릇류는 음료에서 얻은 교훈 적용: **얕고 넓게 + 내용물을 림 가까이** 둬야 낮은 시선각에서 보인다.

### Added (🧪 요리 실험 조합 — 레시피북에 없는 맛을 찾아라)
- **숨겨진 실험 레시피 7종**(무지개 믹스주스=서로 다른 과일 3·문어 그라탕=문어+치즈·해물탕=
  물고기+조개·달콤 토마토잼=토마토+설탕·당근 케이크=당근+밀가루+달걀·코코넛 카레=코코넛+고기·
  진주 크림수프=진주조개+우유). 재료 2~3개 자유 조합(비찬장 최소 1개·실패해도 실차감) —
  매칭은 정확 멀티셋/술어. **발견하면 정식 레시피 카드로 승격**(이후 원클릭 조리, 자율 요리사도
  만듦), 미발견은 메인 패널에 안 보임.
- **정체불명 탄 요리**: 매칭 실패 = 퍽! 검은 연기 + 그을린 클로슈(burnt 틴트 — 3입 단계 전부) +
  도감 1칸 횟수 카운트, 다 먹으면 "💦 으엑… 다신 만들지 말자".
- **🧪 실험 탭**(주방 패널): 선택 슬롯 3 + 끓여보기(활성 조건 표시), 보유 재료 타일 그리드
  (계보 전부+찬장 ∞·수량 배지·선택 링·롱프레스 이름), 푸터에 "아직 N가지 맛" + 탄 요리 카운트.
  실험은 주인 몫(자율 요리사는 실험 안 함 — 도감 철학).
- 검증: E2E 11항목(룰 거절·잼/믹스주스 발견·해금·승격 18타일·재료 정산·탄 요리·랩 UI) +
  패널 회귀 갱신 + world-smoke 15/15. 훅 expTry/expState/labOpen. 소비는 consumeUnit으로 유닛화.


### Changed (🍳 주방 리모델 v2 — 은색 아일랜드 (사용자 리디자인))
- 부스 복제 같던 조형(크림 캐비닛+어닝+간판) 전면 교체 → **스테인리스 아일랜드**: 차콜 바디 +
  쿨그레이 그라디언트 상판·쿡탑(달궈진 히트링 발광)·함몰 싱크+**브라스 구즈넥 수전**·우드
  도마·**빌트인 오븐**(벽돌 화덕 퇴역 — 발광 창+스틸 핸들)·뒷면 오픈 선반(구리 냄비·볼 스택)·
  서빙 선반(완성 접시 지정석 — leftover 좌표 이사)·타일 데크.
- **야외 파빌리온 구조**(천장이 없으니 매달 수 없다 — 사용자 지적): 양끝 슬림 포스트 +
  크로스바 + 루버 쉐이드(어닝의 모던 대체), 바에 브라스 펜던트 2·도구 3형제.
- **📋 A-보드 메뉴판**: 곁에 세운 TODAY'S MENU 입간판 — **누르면 바로 레시피 패널**
  (userData.menuBoard 전용 클릭, 접시 회수 문과 별도). 밤 발광 4종(히트링·오븐 창·펜던트×2).
- 조리 FX 좌표 동기(도마·히트링 위 팬·김) + sweep 통과 + 주·야 스샷 QA + 패널·조리 E2E 회귀 +
  world-smoke 15/15. 훅 menuClick.


### Changed (🍳💌 대접 정책 — 본섬만 배달, 아니면 조리대에 "네 몫")
- **배달은 친구가 본섬에 있을 때만**(사용자 확정). 섬 밖이거나 바쁘면 **조리대에 친구 몫을
  남긴다**(leftover 재사용, for 마크·💌 일기) — 친구가 본섬에 한가하게 돌아오면 20초 결 심부름
  폴링이 찾아가 회수해 냠냠("내 몫으로 남겨져 있었다! 고마워서 더 맛있다").
- **원격 순간이동 버그 수리**: 배달 도착 검증이 상태만 보고 거리를 안 재서, 기슭에서 포기해도
  접시가 친구 손으로 순간이동했다 — 근접 1.8m 검증 + 실패 시 주방으로 돌아가 몫 남기기.
- **요리 확률 과열 방지**(사용자 우려): 밥때 요리가 여가 요리와 쿨다운을 공유하지 않아 끼니
  직후 또 요리하는 클러스터가 가능했다 — 끼니 요리 성공 시 nextCookAt 10~20분 공유 스탬프.
- 검증: E2E 4항목(섬 밖→몫 남기기·귀환 회수 심부름·본섬 배달 회귀·원상 leftover 비움) +
  world-smoke 15/15. 훅 plateErrandNow/petsOrder/petFree.


### Fixed (🙈 숨바꼭질 원정 은신 — 다리 건너 섬에서도 술래가 찾아온다)
- 주인이 다리 연결 섬(놀이터·추억의 섬)에 숨으면 술래가 영영 못 오던 것 — 원인 3겹을 실측 수리:
  ① **경로 빌더에 집 우회**: 광장→NE 다리 직선이 집(회전 사각 차단)을 관통 — 스텝퍼 슬립은
  작은 원용이라 못 돌아 무한 왕복. 다리가 집 벽을 관통하면 어깨 지점(±수직 2.4m)을 한 번
  끼운다 — **월드의 모든 goto가 혜택**.
  ② **원정 모드**: 하이더가 본섬 밖(7m+)이면 25초부터 발소리 전용 — 순찰 다리가 하나만 끼어도
  술래가 왕복 20초씩 본토로 끌려가 예산이 다 탔다(추억의 섬 1.68m 접근 후 무발견 실측).
  ③ **unstick 우회**: 발소리 재발급이 슬립 상태를 리셋해 분수 곁에서 얼어붙던 것 — 한 발짝도
  못 뗐으면 3m 무작위 우회로 몸을 빼고 재시도(보트 후진 리트라이와 동류).
- 다리 없는 곳(휴양지 모래섬·물 위)은 술래가 못 건너므로 **90초 버티면 하이더 승리 — 의도된
  규칙**(AI끼리 판은 종전대로 본섬 전용).
- 검증: 원정 매트릭스 — 놀이터 66초·추억의 섬 81.5초·본섬 구석 34.5초 발견, 모래섬 하이더 승,
  AI 2판·간식 부스 회귀·world-smoke 15/15. 훅 routeProbe/blockedProbe/propsNear/aiOf.


### Fixed (🙈 숨바꼭질 — "나무 뒤에 숨어도 가끔 못 찾음"의 진짜 마지막 원인)
- 주인 은신 모드 재현: 본섬 구석 나무 측면에 숨자 **107초 무발견**(수색 중 최소 접근 4.69m —
  시야 3.2m 밖). 원인 두 겹: ① 순찰 목록에 무인도 프롭까지 40+개라 한 바퀴가 90초 예산 초과 —
  구석 은신처는 순찰 순서 랜덤에 따라 아예 방문이 안 됨("가끔"의 정체) ② 발소리 수색은 순찰
  완주 후에만 시작.
- 수리: 순찰은 본섬(카운트 지점 ≤8m)만 + **"몸이 풀린 술래"** — 25초부터 발소리(하이더 부근
  ±1.4)를 순찰 사이에 절반 확률로, 60초부터 거의 매번 끼워 넣는 단계적 압박. 초반엔 숨는 재미,
  후반엔 반드시 찾아냄.
- hsGo 훅이 빙의 하이더까지 광장으로 끌어오던 것 수리(숨은 자리 존중 — AI 참가자만 정리).
- 검증: 같은 구석 은신 3연속 발견(31~39초) + AI 모드 2연판 회귀 + world-smoke 15/15.


### Added (🕰️🍳 요리 Phase 2-3 — 부재 중 요리: 조리대 위의 접시)
- 자율 요리사가 오프라인 정산에 합류(offlineCap 1): 부재 중 재료가 허락하면 요리 1접시 —
  재료 실차감·도감 기록·이벤트 로그, 그리고 **조리대 위에 클로슈가 조용히 놓인다**(토스트
  금지 원칙 — 흔적은 월드가 말한다). 접시가 이미 있으면 또 만들지 않는다.
- **주방 첫 클릭 = 접시 회수**: 곁의 펫(빙의 우선)에게 건네고 그 펫이 냠냠, 다음 클릭부터
  평소처럼 레시피 패널. 리로드해도 접시 복원(world-kitchen-leftover).
- 검증: E2E 7항목(정산→접시·도감·중복 방지·리로드 복원·회수→냠냠·패널 복귀) + world-smoke
  15/15. 훅 leftover/kitchenClick. **요리 Phase 2 (3/3) 완주.**


### Added (🍚 요리 Phase 2-2 — 밥때 연동)
- 08/12/18시 밥때에 **30% 확률로 밥그릇 대신 직접 만든 요리**로 한 끼(재고가 허락하고 주방이
  한가할 때만). 일기는 "아침으로 직접 만든 ○○를 차렸다 🍳" 플레이버.
- 끼니 소진은 성공 확인 후 원칙: 조리가 걷는 사이 재료 소실로 취소되면 끼니를 환급해 밥그릇으로
  재시도(같은 창 안).
- 검증: E2E 4항목(실제 updateMeals 분기 재트리거 → 요리 식사·끼니 키 유지·재료 정산) +
  world-smoke 15/15. 훅 mealCookP/mealReset/mealDoneOf.


### Added (🍳 요리 Phase 2-1 — 자율 요리사, 17번째 자율활동)
- 재고로 만들 수 있는 요리가 있으면 펫이 스스로 주방에 가서 만든다(weight 10·쿨다운 10~20분·
  식사 준비 무드 11~12시/17~18시 ×1.3·영원 케이크는 제외 — 그건 주인이 시키는 이벤트 요리).
- **절반은 셀프 냠냠, 절반은 접시를 들고 절친에게 걸어가 대접** — "○○가 만든 △△를 받았다"
  일기+선제대화. 절친이 자거나 바쁘면 자연스럽게 내가 냠. 펫→펫 접근은 직전 숨바꼭질 수리로
  검증된 문법.
- 게이트: 유효 활동 수 12.5→13.5종, 1위 로켓 10.5%. 시뮬 동기(재고 게이트는 미모델 주석).
- 검증: E2E 6항목(조리→운반→대접→절친 완식·재료 정산·셀프 경로·재고 없음 nodish) +
  world-smoke 15/15. 훅 aiCookGo/aiCookState.


### Fixed (🙈 숨바꼭질 — 술래가 코앞의 펫을 지나치던 버그)
- 사용자 리포트("술래가 가까이 있는 펫도 잘 못 찾아가") 라이브 재현: 술래가 하이더 1.1m 옆을
  시야 원뿔 판정 때문에 그대로 지나침. **수리 5종**:
  ① 팔 뻗을 거리(0.95m) = 무조건 발견(프롭에 가려도·등 뒤여도 — 부스럭 소리)
  ② 수색 웨이포인트를 프롭 중심(콜라이더 안 — 비비다 헛돎)이 아니라 하이더가 실제 서는
  스탠드오프 지점으로 ③ 하이더가 다 숨기 전에 수색이 시작되던 것 — 정착 완료 대기 + 경로
  막힘 시 재선택(앵커는 항상 카운트 지점 — 하이더 기준 재선택은 섬 밖 스팟을 열어 13m 표류
  실측) ④ 숨을 곳 거리 캡 2.8~6.5m(스팟 좌표 기준 — 멀면 숨다 들키고 가까우면 개시 즉시
  들킴) ⑤ 재수색 절반은 발소리(하이더 부근 ±1.2m) + 참가자가 광장에서 9m 밖(무인도·앞바다
  표류)이면 판 자체를 조용히 불발.
- 검증: E2E 4연판 전부 발견 완주(16.5~27.5초) + world-smoke 15/15. 훅 hsGo/hsState.
  — 자율 요리사 "절친에게 대접"(Phase 2)의 선행 부품(펫→펫 접근) 검증 목적.


### Changed (🍳 주방 패널 — 타일 그리드로 리디자인)
- 17행 텍스트 리스트가 너무 길었다(사용자 피드백) — **흰 바탕 정사각 타일 4열 그리드**로 교체.
  요리는 이미지 자리(지금은 이모지 대체 — 조형 확정 후 교체), 상태는 한눈에: 가능=컬러,
  재료 부족=회색조, 마음 요리 잠금=회색 타일 ❓, 만들어 본 요리=구석 ✓.
- **이름·재료는 펫 모션 메뉴 문법 그대로**: 데탑=hover 툴팁, 터치=길게 눌러 라벨
  ("당근 샐러드 만들기" / "과일주스 — 과일 아무거나×2 부족"), 길게 누른 손가락은 실행 안 됨.
  부족 타일 짧은 탭=부족 재료 토스트.
- 검증: E2E 6항목(17타일·잠금 5·부족 라벨·부족 탭 무해·타일 탭=조리 시작) + 패널 스샷 QA +
  world-smoke 15/15.


### Added (💗 요리 기반 7/7 — 마음 요리 해금 + 영원 케이크)
- **마음 요리 해금 = 요리 도감 마일스톤**(기념비는 장식이라 수치 없음 — 확인 후 폴백 확정):
  서로 다른 요리 1종 → 뼈다귀·병아리 비스킷, 3종 → 베프 와플, 6종 → 베프 스테이크,
  10종 → **영원 케이크**. 해금 순간 토스트("💗 새 레시피가 떠올랐어요")+일기.
- **영원 케이크는 혼자 먹는 요리가 아니다**: 완성하면 절친이 어디서 뭘 하든 달려와(소환 문법)
  반쪽을 받아 마주 보고 나눠 먹는다 — 하트 스월+일기("영원히 베프 🎂💗")+선제대화. 재료도
  "찬장 풀세트+계절 과일+**오늘의 장터 재료 하나**"로 — 그날의 특별한 것을 더하는 설정
  (market '*' 와일드카드 지원).
- 검증: E2E 6항목(실조리 1종→비스킷 해금·마일스톤 3/6/10·듀오 소환→반쪽 수령→둘 다 완식) +
  world-smoke 15/15. 훅 dishesSeed/basketSeed/eternState. **요리 Phase 1 (7/7) 완주.**


### Added (🍳 요리 기반 6/7 — 조리 플로우: 주방이 개업했다)
- **조리 시퀀스** = 실무 문법 스테이션 분해: 손질(도마 위 재료 알 통통+탁탁, dig 모션) → 웍
  (팬 들썩·치익·김 모락, dance 모션) → 플레이팅(김 팡+접시 팝, happy) — 펫 신규 모션 0,
  소품 오버레이가 연기(주방 조형은 병합 정적이라 연출 소품은 별도 그룹, 지오만 dispose).
- **완성품 = 클로슈**(뚜껑 은쟁반 — 요리 디자인 확정 전 임시 그릇, makeFoodGeo 분기·⚠️ 디폴트
  else 앞 규율): 들고 다니고 우클릭 '먹기'로 3입(돔→김 나는 무언가→부스러기) — 기존 취식
  시스템 그대로.
- 주문 경로 2종: 패널 카드 클릭 = 심즈式 대리주문(한가한 펫이 주방으로 걸어가 조리, 재료 차감은
  **도착 후** — 걷는 사이 재료가 사라졌으면 취소) / 빙의 곁 ⌘·카드 = 직접 조리(조리 중 이동
  입력 잠금 — 낚시 파이팅 문법). 재료는 7계보 실차감('*'=많은 것부터).
- **요리 도감**(world-dishes-found): 첫 요리 = 팡파레+토스트+선제대화, 패널 ✓ 배지. 일기 기록.
- 검증: E2E 8항목(대리주문 walk→chop→wok→plate·클로슈 수령·당근 정산·도감·부족 거절) +
  취식 E2E 2항목(빙의 직접 조리·3입 완식 bites 1→2→소멸) + world-smoke 15/15.
  훅 cookGo/cookState/dishes.


### Added (🍳 요리 기반 5/7 — 레시피 17종 + 주방 패널)
- **RECIPES 17종**: 수확 요리 8(과일주스·코코넛 스무디·토마토수프·당근 샐러드·잼 토스트·조개찜·
  생선구이·과일 케이크) + 특별식 4(햄버거·자장면·돈까스 정식·타코야끼 — 타코야끼 문어는 잠수
  채집 재고!) + 마음 요리 5(뼈다귀/병아리 비스킷·베프 와플·베프 스테이크·영원 케이크 — 해금制,
  트리거는 7단계). 재료 스펙 = {src, id, n}, src 7계보(바구니·텃밭·아이스박스·잠수채집·조개·
  장터·찬장), '*'=아무거나.
- **주방 클릭 = 레시피 패널**: 상단 살아있는 재고 스트립(수확 4계보 + 오늘의 장터) + 카드
  3상태(가능=밝음/부족=흐림+빨간 부족 재료/마음 요리=??? 잠금+"우정이 깊어지면…" 힌트) +
  만들어 본 요리 ✓ 배지 자리(도감 KV는 6단계에서 기록).
- 검증: E2E 7항목(17종·잠금 5종·당근 2수확→샐러드 가능 전환·장터 정합·해금 훅·패널 UI) +
  패널 스샷 QA + world-smoke 15/15. 훅 recipes/kitchenOpen/recipeUnlock.


### Added (🍳 요리 기반 4/7 — 야외 주방 조형, 커피 부스 옆)
- 카페 거리(NW 링 안쪽) 커피 부스 옆에 **야외 주방** 신설 — 디테일 조형: 크림 카운터+수납장
  문 2짝(손잡이)·나무 상판·**벽돌 화덕**(돔+아치 입구+불 글로우 발광(밤에 스스로 빛남)+굴뚝+
  구리 냄비·나무 뚜껑)·도구 바(프라이팬·국자·뒤집개 매달림)·도마+칼·양념병 3형제·스트라이프
  어닝·KITCHEN 간판. 전부 정적 = worldBake 병합(조리 연출은 추후 오버레이 별도), 공사모드 이동 가능.
- 배치는 world-layout-sweep가 잡아줌(첫 좌표가 루프길 침범 — 링 안쪽 r2.6으로 이동 후 통과).
  스크린샷 QA 4컷(주·야·오빗) — 부스 3형제(커피·간식·주방) 카페 거리 완성.
- 호버 라벨 "🍳 주방 — 곧 개업!" (패널·조리는 5~6단계). world-smoke 15/15.


### Added (🛒 요리 기반 3/7 — 장터 일일 로테이션 + 찬장)
- **장터**: 페리가 아침 운항에서 봐 오는 특별 재료(고기·생면·치즈·달걀) — 날짜 아발란치 시드로
  매일 2~3종 × 각 2~3개 로테이션(같은 날짜=같은 장, 달 보물 주기 시드 문법). 재고는 항상
  시드에서 재계산하고 **소비량만 저장**(world-market-used, 오늘 키만 유지 — 지난 장은 자연 소멸).
  특별식의 프릭션이자 리듬: "오늘 고기 들어왔네 — 햄버거!"
- **찬장**: 밀가루·설탕·우유·기름·춘장 5종 무한(요리의 접착제 — 프릭션은 장터·수확 몫).
- 검증: E2E 7항목(같은 날짜 결정성·12일 로테이션 유니크 9구성·구성 규칙·소비 차감·소진 거절·
  리로드 영속·찬장) + world-smoke 15/15. 훅 market/marketAt/marketUse/staples.


### Added (🧊 요리 기반 2/7 — 아이스박스: 조과가 재료가 된다)
- 본섬 북 잔교 곁에 **아이스박스**(디테일 조형: 민트 투톤 쿨러·뒤 힌지로 빼꼼 열린 뚜껑 틈으로
  얼음 3알과 물고기 꼬리·양옆 U 핸들·래치·FRESH 스티커·나무 팔레트 받침 — worldBake 병합 대상,
  공사모드 이동 가능). world-layout-sweep 통과(첫 배치는 섬 림 초과로 1회 이동 — 스윕이 잡음).
- **조종 낚시 성공 시 "🧊 보관 / 🌊 놓아주기" 선택** 팝업(6초 방치=놓아주기 — 종전 동작 그대로).
  보관하면 어종별 재고(world-icebox, 기기 동기)로 쌓인다. AI 어획은 도감 규칙과 같은 원칙으로 제외.
- 클릭=재고 패널(바구니 패널 문법, 읽기 전용 — "요리 재료로 쓰여요, 주방은 공사 중").
- 검증: E2E 4항목(보관 실버튼 클릭→재고·놓아주기 불변·패널 어종 표시·리로드 영속) + 조형 스샷
  QA + world-smoke 15/15. 훅 icebox/iceboxOffer/iceboxOpen.


### Added (🍳 요리 기반 1/7 — 팬트리: 텃밭 수확 재고화)
- 수확 즉시 소멸하던 텃밭 작물(당근·토마토·해바라기)이 **팬트리 재고**(world-pantry,
  기기 동기 KV)에 쌓인다 — 과일바구니(basketCounts) 문법의 텃밭판. 요리 기능의 재료
  4계보(과일 바구니·조개·해산물(문어 포함, 이미 카운트 재고였음)·텃밭) 중 마지막 조각.
- 검증: E2E 5항목(익은 작물 심기 훅→클릭 경로 수확→재고 적재→밭 비움→리로드 영속) +
  world-smoke 15/15. 훅 pantry/gardenState/gardenSet/gardenPick.


### Added (📻 라디오 리듬타기 — 16번째 활동)
- 펫이 라디오 곁으로 가서: **주인이 틀어둔 음악이 있으면 곁에서 춤만 추고**(켜고 끄기 불가침),
  꺼져 있으면 **스스로 한 곡 틀고 몸을 흔들다 제가 켠 것만 끈다**(주인이 도중에 곡을 바꾸면
  그것도 존중). 음악 파일이 없으면 킁킁 구경 폴백 — 죽은 게이트를 만들지 않는다.
  weight 8·쿨다운 10~20분·트랙 목록 5분 캐시, 켤 때 토스트 1회(갑자기 나오는 음악의 출처 표시).
- 지침 앵커 표(시뮬 헤더)에 신규 3종 반영: 모래놀이10·뱃놀이8·라디오8.
- 게이트: 유효 활동 수 12.0→**12.5종**, 1위 로켓 11.2%. 검증: E2E 6항목(직접 켜고 춤·스스로
  끔·주인 음악 존중 — /api/radio_list 모킹) + world-smoke 15/15. 훅 radioGo/radioState/radioPlay.


### Added (🚣 나룻배 자율 뱃놀이 — 15번째 활동, 탈것 4형제 완성)
- 페리·잠수정·열기구·로켓만 있던 자율 탑승에 나룻배 합류: 펫이 물가로 걸어가 승선 아크로
  올라타 **스스로 노를 저어** 바다 한 바퀴 돌고 떠난 정박지로 귀항, 뭍으로 폴짝 내린다
  (weight 8·쿨다운 10~20분·부팅 시드 5~13분).
- 나룻배는 항로가 없는 배라 출항마다 생성: **짧은 다리(2.5~4.5m) 랜덤워크**로 군도 사이를
  누비고 마지막 두 다리는 독으로 수렴, 선분+측면 여유 0.55m 샘플 검증(실제 조타는 곡선 —
  직선만 보면 섬을 스침), 좌초 시 후진 2회 리트라이. 노젓기는 기존 stepBoat 그대로(관성·
  노 애니·물소리) — AI는 acc/steer만 공급.
- **선결 수리 5곳**: boatRide를 "내가 탄 배"로 가정하던 코드에 driver 검사 — 유저 방향키가
  AI의 배를 몰던 것(updatePlayer), ⌘/Esc가 AI 사공을 물에 처박던 것(interact·releasePossession,
  E2E 실측), 전화 차단·busyRide 오판. 하이재킹은 공짜: 젓는 펫을 빙의하면 노가 유저 손으로.
- 게이트: 유효 활동 수 11.3→12.0종, 1위 로켓 12.0%. 검증: E2E 5항목 × 2연속(승선 아크·항해
  4.8~5.1m·타 펫 빙의+방향키 오염 무반응·귀항 0.98m·뭍 하선) + world-smoke 15/15.
  훅 boatGo/boatState(항로 노출).


### Added (🏰 모래놀이 자율 승격 — 14번째 활동)
- 모래성은 "클릭=대리 지명"만 있던 반자율이었다(피아노의 옛 처지) — 여가 레지스트리 항목
  1줄로 승격: 펫이 스스로 해변 sandspot(삽질/토닥토닥 두 자리)에 앉아 1.5~3분 놀다 일어난다
  (weight 10·쿨다운 8~16분, petSandPlay의 기존 자율 타이머 재사용).
- petSandPlay가 지정 펫+자율 타이머 조합(auto 인자)과 성공/실패 반환(fire 계약)을 지원 —
  클릭 대리 지명·⌘ 직접 놀기 경로는 종전 그대로.
- 게이트: 유효 활동 수 10.3→11.3종, 1위 로켓 12.2%. 검증: E2E 5항목(마운트·둘이 두 자리·
  만석 시 셀렉터 풀 제외·타이머 기상) + world-smoke 15/15. 훅 sandGo/sandState.


### Changed (오프라인 정산 — 재접속 요약 토스트 제거)
- "🕰️ 다녀오셨네요 — 그동안 펫들이: …" 토스트를 없앰(사용자 피드백: 재접속마다 뜨는 정산
  문구가 소음). 정산 자체는 그대로 — 부재의 흔적은 주입된 이벤트 로그·일기·바닥의 낙과가
  조용히 말해 준다. E2E 훅(offlineLast)도 불변, world-smoke 15/15.


### Added (☕🍞 부스 간식 자율행동 + 신규 활동 지침 박제)
- 펫이 스스로 커피·간식 부스에 들러 랜덤 메뉴를 사 온다(weight 12·쿨다운 8~15분·모닝커피
  7~10시·오후 간식 14~17시 ×1.3, 아침엔 커피/오후엔 간식으로 코인플립이 기움).
- **취식 아크**: 커피 = 들고 배회를 재개하고 걷다 멈춘 틈마다 한 모금(3모금 완식 — 걸으며
  마시면 컵이 입에 떠서 idle 게이트), 간식 = 분수 곁 명당으로 옮겨 자리 잡고 연속 냠냠.
  완식 규칙(3모금/3입 → 제거·happy·일기)은 기존 updateHeldPose 그대로 재사용.
- 손이 차 있으면(음료·간식 — 주인이 사준 것 포함) 다 먹을 때까지 새 딴짓 롤은 스킵.
- 게이트: 부스간식 점유 5.0%(신규 상한 10% 안), **유효 활동 수 9.4→10.3종**, 1위 로켓
  13.2%. 시뮬 동기 + **신규 자율활동 추가 지침 8줄을 시뮬 헤더에 박제**(레지스트리 선택·weight
  앵커 비교·dur 정직·cd 대역·시간대·fire 계약·검증 3종·10% 상한).
- 검증: E2E 7항목(커피 수령→산책 홀짝→완식→디렉터 정리 / 간식 수령→명당 이동→냠냠 완식,
  부팅 선점은 rocketAiStart 청산 문법) + world-smoke 15/15. 훅 treatGo/treatState/heldOf.


### Added (여가 시간대 편성 — 하루의 리듬)
- LEISURE_ACTS의 weightAt 훅(로켓 저녁 2배가 원조)에 하루 리듬 데이터 추가: **아침(6~9시)
  스트레칭 ×3·아침 과일 ×1.5, 한낮(11~16시) 물놀이 ×1.3, 해질녘(17~19.5시) 열기구 ×2·페리
  ×1.5, 밤(19시~) 독서 ×2** — 아침엔 몸 풀고, 해질녘 하늘엔 열기구가 뜨고, 밤엔 도서관 불이
  켜지는 동숲식 일과.
- 총량은 불변(시간대는 하루 안에서의 재배치): 게이트 유효 9.4종 · 1위 로켓 14.3%(밴드 안).
  시뮬 동기 완료.
- 검증: 시간대 E2E 4항목(7·14·20시 부팅 실효 weight 비율 3.00/1.30/2.00 정밀 일치, 물림은
  leisureMark로 중화) + world-smoke 15/15. leisureState 훅에 all 옵션(쿨다운·게이트 무시 —
  공식 검증용).


### Changed (여가 weight 재설계 — 물놀이 화석 지분 청산)
- 물놀이 weight 100은 구 체인 맨 앞자리(p 0.25·최단 쿨다운)를 비례 이식한 화석 — 설계 의도값으로
  재설계: **물놀이 100→35 + 쿨다운 2.5~5분→4~8분**(다른 활동과 같은 자릿수), 그네 28→22, 대신
  짧고 귀여운 활동 상향(트램펄린 14→18·과일 11→14·정리 9→10·스트레칭 6→10).
- 게이트(물림과 합산): 물놀이 12.0→**5.7%**(1위→5위), 1위 로켓 14.8%(밴드 안), 유효 활동
  9.4종, 상위권이 9.9~14.8% 조밀 밴드로. 시뮬 동기 — 체인 모드는 cdChain으로 패치 전 값
  보존(역사 스냅샷 불변 확인).
- 검증: 물림 E2E 5/5 회귀 + world-smoke 15/15.


### Added (자율활동 물림 — 방금 한 건 당기지 않는다)
- 여가 셀렉터에 **포만감 감쇠**: 성공한 활동은 매력(weight)이 0.2배로 꺾였다가 30분에 걸쳐
  회복(LEISURE_SATIETY_MS). 쿨다운(풀 제외)이 끝나도 한동안 덜 당기는 소프트층 — 물놀이 연타
  같은 기계적 반복이 사라지고, 오래 안 한 활동이 자연스럽게 돌아온다. 림월드 여가 내성/심즈
  포만감과 같은 실무 패턴, 펫별 상태(둘이 따로 물림)·세션 한정(저장 안 함 — 부팅 직후엔 전부
  새것 취급이라 오히려 다양하게 시작).
- 실효 weight 계산을 leisureWeightOf 헬퍼로 승격(선호 × 시간대 × 물림 ÷ √지속분) — 셀렉터와
  디버그 훅이 같은 식을 공유. 신규 훅 leisureState(풀·스탬프)/leisureMark/leisureTryRoll.
- 시뮬 동기(계약): --satiety off|초 (기본 1800 = world와 동일). 게이트: 유효 활동 8.7→9.3종,
  1위 그네 16.7%(밴드 밖)→로켓 13.8%(밴드 안), 물놀이 12.0→9.1%.
- 검증: E2E 5항목(0.2 바닥·15분 시점 0.5 회복·펫별 독립·실경로 스탬프) + world-smoke 10/10.


### Added (밸런스 시뮬 — 다양성 게이지)
- 시뮬 출력에 **유효 활동 수**(1/Σ여가점유², 균등할수록 활동 수에 수렴)와 **1위 시간점유**를
  상설 표기, 1위가 15%를 넘으면 밴드 밖 표시 — 신규 활동이 다양성을 퇴행시키면 게이트가 잡는다.
- 현행 측정: 유효 8.7종 · 1위 그네 16.7%(밴드 밖) — 이어지는 물림·재설계 커밋의 개선 목표치.


### Changed (🥤 우주 자판기 우주식 5종 고퀄리티화 + 진짜 먹은 자국)
- 일러스트 기준 시트(현재→보완→1입→2입)대로 5종 전면 재조형. 공통 규율: 균일 축소 금지,
  bakeGrad 정점색 + 병합 1드로우, 부스러기는 베어문 쪽에만.
- 🧃 **주스 파우치**: 도이팩 실루엣 + 포일 크림프 탑 + 은박 세로 시트광 + 행성고리·별 라벨 +
  빨간 굽은 빨대. **은박 질감 = 정점 변위 + 면별 밝기 지터 + 법선 재계산** — 1입은 얕은 금간
  주름 2줄, 2입은 4줄 크럼플로 찌그러져 홀쭉해진다.
- 🍦 **동결건조 아이스크림**: 맨 박스 2개 → 웨하스 상·하판(구멍 도트) + 바닐라·딸기 속층
  인셋 스트립. 베어물기는 오른쪽 스캘럽 실루엣 절단(토스트 문법) + 단면에 층색 + 부스러기.
  (익스트루드 캡 면은 윤곽 정점뿐이라 밴드 정점색이 안 나오는 함정 → 속층은 별도 슬래브로.)
- 🥫 **튜브 우주죽**: 둥근 어깨 + 세로 리지 캡 + 몸통을 감는 초록 밴드 라벨(죽그릇·김·별) +
  크림프 접힘단. 1입=하단 눌림+접힘 1단, 2입=하단 2/3이 접힌 주름 밴드로 말려 윗볼록만.
- 🍪 **별 쿠키**: 도톰한 테두리 림(이중 익스트루드) + 아이싱 별 + 초코칩·설탕 반짝.
  베어물기 = 윤곽을 잘게 샘플해 베어문 원으로 투영하는 2D 물결 절단 — 1입은 오른쪽 위 팔
  하나가, 2입은 오른쪽 절반이 사라진다(팔 단위).
- 🌟 **골드 문크림빵**: 위치 기반 하이라이트 패치(정점색 부스트)로 레어템 광택 + 초승달 스탬프
  + 4각 반짝별 + 밑단 라이너 링. 베어물면 sharp 스쿱으로 **달빛 크림 단면** + 크림 봉긋.

### Changed (밸런스 시뮬 — 맨손 실행 = 현행 구성 게이트)
- `npm run balance:world` 기본값이 도구 제작 시점(패치 전 체인·rate 0.35·그네 600s)에 얼어붙어
  있어 맨손 실행이 옛 세계를 보여줬다 — 기본을 **현행 구성(레지스트리 rate 0.40 · 그네 150s ·
  √dur 예산)**으로 뒤집음. 헤더에 "기본값 = world.js와 동기가 계약" 명시.
- 패치 전 스냅샷은 `--mode chain`으로 보존 — 그네 지속이 모드별 기본(체인 600s/레지스트리 150s)이라
  `--mode both`가 진짜 전/후 대비가 된다. 끄기 노브는 `--dur-budget off`.
- 검증: 맨손 = 종전 명시-플래그 실행과 수치 동일(그네 16.7%·한가로움 14.4%), chain = 종전
  베이스라인 동일(52.4% + ⚠ 경고), fit-rate 재현 플래그 주석 보강.


### Added (🕰️ 오프라인 정산 — 창을 닫아둔 시간에도 펫이 산 흔적이 남는다)
- **원칙: 시뮬레이션하지 않고 결과만 분포에서 뽑는다** (통계 정산). 대상은 상태를 남기는
  활동뿐 — 레지스트리에 `offline(n, win)` 훅이 있는 항목만 (없으면 "연출뿐" 선언).
- **발동 = animate 시간갭 감지(5분+)**: 앱 재시작(부팅, `world-last-seen` 30초 저장)과 창
  가림(rAF 정지 → 재개)이 한 문법으로. 밤(22~06시) 부재는 정산 없음 — 펫도 잤다. 장기 부재는
  최근 7일 클램프, 시계 역행은 음수 갭이라 자연 무시. last-seen을 worldSync 키에 넣어 폰·데탑
  이중 정산 방지.
- **훅 3종**: 🍊 과일(≤4회 — fruitPicked 재성장 시계 정합, 최근 6h 몫만 낙과 실체화 ≤5알
  (개당 1드로우 — draws 예산), 그 전 몫은 시들음 로그) · 🧹 정리(≤8알 — **부재 전 묵은
  낙과만** 바구니로: 이번에 흘린 몫은 주인이 주울 몫) · 🎣 낚시(≤2마리 — **로그만**: AI
  어획은 라이브에서도 도감에 안 오르는 규칙(주인 몫)과 동일, 조건 어종 제외).
- **일기 연동**: 정산 이벤트를 부재 구간의 과거 타임스탬프로 worldEvents에 주입(시간순 정렬,
  ≤8줄) — 어제 일기 이월이 이 소재를 먹어서, 밤새 꺼놔도 아침에 "펫들의 어제"가 채워진다.
  복귀 토스트 "🕰️ 다녀오셨네요 — 그동안 펫들이: …" 요약.
- **상한 철학**: 돌아왔을 때 반갑고, 2분이면 소비되고, 안 돌아와도 손해가 아닌 양 — 접속을
  부추기지도 부재를 보상하지도 않는 중립.
- 검증: world-smoke에 **D 섹션(24h 부재 재진입)** 신설 — 정산 발생(깨어있는 960분 감지)·낙과
  상한·이벤트 주입·draws 209 ≤ 250·무에러, 총 15/15. `__worldDev.offlineLast/settleNow` 훅.

### Added (📊 자율활동 계측 — ?stats=1 오버레이 2행 + __worldDev.actStats)
- 성공한 자율활동 시작마다 `actStats[id]` 카운트 — ?stats=1 오버레이에 `acts: dip 3 · swing 2 …`
  둘째 줄, E2E/콘솔에선 `__worldDev.actStats()`. 이후 weight 튜닝을 추측이 아니라 관측으로.
- world-smoke 10/10 · draws 206.

### Changed (🎹⛏️🙈 전역 폴링 활동도 레지스트리로 — 선언·소비 분리 패턴 청산)
- 피아노(20분/8%)·보물발굴(15분/10%)·숨바꼭질(12분/12%)이 각자 들고 있던 `xAutoAt` 전역
  변수 + 흩어진 소비 블록을 `POLL_ACTS` 항목 + 공용 `updatePollActs()` 하나로 — gym/library
  누락 사고가 났던 바로 그 이중 장부 패턴 제거. **케이던스 의미는 종전 동일**(주기당 1회 추첨,
  게이트 불발도 주기 소모, 첫 주기 시드).
- 듀오/대리 지명 성격은 각 항목의 gate가 표현(숨바꼭질 = 둘 다 한가 + 낮 + 무빙의).
- world-smoke 10/10 · draws 206.

### Added (🧘📚 운동·독서 자율활동 부활 — 레지스트리 항목 2개 추가로 끝)
- 0c21ead부터 `gymAutoAt`/`libAutoAt`가 **선언만 있고 소비 블록이 없어 한 번도 자율 발동이
  없던** 스트레칭·독서를 레지스트리 항목으로 편입 — 새 구조의 목적 그대로, 게임 로직 수정
  없이 데이터 추가(+ `petRead`에 auto 파라미터: 지정 펫도 2~4분 타이머로 일어나게)만으로 부활.
- 죽은 선언 2개 삭제. 시뮬(300일): 독서 5.4%·스트레칭 0.6% 점유로 안착, 기존 활동은 비례
  희석(전 활동 25% 상한 유지). world-smoke 10/10.

### Changed (🛝 그네 한 판 2~3분 + 점유시간 예산 — 첫 의도적 밸런스 변경)
- **그네/시소 탑승 10분 → 2~3분 랜덤**(탑승 순간 bed.rideMs 결정): 10분 시절엔 깨어있는
  시간의 절반 이상(시뮬 52.5%)을 그네 위에서 보냈고, 그동안 둘 다 필요한 숨바꼭질이 사실상
  봉쇄됐다(0.4% → 판당 연 11회꼴).
- 셀렉터에 **점유시간 예산** 보정: weight ÷ √(평균 지속 분) — 긴 활동(로켓 7분대·탈것류)이
  빈도 가중치만으로 하루를 먹지 못하게. 각 레지스트리 항목에 dur(평균 지속 초) 필드.
- 시뮬 게이트(300일, rate 0.4 · dur보정 · ride 150s): 그네 점유 **52.5% → 18.4%**(전 활동
  25% 상한 통과), 한가로움 6.5 → 13.9%, 둘 다 한가 0.4 → 1.9%(숨바꼭질 0.03 → 0.12회/일).
- world-smoke 10/10 · draws 206.

### Fixed (⏳ 실패한 시작이 쿨다운을 태우지 않게 — 성공에만 소진)
- 트램펄린·과일·낙과정리가 `'busy'`(경로 막힘·후보 없음)여도 6~15분 쿨다운이 이미 리셋된 뒤라
  헛방이 다음 기회까지 지웠다 — 이제 셀렉터가 **성공에만 쿨다운**을 찍고 실패는 3초 재시도.
- 같은 패턴의 사일런트 실패 2종도 반환 계약에 편입: `startDip`(입수 지점 10회 탐색 실패),
  `startAiFishing`(물가 24방향 전부 막힘) — false 반환으로 통일 (`startDip`은 유지하던 async
  본문을 sync 검증 + 내부 async로 분리, 호출부 의미 동일).
- 그네 lateCd 특례 제거 — "성공에만 쿨다운"이 기본이 되면서 자리 없음 케이스가 일반 규칙으로.
- 검증: world-smoke 10/10 · draws 206.

### Changed (🎲 여가 셀렉터 — 순차 체인을 2단 가중랜덤으로: 순서 ≠ 확률)
- `rollLeisure`가 ① `LEISURE_RATE 0.40`(총량 노브 — 활동을 추가해도 전체 딴짓 빈도는 유지,
  지분만 재분배) ② 자격자 풀에서 weight 비례 1회 추첨의 2단 롤로 — **배열 순서가 확률에
  간섭하던 구조 청산** (종전: 앞 항목의 (1-p)가 곱해져 그네 raw 0.14가 실효 0.069).
- weight = 종전 실효 확률 비례 마이그레이션 값(물놀이 100 · 그네 28 · 낚시 27 · 트램펄린 14 ·
  열기구 13 · 과일 11 · 잠수정 11 · 로켓 9 · 정리 9 · 페리 8) — **체감 변화 0이 목표**, 구조와
  밸런스를 한 커밋에 안 섞는다. 로켓 저녁 2배는 weightAt 곱셈 보정으로 공식화.
- 게이트: 시뮬 rate 역산(0.40 = 총량 편차 -0.5%) + chain vs registry 활동별 편차 전부 ±5%
  이내(±15% 기준) + world-smoke 10/10 · draws 206.

### Changed (🎛️ 자율 여가 레지스트리 — 체인 80줄을 활동 정의 배열로, 동작 무변경)
- `updateWander`의 idle if-체인 10종을 `LEISURE_ACTS` 레지스트리 + 순차 셀렉터 `rollLeisure`로
  순수 이동 — 확률·시드·쿨다운·폴스루('busy' → 다음 항목) 의미 전부 종전과 동일.
- 새 사물의 자율 행동 추가 = 이제 **배열에 한 항목** (예전: 체인 삽입 위치·시드 관용구·싱글턴
  게이트 다섯 군데 — 그러다 운동 매트·도서관이 실제로 누락됐던 구조를 청산하는 밑작업).
- cdKey는 종전 필드명(`nextTrampAt` 등) 그대로 — endTramp처럼 밖에서 쿨다운을 미는 코드 호환.
- 검증: world-smoke 10/10 · draws 206.

### Added (🎲 밸런스 시뮬레이터 — 자율활동 몬테카를로, npm run balance:world)
- `scripts/balance-sim.mjs`: world.js 여가 결정 로직(체인 순서·시드·싱글턴·쿨다운 선소모
  버그까지)을 미러링해 가상 하루×N을 돌리고 **활동별 시작/일·분/일·점유율 표**를 낸다 —
  이후 레지스트리 리팩터·밸런스 패치의 전후 비교 게이트 (world-layout-sweep과 같은 위상).
- 모드: `--mode chain|registry|both`(편차 표+±15% 플래그) · `--fit-rate`(총량 노브 역산 스윕)
  · `--dur-budget` · `--ride-sec` · 시드 고정 재현(`--seed`).
- **베이스라인 실측(400일)**: 그네/시소 점유 52.5%(진단 "절반 이상" 재현), 둘 다 한가 0.4% →
  숨바꼭질 0.03회/일(사실상 봉쇄), 한가로움 6.5%. 가중랜덤 셀렉터는 rate 0.40에서 총량 편차
  -0.5%, 활동별 편차 전부 ±5% 이내 — 마이그레이션 안전 확인.
- **"오늘 썼음" 플래그가 검사보다 먼저 찍히던 순서 버그**: 22시 직후 월드를 열면 GLB 로딩이
  끝나기 전(`!pets.length`)에 플래그만 소모돼 그날 일기가 영영 안 써졌다 — 이제 플래그는
  일기가 실제로 써진 뒤에만 소모 (성공 확인 전 소진 금지).
- **이월 작성**: 22시에 창이 떠 있어야만 써지던 트리거를 "어제 몫이 비어 있으면 다음 오픈 때
  어제 날짜로 작성"으로 — 밤에 앱을 꺼놔도 아침에 열면 채워진다 (과일 재성장과 같은
  시간-함수 문법). `writeDiary`/이벤트 필터에 대상 날짜 파라미터 추가 (기본값 = 오늘, 수동
  경로 무변경).
- 검사 스로틀 60초 + LLM 실패 시 10분 백오프 — 매 프레임 재검사·호출 폭주 방지.
- 검증: world-smoke 10/10 · draws 209.

### Added (⛲ 광장 패스트 트래블 — 지도 핀, 회수 문법 2단 확인)
- 지도에 **⛲ 핀** — 탭하면 탈것 회수와 같은 확인 버튼("⛲ 광장으로 순간이동" / 무빙의면 "광장 구경
  가기")이 뜨고, 한 번 더 탭 = 실행 (오터치 방지 + 지도 UX 한 문법 통일).
- 지도 탭 히트가 **최근접 아이콘 판정**으로 — 광장 ⛲ 곁에 주차된 🏎️처럼 히트 원이 겹치면
  기존 first-match는 늘 차량 회수로 먹혔다 (실측), 이제 탭한 쪽이 이긴다.
- **빙의 펫은 어떤 상황이든 무조건** 분수 곁으로: 로켓 비행·달 뒷면·해저·탈것 전부 강제 적출 —
  트릭 = "해제→재빙의→TP" 3연타로 검증된 청산 경로 전부 재사용. 로켓 탑승만 특수(비행 중 해제는
  '탑승 유지 오토파일럿'이라 진짜 적출 필요): **동승 절친은 솔로 AI 라이더로 승격**해 여정을 마저
  날고, 빈 로켓은 알아서 귀환. 도착 = 스월 스파클 + 일기.
- 무빙의(구경 중) = 카메라를 광장 부감으로 컷 + 줌 글라이드 동기(망원경 교훈).
- 검증: E2E 8항목 — 듀오 우주 비행 중 적출(광장 도착·조종 유지·절친 승격·절친 단독 완주), 달 EVA
  적출(로켓은 달에 도킹 유지), 무빙의 카메라 + world-smoke 10/10.


### Added (🔭 전망 망원경 — 정거장 갑판, 쌍안경 뷰 + ←→ 풍경 전환)
- (피드백 반영) 쌍원 뷰포트 확대(반경 31%→43% 페더) + 가장자리 대폭 소프트 — 꿈결 비네트.
- (피드백 반영) 보는 중 **드래그 시점 회전·우드래그 이동·휠 줌** — 평상시 카메라 조작 그대로 (탭만 종료,
  드래그는 slop 판정이 클릭과 구분). 조작 중엔 방치 타임아웃도 정지.
- (피드백 반영) **달 뷰를 구 밖 원거리(21m) 공 샷으로** — 원인 2중: 커스텀 휠줌 글라이드가 이전 줌
  거리로 매 프레임 끌어당김(장면 컷마다 zoomTargetDist 동기 필수) + maxDistance 15 클램프(뷰 동안
  44로 확장, 종료 시 복원). 깃발 꽂힌 달이 통째로 보인다.
- (피드백 반영) 풍경에 **🏖️ 휴양지 모래섬 · 🛝 놀이터 섬** 각각 추가 — 총 7곳 순환.
- **조형**: 갑판 세계 쪽 난간(자판기 반대편 균형점)에 관광 쌍안경 스타일 — 기둥+스위블+쌍둥이 경통
  (크림)+레드 밴드+발광 접안점(자판기 글로우 펄스 공유). 기존 POI 버킷 병합.
- **작동**: 클릭 = 주인이 보기 · 곁에서 ⌘ = 펫이 보기(세계 이중 상호작용 규칙). **쌍안경 비네트
  (CSS 이중 원형 마스크) + 카메라 컷** — 두 번째 렌더 패스 없음(발열 규칙 준수), 실제 카메라를
  빌렸다 원위치 복원. 보는 동안 팔로우캠·이동 정지, ⌘/Esc/클릭 = 그만보기, 25초 방치 안전망.
- **풍경 5곳 + ←→(스틱) 전환**: 본토 랜드마크 2곳(집·분수·연못 셔플) · **절친 라이브 찾기**("👀
  강아지는 지금…" — 실측 컷에 연못 목욕 장면이 잡힘) · 꼬마 달(보물 마커가 살아 있으면 "⛏️ 묻힌 게
  보인다!" 힌트 — 망원경이 보물 탐지기 겸직) · 별하늘. 시작은 랜덤, 좌우로 순환.
- **AI 어포던스**: 갑판 산책 롤에 망원경 구경 12% — 걸어가 들여다보고 "본토를 내려다봤다 — 우리 집이
  콩알만 하다 🔭" 일기(유저 카메라는 안 건드림).
- 검증: 망원경 E2E 6항목(보기·5곳 순환·역방향·그만보기·카메라 복원·갑판 유지) + EVA·자판기 회귀 +
  world-smoke 10/10.


### Added (🤖 자율 우주 탐사 + ⛏️ 달 크레이터 보물)
- **자율 탐사**: AI 혼자 타는 비행이 40% 확률로 코스에 **착륙 스톱**을 넣는다 — 별똥호가 달/정거장에
  스스로 착륙, 펫이 혼자 하선해 **1~2분 존 배회**(깃발 구경·저중력 폴짝·남반구 매달리기·자판기 뽑기)
  후 알아서 재탑승·귀환. 광장에서 달에 주차된 로켓이 보이고, 일기에 "혼자 달을 밟고 왔다 👣" 서프라이즈.
  **미발견 POI엔 착륙 안 함**(첫 발견의 팡파르는 주인 몫). 주인이 산책에 합류하면 귀환 타이머 정지.
- **달 크레이터 보물**: 48시간 주기 재보급, ✕+봉분 마커가 **주기 시드로 온 구체 어디든**(남반구·뒷면
  포함 — 구체 탐험의 이유, 이번 실측 스폰도 남반구) 스폰. ⌘ = 공용 Dig 모션으로 파기 →
  **첫 발굴 = 🧑‍🚀 우주 헬멧 코디 해금**(투명 버블+목 링+안테나, 액세서리 시스템 4호), 이후 =
  문스톤(60%)/운석 조각 수집(개수 영속·일기). `world-moon-dug` KV 동기, 패드 발밑은 스폰 제외.
- 검증: 탐사·발굴 E2E 12항목(AI 착륙→솔로 EVA→재탑승→귀환 / 마커→파기→헬멧 해금→영속→착용) +
  EVA·구체/복원 회귀 + world-smoke 10/10.


### Added (🌕 달 구체 전체 보행 + 🔁 우주 세션 복원)
- **갤럭시식 구면 보행**: 달의 "위"를 월드 y가 아닌 **구 법선**으로 정의 — 조종·자율 모두 적도를 넘어
  **아랫면까지 온 구체**를 걷는다(거꾸로 매달린 어린왕자 그림, 힌트에 "달 뒷면(!)"). 위치는 법선×R,
  자세는 법선 정렬 ∘ (요·기울임) 쿼터니언 — updateRocketPose가 매 프레임 단독 소유. 이동은 카메라
  전방의 접평면 투영 + **관성 방향 유지**(같은 입력인데 투영이 적도 너머에서 퇴화·반전되면 가던
  방향 유지 — 종주 스톨 방지), 점프는 법선 방향 저중력(radial 1.1 실측), 속도는 지상과 동일.
  AI 배회는 대원(great-circle) 이동 + 남반구 "거꾸로 매달려 세계 구경" 어포던스 신설.
- **세션 복원 (BotW 앵커 + 심즈 상태요약 하이브리드)**: `world-pets`에 우주 토큰 저장 —
  착륙/도킹/EVA는 `{poi}`(내구 상태 → 재접속 시 **로켓 도킹 유지 + 펫들 표면 산책 중으로 이어하기**),
  비행 중은 `'flight'`(순간 상태 → 여행을 곱게 정산하고 **본토 발사대 곁 물가에서 시작** + 귀환 일기).
  "우주에 있다가 재접속하면 바다에 뜬 채 시작" 버그의 근본 수리.
- **Fixed — 복원 후 로켓 좌표 NaN**: 복원이 이륙 이징 시작점(free*)을 안 채우면 착륙 상태의
  pad lerp가 undefined→NaN으로 좌표 전체 오염(탑승 판정까지 연쇄 붕괴, 실측). 복원 시 free* 필수.
- **E2E 교훈**: 달 위 펫에게 `tp()`(y=지면맵) 금지 — 위치→법선 파생이라 밑바닥으로 워프. 구면
  워프는 전용 `moonTp(법선)` 훅으로.
- 검증: 구체·복원 E2E 7/7(남반구 종주 minY 29.9·법선 점프·{poi} 복원·flight 정산) + EVA·해제·
  자판기·테더 회귀 + world-smoke 10/10.


### Added (🌕 EVA 무중력 걸음 — 바운딩 로코모션)
- 달·정거장 보행이 일반 뒤뚱걸음 → **저중력 바운딩**(실무 표준: 수평 속도는 그대로, 수직·자세만
  오버레이): 달 = 낮고 긴 도약 사이클(0.62s, 높이 0.22) · 정거장 = 호버-글라이드(0.8s, 0.11).
  이동 중 살짝 앞기울임 + 귀 둥실, 멈추면 진행 중 도약만 마무리 착지. 조종·자율 공용, Space 점프
  중엔 점프 물리에 양보.
- **⚠️ 바운스는 mover가 아니라 비주얼 래퍼(wrap.y)에**: mover.y를 올리면 updatePlayer 물리가
  "낙하"로 해석해 절반쯤 되끌어내림(트램펄린 교훈 재확인 — 실측 0.09로 반토막). 엔티티가 매 프레임
  wrap.y를 재작성하므로 **k>0일 때만 절대 덮어쓰기** — 손 떼는 순간 소유권 자동 복귀.
- 검증: EVA 회귀 + 바운딩 진폭 wrap.y 0.22 실측(70ms 촘촘 샘플 — mover 측정·성긴 샘플은 앨리어싱
  함정) + 테더·world-smoke 10/10.


### Changed (🚶 달·정거장 존 배회 — 어색한 따라다니기 제거, 각자 독립 생활)
- EVA의 "리더 직진 추적"(방향 스냅·이진 정지·고정 속도 — 어색함의 원인) 폐지. 무빙의 펫은 조종
  여부와 무관하게 **존 안에서 각자 독립 배회** — 사용자가 아무도 조종 않고 구경만 할 때도 자연스럽다.
- **앵커 배회 + 회전 보간**: 목표는 존(산책 반경) 안에서만 생성(본토 점프 원천 차단 — walkR 클램프는
  보험), 몸 회전은 스냅 대신 보간, 속도 0.62~0.84 랜덤.
- **로컬 어포던스**(장소가 심심풀이를 광고, 심즈 문법): 달 = 깃발 구경 명당에 서서 깃발 마주보기
  22%·저중력 폴짝 세션(2~4연속) 28%·어슬렁 / 정거장 = 자판기 뽑기 12%·**난간에 서서 발밑 세계
  내려다보기** 30%·어슬렁. 도착하면 2~6초 두리번.
- 검증: EVA 회귀 전항목 + 자판기(이번 런에 8% 골드 실제 당첨 — 골드 경로 실전 검증)·해제·밤귀항
  E2E + world-smoke 10/10.

### Added (🧑‍🚀 테더 유영 — 우주 순항 중 생명줄 스페이스워크)
- **해치 = 점프키(Space/🦘)**: 우주 순항 중 점프키로 해치를 열고 나가 로켓에 생명줄(2.2m)로 매달려
  유영. 캐빈에선 점프가 금지라 비어 있던 키 재활용. 모바일은 순항 중 🦘 복원(조종간은 조이스틱이
  담당), 수동·유영 중엔 🔼🔽(W/S) 스왑.
- **RCS 유영 물리**: 방향키/스틱 = 카메라 기준 미세 분사(관성 + 감쇠 + 드리프트 노이즈, 하얀 퍼프
  이펙트), W/S = 상하. **줄 길이 밖은 구면 투영 + 지름 방향 속도 튕김** — 팽팽한 줄의 감각. 자세는
  느긋한 텀블 + 이따금 슬로우 백플립(공중제비 타이머 재사용). 줄 = 앵커↔펫 직선 라인 1드로우
  (무중력이라 처짐 없음이 물리적으로도 정답).
- **⌘ = 줄 감기**(유영 중 최우선 라우팅, 지수 릴인 → 좌석 블렌드가 이어받음). 유영 중엔 조작
  입력이 조종간을 잡지 않음(분사 전용). **코스가 끝나면 마지막 지점 호버 유지하며 자동 릴인** 후
  재진입, **Esc(빙의 해제) = 즉시 릴 + 오토파일럿**. 첫 유영은 일기+선제대화(+영구 스탬프).
- 검증: 테더 E2E 7항목(해치·분사 이동·줄 제약 len 2.2 실측·⌘ 릴인·재유영·Esc 즉시 릴+오토파일럿
  완주) + 수동비행 회귀 + world-smoke 10/10.

### Changed (🗺️ 우주 랜덤 코스 + 🤖 해제 동작 전면 수리 — "도킹 중 해제하면 펫이 섬으로" 버그)
- **Fixed — 착륙·도킹 중 빙의 해제/펫 전환 시 펫이 발사대로 순간이동하고 로켓이 지상 귀환하던 버그**:
  EVA 이전 설계의 "긴급 하차" 경로 잔재. 이제 착륙 상태 해제 = **자동 하선** — 로켓은 도킹 유지,
  펫들은 표면에 내려서 탐험. 아무 펫이나 재빙의하면 바로 달/정거장 산책.
- **무빙의 표면 자율 어슬렁**: 하선한 펫이 동상이 되지 않게 — 산책 반경 안 랜덤 목적지로 슬렁슬렁,
  도착하면 두리번(2~6초), 달에선 35% 저중력 폴짝·20% 깃발 구경, 정거장에선 8% 자판기 랜덤 뽑기.
  (updateWander는 poiWalk 가드 — 섬 스케줄러가 우주 산책자를 건드리지 않는다)
- **밤 자동 귀항**: 취침 시간(22시)에 무빙의 산책자만 남아 있으면 알아서 로켓에 올라 집으로
  (탈것 밤귀항 패밀리). **비행 중 해제 = 오토파일럿 전환** — 펫이 탄 채 로켓이 마저 비행하고
  착륙 후 물가에 내려준다 (우주 한복판 순간이동 소멸). 오토파일럿이 착륙 시퀀스 중이었으면
  4초 쉬고 알아서 이륙.
- **자동여행 랜덤 코스**: 고정 원 궤도 1바퀴 → **탈 때마다 다른 웨이포인트 코스**(열기구 셔플 문법):
  궤도 아크(반지름 8.5~11·방향·길이 랜덤) → 달/정거장 플라이바이(각 75% 포함·최소 1곳·순서 셔플·
  상공 5~8초 호버 구경, 잠수정 정차 문법) → 귀환 아크 → 재진입. 일기에 "오늘의 우주 코스" 기록,
  미발견 POI 상공에선 "조종간 잡고 내려앉아 보고 싶다!" 선제대화. 자율 비행도 같은 코스 풀.
- 검증: 해제 E2E 5항목(오토파일럿 완주·물가 하차·밤 귀항) + EVA 회귀(Esc=하선 유지·자율 어슬렁
  Δ2.5 실측) + 자동비행·AI·자판기·world-smoke 전부 PASS. `scripts/_rocket-e2e.mjs`는 랜덤 코스에
  맞게 고정 lap 어서션 폐기.

### Added (🥤 우주 자판기 — 정거장 갑판, 골라주기 + 랜덤 뽑기 이중 모드)
- **조형**: 갑판 왼쪽 파일런 곁(하선 지점 대각선 시야) — 크림 몸통 + 블루 옆판 + 레드 탑밴드 +
  **어두운 진열 유리 위 불 켜진 선반 2단·마퀴 라이트**(클래식 자판기 문법, vendGlowMat 숨쉬는 펄스)
  + 미니 상품 4종 진열 + 버튼·동전구·배출 트레이 + 노란 안전매트. POI 고도 게이트라 지상 비용 0.
- **이중 모드 = 세계의 이중 상호작용 규칙 그대로**: **클릭 = 골라주기 패널**(푸드 부스 문법 —
  4종 선택, 멀면 펜딩→곁에 가면 자동 배출) · **곁에서 ⌘ = 펫이 직접 랜덤 뽑기**(연타 방지 1.5초).
  호버 라벨 "클릭 골라주기 · ⌘ 랜덤 뽑기" — 의사 프롭으로 등록(PROPS 배열엔 불가침, layoutId 불변식).
- **우주식 5종** (기존 'food' 슬롯·단계별 먹기 재사용): 🧃 우주 주스 파우치(홀쭉) · 🍦 동결건조
  아이스크림(모서리부터) · 🥫 튜브 우주죽(납작) · 🍪 별 쿠키(작아지는 별) · **🌟 골드 문크림빵
  (8%, ⌘ 뽑기 한정)** — 팡파르+반짝+일기+선제대화+첫 획득 영구 스탬프(KV 동기), 패널엔 ??? 티저.
- **무중력 디스펜스**: 간식이 트레이에서 둥실 떠나오는 아크 → 펫이 받아 그 자리에서 냠냠. 절친이
  갑판에 있으면 15% 확률로 따라 뽑기(자판기로 촐래촐래 걸어가서).
- **Fixed — makeFoodGeo 체인 함정**: 분기 체인의 마지막이 디폴트 `else`(컵케이크)라 그 뒤에 else-if를
  붙이면 전체 부팅 사망("Unexpected token else") — 신규 분기는 디폴트 else 앞에. `node --check`(.js)는
  이걸 통과시킴 — **ESM 검증은 .mjs 사본으로**.
- 검증: vend E2E 6항목(패널·펜딩→자동배출·⌘ 뽑기) + EVA 회귀 14항목 + world-smoke 10/10.

### Added (🧑‍🚀 EVA — 달·정거장 표면 산책 + POI 대형화 + 모바일 우주 조종 버튼)
- **하선 산책(EVA)**: 착륙/도킹 완료 후 ⌘ = 하선 — 펫들이 직접 표면에 내려선다. 달은 **저중력
  점프**(중력 7.0→2.6, 붕 떠오르는 문워크), 절친은 촐래촐래 따라다니기, 울타리(walkR) 클램프.
  로켓 곁 ⌘ = 재탑승·곧장 이륙(발사대의 "타면 떠난다" 문법 미러). 산책 중 Esc 해제 시 **펫들이
  알아서 로켓에 올라 자동 귀환**(AI 라이드 전환 — 빈 하늘 미아 방지).
- **POI 대형화**: 달 r2.3→**R7**(정상 캡 r4.2가 산책 마당 — 구면을 언덕처럼 걷는다, 크레이터 14,
  깃발은 걸어가서 보는 위치), 정거장은 **원형 산책 갑판(r4.2, 난간+착륙 패드 링)** 위 파일런
  코어로 전면 리모델 — 코어 클러스터는 갑판 뒤편 가장자리(z−2.9)로 물려 **앞쪽을 열린 광장**으로
  (팔로우 카메라가 코어에 파묻히던 것 스샷 2회 실측 수정). 수동 비행은 POI 몸체 회피 구가 밀어냄.
- 표면 물리는 `poiSurfaceY` + 펫별 `poiWalk` 플래그(playerSupportY 첫 줄 분기·snapToLand 가드) —
  **전역 groundHeightAt에 섞지 않는다**(같은 x,z 바다 물리 오염 방지).
- **모바일**: 우주에서 조종간 잡기·수동 비행 중 🦘 자리에 **🔼🔽 스왑**(딥 다이브·노랑호와 같은
  마크 모바일 문법 — 🔼(W)가 곧 조종간 잡기라 우주 진입 즉시 등장).
- 검증: EVA E2E 14항목 PASS(착륙→하선→저중력 산책→재탑승 이륙→도킹→갑판 산책→Esc 자동
  귀환→발사대 복귀) + 수동/자동/자율 회귀 + world-smoke 10/10.

### Added (🌕🛰️ 별똥호 페이즈 3 — 꼬마 달·별빛 정거장 POI)
- **꼬마 달**(y37, r2.3): 회백 구 + 크레이터 10 패치·림 + 빨간 깃발("우리가 두 번째") — 낮밤 아크를
  도는 장식 달과는 별개 천체. 꼭대기는 착륙 명당으로 비워두고, ⌘ 착륙 시 로켓이 2.2초 이징으로
  **어린왕자처럼 작은 행성 위에 수직으로 내려앉는다**(위치 lerp + 자세 쿼터니언 slerp). ⌘ 재이륙.
- **별빛 정거장**(ISS풍 미니, y34): 크림 캡슐 코어 + 십자 모듈 + 파란 태양전지판 날개(천천히 태양
  추적 회전 — 도킹과 무관한 축이라 안전) + 창문 스트립 발광(Basic) + 점멸 비콘 + 아래 도킹 크래들
  (링+스트럿, 캐빈이 코어 창문과 눈높이). ⌘ 도킹/언도킹 — **도킹 중에도 무중력 유영 유지**(정거장
  안은 우주니까).
- **W 고도 상한 28→44 개방**("한계가 있는 느낌" 리포트): 궤도(26)에서 올려다보면 POI들이 반짝이고
  W로 올라가 직접 찾아가는 구조. 미발견 시 우주 진입 선제대화가 힌트("조종간을 잡고 W로!").
- 첫 방문 발견 = 팡파르 + 일기 + `world-space-poi` 영속(KV 동기 등록). 지상에선 카메라 고도
  게이트(y6)로 POI 숨김 — 천체 재질은 전용 fog:false(공유 M 변이 금지 규칙).
- **Fixed — 언도킹 직후 ⌘가 재도킹되던 무한 루프**: 이륙한 POI는 반경+1.2를 벗어날 때까지 ⌘
  재착륙 금지(poiCd), 힌트도 동일 규칙 (E2E 실측으로 잡음).
- 검증: `rocket-poi-test` 10항목 PASS(W44·달 착륙·발견·이륙·도킹·발견2·언도킹·재진입·복귀) +
  자동 투어·페이즈2 회귀 + world-smoke 10/10.

### Added (🚀 별똥호 — 진짜 우주감 풀옵션 + 수동 조종 "조종간 잡기")
- **아포지 15.2 → 26**: 하늘돔(42)·별밭(39)·은하수 밴드가 우주에서 **카메라 고도를 따라오는**
  스카이박스 추적으로 옛 "y20↑ 돔 껍질 근접" 제약 해제 — 지상이 ~70% 작아지고 구름층(y5)을 한참
  내려다본다 ("비행기 같다" 리포트의 근본 해결).
- **림 아래는 어두운 우주**: 돔 페인트가 우주에선 수평선(0.52) 아래 반구도 어둡게 떨어뜨리고
  밝은 시안은 수평선의 얇은 대기 테두리(0.52~0.565)로만 — 돔 추적 후 온 화면이 하늘색이 되던
  실측 버그의 수정. fog는 별도 `SKY_SPACE_FOG`(깊은 대기 블루)로 분리.
- **대기 베일 디스크**(y8.5, 발광 림 캔버스): 내려다보면 군도가 얇은 대기층 너머로, 세계 가장자리에
  행성 림 글로우 — 충분히 상승한 뒤에만 떠올라(camera y 11~15 스무스스텝) 통과 번쩍임 없음.
- **은하수 밴드**: 돔 안쪽 기울어진 실린더 띠 + 캔버스 베이크(빛 무리 70 + 미세 별 110, 랩 드로우로
  이음매 방지) — 우주 0.5 / 맑은 깊은 밤 지상 ~0.13(별 페이드 재사용, 별자리 놀이와 시너지).
  우주에선 별 크기 0.28→0.44 보강, 저장 별자리 선·라벨은 우주에서 페이드(별밭만 돔 추적이라 어긋남).
- **수동 조종(노랑호 핸들 문법)**: 궤도 유영 중 조작 입력 = "조종간 잡기" → ↑↓ 추진(우주 관성) ·
  ←→ 방향 · W/S 고도 22~28 · 우주 펜스(EXPLORE_R−4). 잡으면 유영하던 펫이 좌석에 착석(zg 블렌드
  재사용). **⌘ = 우주(자동·수동) 어디서든 재진입** — "탑승 중 상호작용키가 아무것도 안 함" 리포트
  해결. 모바일은 스틱 추진·방향 + ✋ 재진입.
- 검증: `rocket-manual-test` 9항목 PASS(아포지 26·돔 추적·베일·은하수·별 보강·조종간·수동 이동·
  W 고도·⌘ 재진입·착륙 복귀) + 페이즈 2 회귀 7항목 PASS + world-smoke 10/10.

### Added (🚀 별똥호 페이즈 2 — 자율 탑승·하이재킹·야간 발사)
- **자율 탑승**: 배회 롤(첫 쿨다운 시드 7~17분, 재쿨다운 12~24분 — 라이드가 길어 뜸하게) → 본섬 남안
  물가로 걷기(onArrive 클로저 소유권, 열기구 규칙) → **갑판 승선 아크**(발사대가 앞바다라 물가에서
  폴짝) → 혼자 카운트다운·우주 산책. 곁에 절친이 있으면 자연 동승. AI 라이드는 토스트 없이 월드
  일기·선제대화로만 — 광장에서 발사를 구경하는 그림.
- **하이재킹**: 비행 중 라이더(또는 동승 절친) 빙의 = 수동 전환 — 우주면 부감 글라이드 재실행,
  힌트·토스트 활성. 디렉터가 AI 라이더를 뺏으면 빈 여정 완주(열기구 문법). AI 하차는 물가로(갑판은
  배회 금지 구역), 라이더·절친은 접선 ±0.55 분리 하차.
- **야간 발사**: 저녁(18.5~22시) 자율 발사 확률 2배("별을 보러 가는 야간 발사" 일기·선제대화) +
  번 동안 PointLight(0xffa050, 플리커)가 갑판·바다를 주황으로 물들임 — 라이트는 scene.add/remove
  토글(발열 규칙).
- 검증: `rocket-ai-test` 7항목 PASS(자율 출발→도보→아크→AI 탑승→야간 번 라이트→우주→하이재킹
  수동 전환→착륙 조종 유지·갑판 하차), pageerror 0.

### Added (🚀 로켓 "별똥호" — 우주 관광 라이드, 페이즈 1 완성)
- **풀 시퀀스**: 해상 발사 플랫폼 탑승(⌘) → 카운트다운 5초(취소 가능) → 점화 → 수직 상승·중력턴 →
  궤도 1바퀴(아포지 15.2, ≈58초) → 나선 재진입 → 역추진 수직 착륙. 절친이 곁에 있으면(손잡기 포함)
  캐빈에 함께 탑승. Esc 긴급 하차 시 빈 로켓은 여정을 완주하고 돌아온다.
- **조형 2.4× 리모델(펫 둘 기준)**: 1.75×에선 혼자 타도 낀 그림(스샷 실측) → 통통한 전망 로켓으로
  전면 재조형. 유리 돔 캐빈(내경 1.13m, 위아래 금속 림)에 둘이 콘솔을 사이에 두고 등맞대기, **실내
  웜 라이닝**(바닥·천장 + BackSide 벽 — 빛 무시 Basic이라 밤·우주에선 실내등 켜진 캐빈이 공짜).
  스웹 핀 3장(실루엣 압출 + 레드 그라디언트, Extrude는 논인덱스라 `toNonIndexed` 정규화 병합),
  노즈 안테나·비콘. 발사 바지선 R2.05로 확장(프로브 재검증 PASS) + 트러스 갠트리(다리·가로대 5·
  X브레이스·정비 플랫폼·꼭대기 타이바+경고등, 접선 방향 배치 — 기본 카메라에서 실루엣 관통 안 함).
- **발사 연출**: 화염 3겹(백열 코어 콘 + 주황 외염 콘 + 스파크 Points 140) + **연기 트레일**(나이
  들수록 커지는 grow 셰이더 — `gl_PointSize × prog`) + 패드 증기 링(동체 밖 1.4에 낮게 깔림 — 상승
  중 로켓에 점 안 뿌려짐) + 이륙·착지 먼지 충격파 링. Points 크기는 그룹 스케일을 안 타므로 월드
  단위 실측(0.85는 동체를 삼키고 0.4는 색종이 → 0.6).
- **우주 연출**: 진입 순간 1회성 카메라 글라이드(~34° 부감 — 로켓 너머 발밑 군도 미니어처, 이후
  유저 마우스 오빗 자유) + spaceF fog.far 62(군도 전체 노출, 우주 순항 draws 248) + 수평선 대기
  글로우 강화(#9fd4ec) + 낮별 + **별똥별**(7~14초마다 가산 스트릭 1줄) + RCS 잔불(화염 대신 청백 펄스).
- **무중력 유영**: 우주 모드에선 좌석을 떠나 캐빈 안을 실제로 떠다닌다 — 리사주 드리프트(위상차라
  둘이 안 겹침) + 기우뚱 텀블 + 이따금 3초 슬로우 공중제비 + 귀 둥실·다리 늘어짐. 진입/이탈 1.2초
  블렌드로 좌석↔유영 연결.
- 통합: 지면 훅 2곳(`groundHeightAt`+`playerSupportY`)·수영 승선 림·`snapToLand` 갑판 유지·보트/페리
  항로/비행기 주차 회피 전부 `ROCKET_PAD_R` 상수 공유. 검증 = `scripts/_rocket-e2e.mjs`(풀 시퀀스
  헤드리스) + `scripts/_rocket-pad-probe.mjs`(플랫폼 이사 시 필수) + world-smoke 10/10(기준 draws 206).

### Changed (☕🥤 음료 9종 디테일화 + 마시기 3단계)
- `makeDrinkMesh`(단색 프리미티브 Group) → **`makeDrinkGeos(d, sips)`**: 과일·음식과 같은 규율
  (전 파트 bakeGrad 정점색 → 병합)로 재조형. 아이스컵 유리벽만 반투명 별도 메시(`glassMat`)라
  안의 음료·얼음이 비친다.
- **뚜껑 제거** — 따뜻한 4종(아메·라떼·카푸치노·초코)이 뚜껑에 덮여 사실상 똑같이 보이고 `color`·`foam`
  값이 화면에 안 쓰이던 것을, 잔 안쪽 우물(어두운 그림자)과 수면이 보이게 열었다. 구분 문제와 마시기
  단계 문제가 함께 풀린다. 컵은 낮고 넓게(낮은 시선각에서도 수면이 보이도록).
- **종별 시그니처**: 라떼 **라떼아트 하트**(줄면 작아지다 소멸) · 카푸치노 **우유 거품 돔 + 코코아** ·
  초코 **휘핑 3단 소용돌이** · 아메 **크레마 테** · 에스프레소 **받침·손잡이·크레마** · 아이스 4종
  **얼음(수면에 반쯤 걸침) + 물방울 + 빨대 색 구분** · 딸기/녹차 **2층(우유/음료) + 과육·가루** ·
  아이스티 **레몬 슬라이스**.
- **Fixed — 마셔도 변화 없던 것**: `gulps += 1` + 소리만 재생하고 액체를 안 건드려 1·2모금에 아무
  변화가 없었다 → **한 번의 마시기 = 한 단계**(시퀀스 첫 꿀꺽에서만 형상 교체), 가득→절반→조금,
  3모금째 소멸(기존 8모금). 얼음·거품·과육·레몬도 단계마다 함께 줄고 내려앉는다.
- `giveDrink`에 `mesh.rotation.y = π` (과일·음식과 동일) — 앞면이 시청자 쪽으로.

### Changed (🍔 푸드 부스 9종 온전 모델 고퀄리티화)
- 토스트·오므라이스·부리또·핫도그·도넛·붕어빵·삼각김밥·츄러스·컵케이크를 **단색 프리미티브 → 과일과
  같은 규율(bakeGrad 그라데이션 정점색 + 병합 1드로우)**로 재조형. 시그니처 디테일 추가: 오므라이스
  **케첩 지그재그**(돔 표면 따라 튜브), 핫도그 **소시지 캡슐을 크게 복사한 빵에 반쯤 파묻힘**,
  도넛·컵케이크 **컬러 스프링클**, 붕어빵 **비늘·눈·지느러미**, 츄러스 **세로 리지**, 삼각김밥
  **김 사다리꼴 wrap**(익스트루드, 속 가림), 부리또 **접힌 자락 파묻기 + 그릴 그을림**.
- **Fixed — 들었을 때 뒷면 보이던 것**: `giveFood`에 `mesh.rotation.y = π` 추가(과일 `giveFruit`와
  동일 메커니즘) — 앞면(케첩·눈·체리·머스터드)이 시청자 쪽으로.
- 원점=바닥·+y 적층 규약, `userData.topH` 유지(입가 정렬). 베어물기 3단계는 후속 작업.

### Changed (🍎 과일 먹기 — 진짜 베어무는 3단계 조형)
- 먹으면 단순히 크기만 줄던 걸 **실제 파인 형상 3단계**로: 온전 → **1입 베어문(오목 카빙+과육색
  단면)** → **거의 안 남은 잔여** → 소멸. `makeFruitGeo(type, bites)`가 몸통을 고해상도로
  파고(`fruitBiteDent`: 베어문 구 안쪽 정점을 중심으로 밀어넣고 과육 그라데이션으로 재색),
  포도·체리·바나나는 전용 베어물기(알 줄이기·단면 크림). 전부 bakeGrad 병합 1드로우 유지.
- **Fixed — 1먹기=1입**: 한 번의 "먹기"가 시퀀스(count 2~3)로 여러 입을 연속으로 씹어 처음
  상태에서 곧장 2입/소멸로 건너뛰던 것(사용자 리포트) → **시퀀스 첫 씹기(idx 0)에서만 한 단계**
  진행(나머지 비트는 우적우적 소리·머리 까딱임 연출). 이제 1먹기=1입, 2먹기=거의 안남음, 3먹기=소멸.
- **Fixed — 베어문 면이 뒤로 가던 것**: 펫은 wrap 공간에서 -z(앞)를 향하는데(mouthLocal 주석)
  들고 있는 과일은 회전이 0이라 베어문 면(+z)이 펫 뒤 = 시청자 반대편으로 갔다(코코넛·토마토처럼
  둥근 과일에서 특히 티). `giveFruit`에서 held mesh를 **Y축 180° 회전** → 베어문 면·코코넛
  얼굴이 펫 앞(시청자) 쪽으로. held 과일 5경로가 전부 이 함수를 거쳐 한 줄로 전 과일 공통 수정.
- **Fixed — 회전체 4종(사과·레몬·딸기·가지) 안팎 반전**: 이들만 `fruitLathe`인데 프로파일이
  **위→아래 순서**라 `LatheGeometry` 법선이 안쪽으로 생겨 초기부터 오목·베어문 자국이 안 보였다
  (사용자 리포트, three 실측으로 확정). `fruitLathe`에서 프로파일을 **아래→위로 뒤집기** 한 줄
  (`pts.slice().reverse()`) — 형상은 동일, 와인딩만 정상화 → 온전·베어물기 양쪽 바깥 법선.

### Added (🧺 원거리 담기 — 조종 전용, 과일 먹기와 같은 묶음)
- 과일 든 조종 펫을 탭/우클릭하면 뜨는 먹기 팝업의 **"과일 먹기" 바로 아래에 "🧺 바구니에 담기"**
  행(같은 묶음, 과일 들었을 때만) — 바구니까지 안 걸어가도 폴짝 던지기 제스처 + 과일이 **큰 아크로
  날아가 바구니에 쏙**(거리 비례 체공, 회전, 착지 반짝+적립 토스트). 손에서 즉시 사라지고 적립은
  착지 순간. 절친 자율 수거·선물은 그대로 걸어서 배달(사용자 지정). (예전엔 라벨 "휙 담기"로 모션
  창에 따로 떴으나, 먹기와 한 묶음으로 정리.) 스모크 10/10.


### Fixed (🤿 허탕 ⌘가 수면으로 튀던 것)
- 잠수 ⌘ 우선순위의 폴백이 '부상'이라 해삼 판정(0.95m)이 빗나가면 곧장 수면행(사용자 리포트)
  — **폴백 제거**(허탕 = 기포 한 방울 피드백). 대신 상승키 완성: **천장에서 W(모바일 🔼)를
  0.25초 더 꾹 = 부상**. 힌트도 "⌘ 줍기·발굴 · W 꾹/⬆️ 부상"으로. E2E 2종 추가(허탕 ⌘ 잠수
  유지·천장 W 부상) — 잠수 30종·모바일 9종·스모크 그린.


### Changed (🪼 해파리 확률 상향 + 🐚 조개 4종 리모델)
- 해파리 6%→**18%/청크**(심해 청크 ~1/3 — 한참 수영해도 안 보인다는 실측 피드백. 게이트 확률
  상향 = 부분집합 보존이라 시드 안전).
- 조개 고퀄 리모델(마지막 조형 대기 카드 소화): **가리비**=방사 리브 7+힌지 귀, **소라**=진짜
  나선 와류 스택+벌어진 입술+뾰족 팁, **분홍조개**=성장 융기 동심 링+힌지 혹, **진주조개**=
  자개(은백~라벤더) 속살 두 껍데기+은은히 빛나는 진주알. 전부 bakeGrad 병합 1~2드로우.
- ⚠️ 실측 함정 2: ①node --check가 이번 오타를 못 잡음 — 부트 프로브가 진짜 게이트
  ②모래섬은 ⌘ 선점 밀집 지대(야자 흔들기→🥥 낙과 연쇄·비행기 1.4m·모래놀이) — 조개 E2E는
  shellPickNearest 훅으로 픽업 경로만 검증. 조개 E2E·스모크·잠수 스위트 그린.


### Added (🪼 해파리 — 해저 레어 야생)
- 시드 스팟(청크당 6% — 열수구보다 레어·수심 2.2m+·무리 2~3, 시드 3계명 준수 체인 끝 추가):
  **납작 버섯 돔 벨**(랩 프로파일 + 립 말림 — 고깔형은 파티모자, 스샷 실측) + 속 심지(겹침이
  빛 응축) + 가는 촉수 8 + 구강완 리본 3. **은은 발광 반투명**(분홍/청록 2색, 전 스팟 공유
  재질 = 누수 제로), 촉수는 켈프 스웨이 셰이더(CPU 0). **전형 메두사 유영**: 수축(위로
  분사)→글라이드(살랑 침강) 비대칭 펄스 + 표류 + 밴드 클램프. 밤바다에서 발광이 살아난다.
- E2E: seaPoiScan에 jelly, 무리 로드·펄스 유영(y 실이동) 검사 — 잠수 스위트 ALL PASS + 스모크 10/10.


### Changed (🎣 낚시 어종 13종 조형 리모델 — 대기 카드 소화)
- oval/sleek/round 공용 몸통을 스쿨피시급으로: **비대칭 유선형**(배가 앞 1/3 — 대칭 방추는
  어뢰) + **포크 꼬리 2엽** + 등(낫)·가슴·배지느러미 + 눈 하이라이트. 종별 시그니처:
  고등어 핀렛 3개·연어 기름지느러미·잉어 등 반점+수염·금붕어 나풀 긴 꼬리. 복어 가시·아귀
  초롱·메기 수염·개구리/가오리/장화/유리병 특수 조형은 유지. ⚠️ 낫 지느러미 rake는 −rotateX
  (+는 앞쏠림 — 도감 렌더 실측). 도감 아이콘은 세션 캐시라 자동 갱신. 절친 낚시 E2E(랜딩
  연출 포함)·스모크 그린.


### Changed (🐟 해저 물고기 떼 리모델)
- 찌그러진 구+콘 하나 → **유선형 몸통 + 포크 꼬리 2엽 + 등/가슴지느러미 + 눈**. 루미넌스
  램프 굽기(등 어둡고 배 밝게) × **개체별 instanceColor 틴트**(은청·청록 4계열 + 명도 지터
  — 문서화된 흰~회색 베이크 패턴) + 체격 변주. 유영에 선회 뱅킹·보브 피치(compose 행렬,
  할당 제로). 10→12마리, 여전히 1드로우. 낚시 어종(FISH_SPECIES) 조형은 별도 대기 카드.


### Fixed (🍆 과일 동기 첫 이주 시드)
- 데탑에서 딴 가지가 폰에 그대로 달려 있던 것 — 원인 2겹: ①데탑 월드 창이 동기 코드 이전의
  옛 세션(창 새로고침 필요 — 코드 배포는 창 리로드 전까지 안 탄다) ②과일 부트에 KV식 시드가
  없어 서버가 비면 '다음 과일 행동'까지 기존 수확 기록이 안 올라감 → **서버 빈 상태면 부트
  때 이 기기 값을 올린다**. 서버 파일 실측으로 확인(world_fruit.json 부재 = 전송 이력 0).


### Fixed (🔄 KV 첫 이주 시드)
- 서버 KV가 빈 첫 이주 때, 빈약한 기기가 먼저 저장하면 풍부한 기기 컬렉션이 부트에서 덮이는
  유실 시나리오 — **부트 때 서버에 없는 키는 이 기기 값을 올린다**(시드). 이후는 키별
  last-writer-wins. 백엔드 8765·3456 재시작으로 신규 엔드포인트 활성(KV 왕복 실측).

### Added (🔄 월드 상태 전면 서버 동기 — /api/world_kv)
- 기기별로 갈리던 나머지 상태 **17종 전부 서버로**: 조개·본섬 보물·해산물·물고기 도감·코디
  해금·해저 발견·해저/무인도 발굴·무인도 발견·전등·펫 이어하기·계절·가로등 밝기·이벤트 로그·
  그림일기 스탬프·우편 읽음·도감 배지. 범용 KV(원시 문자열 그대로 — 스키마 무관): 부트 때
  서버가 로컬을 덮고(모든 리더보다 먼저), 변경 키만 900ms 디바운스 POST. 정적 서빙은 로컬
  캐시 폴백. **기기 유지 4종**: eco(성능)·layout/fruit(전용 API)·last-seen(기기별 리캡).
- ⚠️ 함정 실측: setItem 일괄 치환이 중괄호 없는 `if…else` 한 줄을 깨서 부트 전멸("Unexpected
  token else") — 치환 후 else 인접 스캔 필수. 스모크 10/10 + 잠수 27·메뉴 25·과일 눕기 5 회귀 그린.


### Fixed (🍆 낙과 눕기 + 🔄 과일 상태 서버 동기)
- 가지·바나나 낙과가 세로로 땅에 꽂히던 것(원점=꼭지·몸통이 지면 아래로) — **길쭉 과일은
  착지 때 옆으로 눕는다**(YXZ 야우→눕히기 + 몸통 반지름만큼 띄움, FRUIT_LAY 테이블).
  둥근 과일은 그대로. 정착·리로드 복원·절친 선물·주입 훅 4지점 공통 헬퍼.
- **폰↔데탑 과일 상태 불일치의 원인 = localStorage(기기별)**: 바구니·낙과·재성장 타이머가
  서버에 안 올라가고 있었다. `/api/world_fruit` GET/POST 신설(꽃 심기 문법) — 부트 때 서버가
  로컬을 덮고, 변경 시 900ms 디바운스 저장. 백엔드 없는 정적 서빙은 로컬 캐시로 폴백.
  ⚠️ 같은 이유로 아직 기기별인 것들: 조개·해산물·도감·코디 해금 (요청 시 같은 문법으로).
- E2E 신설 5종(가지/바나나 눕기·사과 그대로·리로드 유지) + 기존 과일 2종·스모크 회귀 그린.


### Fixed (🏎️ 좌석 미세 조정 2차)
- 몸 앞면이 윈드실드에 닿고 여전히 서 있는 느낌(사용자 스샷) — 좌석을 뒤로(0.06→0.13,
  등받이에 붙게) + 아래로(0.165→0.13). 윈드실드 뒤에 폭 파묻힌 오픈카 탑승 자세 완성.

### Fixed (📞📍 소셜 칸 — 첫 실행 반쪽 행)
- 앱 시작 직후(미조종) 펫 우클릭 시 소셜 행에 🙈만 남던 것 — 목록 메뉴 시절 "📞/📍는 조종 중
  전용" 숨김 규칙이 그리드로 그대로 이관된 탓. **항상 3칸 표시**, 미조종 상태에서 📞/📍를
  누르면 **우클릭한 펫을 조종하며 실행**(그 펫에게 시키겠다는 의도 그대로). E2E에 미조종
  우클릭 소셜 3칸 검사 추가 — 25종 ALL PASS + 스모크 10/10.


### Fixed (🏎️ 좌석 앉기 자세)
- 탑승 펫이 '차 위에 서 있는' 실루엣(다리가 수직으로 갑판에 닿음 — 사용자 스샷) — 페리 벤치
  앉기 문법 적용: **다리 앞접기(-1.35) updateCarPose + 좌석 0.22→0.165**(엉덩이가 콕핏
  욕조에 닿는 높이). 이제 둘이 콕핏에 폭 파묻혀 앉는다.

### Added (🏎️ 오픈 스포츠카 리모델 + 호버카)
- **리모델(라디오 문법급 전환)**: 박스 3개 → **옆면 실루엣 압출**(노즈→후드 스웁→윈드실드→
  오픈 콕핏 파임→리어데크→테일 2D Shape 베벨 압출). 지붕 없는 오픈 콕핏(펫 통째로 보임 —
  사용자 지정) + 실루엣을 통째로 따라가는 흰 레이싱 스트라이프 쉘(⚠️ y-리프트만 하면 노즈
  수직면 z-파이팅 — 프로파일 스케일 1.018 이격), 시트백·미니 핸들·사이드미러·머플러·스포일러·
  번호판, **앞바퀴 실조향 + 속도 비례 스핀**, 테일라이트 브레이크 점등(유니크 재질).
- **호버카(지형 불문)**: 뭍 요구 제거 — 해변에서 그대로 물 위로(펜스=세계 경계). 물 위 파도
  부유 바브·가감속 피치·코너링 롤·뭍↔물 전환 스플래시·물보라 웨이크(발차기 물방울 문법).
  물 위 하차 = 퐁당 수영. **절친 자율 드라이브는 뭍 전용**(landOnly — 표류 방지).
- **회수 시스템 편입**: 지도 🏎️ 아이콘 + 탭 회수 + 밤 자동 귀항 — 이 과정에서
  carCollider.def가 (0,0)이던 잠복 버그 발견(회수가 분수 광장으로 갔을 것) → 홈(저장 반영)으로.
- E2E 신설 6종(승차·물 진입·부유 y·물 하차 수영·회수 홈) ALL PASS + 스모크 10/10.
  ⚠️ 수면 y ≈ −0.5 (0 아님 — 보트와 동급), 북쪽 물가는 페리 잔교 회랑이라 테스트 발진 금지.


### Fixed (🎄 홀리데이 모자 회수 + 메뉴 대칭)
- 홀리데이 "모자 마법"이 씌우기만 하고 안 벗겨 영구 착용되던 것 — **마무리 폴짝 정점(96%)에서
  ✨과 함께 뿅 벗김** + 완료 지점 안전망. 우리가 씌운 것만 회수(hatMagic 표식) — 🧳으로
  직접 쓴 산타모자는 그대로 둔다. 데스크톱 펫 홀리데이도 동일 적용(공유 엔티티).
- 메뉴 우측 여백 — min-width 잔재(150px)가 그리드(128px)보다 넓어 비대칭 — **폭은 그리드가
  결정**(width:max-content) + 행 중앙 정렬. E2E 폭 어서션 155px로 조임, 홀리데이 모자
  뿅→벗김 검사 추가 — 24종 ALL PASS + 스모크 10/10.


### Fixed (🎬 조종 유지 — 진범은 updatePlayer 감시자)
- 직전 수정에도 포옹 걷기에서 조종이 풀리던 진짜 원인: **updatePlayer 첫 줄 감시자**
  (`ai.state !== 'player' → releasePossession`)가 gotoAsync 다음 프레임에 발동 — 아래에
  넣었던 양보 브랜치는 도달 불가였다(E2E는 솔로 폴백만 타서 못 잡음). 감시자를 "goto/busy =
  디렉터 대여(유지+힌트+카메라 추종), 그 외만 해제"로 교체. ⌘ 상호작용(피아노·운동·독서
  등)도 같은 규칙으로 조종 유지가 된다. E2E에 감시자 직접 재현(ctrlBorrow/ctrlReturn —
  대여 중 유지·releaseAI 후 player 복귀) 추가, 22종 ALL PASS.

### Fixed (🎛️ 메뉴 3열 + 🎬 모션 중 조종 유지)
- 우클릭 메뉴 4열(~190px)이 펫을 가림 — **3열(162px, 이전 목록 폭과 동급)** + 놀이·소셜
  라벨 텍스트 제거(폭 원흉).
- **포옹·놀이·홀리데이 모션이 조종을 풀던 것**: releasePossession 제거 — 듀오 안무 동안
  디렉터(goto/busy)가 잠깐 몰고(updatePlayer 양보 + "🎬 안무 중" 힌트), 끝나면 **releaseAI가
  idle이 아니라 주인에게 반환**(전역 패치 — 어떤 디렉터든 조종 펫을 놓으면 'player' 복귀).
  카메라도 조종 시와 동일하게 계속 추종. 탈것·잠수·트램펄린 중엔 제자리 솔로 폴백(좌석에서
  걸어 나가는 사고 방지). ⚠️ 훅 who는 열기구가 선점 — 조종 상태 훅은 ctrl.
- E2E 20종(3열 폭·포옹 중 조종 유지·종료 후 player 복귀 추가) ALL PASS + 스모크 10/10.


### Changed (🎛️ 우클릭 메뉴 그리드化 + 🧳 가방 인벤토리)
- **모션 14행 → 이모지 4×4 그리드**(동숲 리액션 팔레트 문법): 데탑 호버 툴팁 + 터치 길게
  누르면 라벨 팁(짧은 탭=실행). 놀이·소셜(🙈📞📍)은 한 줄 아이콘 행. 메뉴 높이 ~700px
  스크롤 → **~275px 스크롤 제로**(구 max-height 230이 소셜 행을 잘라 제거 — 스샷 실측).
- **코디 → 독 🧳 가방**(마크 인벤토리 지향): 조종 중에만 뜨는 상주 버튼(🎣 문법), 패널
  제목 "병아리의 가방" — 그 펫의 착용 상태(✓ 테두리)·해금 슬롯·미해금 🔒 ???(도감 문법).
  탭=입기/벗기, 섹션 구조라 나중에 아이템·수집품 섹션 확장 가능. 우클릭 메뉴에서 코디 행 제거.
- E2E 신설 15종(그리드 14칸·코디 행 제거·클리핑 없음·소셜 노출·착용/교체/벗기/잠금·해제 시
  숨김) ALL PASS + 스모크 10/10.


### Changed (🟡 노랑호 좌석 미세 조정)
- 몸통 하부가 선체에 파묻혀 "낀" 실루엣 + 어깨가 돔 유리 밖으로 살짝 삐짐(사용자 스샷 실측
  2회차) — 좌석을 위로(0.14→0.24×K, 몸통 2/3가 돔 안), 간격은 안으로(0.16→0.14×K =
  0.42m, 유리 반폭 0.44 안쪽). 나란히 어깨 맞댄 관광 2인승 완성.

### Changed (🟡 노랑호 1.5× 스케일업)
- 좌석 센터 간격 0.26m에 펫 몸통 ~0.45m — 둘이 겹쳐 캐노피 밖으로 삐져나오던 것(사용자
  스샷 실측). **SUB_K 하나로 전체 1.5×**(그룹 scale·좌석 0.48m 간격+상체가 돔에 보이는
  높이·전조등/기포 앵커·콜라이더·하차/승선 거리 — HOUSE.k 문법). 계류장 0.7m 바깥 이동
  (긴 꼬리가 해변 안 찌르게, 구 기본값 저장은 자동 추종). E2E에 좌석 간격 어서션 추가.

### Added (🟡 잠수정 "노랑호" — 해저 탐험 C단계 완주)
- **조형**: 노란 캡슐 선체 + 흰 배띠 + 현창 3개 + **유리 버블 캐노피 2인승**(운전석 좌 =
  조종 펫·미니 조향휠, 조수석 우 = 절친 — 돔 유리 안에 머리까지 들어오는 높이로 실측 조정) +
  이마 전조등 + 꼬리 프로펠러(회전)·방향타·스키드. 재질별 병합 ~7드로우, 탑승 시 캐노피
  개폐 연출. 계류장 = 모래섬 북서 물가(공사모드 물 위 이동·sub-1 저장·보트 규칙).
- **탈 때마다 새 항로**: 명소 풀(난파선 + 시드 열수구·켈프숲) 1~2 + 모래 벌판 경치 + 심연
  절벽 피날레 50%를 셔플·지터로 엮은 CatmullRom 폐곡선, 출항 토스트로 "오늘의 항로" 공지.
  **명소 앞 8초 관람 정차**(뱃머리 돌려 호버링 — 페리 dwell 수중판), 발견 시스템 자동 연동
  (투어 = 레어 명소 이지모드). 수심 프로파일 랜덤(바닥 스치기/중층).
- **항로 안전 3단**: 출항 게이트 2점(선석이 섬 곁이라 드나드는 길 강제) → **군도 둘레 R18
  아크 구축**(중앙 클러스터 존 전부 R17 안 — 횡단 구간은 둘레를 감아 돈다, 사후 넛지 검증만으론
  폴백 50% 실측) → 잔여 위반은 ±0.5rad 아크 2점 회피 삽입(협수로는 수로 정중앙, 중복 가드).
  12롤 폴백 0 실측. y는 매 프레임 [해저+0.6, 수면−0.42] 클램프가 안전망.
- **조작 모델**: ⌘ 탑승 = 자동 투어 / **투어 중 아무 조작 = "핸들 잡기"**(수동: ↑↓ 추진·←→
  방향·W/S 수심, 모바일은 🔼🔽 스왑 재사용) / 절친 자율 투어 중 그 펫 빙의 = **하이재킹** /
  ⌘ = 밸러스트 "푸샤—" 부상 후 하차(빈 배 자율 귀항). 손잡고 타면 손→좌석 자동 전환.
- **수중 연동**: 탑승 잠항 = camUnderwater 확정 + 해저 청크 스트리밍 + 카메라 수면캡/바닥
  가드 (딥 다이브 문법 공유). 하차·부상 시 clearSeaFloor 단일 정리.
- **분위기**: 소나 핑(명소 접근 시 간격 단축), 전조등(밤·심해 자동 — scene.add/remove 규율,
  물고기 떼는 잠수정 주변 유영), 잠항/부상 밸러스트 기포 + 스플래시, 꽁무니 기포 트레일,
  절친 자율 투어(시드 쿨다운) + 다녀온 소감 선제대화, 지도 노랑 캡슐 아이콘 + 🧲 회수 +
  밤 자동 귀항 편입, 채팅 상태 라인.
- E2E 17종(승선·조수석·항로 명소·수중 모드·정차 방문·발견 연동·핸들 잡기·W 수심·부상 하차·
  해저 정리·회수·자율 투어·하이재킹·계류 영속) ALL PASS + 기존 27/9종 회귀 + 스모크 10/10.

### Added (📱 모바일 수심 조절 — 🔼🔽 홀드 버튼)
- 모바일 딥 다이브에 수심 조절이 없던 갭(데탑은 W/S) — **마크 모바일 문법의 버튼 스왑**:
  잠수 중엔 쓸모없는 🦘 자리에 🔼🔽 홀드 버튼이 나타나고, 부상하면 🦘로 복귀. 누르는 동안
  heldKeys에 KeyW/KeyS를 주입해 **데탑과 완전히 같은 물리 경로**(별도 코드 없음).
  setPointerCapture + pointerup/cancel/lostcapture 3종 해제 + 스왑 시 잔류 입력 강제 클리어
  (손가락 둔 채 부상해도 안 눌린 상태). 잠수 힌트도 🔼🔽 수심 안내로.
- 모바일 E2E 신설 9종(iPhone 13 에뮬 — 스왑 양방향·🔽 홀드 하강 실측 -1.46→-2.87·🔼 상승·
  부상 후 복귀·잔류 입력 0) ALL PASS + 데탑 27종 회귀 + 스모크 10/10.

### Fixed (📱 모바일 좌상단 겹침)
- 터치에서 조종 힌트가 🗺️ 지도 버튼과 같은 자리(left 14/top 14)라 첫 글자들이 버튼에
  가려지던 것 — 힌트를 지도 버튼 아래(top 68)로. 지도 기능 추가 때부터의 잠복 겹침,
  모바일 잠수 스샷에서 실측.

### Added (📍 해저 명소 발견 시스템 — POI 지도 연동)
- **열수구·켈프 숲 발견**: 7m 접근 시 팡파레 + 토스트("🫧 보글보글 — 해저 열수구를
  발견했다!!") + 이벤트 로그 + 절친 선제대화 소재. 무인도 발견 문법의 해저판 — 레어한
  명소(전 해역 각 ~2곳)가 "찾았다!"로 기록되는 탐험 루프.
- **해저 지도 아이콘**: 발견한 명소가 🫧/🌿로 영구 표시(world-sea-found에 v:/f: 키 —
  기존 소나 인프라 확장), 라벨이 발견 컬렉션 카운트로("🌊 해저 · ✕ 4 · 🫧 1 · 🌿 1" —
  명소는 찾은 것만 센다). 청크 엔트리의 forest가 불리언→좌표 객체로(판정·아이콘용).
- E2E 27종(열수구/켈프 숲 발견 기록 어서션 추가) ALL PASS + 스모크 10/10.

### Added (🫧 열수구 + 🌿 켈프 숲 + ☀️ 커스틱 — 해저 B-3 마감)
- **열수구**: 먼바다 깊은 곳(수심 2.6m+, 청크당 8% 시드)에 검은 현무암 굴뚝 무리(본체+측면
  2기+스커트) — 기포 기둥은 분수 문법 그대로 셰이더 Points(wxTime 루프·CPU 0·전 열수구
  공유 재질 1개). ⚠️ aSize는 픽셀이 아니라 월드크기×2.414 (24px로 넣으면 핵폭발 — 실측).
  **열수구 곁 6m는 새우 스폰 가중 70**(온천 맛집 — 새우 노리면 열수구를 찾아라).
- **켈프 숲 군락**: 청크당 10% 시드(열수구와 배타), 키 1.5~2.7m 잎 13~17장 군락. 모든
  켈프(산발 다발 포함)가 **정점 벤딩 스웨이**로 물결에 흔들린다 — aBend(밑동0→끝1)·aPh
  어트리뷰트 + wxTime, 청크당 1드로우(kelps 버킷 분리 병합).
- **커스틱 정적 굽기**: 해저 모래 텍스처에 빛 그물(물결 폐곡선 셀)을 시드로 구움 — 런타임
  0비용(승인된 발열 설계). 타일 경계는 9-탭 랩으로 이음새 제거. 근거리에서 낙서처럼
  도드라지지 않게 α 0.045~0.10로 실측 튜닝.
- 시드 3계명 준수: POI 롤은 기존 체인 **뒤에만** 추가(보물 tx/tz 불변 — 소나 지도 호환),
  게이트 무관 항상 소비로 체인 길이 고정. E2E 25종(POI 시드 스캔 seaPoiScan→tp→로드,
  드로우 상한 ≤250 — 켈프숲 실측 43) ALL PASS + 스모크 10/10. unload가 Points 지오메트리도
  dispose(공유 재질은 남김).

### Added (🤝 해저 손잡기 + 🗺️ 해저 지도)
- **해저 친구 부르기 = 진짜 동행**: 딥 다이브 중 📞로 부르면 절친이 수면에 퐁당 입수해
  손을 잡고 리더 수심까지 스르륵 헤엄쳐 내려온다(1.6/s — 입수가 눈에 읽히는 속도) + 절친
  기포 트레일. 전엔 절친이 수면에 동동 떠서 손이 안 잡히던 것(y가 지지면=수면 고정) 수정.
  부상하면 손 잡은 채 함께 수면 복귀(0.4m 이상 격차는 스냅 대신 이징). 수면에서 손잡고
  ⌘ = 같이 잠수(손 안 놓음), 잠수 중 힌트에 🤝 표시.
- **해저 지도 (수중 자동 전환)**: 물속에서 🗺️를 열면 해저 레이어 — 외해 해저 원반 + 심연
  절벽 테두리 + 섬 실루엣/근해 셸프 링 + **소나식 포그오브워**(잠수 중 청크가 로드되면 그
  보물 ✕ 자리를 영구히 안다, world-sea-found) — ✕는 발굴하면 희미해지고 72h 재보급 후 다시
  선명. 난파선은 9m 접근 시 발견 팡파레 + 🚢 영구 표시. 부상하면 수면 지도로 자동 복귀.
  펫 마커(네비 화살표+점)는 지상 지도와 공용화(drawPetMarkers).
- **판정 보강**: 얕은 셸프 잠수에서 팔로우캠이 수면께에 떠 camUnderwater가 플랩하던 것 —
  딥 다이브 중엔 수중 확정. 카메라가 모래 사구에 파묻히지 않게 해저 바닥 가드(+0.35).
- E2E 21종으로 확장(통화 소환→손잡기·해저 동행 수심 일치·해저 지도 모드·소나 보물·난파선
  발견·부상 후 손 유지) ALL PASS + 스모크 10/10. ⚠️ 수영 지지높이는 수면−height×0.45 ≈
  −0.65 — 테스트에서 수면을 0으로 가정하지 말 것.

### Added (🌊 해저 레이어 — 해저 탐험 B단계)
- **빙의 딥 다이브**: 바다 수영 중 독 🤿 버튼(또는 ⌘)으로 진짜 물속으로 — 카메라가 수면
  아래로 따라 내려가고, W/S 수심 조절(비행기 고도 문법)·방향키 수평 유영·기포 트레일.
  잠수 중 버튼은 ⬆️(부상)로 바뀌고, 수면 복귀·Esc·빙의 해제 등 모든 출구는 endDive 단일
  복원. 절친(AI)은 기존 A단계 실루엣 블롭 그대로.
- **수중 렌더 모드**(카메라-수면 판정 단일 신호 camUnderwater): 물색 안개(near 2/far 16)·
  하늘돔/태양 숨김·수면 밑면 '밝은 천장' 평면 1드로우·background 물색. **camera.far
  18 클램프 = 안개 너머 지상 드로우 진짜 컬링**(근해 실측 324→231, 먼바다 ~44 draws).
  부상 시 안개·하늘·far 즉시 원복(updateDayNight(true) — 스로틀 대기 30초 물안개 방지).
- **해저 지형 + 청크 스트리밍**(무인도 문법 — 24m 청크·프레임당 1작업·dispose는 지오메트리만):
  seabedHeight = 근해 셸프(-1.1 접합)/먼바다 사구(-4~-6)/EXPLORE_R 밖 심연 절벽. 사구
  변위 그리드 + 시드 드레싱(바위·켈프·산호는 얕은 셸프만) — **인덱스드(grad)와 폴리헤드론
  (rocks) 버킷 분리**(섞으면 mergeGeometries null → new Mesh(null)이 rAF 즉사 — 실측 후
  무인도 문법으로 정정 + null 가드).
- **해저 플레이**: 바닥 실물 채집(성게 등이 바닥에 보이고 다가가 ⌘ — A단계 룰렛의 진화),
  해저 보물 ✕(청크당 16% 시드·72h 재보급·⌘ 발굴 = 진주/코디/동전 — 무인도 보상 문법),
  난파선 1척(31.5,-18.5 반묻힌 선체+돛대+보물상자), 물고기 떼 InstancedMesh 유영.
- **수중 유영 포즈**: 수평 크루즈 + W/S 승강 피치 + 스트로크 바운스(수면 lean 0.3이 남으면
  위에서 본 풍선이 된다 — 스샷 실측 후 mover 피치 전유).
- E2E 16종(지형 3계층·버튼 플립·수중 모드·드로우 상한 250·수심·수거·보물·부상 복원·Esc·
  영속) ALL PASS + 스모크 10/10.

### Added (🤿 잠수 채집 — 해저 탐험 A단계)
- **바다 수영 중 ⌘ = 퐁당 잠수**(동숲 다이빙 문법): 펫이 곤두박질치며 물속으로 — 수면엔
  실루엣 블롭과 기포만 남고(물리는 수면 수영 그대로 = 무개조), 화살표로 느리게 유영, 3.8초
  뒤(또는 ⌘ 조기 부상) 떠오르며 해산물 채집. Esc·빙의 해제 등 어떤 중단에도 가시성 복원.
- **해산물 8종 조형**(과일 규율 — 1종 1드로우): 성게·불가사리·해삼·멍게·미역·굴·새우·
  문어(★3 레어). **근해(섬 5m 이내)/먼바다 풀 분리** — 문어는 먼바다에서만.
- **도감 연동**: 🐟 도감에 해산물 컬렉션 섹션(스냅샷 아이콘·???·존 힌트), world-seafood
  영속, 첫 채집 표시·문어 팡파레.
- **절친 자율 잠수**: 바다 물놀이 중 가끔 혼자 퐁당 — "구경하고 살며시 놓아줬다"(도감은
  주인 전용, 낚시 문법). 25초 폴 무비용.

### Changed (📌 사용자 공사 배치 동결 — 26건 + 차량)
- 데스크톱 공사모드 배치를 기본값으로 확정: 집(3.42,4.305 — HOUSE 앵커·패드·스윕 사각형
  동기), 나무 7·부스 2·피아노·사진판·도서관·정원·우편함·포탈 회전 등 26건 + 차(2.5,-1.15).
  전수검사에 PINNED(사용자 확정 좌표) 면제 도입 — 가쪽 배치는 의도이므로 림·도로·페어 규칙
  제외, 새 소품엔 규칙 유지.
### Fixed (정정)
- "빈 레이아웃 덮어쓰기 사건"(직전 두 커밋의 Known/가드 사유)은 **오진** — 서버 파일은 래퍼
  없이 저장되는데 진단 스크립트가 `get('layout')`으로 읽어 54키 정상 파일을 빈 것으로 오판.
  실제 유실은 없었다. 가드(빈/무지문 저장 거부)는 심층 방어로 유지.

### Fixed (🛡️ 레이아웃 빈 덮어쓰기 원천 차단)
- 구버전 캐시로 도는 창이 서버 레이아웃을 {}로 덮어 실배치를 지우던 사고(07-17 14:27·14:38
  두 차례 실측) — **서버: 소품 5개 미만 또는 _sig 없는 저장 거부**, **클라이언트: 빈 저장
  전송 금지** 양단 방어. 8765 백엔드 재시작으로 즉시 활성(빈 POST 거부 실측), 데스크톱 앱
  백엔드는 앱 재시작 시 적용.

### Changed (🏞️ 연못·텔레포트 가쪽 이전 + 공사모드 해금)
- **연못 → 남서 림 물가**(-3.44,-3.44): 물가(r 0.76)가 도로 외연(3.875)과 2.5cm+ 여유임을
  기하로 검증하고 스윕 도로 규칙에서 연못을 면제(연못가 길 디자인). 패드는 follow 태그로
  다음 로드부터 동행. 수영·낚시 판정(pondPropRef)은 자동 추종.
- **본섬 텔레포트 → 동남 림**(4.98,-2.06).
- 연못·텔레포트 **공사모드 이동 가능**(MOVABLE_TYPES + 라벨 + 저장·기본값 추종 테이블).
### Known
- 14:27경 어떤 클라이언트(구버전 캐시 창 추정)가 서버 레이아웃을 빈 값으로 덮어써 직전
  공사모드 배치가 유실됨 — 월드 창이 열려 있으면 아무 소품이나 옮겼다 놓으면 전체 재저장됨.

### Changed (🫥 지붕 100% 소멸 + 📻 라디오 리모델)
- 지붕 페이드 종점을 8% 유령 → **0% 완전 소멸**로(사용자 지정) — 소멸 구간에선 메시
  visible까지 꺼 드로우도 절약(클릭 레이캐스트는 visible 무시라 판정 불변). 펫 재실 시에도 0%.
- **라디오 리모델**(동숲 문법): 코랄 투톤 바디 + 크림 페이스 + 가로 그릴 슬랫 + 앰버 다이얼
  창(빨간 바늘) + 노브 2 + 가죽 손잡이 + 크롬 안테나. 포치 데크 모서리에 걸쳐 기울던 배치도
  잔디로 정돈(2.85, 0.15) — 전수검사 통과.

### Added (💡 거실 전등 토글 + 🌞 실내 밝기)
- **천장 펜던트 전구 클릭/우클릭 = 전등 켜기/끄기** — 수동 설정은 영속(world-houselight),
  수동 기록이 없으면 밤(18:30~06:00) 자동 점등. 꺼진 라이트는 scene.remove(발열 §1),
  전구 발광(emissive)도 함께. 토스트·이벤트 로그.
- **실내가 어둡던 원인 = 지붕 그림자**: 심즈식으로 지붕·처마·굴뚝 castShadow=false — 실내에
  햇빛이 그대로 든다(실무 3해법: 지붕 그림자 오프 / 실내 라이트맵 베이크 / 별도 앰비언트 존
  — 돌하우스 구조엔 ①이 정답). 빌드 루프가 userData.noShadow 태그를 존중하도록 확장.

### Changed (🫥 펫 재실 시에도 지붕 컷어웨이)
- 동물이 집 안에 있으면(houseFloorY 재실 판정) 카메라가 멀어도 지붕이 걷힌다(k 0.14) —
  안에서 노는·자는 펫이 항상 보임. 나가면 복원. E2E 2항목 추가(재실 걷힘·퇴실 복원).

### Added (🫥 지붕 컷어웨이 + 가구 벽 밀착)
- **카메라 근접 시 지붕이 스르륵 투명해진다**(심즈식 컷어웨이): 지붕·페시아·굴뚝·피니얼이
  거리 기반 알파 페이드(4.5m 안쪽 8%까지), 골조 기둥·보는 남아 "지붕 걷힌 집"으로 읽힘.
  대상은 유니크 재질 복제 + 병합·베이크 제외(userData.roofFade). 실무 3갈래(컷어웨이/디더
  페이드/실내 별도 씬) 중 컷어웨이 채택.
- three.js 함정 해결: transparent 플래그 전환은 needsUpdate 없이는 불투명 셰이더가 캐시된
  채 opacity를 무시 — 플래그 교차 시에만 재컴파일(스파이크 없음).
- **가구 벽 밀착**: 1.2× 확대로 벌어진 뒷공간 제거 — 소파(−1.02)·책장(−1.11)·다락 침대
  (−0.95) 왼벽 밀착, 콜라이더·앉기 좌표 1:1 동기. 근접샷 검증.

### Fixed (🏠 집 골조 이중 스케일)
- 1.2× 집에서 목재 골조(기둥·처마 보)만 1.44×로 떠 보이던 문제(사용자 스샷) — 프롭 내
  병합(mergePropGroup)이 자식 matrixWorld(루트 스케일 포함)를 지오메트리에 굽고 병합본을
  다시 스케일된 루트에 넣어 이중 적용. **루트 역행렬을 곱한 "루트 기준 상대 변환"으로 굽도록
  수정** — 일반 프롭(루트=identity)은 무변화, worldBake(씬 루트에 넣음)는 월드 공간 유지.

### Changed (🏘️ 본섬 리뉴얼 — 집 1.2×·회전 + 도로 정리)
- **복층집 1.2× 확대 + 현관이 섬 중앙을 향해 회전**(rotY −0.65→−2.22, 위치 2.75,2.1):
  배율은 HOUSE.k 하나로 — houseLocal(÷k)·houseWorld(×k)·높이 반환(×k)·지오 그룹 스케일
  4지점만 소비(하드 상수 재유도 없음, 가역). 가구 시트·실내등 높이 동반. 하우스 E2E 12항목
  (1층 0.06·계단 중간 0.498·다락 0.936 = 0.78×1.2, 벽·난간·포치 충돌, 진입 접지) ALL PASS.
- **도로 다이어트**: 스포크 4→2(집·연못 방향만), 링 3.0→3.6 — 중심 밀집 해소.
- **연쇄 재조닝**(그리드 솔버 + 전수검사): 가로등 6개 링 4.25로, 벚나무 남쪽 뜰 바깥
  (1.35,-4.2), tree-3→NE 놀이터 그늘나무(10.9,5.8), 커피부스 광장 서쪽(-2.35,1.0),
  피아노(-1.34,2.28)·사진판(-2.04,-0.94)·🍆덩굴(-3.5,2.5)·우편함=현관 옆(1.15,2.75)·
  밥그릇·라디오=새 마당·차 주차(2.44,-1.64).
- **스윕 집 규칙 = 회전 사각 풋프린트** — 원-vis(2.4)가 코너를 과차단해 섬이 "포화"로
  오판되던 것 수정. 기본값-추종 테이블(MOVED_DEFAULTS) 11종 추가 + car-1 자기적용부 동일
  규칙 — 저장본이 옛 기본값이면 새 배치를 따라간다.

### Fixed (🍒 "일반 나무에 체리?" + 열매 밀착)
- 체리 배정은 데이터상 벚나무 한 그루가 맞았는데, **벚나무 여름 팔레트가 초록**이라 일반
  나무처럼 보였던 것 — 여름도 연분홍(0xf4b4cc)으로 바꿔 "체리는 분홍나무에서만"이 사시사철
  읽힌다 (봄 만개는 그대로 더 화사).
- 좌우 열매가 수관에서 공중에 떠 있던 간격(사용자 스샷 실측) — big 0.55→0.48/−0.59→−0.52,
  small 0.45→0.41로 안쪽 당김. 잎에 닿아 안긴 상태 확인.

### Changed (🍎 동숲 레퍼런스 정합 + 청소부 튜닝)
- **매달림 = 수관 표면 잎 포켓**: 사용자 제공 동숲 스샷 기준 — 캐노피 밑 대롱이 아니라
  로브 표면에 몸통 ~70% 노출로 안김(위 1 + 아래 양옆 2 삼각), 크기 1.3×(수관 폭의 ~1/4).
  낙과도 1.15×. 좌표는 로브 지오메트리와 1:1(나무 치수 바꾸면 fruitAnchors 동기).
- 청소부 확률 0.06→0.04 — 낚시(0.09)·열기구(0.05)·트램펄린(0.06)보다 낮게, 과일 줍느라
  딴 놀이를 못 하지 않도록. (청소부는 원래 조종 중이 아닌 어느 펫이든 가능)

### Added (🍂 낙과 생애주기 — 영속·청소부·시들기)
- **낙과 영속화**: 바닥 과일이 world-fruit-ground에 저장 — 리로드해도 그 자리에 (떨어뜨린
  시각 포함).
- **🧹 절친 청소부**: 3분 넘게 방치된 낙과를 한가할 때 주워 **바구니에 넣어둔다** —
  "강아지가 떨어져 있던 복숭아를 주워 바구니에 넣어뒀다 🍑🧺" (다리 없는 모래섬 낙과 제외,
  자율 낚시 소유권 문법).
- **🍂 시들기**: 6시간 방치된 낙과는 쪼그라들며 흙으로 — "아무도 줍지 않은 사과가 시들어
  흙으로 돌아갔다". 쌓임이 구조적으로 유한해짐.

### Fixed (🍎 과일 가시성)
- 매달림 앵커가 수관 로브 안쪽이라 3알 중 1알만 보이던 문제(사용자 스샷 실측) — 로브
  밑단 아래(big y0.36 / small y0.30)로 내려 캐노피 밑에 대롱대롱. 멀리서도 "열렸다"가 읽힘.

### Added (🍎 과일 시스템 — 동숲식 수확·먹기·바구니)
- **과일 11종 조형**: 🍎🍊🍋🍌🍓🍇🍑🍒🍅🍆🥥 — 종별 전용 실루엣(Lathe 프로파일·포도 클러스터·
  바나나 초승달 아크·딸기 씨앗 8점·코코넛 얼굴)과 투톤 bakeGrad, 전 파트 정점색 병합으로
  **과일 하나 = 1드로우**. 매달림 3알도 프롭당 1메시 병합 (홈 +14드로우 이내).
- **배정**: 벚나무=🍒 고정 · 일반 나무 7그루=주간 셔플(7종이 매주 전부, 배치만 바뀜) ·
  모래섬 야자 4그루=🥥 특산 · 새 덩굴 티피 2개(남쪽 뜰 길목 🍅, 광장 서쪽 🍆).
- **수확 루프**: 열매 곁 ⌘=흔들기(과일 웍블+잎 플러터+러슬음) → 낙과가 통 튀고 정착 →
  ⌘ 줍기(손 비어야, 글린트 힌트 공유) → 부스 음식 문법으로 손에 들고 ⌘ 냠냠(3입×2=완식,
  하트+선제대화). 딴 프롭은 2시간 뒤 재성장(world-fruit-picked 영속).
- **🧺 과일바구니**(집 마당 어귀): 과일 들고 ⌘=담기 — 담긴 과일이 실제로 소복이 보임(최근
  8알 병합 1메시). 클릭=11칸 패널(카운트·꺼내기). world-fruit-basket 영속.
- **절친 자율 수확**: 한가할 때 나무를 흔들어 주워 먹고 — 절반 확률로 **주인에게 물어다
  준다** 🎁 (발밑에 톡 + 하트 + "물어다 줬어요!!" 토스트). 자율 낚시 소유권 문법.

### Changed (🛝 재스왑 + 📱 PWA 버전 워치독)
- 사용자 지정 재스왑: **그네 = 북쪽 가장자리(11.7, 8.2, 등 뒤가 바다)**, 시소 = 남동(13.15,
  4.6). 세대 레버 v3 범프 — 모든 저장 사본이 다음 로드에서 1회 리셋.
- **🔄 버전 워치독**: 폰 홈화면(PWA) 웹뷰가 오래 살아 새 배포를 모르던 문제 — 화면 복귀
  시 world.js ETag를 HEAD로 재확인, 바뀌었으면 토스트 후 자동 새로고침. ETag 없는 환경
  (로컬 테스트 서버 등)에선 조용히 무력화.

### Fixed (🛝 놀이터 리뉴얼이 실제 월드에 적용 안 되던 근본 원인)
- **저장 레이아웃 세대(v2) 도입**: 지문(ISLAND_SIG)에 'v2|' 접두 — 배치만 바꾸는 리뉴얼도
  접두를 올리면 모든 클라이언트 사본(서버 파일·각 오리진 localStorage)에서 위성섬 소품이
  1회 전체 리셋된다. 좌표 테이블(MOVED_DEFAULTS) 방식은 사본마다 세대가 달라 실패한다는
  실측 교훈. `_sig !== undefined` 가드도 제거 — 지문 도입 이전의 고대 저장본이 영원히
  리셋을 피하던 구멍.
- 열기구 저장 적용부(PROPS 루프 밖)에도 같은 세대 규칙.
### Changed
- 사용자 지정 스왑: **그네 = 남동 존(13.15,4.6, 섬 안쪽 향)** ↔ **시소 = 북 중앙(11.7,7.6,
  플랭크는 해안선과 나란히)** — 가운데 몰림 해소. 전수검사·E2E(세대 리셋 → v2 커스텀 보존 →
  그네 양쪽 랜덤) ALL PASS.

### Changed (🛝 놀이터섬 분산 배치 + 그네 자리 랜덤)
- 확장(r 3.8) 후에도 북쪽에 몰려 있던 요소들을 존별로 분산: 그네=북 중앙(11.7,7.6),
  운동=북서(8.9,6.8), 시소=남동(13.15,4.6), 트램펄린=남서(유지), **열기구 계류장=동쪽
  가장자리(14.0,7.25)** — 바닷가 림에서 이륙하는 그림. 전수검사 통과.
- **기본값-추종 마이그레이션**: 섬 지문(ISLAND_SIG)이 안 바뀌는 "배치만 리뉴얼"의 사각지대
  보완 — 저장값이 옛 기본값 그대로면(±6cm) 손대지 않은 것으로 보고 새 기본값을 따라간다.
  직접 옮긴 커스텀 배치는 보존(E2E로 확인). 열기구 저장 적용부에도 동일 규칙.
- **그네 자리 랜덤**: 양쪽 다 비어 있고 둘 다 닿으면 무작위 시트 — 늘 같은 쪽만 타던 것 수정.

### Changed (🤸 트램펄린 튜닝)
- **사이즈 업**: 매트 반경 0.62→0.70 (프레임·스티치·다리 비례 확대) — 펫 대비 작던 체감 수정.
- **묘기 확률화**: 강한 발사마다 무조건 구르던 것 → 30%(부스트급 60%)만 발동, 그중 25%는
  새 동작 **별포즈**(정점에서 몸 젖히고 다리 쫙). 더블 바운스는 지금처럼 100% 앞구르기.
  원안(②)의 "가끔이라 귀하다" 감각 복원.

### Changed (🗂️ 독 정리 — 12버튼 → 상주 7)
- 우측 버튼 레일을 A안으로 재편: 상주 = 📷 스크린샷 · 🐥/🐕 조종(이모지 교체) · ＋/－ 줌,
  나머지는 서랍 2개 — **📖 컬렉션**(🐟 도감·📔 그림일기·💬 대화 기록), **⚙️ 도구**(🌦️ 날씨·
  🔨 공사·⚡ 절전). 왼쪽 가로 펼침, 하나만 열림, 자식 클릭·4초 무입력 시 자동 접힘.
- **🎣 낚싯대는 문맥 버튼** — 빙의 중에만 나타난다.
- **배지**: 공사/절전이 켜져 있으면 ⚙️에 초록 점, 도감에 새 어종이 쌓이면 📖에 빨간 점
  (도감을 열면 해소). 스모크의 공사모드 항목은 서랍 경유 플로우로 갱신.

### Added (🤸 트램펄린 + 놀이터섬 확장)
- **놀이터섬 r 3.2→3.8** (방위각 유지 델타 이동으로 본섬 간격 2.6m 보존, 다리·그네·시소·운동·
  가로등·나무·열기구 계류장 동반 이동, 전수검사 통과). 남쪽 새 공간에 **트램펄린**.
- **자동 통통**: 매트(높이 0.24 — 걷기 턱 미만)에 걸어 올라서면 바로 바운스, 뛸수록 사다리처럼
  상승. 조종 중 Space=부스트. 발사가 세지면 **앞구르기 — 인사할 때 그 목소리**(petVoice)와 함께.
- **절친 자율 이용**: 한가한 낮에 혼자 달려와 예닐곱 번 뛰고 만족하며 내려감(소유권 문법).
  클릭=절친 부르기. **듀오 엇박 통통**: 병아리가 깃털처럼 살짝 더 떠서 주기가 어긋나고,
  페이즈가 겹치는 순간 **더블 바운스**(1.5× 발사+앞구르기+반짝) — 30초 스로틀 로그.
- 보잉 사운드 절차 합성(220→130Hz 스프링, 발사 속도 비례 피치), 매트 스쿼시&스트레치.
  파티클 0 — 전부 포즈 수학 (발열 §1).
### Fixed
- **playerSupportY에 매트 지지면 누락** — AI 접지가 트램펄린 속으로 가라앉던 문제(Δy −0.24
  실측). groundHeightAt과 playerSupportY는 별개 합성 — 새 보행면은 반드시 둘 다 훅.

### Added (🏝️ 무인도 개성 + ⛏️ 보물찾기 + 🐚 조개 체감 수정)
- **무인도 테마 5종**: 🌲침엽수숲 / 🌼꽃섬 / 🌴야자섬(굽은 줄기+프론드 6장+코코넛) /
  🪨바위섬(우뚝 선 기암 실루엣) / 🐚조개섬(민모래+특산 조개 넉넉). 발견 토스트·지도
  점 위에 테마 이모지. 지형(모래/풀)은 테마가 결정.
- **보물찾기**: 무인도 42%에 ✕ 십자 널빤지+봉분 표식 — 곁에서 ⌘ 파기(전용 안무·흙먼지)
  → 궤짝이 둥실 → 진주조개 50% / 코디 언락 22% / 반짝이는 옛 동전. 판 섬은 48시간 뒤
  파도가 새 보물을 실어온다(재보급). 발견 시 "뭔가 파묻혀 있는 것 같다" 힌트, 7초 글린트.
- **조개 안 보이던 체감 버그**: 세션마다 0개 시작+첫 스폰 50초 → 로드 첫 프레임에 1~2개
  즉시 시딩. 비행기 제외 반경 1.3→0.9. 모래 무인도에도 특산 조개(주우면 같은 도감).
### Fixed
- 시드 상관관계로 링 전체가 한 테마로 뭉치던 문제(실측 꽃 5/7) — 테마·이름은 황금비
  아발란치 재해시 체인(rngT)에서. 이름 중복(솔바람섬 ×2)도 해소, 링 7섬에 5테마 전부 등장.
- 낚시 빈 풀·페리 무경로 항해 시 월드가 얼어붙는 대신 경고+자가복구(불변식 가드 2점).
- 무인도 밀도 24%→32% (기존 발견 섬은 게이트 수학상 부분집합 — 전부 보존).

### Added (🧲 탈것 회수 — 지도 탭 + 밤사이 자동 귀항)
- **지도에서 회수**: 미니맵의 🚣/🛩️ 아이콘을 탭하면 "원위치로 회수" 메뉴 — 즉시 기본
  정박/주기장으로 (탑승 중이면 거부 토스트). 원양에 두고 온 탈것 실종 문제 해결.
- **🌙 밤사이 자동 귀항**: 수면 시간대에 2시간 이상 미사용 + 홈에서 12m 이상이면 스스로
  돌아옴 — 이벤트 로그 "밤사이 나룻배가 스스로 집 앞으로 돌아와 있었다". 사용 시각은
  승하차·공사 배치·회수 때 갱신. 페리·열기구는 자가 귀환이라 제외. 5분 틱(무비용).
- E2E(원격 배치→지도 회수→강제 자동 귀항→리로드 영속) ALL PASS, 지도 E2E·스모크 회귀.

### Added (🗺️ 지도 — 좌상단 미니맵, 네비식 방향 화살표)
- 좌측 상단 🗺️ 버튼 → 190px 미니맵 패널. **네비식 자동 줌 2단**: 마을(≤22)에선 군도 확대,
  원양(>26)에선 해역(74) 전체 — 히스테리시스로 경계 깜빡임 방지.
- 표시: 코어 군도 + **발견한 무인도만**(포그 오브 워 — 시드로 위치 재계산, 언로드여도 표시),
  탈것 아이콘 4종(🚣🛩️⛴️🎈 — 확대 뷰 밖이면 가장자리에 클램프해 나침반처럼 방향 표시),
  펫 점 2 + **조종 펫은 헤딩 회전 네비 화살표**, 발견 카운트.
- 발열: 닫으면 비용 0, 열려 있을 때만 2Hz 캔버스 리드로우(~0.1ms). E2E(토글·섬/화살표 렌더·
  닫힘) ALL PASS, 스모크 10/10.

### Fixed (🏊 원양 한가운데 "수영이 안 되고 점프로만 전진"하던 유령 벽)
- 해역을 74로 열면서 수영 리시(18)를 그대로 둬서, 옛 경계가 원양 한가운데 보이지 않는 벽으로
  남았음(사용자 스샷 — 손잡고 수영하다 특정 지점에서 정지, 점프는 공중 판정이라 통과).
  SWIM_LEASH = EXPLORE_R로 통일 — 이제 해역 전체를 헤엄칠 수 있고 벽은 탈것과 같은 74뿐.
- 회귀 E2E: 옛 벽(18)을 헤엄쳐 통과(z 16.8→22.2) 확인.

### Added (🌊 하이브리드 스트리밍 1단계 — 수평선 너머 시드 무인도)
- **탐험 해역 개방**: 탈것 경계 18~20.5 → **74** (보트·비행기·공사 배치·수영은 무인도 기슭 허용),
  오션 링 40→82 확장 + 파도 페이드 62~76(CPU 미러는 fade=1 가정이라 정합). 페리·열기구
  노선은 코어 서비스 그대로.
- **시드 절차 무인도**: 24m 청크 그리드, 청크 24%에 시드 결정적 무인도(같은 좌표 = 언제나
  같은 섬 — 마크 시드 문법). 코어 버퍼(34) 밖 ~ 해역 끝. 잔디/모래 2종, 저작 섬과 동일
  빌더(buildIslandMeshes 재사용 — 스커트·절벽·해변 그대로) + 상록 침엽수·풍화 바위 드레싱
  (재질 공유 버킷 병합 2메시).
- **카메라 근접 스트리밍**: 1Hz 스캔 + **프레임당 1작업 큐**(로딩 스파이크 금지 — 발열 §1),
  로드 28/드롭 33 히스테리시스(코어에선 로드 0 = 기존 성능 그대로), 언로드는 지오메트리만
  dispose(공유 재질 금지) + ISLANDS/PROPS/seasonGrass 정리, 펫이 있는 섬은 보류.
  무인도는 수평선 아래서 **떠오르는 연출**(팝인 은폐).
- **ISLANDS push 설계**: 지형·충돌·림 가드·수영 리시·낚시 바다 판정·스냅·페리 항로 회피가
  전부 자동 적용 — 무인도에서 배회·낚시(바다 어종)·상륙이 그냥 된다. 다리 인덱스 매핑은
  buildRouteWalk 폴백 가드로 안전.
- **🏝️ 발견 이벤트**: 조종 펫 첫 상륙 시 팡파르 + "새 섬 발견 — ○○섬! (n번째)" + 이벤트
  로그 + 선제대화, 'world-discover' 영속. 섬 이름은 시드 생성(달팽이섬·조약돌섬…).
- E2E 8항목(시드 스캔·코어 무로드·접근 로드·발견·무인도 낚시·귀가 언로드·영속) ALL PASS,
  낚시·비행기·페리·열기구·조개 E2E + 스모크 회귀. 원양 드로우 31(코어 컬링).

### Added (🐚 조개줍기 — 휴양지 해변의 희귀 수집)
- 모래섬 마른 모래에 조개가 드물게 씻겨온다: **동시 최대 3개**, 스폰 롤 2~4분(65%) — 평균
  1~2개라 갈 때마다 있을까 말까(페리 타는 이유 하나 추가). 7초마다 하나가 은은히 반짝(발견 힌트).
- 4종 절차 조형: 가리비·소라(나선 팁)·분홍조개·**진주조개(레어 가중 8 — 벌어진 껍데기 속
  진주알, 팡파르+선제대화)**. 곁에서 ⌘ = 줍기(둥실 떠오르며 축소 + 반짝 + 차임), 컬렉션은
  'world-shells' 영속.
- **🐟 도감 패널에 "조개 컬렉션" 섹션 추가** — 물고기와 같은 1회 스냅샷 아이콘, 미수집은
  실루엣+???+"해변에서", 진행도 n/4.
- E2E(스폰 상한·줍기·영속·도감 섹션) ALL PASS, 낚시 E2E·스모크 10/10 회귀.

### Fixed (🎣 절친이 낚시 성공할 때마다 하늘로 솟던 버그)
- 랜딩 연출의 폴짝(`position.y += hopY`)이 원인 — 조종 펫은 매 프레임 서포트 클램프가 기준선을
  복원하지만 **AI 펫(busy)은 Y 리셋이 없어 잡을 때마다 적분 누적**, 떠오른 뒤엔 steerToward
  턱 규칙(0.26)에 막혀 걷지도 못했다. AI는 `지면 + 이번 프레임 폴짝` 절대값으로 + 세션 종료
  시 리그라운드(endDip 문법). E2E에 접지 회귀 체크 추가(y−지면 −0.00 확인). wrap.y와 같은
  "기준선 없는 += 누적" 계열 3호 — PATCHING-GUIDE §4에 등재.

### Added (📞 친구 부르기 · 📍 친구한테 가기 — 조종 펫 우클릭 메뉴)
- **📞 친구 부르기**: 조종 펫이 스마트폰(다크 바디+라이트 스크린)을 꺼내 귀에 대고 통화하는
  전용 안무(꺼내기→통화→내리기 3박자) — 인사 모션의 목소리(petVoice)로 시작, 통화 중 부리
  옹알이·고개 기울임·날개 모음·강아지는 꼬리 살랑. 통화가 끝나면 절친이 **뭘 하고 있었든**
  (잠·침대·물놀이·자율 낚시/열기구/페리·탈것 좌석 전부 해제) 포르르 나타나 **손을 잡은 채**
  옆에 선다(하트 이펙트 + 절친 대답 목소리).
- **📍 친구한테 가기**: 절친 곁 0.7m로 순간이동(8방위 막힘 탐색, 물이면 수영 상태로) — 친구를
  바라보되 **손은 안 잡음**(친구 하던 일 방해 금지). 탈것 탑승 중엔 두 기능 모두 안내 토스트.
- 항목은 조종 중인 펫의 우클릭 메뉴에서만 표시. E2E(통화→소환+손잡기 gap 0.4 / 텔레포트
  gap 4.4→0.7·손 안 잡음) ALL PASS, 스모크 10/10.

### Fixed (🛥️ 레이더 아치 크로스바가 다리와 분리돼 떠 있던 문제)
- 다리가 뒤로 기울어(rotX 0.3) 꼭대기가 z −0.66에 오는데 크로스바를 z −0.78에 둬서 공중에
  분리(사용자 스샷). 바·안테나를 다리 꼭대기(y0.77, z−0.66) 바로 위로 — 이제 한 몸 아치.

### Fixed (🛥️ 레이더 아치가 승객 머리를 뚫던 문제)
- 아치(흰 문+안테나)가 벤치 바로 위(z −0.42)에 서 있어 병아리 몸·강아지 머리를 관통(사용자
  스샷). 선미 쪽(z −0.72~−0.78)으로 이전 + 살짝 상향 — 좌석 뒤 롤바 느낌으로.

### Changed (⛴️ 요트 리디자인 + 🔨 운송기 공사모드 이동)
- **통통호 → 화이트 모터요트**: 화이트 선체 + 네이비 흘수선 스트라이프, 틴티드 윈도우 밴드의
  캐빈 + 플라이브리지(슬랜트 윈드실드) + 레이더 아치·안테나 + 선수 레일 + 티크 애프트
  선덱(네이비 쿠션 벤치 — 승객석 이전) + 구명튜브. 굴뚝 연기 제거(요트는 클린).
  RoundedBox는 별도 병합 버킷(함정 재확인).
- **저장 정박 마이그레이션**: 잔교/선석 신설 자리에 낀 옛 보트·비행기 정박은 로드 시 기본
  위치로 — "페리가 나룻배에 껴서 못 움직이던" 문제 해소.
- **🔨 공사모드에서 운송기 이동**: 보트(열린 물만)·비행기(뭍/물 어디든 — 수륙양용)·열기구
  계류장(섬 위, 데크·포스트·팻말째 이사, 회전 없음)을 드래그로 재배치 + 45° 회전(보트/비행기
  헤딩). 사용 중(탑승/운항)엔 이동 금지 토스트. 배치 규칙: 선석·잔교·다른 탈것과 간섭 거부.
  페리는 노선 시설(잔교·항로와 한 몸)이라 고정. 위치는 boat-2/plane-1/balloon-1로 저장·복원.
- E2E 신설(이동 규칙 7종 + 리로드 영속 3종 ALL PASS), 페리·열기구·비행기 E2E + 스모크 10/10 회귀.

### Added (⛴️ 통통호 — 자동 운항 페리, 모래섬 정기 항로)
- **잔교 2곳 신설**: 본섬 북 잔교(허브) + 휴양지 모래섬 동 잔교 — 다리 문법(onBridge) 재사용으로
  펫이 잔교 데크를 걸어 나간다(평평한 데크 0.12, 말뚝·계류 기둥·황동 종 비네트). 보트 기본
  정박은 동편(2.4, 6.95)으로 이동(서편은 페리 선석).
- **자동 운항**: 승선(⌘)하면 붕— 경적과 함께 출항 — 폐곡선 Catmull-Rom, 외해 링 경유.
  **모래섬 잔교에 ~20초 정박**(딸랑딸랑 종 + 닻 첨벙, 승하차 자유) 후 경치 레그 돌아 본섬
  종점 복귀(전원 하선). 한 바퀴 ≈ 2분 15초.
- **바다의 3가지 차이 반영**: ① 항로는 생성 직후 160스텝 샘플링 검증(섬+0.6·다리 차선 1.25·
  정박 보트 1.4·주차 비행기 1.7 회피) — 실패 시 리롤, 최후엔 스윕이 보장한 원본 링 ② 다리
  밑 통과 불가 → 위성섬 corridor 대신 외곽 링 ③ 방향 플립은 정박을 루트 끝으로 밀어 제거
  (다양성은 경유지 스킵 0~3 + 지터 ±1.1로).
- **연출**: 선수/선미/좌우 4점 waveYAt 샘플 → 실제 파도 피치·롤, 선미 항적 물보라, 굴뚝 연기
  퍼프, 경적(저음 2화음)·정박 종(신스), 승객은 벤치에 다리 접고 앉아(앉기 문법) 각자 두리번.
- **탑승 문법**: 절친 근접 자동 동승, 출항 15초 내/정박 중 우클릭 "친구 태우기"(잔교→갑판
  폴짝), 항해 중 ⌘ = 퐁당 하차(배는 노선 완주 후 귀항), Esc = 본섬 잔교로. 한가한 펫은 가끔
  혼자 타고 모래섬 뱃놀이(자율 문법 + 하이재킹 = 동승 전환).
- **데이터 주도**: 잔교·외해 링은 world-layout.js(FERRY_PIERS/FERRY_SEA_POINTS) — 새 섬을
  추가해도 검증이 자동 회피, 스윕에 항로 유효성 검사 추가(이번에 실간섭 3곳을 사전에 잡아
  링 재설계: 모래섬 북동 턴 부표·SW/SE 코너 후퇴).
- E2E 10항목(승선→출항→소환→정박→하차/재승선→종점 하선→퐁당→빈 배 완주→AI 승선) ALL PASS,
  낚시·비행기·열기구 E2E + 스모크 10/10 회귀 통과.

### Fixed (🎈 2인승 곤돌라 + 이륙 직후 친구 태우기)
- **두 마리 탑승 시 바구니에 파묻히고 뒷통수가 붙던 문제**: 마주보기 계산이 실제로는 등지기
  배치였고(뒷통수 밀착), 바구니 반지름 0.33이 두 머리 합 0.36보다 좁았음. 바구니를 2인승
  곤돌라(0.42)로 넓히고, **나란히 서서 앞을 보며 각자 다른 위상으로 두리번**하는 관광객
  배치로 교체 + 얼굴이 림 위로 나오게 상승(+0.24). 두 마리 비행샷 확인.
- **이륙 직후(15초 내) 열기구 우클릭 = 👥 친구 태우기**: 절친이 계류장 데크에 포르르 나타나
  움직이는 바구니까지 큰 점프 아크로 승선 (보트·비행기 소환 문법). 15초 지나면 "다음 탑승
  때 함께 타요" 안내. E2E에 소환 단계 추가 — ALL PASS.

### Added (🎈 열기구 — 자동 관광 라이드, 매번 다른 하늘 산책)
- **NE 놀이터섬 동쪽 계류장**(나무 데크·계류 포스트·팻말)에 정박한 열기구. ⌘ 탑승하면 혼자
  두둥실 떠올라 **폐곡선 Catmull-Rom 스플라인**을 타고 하늘 산책 — 물리 0, 프레임당 곡선
  평가 1회 (실무 레일 라이드 문법).
- **매번 다른 경로**: 경유지 풀 9곳(광장 분수·집·연못·모래섬·모험의 섬·추억의 섬·먼바다 2곳…)
  에서 3~6곳 셔플 + 최근접 이웃 정렬 + 방향 코인플립 + 제어점 지터. 순항 밴드(4.6~6.0)
  위라 어떤 조합도 충돌 없음. **한 바퀴 ≈ 2분 15초**, 안 내리면 새 경로로 무한 루프.
- **저공 물스침 구간 1곳 보장**(고도 1.7) — 그 구간에서 ⌘ = 바로 퐁당 하차, 빈 열기구는 혼자
  귀환. 그 외 ⌘ = 정거장 귀환 요청. Esc = 라이더 데크로 + 빈 열기구 자율 귀환.
- **연출**: 세로 8고어 스트라이프 봉투(정점색), 버너 6~11초 간격 "푸쉬—"(사운드+글로우+봉투
  은은한 발광 — 밤엔 더 밝게), 바람 스웨이+선회 기울임, 탑승 펫은 바구니에서 두리번(라이더)·
  마주보기(절친), 경유지 통과 시 happy + 선제대화("○○ 상공을 지나는 중!").
- **절친 자율 탑승**: 한가한 낮에 가끔 혼자 계류장으로 걸어가 한 바퀴 타고 온다 (자율 낚시
  소유권 문법). 라이더를 빙의하면 그대로 수동 라이드로 전환(하이재킹). 첫 탑승 쿨다운은
  시드만 — 앱 시작 직후 열기구가 떠나버리는 문제 E2E에서 실측 후 수정.
- 발열: 이동 그룹 1 + 병합 메시 5(봉투·나무·모래주머니·버너·계류줄) — 계류 중 사실상 무비용,
  탑승 구경은 12초 후 30fps 티어 자동 진입(비행샷에서 확인).
- E2E 10항목(탑승→순항→경로 생성→드리프트→귀환 하차→재탑승→Esc→AI 도보 탑승→하이재킹→
  귀환) ALL PASS, 비행기 E2E·스모크 10/10 회귀, 배치 전수검사 통과.

### Changed (🛩️ 탠덤 간격 재조정 0.32 → 0.46)
- 0.32는 두 머리 반지름 합(~0.36)보다 작아 강아지 얼굴이 병아리 뒤통수에 파묻혔음(사용자
  스샷). 머리가 겹치지 않는 최소+아늑함 지점인 0.46으로 — 뒷콕핏 시트 유닛도 함께 이동.

### Added (🛩️ 콕핏 시트 유닛 — "의자에 앉은 느낌")
- 펫이 동체와 합쳐져 보이던 문제(사용자 피드백)를 시트 유닛 4종으로 해소:
  ① 콕핏 내부 어두운 구멍(깊이 착시) ② 가죽 시트백+헤드레스트(뒤로 살짝 기움, 새들 브라운)
  ③ 도톰한 가죽 패딩 림 ④ 앞좌석 조종간(빨간 노브) — 조종사는 날개를 조종간 쪽으로 모으는
  그립 포즈(탑승 중 상시), 승객은 편안히. 맞바람 귀 눕기는 비행 중만.
- 전부 재질 버킷 병합(+3드로우) — RoundedBox는 Torus와 속성 불일치로 별도 버킷
  (mergeGeometries null 함정 — 혼합 병합 시 주의).

### Changed (🛩️ 탠덤 간격 사용자 튜닝)
- 앞뒤 좌석 간격 0.64 → 0.32 (사용자 스샷 피드백 — "좀 멀다"): 뒷콕핏 링·윈드실드도 함께
  전진. 아늑한 밀착 탠덤.

### Fixed (🛩️ 동체 연장 + 탑승 자세 — 사용자 스샷 피드백)
- 동체 1.3→1.62 연장(2인승 프로포션), 앞뒤 좌석 간격 0.38→0.64 — 강아지가 병아리에 밀착하던
  문제 해소, 각자 자기 콕핏에 앉는다. 꼬리·카울·프로펠러·기어 위치 재정렬.
- 좌석 +0.06 상승 — 병아리 부리가 동체 라인에 묻히던 문제 해소(부리·주둥이까지 림 위,
  머리는 여전히 윗날개 아래). 두 마리 탠덤 비행샷 재확인.

### Fixed (🛩️ 좁은 섬에서 바다로 활주 이륙)
- 절벽 턱 규칙이 양방향을 다 막아 **섬 가장자리에서 바다로 활주가 안 되던 문제**: "오르기"만
  금지로 완화 — 림을 넘으면 전진 관성 그대로 짧은 중력 낙하(기수 살짝 숙임) 후 첨벙 착수,
  수면 활주로 그대로 이어진다 (낙하 중엔 이륙 잠금 — 착수 먼저). 좁은 섬에선 바다로 굴러
  내려가 활주 이륙하는 동선이 열림. 바다→절벽 오르기는 여전히 불가(비행으로).
- E2E에 본섬 절벽 활주 시나리오 추가(초원 남향 활주→림 낙하→바다 진입), planeTp 훅.

### Fixed (🛩️ 친구 태우기 접근성 + 탑승 머리 클리핑)
- **조종 중 "친구 태우기"가 어렵던 문제**: 우클릭 판정을 기체 메시 정조준(얇은 날개라 명중이
  어려움) → **기체 주변 1.5m 반경 판정**으로 바꾸고, 탑승 중엔 비행기 메뉴를 펫 메뉴보다
  우선(조종사 머리가 가장 큰 클릭 타깃이라 펫 메뉴가 가로챘음). 근처 아무 데나 우클릭하면 됨.
- **탑승 펫 머리가 윗날개를 뚫던 문제**(사용자 스샷 확인): 윗날개를 파라솔 높이로(0.58→0.78,
  스트럿 연장) 올리고 좌석을 펫 키 비례로 낮춰 몸은 콕핏 속·머리만 림 위 — 병아리·강아지
  둘 다 림~날개 창에 들어온다. 두 마리 탠덤 비행샷으로 확인.
- 훅 planeSummon 추가, 탭 시 비행기 메뉴 자동 닫힘 (보트 메뉴와 동일).

### Added (🛩️ 경비행기 — 수륙양용 복엽기, W/S 고도 조종)
- **휴양지 모래섬 남쪽 해변에 주차된 오픈콕핏 복엽기**: 통짜 Lathe 동체 + 복엽 날개(빨간 윙팁)
  + 스트럿 + 오픈콕핏 2자리(링·윈드실드) + 랜딩기어, 전부 절차 생성. 정적 파츠는 재질별
  지오메트리 병합 6드로우(발열 예산 준수), 가동부(프로펠러·모션블러 디스크·바퀴)만 분리.
- **차·보트와 같은 탑승 문법**: ⌘ 근접 탑승(물에 떠 있어도 헤엄쳐 승선), 우클릭 "친구 태우기"
  = 절친이 포르르 나타나 뒷좌석으로 폴짝(탠덤, 둘 다 전방 주시). Esc = 비상 착륙 후 하차.
- **수륙양용 활주**: 뭍·물 어디서나 굴러가고 물에선 물살 스프레이 + 파도 살랑임. 전속력에
  도달하면 자동 로테이트로 이륙(뭍·물 모두), 착지 후엔 감속해야 재이륙(터치다운 튕김 방지).
  절벽 턱은 활주로 못 오름(해변 경사만) — 섬 사이 이동은 비행으로.
- **비행: W/S = 고도(상승/하강, 한/영 무관 e.code), ←→ = 선회(뱅킹 롤), ↑↓ = 속도.** 하강해
  지면/수면에 닿으면 착륙(물이면 착수 물보라). 천장 7.5, 경계에선 미끄러지듯 선회 유도.
  탑승 펫은 기체 피치 따라 몸이 기울고, 비행 중 맞바람에 귀·날개가 뒤로 눕는다.
- 푸드덕 엔진음 루프(rpm 피치·속도 볼륨·거리 감쇠), 프로펠러 저속 실블레이드/고속 디스크 전환,
  바퀴 구름 애니, 이착륙 이벤트 로그 + 채팅 상태줄("하늘을 나는 중!").
- 주차 위치는 plane-1로 저장·복원(차 car-1·보트 boat-2 문법) — 뭍이든 물이든 내린 자리 그대로.
- 검증: E2E 전 사이클(탑승→활주→자동이륙→W상승→S하강→착수→하선→물에서 재탑승→물살 활주)
  ALL PASS, 낚시 E2E·스모크 10/10 회귀 통과, 배치 전수검사 통과, 주차/비행 스샷 확인.
  발견 함정 기록: terrainHeight는 섬 밖 0 반환(유령 선반) — planeSupportY는 섬 밖에서 수면만 사용.

### Fixed (섬 이동 후 위성 소품이 옛 자리에 남던 "반쪽 이사" — 마이그레이션 가드 강화)
- 섬을 옮기거나 크기를 바꾸면(world-layout.js) 저장 배치(world_layout.json)의 옛 절대좌표가
  이동한 섬 **안 엉뚱한 자리**에 남던 버그를 근본 차단. 기존 가드는 "물 위에 고립된 좌표"만
  버렸는데(이동한 섬 안에 떨어진 드리프트는 통과), 저장본에 **섬 정의 지문(`_sig`)**을 심어
  지문이 바뀌면 위성섬(섬 index ≥1) 위 이동가능 프롭의 옛 좌표를 버리고 **base(개발자가 함께
  옮긴 정위치)로 자동 스냅**한다. 본섬 프롭 커스터마이징은 유지. `saveLayout`이 저장 때마다 현재
  지문을 동봉하고, 첫 로드/지문 변경 시 1회 재저장으로 감지 기준점 확보. (가짜 옛 지문 + 드리프트
  그네 주입 시뮬레이션으로 base 스냅·지문 갱신·본섬 유지 검증)

### Docs
- **PATCHING-GUIDE.md 추가** — 패치 시 필수 규칙 모음(발열 불변식·레이캐스트·배치 스윕·포즈 규칙·검증 루틴·git 수칙·백로그).
- PATCHING-GUIDE.md 보강 — ⛔ 최우선 5계명 + ⭐ §14 고퀄리티 방법론(모션 비트 분해·2차 모션·통짜 조형·비네트·지터·랜드마크 단위 패스·헤드리스 자가 눈검사).

### Changed (🗺️ 월드 배치 리뉴얼 — 본섬 확장·군도 간격·존닝 재배치)
- **본섬 5.6 → 6.2**: 점유율 ~57%→~44%로 낮춰 답답함 해소. 빈 초원이 생기고 어떤 두 소품도
  통로 여유 0.5m+ (가로등 0.3, 나무끼리는 숲 무리 허용).
- **위성섬을 진짜 군도로**: 가장자리 간격 0.30~0.58m(사실상 붙음) → **2.6~2.7m**. 다리가
  문지방에서 진짜 다리(스팬 3.6~3.7m, 플랭크 밀도 길이 적응)로. 방위각 유지 델타 이동이라
  섬 위 소품·평탄 패드·언덕이 함께 이동. SWIM_LEASH 18 안쪽 수렴 확인.
- **본섬 존닝 재배치**: 분수가 **광장 정중앙 랜드마크**로(하트 포장은 분수 곁), 스트리트
  피아노·사진 게시판이 광장 가장자리(둘 다 뭉침의 주범이었던 집 앞·서쪽 무더기에서 이사),
  우체통·텃밭·꽃바구니는 N 뜰, 커피·간식 부스는 NW 카페 거리, 도서관·울타리는 W 뜰,
  해먹·나무들은 S 쉼터/숲 무리, 선베드는 E 물가. 집 마당은 집+밥그릇+라디오만.
- 보트 기본 정박을 새 물가(1.2, 6.8)로 (구 좌표는 확장된 본섬 안). 섬 확장/이동으로 물에
  고립된 저장 배치(공사모드)·정박은 자동으로 새 기본값 폴백.
- **scripts/world-layout-sweep.mjs 추가**: 겹침·섬이탈·도로침범·집 풋프린트·다리 기하
  전수검사 — 배치 좌표를 만지면 반드시 이걸 돌린다 (이번 리뉴얼 기준 위반 77→0).
- 검증: 스모크 10/10 (draws 203, 전경 줌아웃 238 ≤ 250), 헤드리스 무에러, 전경 스샷 확인.

### Fixed (🎣 낚싯대 그립 밀착 + 캐스팅 회전 누적)
- **낚싯대가 몸에서 멀리 떠 있던 문제**: 그립 오프셋이 절대 미터 상수(rodSide/rodFwd 0.3)라
  키 0.4m 병아리 기준 날개 끝보다 한참 밖이었다 — 펫 키 비례(×height/0.85)로 축소해
  날개/앞발 바로 옆에 붙임. 릴링 땐 몸 중앙 양손 그립으로 모임.
- **다시 던질 때마다 몸이 한쪽으로 감기던 문제(근본 원인)**: 공통 적용부의
  `wrap.rotation.y += twistY` — 엔티티는 wrap의 x/z축만 매 프레임 다시 쓰고 **y축은 모션 종료
  때만 π로 리셋**하므로, 백스윙/훅셋/릴링 비틀기가 프레임마다 적분 누적됐다. 절대값
  (`= π + twistY`)으로 교체. 검증: 3라운드 연속 캐스팅 후 wrapDrift 0.0000 (신규 ?stats=1 훅
  wrapDrift/aiFishSnap 추가), 플레이어·절친 E2E·스모크 10/10 재통과.

### Added (🎣 낚시 페이즈 2 — 조건부 어종·도감 패널·절친 자율 낚시)
- **어종 8→13종**: 조건부 5종 추가 — 개구리(비 오는 연못)·메기(밤 연못)·빙어(겨울 연못)·
  연어(비 오는 바다)·아귀(깊은 밤바다, 발광 초롱). 밤(19~06시)·비(wxF)·계절을 실제 월드 상태로
  판정해 풀에 편입, 조건 활성 시 가중 1.6배 — "밤낚시 나온 보람"이 확률로 느껴진다.
  신규 몸통: 개구리(웅크린 다리·볼), 메기 수염 4가닥, 아귀 초롱(램프 재질 발광 구슬).
- **독 🐟 도감 패널**: 13종 그리드 — 아이콘은 실물 어종 메시를 **첫 열람 때 1회 오프스크린
  렌더 → dataURL 캐시 후 컨텍스트 즉시 폐기**(발열 0, 외부 에셋 0). 못 잡은 종은 검은 실루엣
  +???+서식 힌트, 잡은 종은 이름·★희귀도·마릿수·최대 크기. 진행도 카운터(잡동사니 제외 n/11,
  완성 시 🎉).
- **절친 자율 낚시**: 한가한 낮에 (~8% 확률, 5~10분 쿨다운) 낚싯대를 어깨에 메고 물가로 걸어가
  두세 판 낚시하고 온다 — 같은 상태기계·안무를 그대로 타되 반사신경 랜덤(0.15~0.55초)에 85%
  챔질 성공. 조과는 토스트("절친이 ○○를 낚았다")·이벤트 로그만, **도감은 주인 조과 전용**.
  빙의(🐤/🐶)·듀오 연출·취침 시간이 뺏어가면 즉시 접는다(소유권 = onArrive 클로저 정체성 +
  began 플래그 — 다른 디렉터의 goto/busy는 건드리지 않음). 채팅 상태줄에 "물가에 앉아 혼자
  낚시하는 중" 노출.
- **리그 캐시**: 낚싯대·줄·찌·그림자 리그를 플레이어/절친 각 1회 생성 후 보였다 숨겼다 —
  장비 토글 반복에도 지오메트리 재생성·GC 없음.
- 헤드리스 E2E 추가: 도감(스냅샷 13종 렌더·???·진행도) + 자율 낚시 전 사이클(도보→캐스팅→
  입질→챔질→릴링→랜딩→정리) 통과, 스모크 10/10 (draws 209).

### Added (🎣 낚시 — 동숲식, 어떤 물가든, 전용 안무)
- **독 🎣 버튼**: 낚싯대 들기(조종 중 아니면 병아리 자동 조종). 낚싯대·낚싯줄(8점 커브)·2색 찌
  전부 절차 생성. **물을 클릭해 캐스팅** — 수면 정밀 교차 + 관용 조준(클릭 방향 2.2m) 이중 해석,
  바다·연못·보트 위 어디서든. 이동하면 조용히 걷어들이고, 시전·파이팅 중엔 입력 잠금.
- **전용 안무(캔 모션 재활용 없음)**: 장비 빙글 → 캐스팅 3박자(백스윙 멈칫·스윙 오버슈트·감쇠
  팔로스루) → 대기(웅크려 응시·갸웃·하품, 20초 넘으면 다리 접고 앉아 물멍) → 니블 알럿(몸 숙임·
  눈 커짐) → 훅셋 저크(0.15s 스쿼시) → 릴링 버둥(뒤로 기대 줄다리기·좌우 흔들·낚싯대 휨·월척은
  끌려갔다 버티기·보트 위면 배가 출렁) → 랜딩(한손 번쩍 자랑 / 월척 만세+폴짝 / 꽝은 갸웃하다
  축 처짐) → 놓침(휘청+처짐). 강아지 귀·병아리 날개·눈·부리가 상태를 연기.
- **어종 8종 절차 생성**(외부 에셋 0): 연못 붕어·금붕어·잉어 / 바다 고등어·복어(가시)·가오리 /
  꽝 헌 장화·유리병. 물별 풀 + 희귀도 가중 + 크기 랜덤 + 월척 판정. 물고기 그림자가 다트-멈칫
  접근 → 니블 1~3회 → 진짜 바이트(0.65초 챔질 윈도우 — ⌘/Space/클릭).
- **도감**: localStorage 'world-fishdex'(종별 마릿수·최대 크기), 첫 조과 표시. 조과 차임(5도 상승
  2음)·반짝 링·절친 환호·이벤트 로그·LLM 선제대화 연동.
- 헤드리스 E2E: 실클릭 캐스팅 밴드 확인 + 입질~랜딩 전 과정 통과 (?stats=1 훅: fishState/tp/castAt/aim).

### Changed (모바일 터치 버튼 — 점프 ↔ 상호작용 위치 스왑)
- 펫 조종 중 우하단 액션 버튼 세로 배열에서 점프(🦘)와 상호작용(✋)의 위치를 맞바꿈 —
  위→아래 순서가 `✕ 해제 · 🦘 점프 · ✋ 상호작용`으로 (✕ 해제는 맨 위 유지). btnCol의
  DOM 추가 순서만 교체.

### Added (펫 이어하기 — 껐다 켜도 하던 자리에서 하던 대로)
- 위치·방향·침대(앉기/잠자리)·수영 상태를 **8초마다 + 창 닫힐 때(pagehide)** localStorage에 저장,
  켜면 그 자리에서 복원 — 항상 광장 중앙에서 다시 시작하던 것이 사라진다. 침대에 있었으면 빈
  자리 확인 후 다시 앉고/눕고, 연못·바다에서 수영 중이었으면 그 물에서 이어서 헤엄친다.
  활동의 "지금 뭘 할 시간인가"는 이미 실제 시계 기반(식사·취침·낮밤)이라 심즈(상태 저장)+동숲
  (스케줄 유도) 하이브리드로 자연스럽게 이어진다. 복원 시엔 이사 인사(wave) 생략. 저장 위치가
  막힌 곳이면 기본 스폰 폴백. 라운드트립 자동 검증(모래섬 복원 포함).

### Fixed (모래놀이 자세 3차 — 진짜 앉기 + 구경 친구 빙글 제거)
- **실무 게임식 앉기**: 몸 기울이기(tilt)를 없애고 몸은 곧게, **다리만 앞으로 접어 눕힌다**
  (앉아 있는 동안 매 프레임 feet.rotation.x 접기 — 일어나면 엔티티가 자동 원위치). 서 있는
  다리로 기대는 어색함 제거.
- **구경 친구**: 빙글 도는 happy 모션 제거 — 삽 없이 **맨손으로 같이 파는 dig 모션**으로.
  두 마리가 나란히 앉아 함께 모래를 판다.

### Fixed (모래놀이 자세 2차 + 수영 스프린트)
- **앉기 진짜 접지**: tilt만으론 맨모래에서 "서서 기대는" 자세로 읽혔다 — 몸을 모래에 살짝
  가라앉혀(-0.06, 다리가 모래에 묻힘) 엉덩이 붙이고 더 뒤로 기댄(-0.52) 모래놀이 자세로.
  삽은 날개/앞발 높이(+0.1)·코앞(전방 0.09)으로 — 쥔 것처럼. 모래 폴폴·토닥 반짝도 재조정.
- **수영 속도**: 1.05 고정 → 기본 1.4, **Shift(또는 스틱 끝까지) 스프린트 2.6** — 물에서도
  달리기 문법이 그대로 통한다.

### Fixed (모래놀이 자세 + 🐤🐶 조종 바로가기 버튼)
- **모래놀이 앉기 접지**: sit-스팟의 lie.y가 쿠션/의자 관습(+0.16)이라 맨모래 위에 붕 떠 있었다
  → 바닥 앉기(+0.03). 삽도 앞발 바로 앞으로(전방 0.17→0.12·측면 0.055, 손 높이) 붙이고 모래
  폴폴·토닥 반짝 위치를 새 높이에 맞춤.
- **독에 🐤/🐶 버튼**: 누르면 카메라가 그 펫에게 날아가며(팔로우 캠) 즉시 조종 시작 — 같은 버튼
  다시 누르면 해제, 다른 펫 조종 중에 누르면 갈아타기, 공사 중이면 저장하고 나와서 조종.

### Added (보트 마주보기 + 모래성 모래놀이)
- **보트 승객이 노잡이와 마주 본다**: 뱃머리 절친의 방향을 heading+180°로 — 승선 아크 착지
  방향도 동일. 노 젓는 내내 서로 바라보며 항해.
- **모래성 모래놀이**: 성 곁에 sit-스팟 2자리(침대 문법 — 접근·앉기·하차 공짜). 클릭하면 한가한
  펫이 걸어와 앉아 **한손 삽(코랄 장난감 삽)을 들고 모래를 판다** — 삽이 박자 따라 스윙하고
  퍼낸 모래가 폴폴 날리고 dig 모션이 간간이. 이미 한 마리가 놀고 있으면 다음 클릭에 **다른
  친구가 남은 자리에 앉아 모래성을 토닥토닥**(손끝 모래 반짝 + happy). 조종 중엔 ⌘(일반 침대
  문법)로 직접 앉는다. AI는 1.5~3분 놀다 스스로 일어남.

### Fixed (휴양지 후속 — 테두리 톱니 제거 · 보트 정박 리셋 · 친구 태우기 순간이동)
- **섬 테두리 "사다리꼴 모래 조형" 제거**: terrainHeight가 정확히 반지름 경계(rr ≥ r)에서 0을
  반환해 최외곽 링이 해변 높이(-0.55)에서 0으로 튀어 오르며 왕관 모양 톱니 벽을 세웠었다 —
  높이 샘플을 경계 살짝 안쪽으로 클램프해 해변이 물속 모래턱으로 매끈하게 이어진다 (스샷 검증).
- **보트 초기 정박 원복**: 정박 저장 키를 boat-1→boat-2로 세대교체 — 테스트 중 모래섬 곁에
  저장된 옛 정박이 리셋되고 초기 위치가 다시 메인 땅 북쪽 물가가 된다. 이후 정박은 boat-2로
  정상 저장·복원, 육지에 찍힌 비정상 저장값은 무시.
- **친구 태우기 = 순간이동 + 폴짝**: 걸어오면 오래 걸리고 장애물에 막혀 멈추던 것(실사용 확인)
  — 배에서 제일 가까운 물가로 포르르 나타나 0.6초 아크로 뱃머리에 뛰어오른다(움직이는 배도
  따라잡음). 자고 있어도 깨우고, 해먹·그네·시소는 내리고(forceEndBed), 물놀이는 곱게
  끝내고(endDip) 온다 — 어떤 상태든 부르면 온다.

### Changed (휴양지 마감 — 해변이 물에 잠기고, 조수가 생기고, 야자수가 다시 태어남)
- **섬↔바다 연결 자연화**: 모래섬 지형이 전용 해변 프로파일로 — 중심의 낮은 사구에서 바깥으로
  갈수록 내려가 **가장자리 띠가 물밑에 잠긴다**(f≈0.95에서 수면과 교차). 섬이 물 위에 떠 있던
  느낌이 사라지고 물가선이 모래 중턱을 지나가며, 걷다 보면 발이 찰랑이는 얕은 물에 닿는다.
  수중 모래턱 경사·스커트·서프 거품 링(물가선 위치로 이동)도 새 높이에 맞춤.
- **밀물썰물**: ≈5분 주기 ±5.5cm 조수를 오션 셰이더·waveYAt·수영 높이에 동일 상수로 추가 —
  해변 물가선이 조수와 파도를 따라 밀렸다 쓸렸다 한다 (경사가 완만해서 수평 이동이 잘 보인다).
- **야자수 재조형**: 세그먼트 조립(몸통이 분리돼 보임) → 정점 벤딩 통짜 트렁크 + 커브를 타는
  마디 링, 덩어리 잎 → **톱니 잎맥 알파 프론드 9장**(벤트 스트립 + frondTex, 알파 인지 깊이
  재질로 그림자도 톱니 모양) + 잎 밑동 뭉치 + 코코넛. 배치도 해변 가장자리(중심 2.0m)로 옮겨
  트렁크가 바다 쪽으로 기운다 — rotY는 커브가 바깥을 보도록 계산.
- **보트 친구 태우기**: 노 젓는 중 배를 우클릭 → "👥 친구 태우기" — 절친이 물가까지 걸어와
  뱃머리에 오른다 (배가 기슭에서 멀면 더 가까이 대라고 안내). 월드 베이크가 알파 그림자
  재질(customDepthMaterial)을 병합본에 승계.

### Changed (🌐 남은 중국어 잔재 청소 — UI 한글화 · 프롬프트/로그 영어화)
- **화면 UI 중국어 → 한국어**: 오버레이 타이틀(탄막/자막/캡처 영역), 오픈소스 About·라이선스
  다이얼로그, A2UI 동적 폼 라벨(플레이스홀더·별점·스위치·날짜), 펫 컨트롤 툴팁, 에러 토스트·
  Excel 내보내기, TTS 샘플/Volcengine 모델 라벨 등 하드코딩 중국어 일괄 한글화.
- **i18n fallback 안전화**: `t('key') || '中文'` 중 ko-KR.js에 키가 없어 실제 중국어가 뜨던
  3건(upload · backToDefaultView · refreshFailed)을 ko/en/zh 로케일에 추가, 죽은 중국어
  fallback 문자열도 한국어로 정리.
- **LLM 프롬프트 → 영어**: 번역·캐릭터카드 생성·시스템프롬프트 최적화·TTS 음색태그·애정도
  시스템 프롬프트를 영어로(`{{char}}`·`${var}` 플레이스홀더 보존).
- **백엔드 터미널 로그 → 영어**: server.py + py/* 20개 파일의 print/logger 중국어 로그 영어화.
- 웨이크워드(`小派`)·언어 네이티브명(`中文`/`日本語`)·모션 매칭 정규식 등 기능성 문자열은 유지.

### Added (🏝️ 휴양지 모래섬 + 🚣 노 젓는 보트)
- **휴양지 모래섬** (-3.2, 11.8 · r 2.6, 다리 없음 — 보트/수영 전용): kind:'sand'가 섬 문법을
  가른다 — 전용 고품질 모래 텍스처(알갱이 900점 + 바람 물결 라인 쌍 + 조가비 점), 사구 톤
  정점 얼룩, **절벽 대신 완만한 해변 경사**(지층 밴드 없음, 젖은 모래 립), 열대 상록이라 계절
  시스템 제외(겨울에도 모래). **모래 위를 걸으면 바스락**(전용 합성 발소리 — 입자 감쇠 노이즈,
  1.5kHz 로우패스; 잔디·길·나무와 다름). 위엔 야자수 4그루(굽은 마디 트렁크 + 프론드 8장 +
  코코넛)와 모래성(킵 + 코너 타워 4 + 성벽 + 총안 + 깃발). 잔디 스캐터는 본섬 한정이라 안 뿌려짐.
- **노 젓는 보트** (본섬 북쪽 물가 정박, 뭍에서 바로 승선 — 검산 완료): 스포츠카의 물 버전 —
  조종 중 ⌘로 타고(곁의 절친이 뱃머리에 동승), ↑↓ 노 젓기·후진 ←→ 방향, 노가 속도에 맞춰
  실제로 젓고 스트로크마다 물소리. 물 위 어디든 정박(위치 boat-1로 저장), ⌘로 내리면 그 자리에서
  퐁당 수영 시작(수영 반경 13→18로 확장 — 모래섬까지 헤엄쳐 갈 수도). 절친이 타고 있으면 기슭
  근처에서만 하선(먼바다에 AI 혼자 안 띄움), 헤엄쳐 가서 승선도 가능. 파도 위 살랑임은 오션
  셰이더와 동일 상수의 waveYAt로. 고물엔 밤에 켜지는 랜턴.
- 라우팅 가드: 다리 없는 섬은 직선 폴백(AI는 물가에서 곱게 포기) — 크래시 원천 차단. 강수 범위
  14→16(휴양지에도 비가 온다).

### Changed (더블점프 — 뛰어내릴 때도 온전히 2회)
- 점프를 불리언에서 "소지 수 2" 카운터로: 점프로 지면을 떠나면 1회 남고, **걸어서/뛰어내려서
  떠나면 2회 그대로** — 높은 사물(커피부스 등)에서 내려오면서 점프+더블점프가 온전히 된다
  (낙하 중 첫 발동은 지상급 3.0, 둘째 2.7). 착지·기슭 상륙·조종 해제마다 2로 리셋.

### Added (더블점프)
- 공중에서 Space/🦘 한 번 더 = 더블점프(시중 게임 문법): 추진 2.7로 살짝 약하게, 발밑에 공기
  폴짝 이펙트(글로우 링 6점), 착지·상륙·조종 해제 시 리셋. 정점에서 쓰면 합산 ~1.15m — 부스
  지붕·우편함까지 닿는 사다리 한 칸. 데스크톱 조종 힌트에 "(공중 더블)" 표기.

### Fixed (등반 마감 — 방향 의존 + 나무 위 공중부양)
- **방향에 따라 못 올라가던 문제**: 충돌원은 소품 중심의 원인데 길쭉한 메시(선베드 등)의 짧은
  쪽에선 원 경계와 메시 사이에 빈 띠가 생겨 그 띠에서 벽 판정 — 공중에서는 "표면이 없는 칸은
  통과"(마크 규칙)로 바꿔 어느 방향에서 점프하든 올라선다. 지상 보행 규칙은 그대로.
- **나무 위 공중부양**: 계절 꽃잎/낙엽 Points 구름이 여름엔 안 보여도 레이캐스트엔 잡히고
  (Raycaster는 visible 무시) Points 기본 threshold가 1m라 크라운 위 허공이 바닥으로 잡혔다 —
  메시 히트만 바닥으로 인정(Sprite·Points 제외), 꺼진 겨울 눈모자(투명 캡)도 제외. 이제 발이
  올라선 사물 표면에 바로 붙는다.

### Fixed (등반 크래시 — Sprite.raycast의 camera 요구)
- 사물에 올라가려는 순간 앱 전체가 멈추던 오류(Cannot read properties of null (reading
  'matrixWorld')) — 소품 그룹 안의 Sprite(할로 등)는 raycast 때 raycaster.camera를 읽는데 등반
  레이캐스터에 카메라를 안 넣었었다. camera 지정 + 스프라이트 히트 제외(빌보드는 바닥이 아님) +
  소품 단위 try/catch 가드(특이 자식이 던져도 앱이 얼지 않게, 1회 로그). 헤드리스 재현 테스트:
  빙의→달리기·점프 8초 pageerror 0건.

### Added (마크식 등반 — 조종 펫이 사물 위에 올라선다)
- 조종 모드에서 점프(Space/🦘)로 소품 위에 올라서고 위를 걸을 수 있다: 옆면은 벽(bonk),
  윗면은 길 — 발이 윗면 높이 이상일 때만 차단원을 통과·착지한다(지붕 밑에서 뚫고 올라가기 방지).
  지지면은 근처 소품 원본 메시에 발밑 하향 레이캐스트(propTopAt)로 — 베이크 병합 메시는 삼각형이
  수만 개라 레이캐스트 금지, 원본은 숨겨져도 Raycaster가 무시하지 않아 정확하고 싸다.
  가장자리를 걸어 나가면 중력으로 낙하, 달리는 차 지붕에서도 성립. 점프력 2.5→3.0(~0.64m —
  테이블·차 지붕·우물·울타리가 닿는다). AI 펫 배회는 기존 규칙 그대로(경로 탐색이 등반을 모름).

### Fixed (텃밭 클릭 — 진짜 원인: 픽킹 패드가 너무 작았다)
- 헤드리스 재현으로 확정: 클릭은 텃밭에 닿는데 흙판·틀에 맞으면 plotIdx가 없어 "밭 한 칸을 콕
  집어" 안내만 반복 — 픽킹 패드(0.5×0.38)는 화면에서 몇 픽셀이라 사실상 못 맞힌다. 이제 텃밭
  어디를 클릭해도 히트 지점에서 가장 가까운 밭 칸으로 해석(심기/물주기/수확). 수리 후 재현
  테스트에서 같은 클릭이 "토마토 씨앗을 심었어요"로 확인.

### Fixed (나무 형태 원복 + 우체통 리모델 + 텃밭 기능 수리)
- **나무 형태 원복(사용자 픽)**: 퍼프 변위·Lathe 트렁크·가지 스텁을 롤백 — 원래의 매끈한 구
  로브 + 원기둥 트렁크로. 질감은 유지: 방향 노이즈 dapple(볼록 밝고 골 어두운 얼룩)을 정점색에만
  굽는다(형태 불변, 계절 리베이크에도 보존). 눈모자 1.045배 복귀.
- **우체통 리모델**: 발치 둔덕·덤불 블롭 등 "덩어리 모음" 탈피 — 반원 지붕+직벽 프로파일을 통짜
  Extrude(베벨)로 뽑은 클래식 터널형 몸통, 축소판 문판(테두리가 몸통색으로 남음)+투입구+꿀색
  걸쇠, 가는 기둥+받침판. 깃발 애니메이션·클릭·징검돌은 그대로.
- **텃밭 수리 2건**: ① 성장 티커 미배선 — gardenStageHash를 만들기만 하고 비교하는 코드가 없어
  씨앗이 자라도 다시 클릭하기 전엔 화면에 반영이 안 됐다 → 10초 폴로 단계 변화 감지·재그리기.
  ② 월드 베이크 유령 — 작물 비주얼(plotIdx 그룹의 자식)이 계절 전환/공사모드 재베이크에
  병합·숨김되면 수확해도 유령이 남았다 → plotIdx/keyIdx 서브트리 전체를 베이크에서 제외.

### Changed (나무 질감 + 섬 테두리 관통 + 물비늘 격자 수리)
- **나무(일반·벚·쪼아쪼아 공통)**: 매끈한 구 로브 → 올록볼록 퍼프 크라운(동숲 "브로콜리" 실루엣) —
  방향 기반 저주파 럼프로 정점 변위 + 볼록한 데 밝고 골 어두운 dapple을 정점색에 굽는다.
  dapple은 geo.userData에 저장돼 계절 잎색 리베이크(gradColors)에도 질감이 보존된다. 트렁크는
  밑동 플레어 Lathe + 크라운으로 뻗는 가지 스텁 2개. 겨울 눈모자 1.045→1.13배(럼프 관통 방지).
- **섬 테두리 관통("땅뚫림") 수리**: 굴곡진 잔디 끝단과 고정 높이 절벽 상단 사이 틈으로 낮은
  앵글에서 섬 내부가 보이던 것 — 테두리에서 절벽 안쪽으로 말려 내려가는 잔디 스커트(turf lip,
  흙그늘 톤)로 전 방향 봉인 + 잔디/절벽 DoubleSide(반투명 바다 너머로 껍데기 뒷면이 컬링돼
  하늘이 비치던 각도 제거).
- **물비늘 격자무늬 수리**: sin×sin 교란이 디퓨즈에 배어나 근거리 수면에 초록 체커가 그려지던 것
  (스샷 확인) — 주파수 ~2.4배(파장 ~10cm, 격자가 아니라 입자로), 진폭 절반, 저주파 패치 봉투로
  균일 반복 깨기.

### Fixed (하늘 — "낮인데 흐린 단색" 수리, 동숲풍 채도)
- 원인 ①: 스카이돔 그라데이션 스탑 [0,.45,.8,1]이 구면 전체 기준이라, 쨍한 파랑은 천정(화면 밖)에만
  있고 카메라에 보이는 띠(수평선~고도 30°, UV 0.28~0.52)엔 제일 연한 두 색만 걸렸다 — 밤낮 사이클
  도입(b23d805) 때부터. 원인 ②: ACES 톤매핑이 하늘 색을 추가로 탈색(279bfe4부터).
- 수리: 스탑을 보이는 띠로 재배치 [0, .3, .44, .52] + 수평선 아래 반구는 수평선 색 고정, 스카이돔·
  해·달에 toneMapped:false(게임 스카이박스 표준 — 팔레트 hex가 곧 최종 색), 낮 팔레트를 동숲풍
  아주르→아쿠아→민트(#2f9de8→#5fbcee→#a9e2f0→#e9f6ec)로. 노을 오렌지도 이제 수평선 위 보이는
  자리에 걸린다(예전엔 0.8 스탑 = 수평선 아래라 거의 안 보였음). 밤·비 하늘은 저작색 그대로.

### Changed (퀄리티 피드백 — 원뿔 잔디 복귀(자연화) + 바다 물비늘)
- **잔디**: quad 풀잎 → 원뿔 복귀(사용자 픽), 대신 자연스럽게 세 겹 — ① 포기 = 원뿔 3개 다발
  (꼿꼿한 중심 + 반대로 기운 곁가지 둘: 스파이크가 아니라 포기로 읽힘) ② 다발(ci)마다 체격
  0.66~1.4 (무성한 데/성긴 데) ③ 포기마다 키·퍼짐 독립 스케일 + 기울기 + instanceColor 밝기·
  웜쿨 색조 지터. bakeGrad 밑동 그늘 × 계절 틴트 그대로, 그림자 복구, 여전히 1 드로우콜.
  (bladeTex는 제거 — quad 풀잎 전용이었음)
- **바다 물비늘(sun glitter)**: 프래그먼트 셰이더에서 월드 xz 기반 고주파 사인 리플 3겹으로 픽셀
  노멀을 잘게 흔들어 스페큘러가 파도 위에서 반짝반짝 부서진다. 하이라이트 살짝 타이트하게
  (shininess 42→68, specular 0xaee6ff). 수평선 페이드로 원양에선 잦아들어 앨리어싱 방지 —
  정점 4.6k로는 못 내는 입자감을 픽셀 단위 공짜로. 스모크 10/10 · draws 209.

### Changed (퀄리티 Q4 — 복층집 리모델: 1.3배 확장 + 외장·내부 전면 개선)
- **몸집**: 풋프린트 2.0×1.6 → **2.6×2.08**, 벽높이 1.2→1.55, 다락 0.62→0.78, 지형 패드 1.7→2.05
  (사용자 승인 — 집·마당 확대). 워크 헬퍼(houseFloorY/houseBlocked)의 계단 구간·다락 모서리·난간
  띠·포치 기둥을 지오메트리와 1:1로 재계산, 계단 8→10단(경사 동일 ~35°).
- **외장**: 기와 리페인트(장별 톤 + 겹침 그림자/하이라이트 — 민무늬 탈피) + 처마 페시아 + 꼭대기
  피니얼, 회벽 스터코 리페인트(미장 얼룩·모래알·흙손 자국), 모서리 목재 기둥 4 + 층간 띠 + 정면
  인방(목구조 프레임), 십자살 창 + 창턱 + **창가 화단(꽃 3송이)**, 다락 뒷창 신설, 벽돌 굴뚝(+캡),
  포치 데크 확장 + 계단 + 원형 도어매트.
- **내부**: 바닥·다락이 마루널 텍스처(이음선+나뭇결), 소파 리모델(좌방석 2·등쿠션 2·다리),
  테이블(받침다리+찻잔), 러그 리페인트(동심 링+스티치), 책장 2단 8권+화분, 침대(헤드보드·접힌
  이불깃·베개 2), 협탁+촛불, **벽그림 2점(바다 노을·병아리와 강아지)**, 벽시계, **펜던트 조명**
  (밤이면 lampGlobeMat과 함께 켜짐 — 실내등 위치·도달도 새 거실에 맞춤 2.4→3.0).
- **호환**: 밥그릇 기본 위치를 앞마당으로(옛 자리가 확장 벽에 먹힘 — 겹침 검산), 예전 저장
  레이아웃의 소품이 커진 풋프린트에 삼켜졌으면 로드 시 현관 밖으로 자동 밀어내는 마이그레이션.
  소파/침대 좌표·가구 콜라이더·그림자 블롭 동기화. 스모크 10/10 · draws 209(상한 250 내).

### Changed (퀄리티 Q1+Q5 — 잔디와 물: 원뿔 콘·플라스틱 원판 탈피)
- **잔디**: 진초록 원뿔 콘(근경에서 스파이크로 읽힘) → 십자 quad 풀잎 인스턴스. bladeTex는
  루미넌스(흰~회)로 그려 계절 틴트(material.color)가 착색 — 여전히 1 드로우콜, alphaTest라 정렬
  불요, 그림자는 끔(알파 quad는 사각 그림자를 만듦). 지면 grassTex/snowGroundTex는 256px로
  리페인트: 4톤 삼각형 + 위치·크기 지터 + 저주파 얼룩 + 가는 잎날 스트로크(시드 고정 난수 —
  로드마다 동일). 타일 카펫 반복감 제거가 목적.
- **꽃**: 들꽃·심은 꽃(꽃심기 모드)·연못 수련꽃의 "구슬 막대사탕" 머리 → 수평 꽃송이 quad
  (flowerTex: 루미넌스 꽃잎 6장 × instanceColor + 웜 옐로 수술). 위에서 내려다보는 카메라라
  수평 디스크가 제일 꽃으로 읽힌다.
- **바다**: 기슭 여울 정점색(가까운 섬 기슭까지의 거리 기반 — 근해 밝고 초록끼, 원양 살짝 깊게;
  곱셈 틴트라 낮/밤/계절 물빛과 그대로 합성) + 거품 링에 foamTex alphaMap(매끈한 도넛 → 방울이
  뭉치고 끊기는 유기적 거품선).
- **연못**: 단색 플라스틱 원판 → 깊이 라디얼 그라데이션(pondTex, 중심 진청→가장자리 여울) +
  잔물결 호 + 살짝 매끈한 표면, 노치 있는 수련잎 3장. 투명은 유지(발장구 비침).
- 스모크 10/10 · draws 190 유지.

### Changed (발열 7/7 — 회귀망 잠금 + 최종 매트릭스)
- 스모크가 10항목이 됐다: **draws 상한 250 어서션**(베이크 등록 누락 회귀망, 현재 190) 추가,
  hour=14 고정(심야 자동취침이 우클릭 프로브를 '깨우기'로 소모하던 시각 비결정성 제거),
  우클릭 프로브를 격자 스캔→__worldDev.petScreenXY 정조준으로 교체(10/10 결정적 PASS),
  --use-angle=metal(SwiftShader 4fps 오측정 방지 — perf 프로브와 동일).
- 발열 시리즈 최종: **60fps 고착(실사용) → 티어드 60/30/15**, **402→190 draws**, 오션 CPU→GPU,
  낮 라이트 12개 제거, 펫 창 15fps 바닥. 남은 후보(필요 시): 유니크 텍스처 소품(간판·어닝 18콜)
  아틀라스화, 제외 타입(포토보드·포탈 등) 정적 부품 부분 베이크.

### Changed (발열 6/7 — 펫 창도 조용하면 15fps)
- 데스크톱 펫 창(vrm.js)에 월드와 같은 3단 티어: 활동 60fps / 활동 후 30fps / **30초+ 조용하면
  15fps**. 항상 떠 있는 투명 창이라 이 바닥값이 상시 발열을 결정한다 — 작은 창의 숨쉬기·깜빡임은
  15fps로 충분히 읽힌다. 모션·말하기·VMC·XR·입력 4초 여운은 기존대로 즉시 60fps 복귀.
  (호버 자동숨김 중 렌더 스킵은 기존 그대로.) ※ 펫 창은 Electron 전용이라 앱 재시작 후 반영.

### Changed (발열 5/7 — 꺼진 라이트는 씬에서 뗀다)
- 가로등 12개가 낮에 intensity 0으로 씬에 남아 있었다 — three 포워드 렌더러는 셰이더에 박힌
  라이트 수만큼 모든 픽셀이 비용을 내서, 맑은 낮에도 포인트라이트 12개 값을 레티나 해상도로 내던
  구조. 이제 lampGlow가 0이면 원래 부모(씬/동굴 그룹)를 기억해 뒀다 떼고, 황혼·비에 되붙인다
  (경계 프레임 셰이더 재컴파일은 세션당 상태별 1회 캐시). 번개 DirectionalLight도 뇌우 동안만 씬에.
- 실측 라이트 센서스: 낮 point 1(동굴)·dir 1 / 밤 point 13 / 폭풍 dir 2(번개 합류) — 밤 룩 동일.

### Changed (발열 4/7 — 월드 베이크: 마크식 크로스 소품 병합, 402→190 draws)
- **bare M(color)가 색상별 공유 재질이 됐다** (extra 있으면 키에 map uuid 등 포함해 공유, unique:true로
  옵트아웃 — 계절이 color를 만지는 잔디/자갈 인스턴스 2곳만 해당). 나무색 등 11곳에서 따로 만들던
  woodTex 재질이 1개로.
- **worldBake()**: 소품 경계를 넘어 같은 재질끼리 한 덩어리로 굽는다(마크 청크 메싱 원리). 원본은
  visible=false로 남아 Raycaster(r178은 visible 무시)가 그대로 맞으므로 클릭·호버·공사 판정 무변경.
  나무도 포함(타입 병합과 달리 원본을 지우지 않아 seasonLeaves 리베이크가 살아있음) + 다리·길 리본
  (WORLD_STATIC_ROOTS) + 그림자 블롭(전부 1콜). 재베이크 훅: 공사모드 진입=unbake/종료=bake,
  계절 전환 시작=unbake/페이드 완료=bake.
- 실측: **402→190 draws**(전환·공사 중엔 원본 388~416으로 복귀 후 재베이크 확인), tris 124k→136k
  (+10%, 병합 덩어리는 프러스텀 컬링 포기 — 이 규모에선 남는 트레이드). 낮/밤/겨울 스킨·공사 토글·
  계절 왕복 검증. 심야 스모크의 우클릭 WARN은 자동취침(기존 시각 비결정성)이 원인 — 낮 고정 재검 PASS.
- 디버그: ?stats=1 훅에 scene(draw 브레이크다운)·season(계절 강제) 추가.

### Changed (발열 3/7 — 오션 파도 GPU 이행)
- 바다 4파 사인 파고와 해석 법선을 버텍스 셰이더로 이식(onBeforeCompile, 강수·분수와 같은 공유
  wxTime 시계) — CPU가 4,592정점을 매 프레임 재계산하고 position+normal ~110KB를 재업로드하던
  것이 uniform 1개로 줄었다. updateOcean에는 거품 링 2개만 남음. 상수는 CPU 버전과 동일(수영
  펫이 파고를 CPU 샘플링하게 되면 이 식과 맞출 것). 검증: 셰이더 에러 없음, 근해 수면 클립
  2초 diff로 파도 움직임 확인, 낮 전경/저각도 룩 동일.

### Changed (발열 2/7 — ambient 프레임 티어: 안 볼 때는 15fps)
- 프레임 게이트가 2단(60/30)에서 티어 함수(frameIntervalMs)로: 활동 60fps, ⚡절전/배터리 30fps,
  포커스 구경 30fps → **60초+ 구경은 15fps**, **비포커스는 15fps**(말풍선·토스트 동안만 30fps).
  동숲이 30fps 고정인 것과 같은 원칙 — 균일한 낮은 fps가 출렁이는 60보다 부드럽고, 임계값(65ms)은
  120Hz/60Hz 틱 정수배 바로 아래라 페이싱이 균일하다. 실측: long-idle 15fps·unfocused 15fps 진입
  확인(프로브 E/F 신설, ?stats=1 ageInput 훅).

### Fixed (발열 1/7 — 웨이크 정책: idle 30fps가 실사용에서도 걸리게)
- **정지 커서 60fps 고착 수리**: 커서를 창 위에 둔 채 손만 떼면 콘텐츠가 커서 밑에서 움직일 때
  Chromium이 쏘는 움직임 0짜리 합성 pointermove가 12초 idle 타이머를 계속 리셋해 영영 60fps로
  돌던 버그(실측 20초+ 유지). 이제 실제 움직임(movementX/Y≠0)만 입력으로 친다 — 터치는 기기별로
  movement가 0으로 와서 pointerType으로 무조건 통과(모바일 조작 회귀 없음).
- **말풍선·토스트의 12초 풀 리셋 제거**: showBubble/showToast가 wakeInput 대신 wakeSoft(4s/3s)로
  잠깐만 깨운다 — 선제대화가 있는 한 60fps가 기본 상태가 되던 누수. 실측: 토스트 직후 60fps →
  6초 뒤 30fps 복귀 (전엔 12초+ 유지).
- **npm run perf:world**: headless(Metal GPU) 발열 프로브 신설 — active/parked/toast/ambient
  시나리오별 fps·draws·CPU%를 찍어 최적화 전후를 숫자로 비교한다(?stats=1 한정 __worldDev.toast
  훅 포함). 기준선: active 60fps/CPU 34%, ambient 30fps/CPU 36%, 402 draws.

### Fixed (홀리데이 후 납작 병아리)
- 홀리데이 피날레의 착지 스쿼시가 0.92배로 눌린 채 모션이 끝나 펫이 살짝 납작하게 남던 버그 —
  반사인 곡선으로 눌렸다가 정확히 1.0으로 복귀하며 끝난다. 방어선으로 모든 모션 종료 시
  scale/x를 무조건 원상복구하는 가드도 추가.

### Added (14번째 모션 — 🎄 홀리데이, 모션 로드맵 완주!)
- **홀리데이 (Holiday)**: 반짝임 속에 산타모자가 뿅 씌워지고(코디 없을 때만, 모션 후 유지) →
  좌우 스텝-홉 8박 캐럴 스텝(기울기는 스텝 반대 위상, 병아리 날개 지휘·강아지 꼬리 2배박+귀
  플롭) → 피날레 폴짝 정점에서 계절 파티클 팡. 겨울(11~2월)엔 눈송이+금별, 그 외엔 색종이+금별.
- **듀오 안무 (월드)**: 절친이 있으면 걸어와 마주보고 반박자(duoShift) 어긋난 거울 스텝(dir ±1)
  으로 함께 춘다 — worldHug 디렉터 패턴 재사용. 축제답게 침대·그네·물놀이 중인 절친도 내려서
  합류(잠·조종·손잡기 중만 제외).
- **듀오 안무 (데스크톱 펫 창)**: 허그처럼 main이 두 창을 지휘 — 창이 나란히 슬라이드(포옹보다
  넓은 간격)한 뒤 두 마리가 같은 안무를 반박자 차이로 추고 제자리 복귀. vrm-holiday IPC 3층
  배선(main·preload·vrm). ※ main.js 변경이라 앱 재시작 후 반영.
- **월드 3D FX**: 눈송이/색종이+금별 12개 풀(생성/폐기 없음), 위로 팡 → 살랑 낙하. 징글벨
  모티브 4음(E·E·E·G, 기음+3배음 벨톤). 메뉴·행동 태그(<motion=holiday>) 자동 배선.

### Added (13번째 모션 — 💗 하트)
- **하트 (Heart)**: 움츠림 → 갸웃·갸웃("너 좋아" 리듬) → 폴짝 점프 정점에서 하트 팡 → 감쇠
  진동 스쿼시 착지. 병아리는 양 날개를 가슴 앞으로 모으고, 강아지는 고개를 젖히며 꼬리 만개,
  둘 다 눈이 ^^ 스마일이 된다. 중간에 끊겨도 몸이 눌린 채 남지 않게 베이스라인이 scale을 복구.
- **표면별 FX**: 월드는 진짜 입체 하트(Extrude+bevel, 두 톤 분홍) 8개 풀이 정점 프레임에 팡 —
  개별 위상으로 떠오르며 돌고, 큰 메인 하트가 잠깐 머문다(풀 재사용, 생성/폐기 없음 — 발열
  규칙). 완전5도 상승 차임 2음이 같은 프레임에 울린다. 펫 창은 💗 이모지 버스트 + 잔여 하트.
- 메뉴·채팅 행동 태그(<motion=heart>)에 자동 등장 — "사랑해!" 하면 펫이 하트로 답할 수 있다.

### Added (안정성 3종 — 테스트·백업·에러 가시성)
- **npm run test:world**: Playwright 스모크 테스트 정식 편입(scripts/world-smoke.mjs) — 정적 서버를
  스스로 띄워 앱 없이 30초 안에 데스크톱 로드·무에러·공사모드 토글·모바일 터치 UI·저장 레이아웃
  적용을 검증한다. 사물 하나 추가할 때마다 이 한 줄로 회귀 확인.
- **💾 백업/복원**: 💬 패널 하단에서 월드 개인 데이터(배치·일기·소원·캡슐·텃밭·별자리·우편·꽃 +
  펫별 대화 기억)를 zip으로 내려받고(📥) 되돌린다 — /api/world_backup GET/POST, 복원은 경로
  검증 후 world/·world_chat/만 풀어놓는다. 맥 교체·재설치 대비.
- **에러 가시성**: 스크립트 오류를 토스트로 보여주고(폰엔 콘솔이 없다) /api/world_log →
  USER_DATA_DIR/world/client-errors.log 로 적재(200KB 롤링, 같은 메시지 30초 도배 방지).

### Changed (브랜딩 — Desktop Pet World v1.0.0)
- 앱 이름 Super-Agent-Party → **Desktop Pet World**, 버전 1.0.0 (package.json productName/version,
  트레이 툴팁). dmg 파일명도 Desktop Pet World-1.0.0-Mac.dmg 로.
- **아이콘 전면 교체**: 실제 병아리·강아지 GLB를 three.js로 렌더(파스텔 하늘·잔디 그라데이션,
  스쿼클)해 icon.icns(전 사이즈)·icon.png·트레이 아이콘(32px 투명)까지 통일.

### Added (배포 가이드 통합 + 원커맨드 빌드)
- **doc/deploy-guide.md**: 배포 표면 2종(데스크톱 dmg / 웹 백엔드)을 한 파일로 — dmg 빌드·GitHub
  Releases 절차, 업데이트 시 재빌드 정책(개발은 소스로, 릴리즈 때만 굽기), 웹은 상황별 3길
  (Tailscale=내 폰만·공짜 / Cloudflare Tunnel=맥 상시구동+도메인 / Railway=맥 꺼도 동작).
  deploy-railway.md는 이 파일로 흡수.
- **npm run dist:mac**: PyInstaller→electron-builder 두 단계를 한 줄로.

### Changed (배포 준비 — Railway·패키징 겸용 백엔드 정리)
- **월드 개인 데이터 위치 이전**: world_*.json 8종(배치·일기·소원·캡슐·텃밭·별자리·우편·꽃)이
  레포 폴더(config/)가 아니라 USER_DATA_DIR/world 에 저장된다 — 패키징된 앱에선 앱 번들이 읽기
  전용이고 Docker/Railway에선 재배포마다 초기화되기 때문. 예전 파일은 첫 접근 때 자동 복사
  (기존 로컬 데이터 그대로 이어짐). git에 개인 데이터가 들어갈 걱정도 원천 차단.
- **토큰 인증 게이트**: SAP_AUTH_TOKEN env가 설정돼 있으면 /health 제외 모든 http·websocket에
  토큰(첫 1회 ?token= → 90일 쿠키, 또는 Bearer 헤더)을 요구한다. 미설정(로컬/Electron)이면
  완전 무동작 — 기존 동작 불변.
- **PORT/HOST env 존중**: Railway 등 PaaS가 주입하는 PORT를 argparse 기본값으로 읽고,
  Dockerfile CMD도 ${PORT:-3456}로. doc/deploy-railway.md에 볼륨(/app/data)·변수 설정 가이드.


### Changed (퀄리티 패스 ④ — 팔레트 이탈자 보정 🎨)
- 회색 계열 이탈자들을 파스텔 월드의 웜 베이지로: 동굴 바위(0xa89f92→0xbcaa90 계열),
  바위(0xb3ab9f→0xc2b096), 연못 돌. 멀리서 검은 판으로 읽히던 텃밭 흙은 웜 브라운 +
  톱라이트 그라디언트로. `M()` 옆에 월드 팔레트 가이드 주석(나무/돌/파스텔 포인트 대역 +
  "정적 부품엔 bakeGrad가 기본 문법") — 새 프롭이 따라갈 기준.

### Changed (퀄리티 패스 ③ — 비네트: 소품이 "장면"이 되게 🪴)
- 최근 기능들이 프롭 하나 덜렁이 아니라 받침+동무 소품이 있는 한 장면으로: **우편함** 징검돌
  두 장+덤불 한 쌍 · **운동 공간** 수건 롤+물병(받침 데크는 시도 후 철회 — 이 월드의 평탄
  패드는 지반 0으로 눌러 만드는 얕은 크레이터라 낮은 판이 턱에 묻힌다, 그네·시소처럼 패드
  바닥 직치기가 문법) · **도서관** 라운드 사이드테이블+찻잔·받침+바닥 책 두 권 · **분수**
  둘레 꽃 여섯 송이 · **꽃바구니** 미니 씨앗 팻말. 전부 프롭 그룹의 자식이라 공사모드 이동을
  따라가고 병합도 자동, 비차단 장식이라 콜라이더 변화 없음.
- 디버그: `?nomerge=1` — 정적 병합 끄고 비교 (병합 의심 시 이분용).

### Changed (퀄리티 패스 ② — 지면·스캐터 🌿)
- **길 테두리 굽기**: 리본 UV의 폭 방향(y 0..1)에 맞춰 pathTex에 가장자리 어둠띠 + 은은한
  중앙 밟힘 하이라이트를 구웠다 — 모든 길·스포크가 잔디 위 스티커가 아니라 "파인 길"로
  읽힌다. 지오메트리·드로우콜 변화 0.
- **잔디 저주파 얼룩**: 섬 버텍스 컬러에 2~3m 붓터치의 웜/쿨 그린 패치를 추가 — 균일한 초록
  카펫 느낌이 깨지고 프롭이 지면에 앉는다. 곱셈 틴트라 계절/설원과 그대로 합성.
- **스캐터 클러스터화**: 잔디 콘 380개 균일 랜덤 → 68다발×4~7포기, 들꽃 75송이 → 17다발×3~6송이
  **한 다발 = 한 색**(진짜 화단처럼). 길/광장/프롭 회피는 기존 스팟 로직 그대로.

### Changed (퀄리티 패스 ① — 동숲식 조형: 최근 6종 리모델링 🎨)
- **그라디언트 굽기 범용화(`bakeGrad`)**: 나무 잎에만 있던 "위-밝음/아래-어두움 버텍스 램프"를
  아무 지오메트리에나 굽는 유틸로 일반화. 공유 재질(gradMat/gradMatWood/양면 gradMatDS)이라
  병합 후 프롭당 드로우콜도 줄어든다 (전체 430→399). 인스턴스드 장식(잔디 콘·들꽃 머리·자갈·
  심은 꽃 머리)에는 흰~회 루미넌스 램프를 구워 — instanceColor·계절 틴트와 곱해져 색은 그대로,
  밑동만 자연스럽게 그늘진다.
- **분수 재조형 + 이전**: 원기둥 스택 → Lathe 곡선(도톰하게 말린 립·잘록한 받침·치맛단은 지면
  아래로). 물방울 착수점도 수면 높이로 보정. **자리도 남쪽 뜰(0.4,-4.3)로 이전** — 원래 자리는
  자동차 기본 주차(2.5,-1.35)와 정면충돌 + 도로 링에 가장자리가 걸려 있었다(마지막 6종 겹침
  검사에 차/도로가 빠졌던 것).
- **우편함 재조형**: 테이퍼 기둥+발치 둔덕, 통통한 몸통 위 돔 뚜껑, 볼록한 투입구·손잡이.
  깃발 피벗/애니메이션은 그대로.
- **운동 공간 재조형 + 지형 패드**: 매트 더 도톰+큰 라운딩+그라디언트, 바는 그네와 같은 나무
  언어(테이퍼 기둥+둥근 캡+꿀색 봉), 아령은 통통한 코랄/민트 볼. **FLAT_SPOTS에 gym 패드 추가**
  — 구릉이 매트를 뚫고 올라오던 기존 문제 수정 (그네·시소 패드와 같은 원리).
- **도서관 재조형**: 책장 그라디언트+크라운 몰딩, 책은 더 통통하게·살짝 기울여(전부 재질 공유
  → 병합 시 1콜), 방석 더 둥글게. 좌석 좌표 불변.
- **꽃바구니 재조형**: 배불뚝이 Lathe 바구니+말린 테+속 채움판+큼직한 꽃송이·잎.
- **`?cam=px,py,pz[,tx,ty,tz]` 프리뷰 카메라**: `?hour=`처럼 개발용 — 소품 클로즈업을 헤드리스
  스크린샷으로 검증할 때 쓴다.

### Optimized (발열 잡기 🔋 — 같은 그림을 반값에)
- **펫 창 프레임 페이싱**: 데스크톱 펫(vrm.html)의 rAF가 무제한이라 ProMotion 맥북에서
  하루 종일 120fps로 돌던 것을 월드와 같은 게이트로 교체 — 활동 중(포인터·모션 재생·
  말하기·VMC·XR)엔 60fps, 가만히 숨 쉬는 idle은 30fps. 픽셀비도 2x로 캡. 호버 자동 숨김
  중(캔버스 opacity 0)엔 보이지 않는 프레임을 아예 그리지 않는다.
- **월드 입력 idle**: 창이 포커스여도 12초간 입력이 없으면 30fps 구경 모드, 입력 즉시
  60fps 복귀. 저더가 제일 잘 보이는 카메라 조작은 입력 그 자체라 언제나 60fps고, 펫이
  말을 걸어오면(말풍선·토스트) 잠깐 깨워서 대답 모션이 매끄럽게 보인다.
- **그림자 30Hz**: 그림자 맵(2048² PCFSoft)을 매 프레임 굽던 것을 2렌더에 1번으로 —
  캐스터 전부를 다시 그리는 depth 패스 비용이 반값, 소프트 블러 안이라 차이는 안 보인다.
- **바다 법선 해석적으로**: 매 프레임 4,600버텍스 computeVertexNormals(9천 삼각형 순회 +
  면 평균) → 파고 사인의 도함수로 직접 계산. 훨씬 싸고, 근사가 아니라 정확한 값이라
  스페큘러 반짝임이 오히려 매끈해졌다.
- **분수 물방울 GPU화**: 초당 20개 Sprite 생성/폐기 + 방울당 드로우콜(동시 ~24) → 강수
  패턴을 재사용한 Points 하나(드로우콜 1, CPU 0). 버텍스 셰이더가 방울마다 포물선
  (p = v·t − ½g·t²)을 제 수명 주기로 돌린다 — 분포·크기·색 동일.
- **꽃밭 인스턴싱**: 심은 꽃 송이당 메시 2개(+개별 재질) → InstancedMesh 둘(줄기·머리,
  머리 색은 instanceColor). 150송이 만발해도 드로우콜 2.
- **정적 병합**: 프롭 26종(집·울타리·부스·동굴·도서관·그네 프레임…)의 절대 안 움직이는
  부품을 재질 인스턴스별로 병합하고 구름 20로브도 1메시로. 그네 시트·시소 플랭크·우편함
  깃발·피아노 건반·텃밭 칸·눈모자처럼 움직이거나 토글되는 부품은 자동 제외 — 클릭 판정·
  공사모드·계절 틴트 전부 이전과 동일. 결과: **드로우콜 540→430 · 씬 오브젝트 884→731**
  (같은 카메라 기준). 이제 프롭이 늘어도 프레임 비용이 훨씬 완만하게 는다.
- **계측 오버레이**: ⚡ 버튼 더블클릭(또는 `?stats=1`)으로 fps · 드로우콜 · 트라이앵글 ·
  오브젝트 수를 왼쪽 위에 표시 — 다음 최적화의 전후 비교용.

### Added (마지막 6종 — 우편함 📮 · 운동 공간 🧘 · 도서관 📚 · 분수 ⛲ · 꽃심기 🌸 · 반딧불이 ⭐)
- **본섬 확장 (5.2→5.6) + NE 놀이터 섬 확장 (2.2→3.2)**: 밀도가 높아진 본섬에 서쪽 뜰을 새로
  열어 도서관을 앉혔고, 운동 공간은 그네·시소가 있는 NE 놀이터 섬으로 보내 한 존으로
  묶었다 — 이번 배치는 좌표 겹침·섬 이탈 여부를 스크립트로 전수 검사해 확정했다.
- **우편함**: 집 앞길의 빨간 우편함. 클릭해서 편지를 넣으면 병아리·강아지가 함께(서버
  LLM, 둘의 말투가 번갈아 섞인 답장) 편지를 쓰고 4~12분 뒤에야 "배달"된다 — 도착하면
  깃발이 서고 알림이 온다. 편지함은 서버(`config/world_mail.json`)에 남아 다시 읽을 수
  있다.
- **운동 공간**: 요가 매트 둘 + 스트레칭 바 + 아령. 매트 클릭 = 가까운 펫이 와서 스트레칭,
  조종 중 ⌘ = 내 펫이 직접, 한가할 땐 스스로도 한다. **스트레칭(Stretch) 신규 제작 —
  16번째 모션**: 위로 쭉 폈다가 좌우로 기울이고 탈탈 터는 4단계.
- **도서관 코너**: 책장(색색의 책 3단) + 독서 의자 둘(sit-침대) + 독서등. 앉으면 앞에 책이
  펼쳐지고 가끔 골똘히 생각에 잠긴다(💭). 조종 중 ⌘ = 내 펫이 바로 앉아 읽고, 자율
  독서는 2~4분 후 스스로 일어난다.
- **분수**: 자체 돌 둘레+물그릇을 갖춘 독립 분수 (기존 수영 연못과 겹치지 않게 별도 배치).
  물방울이 끊임없이 솟아 떨어지고, 가까이 가면 잔잔한 물소리가 커진다.
- **꽃 심기 챌린지**: 바구니를 클릭해 심기 모드를 켜고 잔디를 클릭하면 그 자리에 꽃 한
  송이 — 100송이를 심으면 축포와 함께 챌린지 달성. 심은 꽃은 서버
  (`config/world_flowers.json`)에 남아 어느 기기에서나 같은 꽃밭을 본다.
- **반딧불이**: 연못가에 초록 불빛 두 무리가 떠다니며 어긋난 위상으로 깜빡인다 — 강수
  Points 셰이더를 그대로 재사용(느린 낙하+큰 스웨이=떠다님)해 발열 비용이 사실상 0이고,
  깊은 밤 + 맑은 하늘에서만 떠오른다.

### Added (본섬 생활 4종 — 텃밭 🥕 · 피아노 🎹 · 사진 게시판 📌 · 별자리 ⭐)
- **텃밭**: 예약돼 있던 북서 뜰에 2×2 나무 밭. 빈 칸 클릭 = 씨앗 심기(당근·토마토·해바라기
  중 랜덤), 자라는 중 클릭 = 물주기(다음 단계까지 절반 단축, 단계당 1회), 다 자라면 클릭 =
  수확(반짝+펫이 좋아함). 성장은 실시간 — 씨앗→새싹 1시간, 새싹→수확 4시간. 상태는 서버
  (`config/world_garden.json`)라 폰·데스크톱이 같은 밭을 본다.
- **피아노**: 서쪽 잔디의 8건반 무지개 미니 피아노. 건반 클릭 = 그 음(건반이 폭 눌리는 애니
  +2겹 오실레이터), 몸통 클릭 = 가까운 펫이 걸어와 춤추며 펜타토닉 즉흥곡 연주, 조종 중
  ⌘ = 내 펫이 직접 연주. 한가할 때 스스로 한 곡 치기도 한다.
- **사진 게시판**: 집마당가 코르크 보드에 최근 스크린샷 6장이 핀으로 꽂힌다 (📷로 찍으면
  즉시 갱신). 클릭하면 라이트박스로 크게 — ◀ ▶로 전체 앨범 넘기기. 새 엔드포인트
  `/api/screenshots_list` + `/screenshots` 정적 마운트.
- **별자리 만들기**: 밤에 전망대를 클릭하면 별 잇기 모드 — 별을 차례로 탭해 잇고 이름을
  붙이면 그 별자리가 이름표와 함께 **매일 밤 하늘에 남는다** (서버
  `config/world_constellations.json`, 별과 함께 뜨고 진다). 이를 위해 별밭을 고정 시드로
  바꿔서(한 번의 배치 변화) 저장된 좌표가 부팅을 넘어 유효하다. Esc 취소.
- 부스러기: 클릭 디스패치가 히트 정보를 넘겨 자식 메쉬(밭 칸·건반)를 판별하고, 텃밭·피아노는
  `<goto=garden|piano>` 태그와 공사 모드 이동을 지원한다.

### Added (모험의 섬 5단계 — 워프 포탈 🌀, 섬 완성)
- **돌링 포탈 한 쌍**: 광장가 ↔ 모험의 섬. 링 안에서 청록→보라 나선이 도는 소용돌이
  셰이더(드로우콜 1)가 일렁이고, 통과하면 양끝에서 반짝이가 터지며 슝— 하는 스윕음이 난다.
- **펫도 지름길을 안다**: 경로 빌더가 걷는 길(다리 경유)과 포탈 경유를 비교해서 확실히
  짧을 때만 포탈 웨이포인트를 심는다 — "보물 파러 가자"라고 하면 광장에서 포탈로 쏙.
  조종 중인 펫은 소용돌이에 들어서면 바로 넘어간다(운전 중 제외, 2.5초 쿨다운).
- 이것으로 모험의 섬 6단계 완성: 언덕·동굴·전망대·비 피신·보물찾기·포탈.

### Added (모험의 섬 4단계 — 보물찾기 ⛏️ + 새 코디 2종)
- **매일의 보물**: 모래밭의 X 세 곳 중 매일 한 곳이 은은하게 반짝인다. 조종 중인 펫을
  데려가 ⌘/✋로 파거나, 채팅으로 "보물 파러 가자"(`<game=treasure>`)라고 시키면 펫이 달려가
  파내고, 15분에 한 번쯤은 펫이 스스로 발굴하러 가기도 한다(하루 한 번).
- **파기(Dig) 모션 신규 제작** — 15번째 모션: 웅크렸다가 좌우 갈퀴질(병아리 = 날개 갈퀴질
  + 부리 콕콕, 강아지 = 몸통 리듬 + 신난 꼬리 + 펄럭 귀), 흙먼지가 튀고, 끝나면 만족스럽게
  몸을 편다. 펫 창 모션 메뉴에서도 쓸 수 있다.
- **보상 = 코디 언락**: 첫 보물은 👒 밀짚모자, 둘째는 🎀 리본(신규 제작 액세서리 2종 —
  산타모자와 같은 시스템), 다 모으면 반짝이는 동전. 잠긴 코디는 메뉴에 🔒 ???로 보이고
  발굴하면 열린다(산타모자는 기존대로 항상 열려 있음). 발굴은 이벤트 로그에 남고 펫이
  자랑하러 오기도 한다.

### Added (모험의 섬 3단계 — 비 피신 ☔, 보류했던 날씨 펫 반응 v2)
- **비를 피하는 펫들**: 비나 뇌우가 내리기 시작하면 한가한 펫들이 동굴 쿠션(자리가 없으면
  집 소파)으로 뛰어가 앉아서 기다리고, 날이 개면 스스로 일어나 나온다. 눈은 예외 — 눈밭
  산책은 낭만이니까. 식사·탑승·조종·듀오 중인 펫은 양보하고(숨바꼭질에서 확립한 선점
  규칙), 이동·착석·기상은 전부 기존 침대 시스템 표준(mountBed/bedExit)을 재사용해서 전용
  코드가 30줄이 안 된다. 피신은 이벤트 로그에 남아 펫의 기억이 된다.

### Added (모험의 섬 1단계 — 동굴 🕳️)
- **아늑한 바위 동굴**: 언덕 남서면 포켓에 고인돌풍 바위 셸이 앉았다 — 두꺼운 셸이 해를
  등지고 스스로 내부에 그림자를 드리워서(별도 렌더 트릭 없이) 낮에도 어둑하고, 호박빛
  랜턴이 불꽃처럼 일렁이며 그 안을 데운다. 러그 위 쿠션 두 개는 앉는 침대로 등록돼 조종
  중 ⌘/✋로 앉을 수 있고, 다음 단계의 비 피신 자리가 된다(shelter 플래그).
- **연동**: 셸 바위별 충돌원(입구만 뚫림), `<goto=cave>` 태그, 위치 스냅샷 "동굴 안",
  숨바꼭질의 대형 엄폐물, 겨울 눈모자. 지형(평탄 패드+언덕 절개)과 한 몸이라 공사 모드
  이동은 불가.

### Added (모험의 섬 0단계 — 네 번째 섬 + 언덕 지형)
- **모험의 섬**: 남동쪽에 위성 최대 크기(반지름 3.5)의 새 섬이 떴다 — 본섬 남동 럼에서 세
  번째 다리로 연결(경로·다리 메쉬·배회 노드 자동 편입). 다리목 가로등, 해안 큰 나무, 새
  프롭 타입 '바위(boulder)' 셋(언덕 크래그·평지·완사면)으로 기본 드레싱. 강수 실린더도
  네 섬을 덮게 반경 12.5→14로 확장.
- **언덕(HILLS) 지형**: terrainHeight에 데이터 기반 고원형 봉우리가 생겼다 — 정상부 35%는
  평평(전망대 자리), 사면은 펫이 그냥 걸어 오르는 완경사. 평탄 패드(FLAT_SPOTS)가 언덕도
  눌러서 남서면에 동굴 포켓(1단계 예정)의 절개 벽이 자연히 생긴다. 계절 시스템(설원·짚빛)
  이 언덕에도 그대로 적용된다.

### Changed (상호작용 문법 통일 — "세상은 클릭, 펫 몸은 ⌘")
- **호버 프롬프트 (Roblox式)**: 마우스를 소품에 올리면 그 자리에서 라벨이 떠서 무엇이 되는지
  알려준다 — "🪙 소원 빌기 · 클릭", "💡 가로등 밝기 50% · 클릭", "🛝 그네 — 조종 중 ⌘/✋로
  타요". 클릭형은 커서도 손가락으로 바뀐다. 어떤 소품이 클릭인지 ⌘인지 외울 필요가 없어졌다.
  (레이캐스트는 초당 ~8회만 — 발열 예산 준수. 터치는 마크처럼 탭=실행이라 라벨 없음)
- **클릭 승격 (심즈式)**: 스위치·패널류는 이제 조종 없이도 클릭 — 라디오(패널), 가로등(밝기
  사이클+토스트), 커피/간식 부스(주문 패널). 부스에서 고르면: 조종 중인 펫이 부스 옆이면 바로
  받고, 아니면 **한가한 펫이 부스까지 걸어가 대신 받아온다**.
- **⌘ 병행 유지 (마크式)**: 기존 ⌘/✋ 경로는 전부 그대로 — 탑승·운전·손잡기는 여전히 펫의
  몸으로, 라디오·부스도 ⌘ 근접 주문이 계속 통한다. 규칙은 한 줄: 만지고 싶으면 클릭,
  태우고 싶으면 ⌘.

### Added (추억의 섬 — 쪼아쪼아나무 💗 · 소원우물 ⛲ · 타임캡슐 🕰️ + 기념비 이사)
- **SW 위성섬이 "추억의 섬"으로**: 반지름 2.0→2.9로 넓히고 중앙에 평탄한 뜰을 깔았다.
  베프 기념비가 광장에서 이 섬으로 이사해(다리 쪽을 바라봄) 다리를 건너 만나러 가는
  우리만의 성지가 됐다. 포옹 포인트는 광장에 그대로.
- **쪼아쪼아 나무**: 사시사철 장미빛 하트잎 캐노피에 하트 열매 다섯 알(겨울엔 눈모자만).
  클릭하면 "쪼아쪼아~" 네 번의 쪼기 소리와 하트가 반짝, 두 펫이 나무 아래 모이면 하트가
  터지며 포옹으로 이어진다(7분 쿨다운).
- **소원 우물**: 지붕 달린 돌우물. 클릭하면 소원 패널 — 소원을 적고 🪙 동전 던지기를 누르면
  금화가 포물선을 그리며 퐁당(물소리+반짝), 소원은 서버(config/world_wishes.json)에 쌓여
  언제든 다시 읽을 수 있다. 펫이 "무슨 소원일까" 궁금해하기도 한다.
- **타임캡슐**: 흙무덤에 반쯤 묻힌 상자. 미래의 우리에게 남길 말과 개봉 날짜를 정해 묻으면
  🔒 D-n으로 잠겨 있다가, 날이 오면 🎁 열어보기로 개봉(축포+기록). 열 때가 된 캡슐이 있으면
  월드가 알려주고 펫이 먼저 들썩인다. 저장은 config/world_capsules.json.
- **연동**: 셋 다 공사 모드로 옮길 수 있고, `<goto=pecktree|well|capsule>` 태그·위치 스냅샷·
  숨기 스팟(나무·우물 뒤)에 등록됐다. 새 엔드포인트 GET/POST `/api/world_wishes`·
  `/api/world_capsules`.

### Added (월드 광장 세트 — 베프 기념비 🗿 + 포옹 포인트 💕, P1 완성)
- **베프 기념비**: 처음부터 예약돼 있던 중앙 광장 북쪽 자리에 섰다 — 돌 받침 두 단 위 비석에
  "🐕🐣 베프 포에버 💕" 각인 판, 꼭대기엔 분홍 하트. 월드를 열면 제일 먼저 보이는 마을의
  중심이다. 숨바꼭질에서 기념비 뒤에 숨을 수도 있다.
- **포옹 포인트**: 기념비 바로 앞(광장 남쪽)에 하트 바닥 + 은은하게 숨쉬는 빛의 링. 두 펫이
  같이 올라서면 하트·반짝이가 터지며 자동 포옹이 시작되고 4음 아르페지오가 울린다(5분
  쿨다운 — 자주 터지면 마법이 아니니까). 주인이 조종해서 데려간 경우엔 조종을 뺏지 않고
  절친이 하트 포즈로 화답한다. 광장은 펫 통행량이 가장 많은 곳이라 우연한 자동 포옹이
  실제로 일어난다.
- **연동**: 둘 다 공사 모드로 옮길 수 있고(포옹 판정·꽃잎 링도 따라감), `<goto=monument|
  hugspot>` 태그와 위치 스냅샷("베프 기념비 근처")에 등록돼 펫이 그 자리를 안다. 광장
  정중앙에 살던 스포츠카는 남동쪽 길가로 기본 주차를 옮겼다(끌어서 재배치 가능).

### Added (월드 그림일기 📔)
- **펫이 쓰는 하루**: 밤 10시가 넘으면 병아리와 강아지가 각자 그날의 그림일기를 쓴다 — 하루
  동안 이벤트 로그에 쌓인 일들(밥·낮잠·그네·드라이브·날씨·숨바꼭질…)만 가지고, 페르소나와
  주인과의 장기기억(world_chat 요약)을 얹어 1인칭 4~6문장 + 마지막 줄 '기분: …'으로. 없던
  일은 지어내지 않는다.
- **노트 패널**: 독의 📔 버튼이 종이 노트 스타일 패널을 연다 — ◀ 날짜 ▶ 넘기기, 🐤/🐶 탭,
  오늘 일기는 ✍️ 버튼으로 먼저 쓰거나 다시 쓸 수 있다. 집 탁자 위에 조그만 일기장 소품도
  올라갔다.
- **보관**: 서버 `config/world_diary.json`에 날짜·펫별 저장(공사 레이아웃과 같은 방식 — 폰/
  데스크톱 공유, localStorage 초기화에도 살아남음). 새 엔드포인트 GET/POST `/api/world_diary`,
  같은 날 재요청은 저장본 반환. 일기 재료가 되도록 이벤트 로그 보관량을 40→120개로 늘렸다.

### Added (월드 숨바꼭질 🙈)
- **한 판의 흐름**: 술래가 광장 가운데서 눈을 가리고 머리 위로 숫자를 세면(10초), 상대는
  술래 반대편 프롭 뒤 — 나무·집·부스·그네·시소·울타리·라디오 — 로 뛰어가 숨는다. 술래는
  숨을 만한 자리들을 가까운 곳부터 순찰하고, "발견"은 거리 + 시야 원뿔 + **시선 판정**을
  모두 통과해야 한다. 시선은 펫이 부딪히는 것과 같은 프롭 충돌원에 대고 샘플링해서, 걸음을
  막는 것이 시야도 막고 공사 모드로 옮긴 배치도 그대로 반영된다. 찾으면 ❗와 함께 달려가
  응원 세리머니, 90초를 버티면 숨은 쪽 승리.
- **주인도 숨는다**: 펫을 조종 중일 때 시작하면 주인이 숨는 쪽 — 술래가 15초 세는 동안
  직접 뛰어가 숨는다. 집 안에 숨으면 술래가 들어와야 보이고(벽 너머로는 안 보임), 들키면
  "들켰다! 😆", 버티면 승리 토스트. 결과에 따라 술래가 먼저 말을 걸어오기도 한다.
- **세 가지 시작**: 펫 우클릭 메뉴의 🙈 숨바꼭질, 채팅 행동 태그 `<game=hideseek>`(펫에게
  "숨바꼭질 하자"라고 하면 됨), 그리고 둘 다 한가한 낮에 아주 가끔 스스로 한 판. 식사·취침·
  탑승·드라이브 중이거나 공사 모드면 시작하지 않고, 도중에 술래를 조종하면 판을 접는다.
  모든 판은 이벤트 로그에 남아 펫의 기억이 된다.

### Added (월드 오로라 — 특별한 밤)
- **오로라 커튼**: 초록→보라로 물드는 셰이더 커튼 두 장이 섬 뒤 밤하늘에 호를 그리며 일렁인다
  (이 월드의 첫 커스텀 셰이더 — 가산 블렌딩이라 별은 그대로 비친다). 맑은 밤에만 보이고,
  구름이 끼거나 해가 뜨면 스르르 사라진다. 기본 카메라에선 수평선 위로 하단이 걸리고, 시점을
  낮춰 하늘을 올려다보면 커튼 전체가 보인다. 발열 예산: 드로우콜 2개, 풀스크린 작업 없음.
- **자동 + 수동**: 자동 모드의 맑은 밤에 ~8분마다 추첨해 가끔(14%) 6~11분짜리 오로라가 뜨고
  (이벤트 로그 기록 + 강아지가 먼저 알려줄 수도), 🌦️ 패널의 오로라 항목으로 언제든 고정할 수
  있다(밤이 되면 나타남). `?weather=aurora` 미리보기. 채팅 스냅샷에도 반영되어 펫이 오로라를
  안다.

### Fixed (월드 날씨 패널 — 창 뚫림)
- **패널이 창 밖으로**: 날씨+계절 항목이 10개가 되면서 위로 자라는 패널이 작은 창에선 천장을
  뚫었다. 열 때마다 버튼 위 공간에 맞춰 최대 높이를 잡고 넘치면 스크롤하며, 행 패딩·글자도
  한 단계 줄였다.

### Added (월드 계절 — 봄벚꽃·여름·가을단풍·겨울눈)
- **사계절 시스템**: 기본은 실제 달력(3~5월 봄 / 6~8월 여름 / 9~11월 가을 / 12~2월 겨울),
  🌦️ 패널 하단의 계절 줄에서 수동 고정도 된다(계절은 모드라서 저장됨; `?season=` 미리보기).
  전환은 2.5초 크로스페이드 — 잎 버텍스 컬러를 통째로 리베이크하고 잔디·데코·바다·하늘을
  함께 갈아입힌다. 여름이 원본 룩.
- **가을**: 나무마다 금빛/주황/빨강 중 하나로 물들고(잎사귀 로브별 밝기 변주), 잔디는 짚빛,
  풀포기는 호박색, 은은한 낙엽이 하늘에서 진다. **겨울**: 잔디 텍스처가 설원으로 바뀌고
  캐노피와 집 지붕에 눈모자·눈이불이 얹히며, 꽃·풀포기는 눈 밑으로 사라지고 조약돌·바다·
  반사광이 차가워진다(접지 그림자도 옅게). 자동 날씨의 강수도 계절을 따라 겨울엔 눈.
- **벚꽃나무 (P1 ③)**: 남쪽 잔디에 대형 벚나무가 새로 심겼다 — 봄이면 분홍으로 만개하고
  나무 주변에 꽃잎이 흩날린다(나무-로컬 파티클이라 공사 모드로 옮겨도 따라간다). 여름엔
  초록, 가을엔 주홍, 겨울엔 창백한 가지색.
- **계절 낮 길이**: 일출·일몰이 계절을 따른다 — 여름 5:20~19:35, 겨울 7:20~17:25. 태양·달
  궤적, 황혼 글로우, 가로등 점등이 전부 이 창에 맞춰 움직인다.
- **펫 연동**: 채팅 스냅샷의 계절이 수동 계절을 반영하고, 주인이 계절을 바꾸면 이벤트
  로그에 남아 펫이 먼저 말을 걸 수도 있다.

### Changed (월드 날씨 — 켜면 맑음)
- **첫 화면은 항상 맑은 하늘**: 월드를 열면 무조건 맑음으로 시작한다 — 첫 자동 강수는 10~25분
  뒤에나 온다. 기존에는 스케줄러 초기화 버그(첫 맑음 에피소드의 만료 시각이 0이라 첫 프레임에
  바로 비로 굴러감)에 더해, 지난 강수 에피소드와 🌦️ 수동 선택이 저장돼 있다가 재시작 때
  복원되면서 켜자마자 비가 오곤 했다.
- **날씨는 세션 한정**: 🌦️ 버튼 선택을 포함해 창을 닫으면 날씨는 리셋되고, 다시 열면 늘
  맑음+자동 모드다. `?weather=` 미리보기는 그대로 동작하고, 예전 저장 키는 부팅 때 정리한다.

### Added (월드 펫 목소리 — 짹짹·멍멍)
- **울음소리 합성**: 빗소리·엔진음처럼 파일 없이 WebAudio 합성(sfxMaster 공유, 350ms 중복
  방지). 채팅 대답(말풍선과 동시)·메뉴의 인사(Wave)·행동 태그 인사에서 운다.
- **자연스러운 v2**: 병아리는 FM 트릴 + 몸통 공명(밴드패스)을 얹은 2~3연음 삐약, 강아지는
  톱니 성대를 두 포먼트('아' 모음 성도)로 울리고 노이즈 숨을 섞은 "왕!". 매번 음높이·길이·
  횟수·간격이 살짝 달라져 반복 티가 안 난다.
- **실녹음 우선 재생**: `static/sounds/voice/{chick|puppy}_{0..2}.(ogg|mp3)`를 넣어두면 자동
  감지해 합성 대신 재생한다(재생마다 피치 ±6% 랜덤). 진짜 소리가 필요하면 CC0 녹음을 그 이름
  으로 떨어뜨리면 끝.

### Fixed (월드 대화 패널 — 기억 초기화 버튼)
- **탭처럼 보이던 파괴 버튼**: 💬 패널 헤더의 "🧹 병아리/강아지"가 펫별 로그 전환 탭처럼 보였지만
  실제로는 기억 전체 초기화 버튼이었다. 하단의 "🧹 기억 초기화:" 줄로 내려 오해를 없앴다.
- **OS confirm() 제거**: 네이티브 확인창은 렌더 루프까지 멈추는 차단 다이얼로그라(월드가 통째로
  얼어붙음) 인페이지 2단 확인으로 교체 — 한 번 누르면 빨갛게 "정말요? 다시 탭"으로 바뀌고 2.5초
  안에 다시 누르면 실행된다.
- **초기화 피드백**: 성공 여부(res.ok)를 확인하고, 로그에 "— 기억이 초기화됐어요 —" 시스템 줄을
  남기며, 이름 생략 시 대화를 이어받던 연속성(responder)도 함께 리셋한다.

### Added (월드 채팅 P3 — 먼저 말 걸고, 둘이 떠들고, 더 놀 줄 아는 펫)
- **선제 대화**: 특별한 순간에 펫이 먼저 말을 건다 — 오랜만에(3시간+) 월드에 돌아왔을 때,
  비/눈이 내리기 시작할 때(주인이 날씨 버튼으로 바꾼 경우 포함), 비 갠 뒤 무지개가 떴을 때
  (수영 좋아하는 병아리 담당), 공사 모드로 배치를 바꾸고 나왔을 때. 8분 쿨다운에 대화·공사
  중이거나 창이 백그라운드면 건너뛰어 호출을 아끼고, 말을 건 계기는 서버 대화 기록에 남아
  펫의 기억으로 이어진다.
- **절친 거들기**: "얘들아"처럼 무리로 부르거나 두 이름을 다 부르면 반드시, 이름 없이 말하면
  가끔(35%) 다른 펫이 첫 펫의 대답을 들은 것처럼 한마디 얹는다 — 첫 말풍선의 타자 연출이
  끝날 즈음 이어받아 사람처럼 티키타카가 된다.
- **행동 태그 2종 추가**: `<swim=pond|sea>` — 기존 물놀이 시스템을 재사용하되 이제 연못
  첨벙/절벽 다이빙을 골라 지시할 수 있다. `<drive=car>` — 차까지 걸어가 올라타고 잠깐 스스로
  몰다가 내리는 자율 드라이브. 차 물리를 stepCar()로 추출해 키보드/조이스틱 조종과 공유하고,
  벽에 막히면 핸들을 반대로 꺾는다. 운전 중인 펫을 조종하면 그대로 수동 운전으로 넘어온다.

### Added (월드 공사 모드 — 동물의 숲식 사물 옮기기)
- **🔨 독 버튼 → 공사 모드**: 사물을 누르면 살짝 떠올라 손가락/마우스를 따라오고, 바닥 링이
  초록/빨강으로 배치 가능 여부(다른 사물과 겹침 · 물 · 섬 밖)를 실시간으로 보여준다. 못 놓는
  곳에 놓으면 원래 자리로 되돌아가고, 빈 땅 드래그는 평소처럼 카메라 회전 — 모바일 터치 동일.
- **상단 툴바**: ↺ 45° 회전 · 원위치 · 전부 원위치 · ✓ 완료(저장). Esc로도 종료. 진입하면 조종을
  풀고 펫을 전부 하차시키며, 공사 중엔 탑승(mountBed)이 잠긴다.
- **이동 대상 13종**: 나무 · 그네 · 시소 · 커피/간식 부스 · 라디오 · 가로등 · 선베드 · 해먹 ·
  밥그릇 · 울타리 · 자동차 · 집. 놓는 순간 충돌 원, 그네/시소/침대 탑승 좌표(bakePropBeds 재굽기),
  블롭 그림자, 가로등 불빛, 집 내부 파생물(가구 콜라이더 · 소파 · 2층 침대 · 실내등)이 함께
  이사한다 — 펫 AI는 라이브 참조라 새 위치로 알아서 찾아가고, 옮긴 사실이 월드 이벤트 로그에
  남아 펫이 대화에서 알아챈다. 집은 이동만(회전축 상수는 로드 시 고정), 연못은 지형 함몰이라 고정.
- **저장·복원**: 배치를 id(타입-순번)→{x,z,rotY}로 서버 `/api/world_layout`
  (config/world_layout.json — 폰·데스크톱 공유) + localStorage 폴백에 저장한다. 시작 시 씬을 짓기
  **전에** PROPS/HOUSE/FLAT_SPOTS에 덮어써서 지형 패드·탑승 좌표·그림자가 전부 새 위치 기준으로
  구워진다(그네·시소·집의 평탄화 패드는 follow 태그로 프롭을 따라감 — 리로드 후 적용).

### Added (월드 채팅 P1·P2 — "아는" 펫, "움직이는" 펫)
- **월드 전용 채팅 세션 (P1)**: 월드 채팅이 메인 UI 에이전트 원격조종(`/ws` 타이핑 → TTS 청크
  엿듣기)에서 전용 백엔드 `/api/world_chat`로 완전히 분리됐다. 펫별 대화 히스토리 + 페르소나
  (병아리 🐤 텐션 높은 호기심 대장 / 강아지 🐶 느긋한 맏형)가 서버에 살고, 대화가 30개를 넘으면
  오래된 절반을 LLM이 350자 요약으로 접어 장기기억화(Generative-Agents-lite). 이름을 안 부르면
  직전 대화 상대가 이어받고, 답장은 말풍선 타자기 연출로 표시(TTS는 나중에). 💬 독 버튼 =
  세션 대화 로그 + 펫별 🧹 기억 초기화.
- **상황 스냅샷 주입 (P1)**: 매 턴 시각·계절·날씨(비/눈/무지개), 두 펫의 위치("그네 근처",
  "집 2층")와 상태(수영/그네/커피 들고 있음/산타모자…), 주인의 조종 여부를 한국어로 요약해
  프롬프트에 넣는다 — Peridot처럼 "인지=텍스트 변환". 여기에 월드 이벤트 로그: 밥때·취침·기상·
  물놀이·그네/시소·커피/간식 주문·드라이브·포옹/공놀이·손잡기·조종·날씨 변화·스크린샷이
  타임스탬프와 함께 기록되고(최근 8개 주입, localStorage 유지) "아까 뭐 했어?"에 진짜로 답한다.
- **행동 태그 + 실행기 (P2)**: 답장에 `<motion=dance>` `<goto=swing>` `<mount=seesaw>`
  `<drink=latte>` `<snack=donut>` `<hat=santa-hat|off>` 태그를 심으면 월드가 화이트리스트
  검증(모션 10종·장소 12곳·탈것 6종·음료 9·간식 9) 후 말풍선에서 지우고 순서대로 실행한다.
  이동은 gotoAsync, 탑승은 mountBed, 음료/간식은 부스까지 걸어가 받기, 포옹/공놀이는 듀오
  안무 재사용 — 기존 안전장치(점유·충돌·다리 경유)가 그대로 적용된다. 새 채팅이나 🎮 조종
  시작은 실행 중인 스크립트를 즉시 무효화하고, 조종/손잡기 중인 펫은 이동류 태그를 양보한다.

### Added (월드 모바일 터치 — 마인크래프트식 조작)
- **탭 = 펫 상호작용**: 터치 기기에서 펫을 탭하면 데스크톱 우클릭과 동일하게 동작한다(앉은 펫
  일으키기 · 자는 펫 깨우기 · 모션/코디/조종 메뉴). 이벤트의 pointerType으로만 분기해서 마우스
  동작(좌클릭=카메라, 우클릭=메뉴)은 터치스크린 노트북에서도 그대로다. 손가락 떨림을 감안해
  터치 탭 판정 반경은 6→13px.
- **핀치 줌**: 두 손가락 벌림/오므림이 휠과 같은 부드러운 줌 타겟(camZoom)을 움직인다 —
  min/max 클램프·글라이드 동일. OrbitControls의 두-손가락 팬은 조종 팔로우캠과 타겟을 두고
  싸우고 섬을 화면 밖으로 밀 수 있어 터치에서만 비활성(touches.TWO=-1); 한 손가락 회전은
  기존 그대로. 두 번째 손가락이 탭으로 오인되지 않게 탭 후보를 무효화하고, iOS가 제스처를
  가로챌 때(pointercancel)도 정리한다.
- **마크식 조종 UI**: 터치 기기에서 조종 중일 때만 좌하단 가상 조이스틱 + 우하단 액션 버튼
  (✕ 해제 44px · ✋ 상호작용 · 🦘 점프 — 독 카메라 버튼과 같은 48px). 조이스틱은
  setPointerCapture로 카메라 드래그와 분리돼 왼엄지 이동 + 오른엄지 시점 회전이 동시에 되고,
  스틱을 70% 넘게 밀면 달리기(Shift 대체). 차 운전은 스틱 전후=가속·후진, 좌우=핸들. 키보드
  Space/Ctrl·⌘/Esc 인라인 블록은 doJump/doInteract/escapeAction 함수로 추출해 버튼과 공유
  (동작 불변 — 차·그네·시소·침대·손잡기·커피·간식·라디오·가로등 우선순위 그대로). 조종 힌트는
  터치에선 좌상단으로 옮기고 키 이름 대신 버튼 아이콘으로 안내한다(✋ 차 타기 등).
- **터치 UI 크기**: 독 버튼 40→48px, 메뉴 행 패딩·폰트 확대(15px), 메뉴 화면 클램프를 하드코딩
  치수 대신 실측 기반으로, 사이드 패널 폭 가드(min(…, 100vw-90px)), 채팅 입력 16px(iOS 포커스
  줌 방지), 하단 UI에 safe-area 여백(홈바 겹침 방지).
- **모바일 성능 기본값**: 터치 기기는 절전(30fps·1.5x 해상도)이 기본 — ⚡ 버튼으로 끄면 그
  선택이 저장된다. 시작 시 applyPixelRatio()를 한 번 호출해, getBattery가 없는 iOS/Safari에서
  저장된 절전 픽셀비가 리사이즈 전까지 적용되지 않던 기존 갭도 수정.
- **모바일 뷰포트 기초**(world.html): viewport 메타(maximum-scale=1·viewport-fit=cover)와
  touch-action:none 등으로 더블탭 줌·당겨서 새로고침이 월드 조작을 가로채지 않는다.
  폰 접속: 설정 networkVisible='global' 후 같은 와이파이에서 http://<PC IP>:3456/world.html

### Added (월드 날씨 — 비·눈·무지개)
- **날씨 시스템**: 실제 시계 기반으로 맑음(10~25분)과 강수(3~8분)가 오간다. 11~2월엔 비 대신
  눈이 내리고, 현재 날씨는 저장돼 창을 다시 열어도 이어진다. 흐림 계수가 낮밤 파이프라인에
  합성돼 하늘이 회색으로 내려앉고(밤엔 밝아지지 않게 감쇠) 안개가 가까워지며 해·달·별이
  가려지고 바다가 잿빛이 되는데, 비 오는 낮에는 가로등이 은은하게 켜진다(아늑함).
  미리보기·테스트: `world.html?weather=rain|snow|clear|rainbow`.
- **강수 렌더링 — 발열 예산 준수**: 비 2000방울·눈 850송이가 각각 드로우콜 1개짜리 포인트
  클라우드다. 버텍스 셰이더가 시간 유니폼 하나로 낙하를 순환시키므로(눈은 사인 드리프트 추가)
  CPU는 프레임당 아무것도 쓰지 않는다 — 게임식 단일 포워드 패스 원칙 그대로. 빗소리는 기존
  물소리처럼 노이즈 합성이라 파일이 없고, 눈은 무음.
- **무지개**: 비가 낮에 그치면 바다 위로 무지개 아치가 3초에 걸쳐 떠올라 75초 머물다 사라진다.

### Added (월드 놀이기구 — 그네·시소)
- **2인 그네 (NE 위성섬)**: A자 프레임에 좌석 두 개가 나란히 매달린 그네를 다리 건너 NE 섬에
  놓았다. 조종 중인 펫을 그네 옆으로 데려가 ⌘(상호작용)를 누르면 올라타고, 한 자리가 차 있으면
  다른 펫은 남은 자리에 앉는다. 좌석은 진자 물리로 앞뒤로 흔들리며(바닥에서 펌핑 + 가벼운 감쇠로
  잔잔한 진폭 유지), 탄 지 10분이면 스스로 내려오고, 배회하던 펫이 가끔 알아서 타러 오기도 한다.
  밤 취침 자리로는 선택되지 않는다.
- **2인 시소 (NE 위성섬)**: 그네 옆에 중앙 받침점에서 기우는 널판 시소를 추가했다. 양 끝 좌석이
  하나의 기울기를 반대로 타서 한쪽이 내려가면 반대쪽이 올라가고 두 펫이 마주본다. 탑승 방식·10분
  하차·가끔 자동 탑승은 그네와 동일하다.
- **재사용 구조**: 두 놀이기구 모두 기존 침대(BEDS) mount 시스템 위에 얹어 ⌘ 상호작용·근접 힌트·
  다리 경유 이동(gotoAsync)·approach→mount→dismount 트윈·자동 하차를 그대로 공유한다(신규 하위
  시스템 없음). 전용 코드는 진자(updateSwings)·시소 틸트(updateSeesaws) 두 함수뿐이고, 배치는
  `world-layout.js`에 데이터 두 줄 + 평탄화 패드로 끝난다.

### Changed (월드 렌더링 — 게임식 전환)
- **Post chain replaced by bake & fake**: the GTAO→bloom→SMAA composer re-rendered the whole
  scene each frame just for normals and pushed ~19 fullscreen half-float passes — the most
  expensive possible shape on Apple's tile-based GPUs, and why the world still heated MacBooks
  after the 1.5x/60fps taming below. The world now draws ONE forward pass per frame, the way
  production games handle a mostly-static scene: contact shading is baked at load as soft blob
  discs under the props, sun/moon/lamp halos are additive glow sprites (golden-hour tint and
  the 💡 slider still apply), AA went back to canvas MSAA (near-free on tile-based GPUs) at
  full retina 2x, and the ACES look is unchanged.
- **Adaptive pacing**: 60fps only while the window is focused and on mains power; 30fps when
  the world sits unfocused beside other work, when ⚡ 절전 is on (now: 30fps + 1.5x pixels,
  persisted), or automatically on battery.

### Fixed (월드 발열)
- **MacBook heat after the quality pass**: the post chain (≈15 fullscreen passes) was running
  at retina 2x AND at 120fps on ProMotion panels — several times the old GPU load. Now: pixel
  ratio capped at 1.5 (SMAA keeps it crisp), GTAO trimmed to 8/6 samples, canvas MSAA dropped
  (redundant behind SMAA), the sim+render loop capped at 60fps, and a ⚡ dock button toggles
  절전 mode (plain forward render, persisted) for hot days.

### Changed (월드 그래픽)
- **Render-quality pass**: the world now draws through a post chain — GTAO contact shading,
  a subtle bloom on the sun/lamps/moon, tone-mapped output and SMAA. Screenshots go through
  the same chain; pixel ratio is capped at 2.
- **CC0 asset-kit pilot (Kenney Nature Kit) — tried and reverted**: the six trees briefly ran
  as kit models with seven kit rocks added, but the angular kit style clashed with the chubby
  pastel world, so trees/rocks are back to the procedural look and the vendored GLBs were
  dropped. The plumbing stays for a better-matching pack later: `world-kit.js` (cached GLB
  prop loader with procedural fallback) plus `variant`/`kitScale` fields on layout entries.
  Pets (병아리/강아지) were never touched.
- **Layout data module**: islands, bridges, the house anchor, flat pads and all prop
  placements moved to `static/js/world-layout.js` — growing the world is now a data edit
  (props accept optional `scale`, `variant`, `kitScale`).

### Changed (Dock·월드 메뉴 배치)
- **Single Dock icon**: the dev Electron binary now sets the party icon on the macOS Dock via
  `app.dock.setIcon` (was the default atom icon), and the desktop launcher applet is marked
  `LSUIElement` so it never occupies the Dock — launching shows exactly one icon.
- **World pet menus no longer cover the pet**: the right-click motion/control menu opens ~80px
  to the right of the click point (clamped to the window edge), and the 먹기/마시기 chooser now
  stacks directly ABOVE the motion menu (same left edge, bottom-anchored) instead of sitting to
  its left over the character.

### Fixed (실행 안정성)
- **Blank-white windows / zombie app on launch**: launching could leave nothing but white
  sheets on screen (empty 펫 월드 window included), and re-clicking the desktop launcher then
  did nothing. Three `main.js` fixes: (1) a crashed renderer (e.g. Chromium's network service
  dying at startup) now auto-reloads its window, max 3 times/min, instead of staying a blank
  sheet; (2) `before-quit` no longer awaits `executeJavaScript` on a dead renderer — that hang
  used to leave a zombie Electron holding the single-instance lock so later launches were
  silently swallowed; (3) the pet-world/VRM windows retry `loadURL` while the backend is still
  coming up instead of failing once with `ERR_CONNECTION_REFUSED` plus an unhandled rejection.

### Changed (클릭·메뉴)
- **Pet menus are right-click only**: clicking a pet with the left button used to open the
  motion/control menu, which clashed with left-drag camera moves. The motion/control menu (and
  wake/stand-up taps) now only respond to right-click; left-click is camera-only. Holding a
  drink/snack, right-clicking the pet opens the motion menu with the 먹기 chooser shown right
  beside it (both close together) instead of replacing it.

### Added
- **Snack booth + two-handed food (🍞 간식 부스)**: a mint-striped snack stand (griddle, display
  snacks, SNACK sign) joins the plaza beside the coffee booth. Ctrl/⌘ opens a 3×3 menu of nine
  snacks — 토스트, 오므라이스, 부리또, 핫도그, 도넛, 붕어빵, 삼각김밥, 츄러스, 컵케이크 — each a
  canvas icon plus a little 3D model. The snack goes in the pet's OTHER paw/wing, so a pet can hold
  a drink and a snack at once (양손 자유도); the puppy grows a matching arm+paw on that side too,
  raycast-anchored to the fur like the drink hand. Right-clicking the pet now lists whatever it's
  holding (마시기 / 먹기); picking food runs a 2~3-bite 우적우적 sequence with a munch sound, the
  snack shrinking per bite until it's gone after ~6 bites (then a happy hop). And drinks/snacks
  ride every motion — dance, cheer, wave all work while holding, cup and snack bobbing along.
  Beds put both hands' items down before climbing in.

### Fixed (클릭·음료 후속 3)
- **Cup rim meets the beak**: while drinking, the cup group's origin (its bottom) was placed at the
  mouth, floating the whole cup above the chick's beak. Each drink now knows its rim height and the
  cup is lowered by it — the rim touches the beak/lips for every cup type (iced/hot/espresso).
- **Drink arm attaches to the body for real**: bounding-box math put the stub arm off the flank on
  the puppy's chunky shape. `giveDrink` now raycasts into the body at cup height to find the actual
  fur surface — the arm anchors just inside that hit point and the cup rests just outside it, so
  the limb is always rooted in the body regardless of shape, and stretches naturally to the mouth
  while drinking.

### Fixed (클릭·음료 후속 2)
- **먹기 popup opens on the pet and stays open**: on macOS the browser's contextmenu event fires on
  mouse-DOWN, so the popup opened and was instantly wiped by the pointer-up handler. All popup
  logic moved into the pointerup raycast: right-click ON the drink-holding driven pet → 먹기
  (background right-clicks show nothing, as requested); the contextmenu handler now only suppresses
  the browser menu. The puppy's drink arm also shrank to a chick-wing-length stub anchored at the
  cup (no more long limb from the shoulder), and the cup rests a touch closer to the body.

### Fixed (클릭·음료 후속)
- **Right-click menus are back**: the recent mouse-button separation accidentally made pet menus
  (조종하기/모션) left-click-only — both buttons open them again like before. Right-clicking empty
  ground while holding a drink is the only thing that shows the 먹기 popup (and drags never do).
- **Puppy cup/arm no longer stuck in the body**: the cup's rest position and the arm's shoulder
  anchor are now computed from the model's measured size (`pet.dims`) instead of fixed numbers, so
  they sit just outside the fur on any body shape.

### Changed (음료 리워크)
- **Drinking looks right now**: the cup's drinking position is no longer a fixed guess — it reads
  the pet's actual mouth node (chick beak / puppy tongue) live every frame and hovers just in front
  of the lips, so the cup never sinks into the body (the puppy especially). The puppy also grows a
  little arm + paw while holding a drink (a fur-toned limb stretched dynamically from the shoulder
  to wherever the cup is). Right-click now opens a small "🥤 먹기" popup instead of sipping
  instantly; picking it runs a 2~3-gulp drinking sequence — cup held to the mouth, head tipping
  rhythmically, 꿀꺽 sound per gulp — and the cup finishes after ~8 gulps total. Left/right mouse
  buttons are now cleanly separated (right never triggers pet clicks or closes menus).

### Added
- **Coffee booth takeout (☕ 커피 부스)**: a striped-awning coffee stand (espresso machine, cup
  stack, COFFEE sign) joins the plaza's west side. Drive a pet up and press Ctrl/⌘ to open a 3×3
  takeout menu of nine drinks — 아메리카노, 아이스 아메리카노, 에스프레소, 카페라떼, 카푸치노,
  초코라떼, 딸기라떼, 녹차라떼, 아이스티 — each drawn as a little canvas icon (clear cups with ice
  and a straw for iced, sleeved paper cups with lids for hot, a saucer demitasse for espresso).
  Picking one puts a matching 3D mini-cup in the pet's paw/wing, parented to the motion wrap so it
  bobs along with walking, running, even swimming. Right-click sips: the cup rises to the mouth
  with a head-tip and a gulp sound, and after four sips the cup is finished with a happy hop.
  Climbing into a bed puts the cup down first, and the world canvas no longer shows the browser
  context menu.

### Fixed
- **조종하기 always works now**: possession used to be refused while a pet was mid-anything
  (meal, dip, bed approach, duo) — which is why it often "didn't take", especially right after
  launch when the meal window or the first random swim kicks in. Taking control now forcibly and
  safely inherits the pet from ANY activity: beds/seats dismount instantly (occupancy cleared),
  a passenger hops out of the car, dips wind down on the next frame, and any director awaiting
  that pet's arrival is resolved so nothing deadlocks — their later cleanup calls no-op against
  the player state (releaseAI now respects 'player').
- **Zoom buttons actually zoom now**: the `camZoom` helper had been deleted along with the old
  camera button panel, so the ＋/－ buttons and keyboard +/- were silently throwing on every press —
  the function is back (steering the same smoothed distance target the wheel uses). Also the sports
  car now spawns in the middle of the plaza (it used to clip into the new bigger house), and it can
  cross the bridges to the satellite islands — bridge decks count as road for the car, lifting it
  over the arch (wheels overhang the narrow planks, by popular demand).

### Added
- **Two-story house + sports car + grass-sound fix (복층집·스포츠카)**: the little cottage became a
  walk-in two-story dollhouse — open front with porch posts so the camera sees inside, wooden floor,
  plastered walls with side windows, a real stair ramp along the right wall up to a loft with a
  railing (the loft edge line doubles as the under-loft partition), and a shingled roof + chimney.
  The interior is part of the world heightfield (`houseFloorY`), so pets and the player walk in,
  climb the stairs and stand on the loft; a new max-step rule stops anyone hopping ledges. Furnished
  with a sofa (Ctrl/⌘ = 앉기 — new `sit` seat mode, tap the sitter to get them up), low table with a
  reading lamp (tied to the lamp glow system), rug, bookshelf, and a loft bed that joins the
  night-time sleep pool. Outside on the driveway sits a red sports car: Ctrl/⌘ boards it (a held or
  nearby friend hops into the passenger seat 👥), arrow keys drive at 3× walking speed with
  speed-scaled steering, a synth engine that revs with the throttle, spinning wheels, and a moving
  collider so wanderers steer around it; Ctrl/⌘ hops out (Esc too). Cars are main-island only — the
  bridges are too narrow. Also fixed: grass right next to a road no longer clicks like pavement
  (footsteps now use a stricter on-road test), and house floors sound like wood.
- **Footstep & water sounds (발소리·물소리)**: the world now sounds like somewhere. Footsteps fire
  in sync with the waddle gait (a footfall per half period of the sin(t×8) leg swing) and pick the
  right sound for the ground — grass on the meadow, concrete on the roads/plaza, wood planks on
  the bridges — from the bundled Kenney "Impact Sounds" CC0 set (5 variants each, random
  pitch/volume so nothing machine-guns). Swimming plays a soft synthesized water lap loop plus a
  swish per stroke, splashes got a real splash sound, and landing a jump thumps. The driven pet is
  loudest; AI pets fade with camera distance. Personal sounds (e.g. game rips for private use) go
  in `static/sounds/custom/` with a manifest.json — that folder is git-ignored so they never reach
  the public repo, and any missing set falls back to the defaults (or a synth burst if files fail).
- **Run mode + hand-holding (달리기·손잡기)**: Shift toggles the driven pet between 걷기 and 달리기
  (2× walk speed; the hint header flips to 🏃 달리는 중 and shows the toggle). Walk up to the friend
  and press Ctrl/⌘ to take its hand — it parks in a dedicated `held` state and walks, runs and even
  swims side-by-side at your pace, staying on the side you grabbed, leaning gently into you with
  little 💕 hearts drifting up now and then. On narrow ground (bridge decks) it tucks into single
  file behind you; press the key again (or Esc) to let go, and mid-water releases snap the friend
  safely back onto land (shared `snapToLand` helper now also backs possession release). Held pets
  are excluded from duo partnering, menu motions, meals and auto-sleep; grabbing hand-offs cleanly
  when you switch which pet you drive. Interaction priority: climb-out → hand → bed → radio → lamp.
- **Radio, screenshots, follow-cam + zoom fixes (라디오·스샷·팔로우캠)**: a little radio prop sits at
  the plaza edge — drive a pet up and press Ctrl/⌘ to open a scrollable playlist of whatever audio
  files you drop into `static/music/` (new `/api/radio_list` backend endpoint; picking a track
  loops it, ⏹ stops, ✕/Esc closes). A 📷 button (top of the right-side dock) renders a fresh frame
  and saves it through the new `/api/save_screenshot` endpoint into the `screenshots/` folder,
  with a white flash + toast naming the file (both folders are git-ignored). While driving a pet
  the camera now follows it — the orbit target glides after the pet and the camera slides along,
  preserving your angle/zoom, with drag/wheel still usable mid-follow. The zoom buttons were
  rebuilt: moved above the chat-bar row (bottom:70, z-index 95) so nothing swallows their clicks,
  bigger steps and faster hold-glide, and keyboard +/− (numpad too) now zooms as well. Daytime
  pastel toned down another notch (environment 0.15, exposure 1.0).
- **Archipelago: two bridge-connected satellite islands (군도·다리)**: the world becomes three
  islands — the main one plus a NE (r2.2) and SW (r2.0) satellite, each built by the same
  grass/cliff generator (resolution scales with radius) with its own foam rings, a tree and a
  bridgehead lamp, otherwise left as open ground for future features. Wooden arched bridges
  (stepped planks, posts, rails) connect them; bridge decks count as ground everywhere — terrain
  height, blocking, particle floors, the player's support/fall logic — so anyone can walk (or
  arch-climb from a swim onto any island's rim). Pets route cross-island trips through the right
  bridge via a waypoint system used by both wandering and the approach walks (beds/meals/duos), and
  bridge nodes joined the wander destinations. Swimming leash widened to reach the satellites.
- **Lamp brightness moved onto the lamps + zoom buttons (가로등 상호작용·줌 버튼)**: the 💡 slider is
  gone from the window — walk a possessed pet up to any streetlamp and press Ctrl/⌘ to cycle its
  brightness (0→25→50→75→100%, shown in the hint, persisted). The bottom-right corner now hosts
  ＋/－ zoom buttons instead (tap = one step, hold = smooth glide — same eased zoom the wheel
  drives), for setups where scrolling is awkward. Daytime look was also toned down a notch
  (environment 0.4→0.25, exposure 1.12→1.06) — the extra brightness had crept in with the texture
  pass's IBL, not just the daylight.
- **Island expansion + road network (섬 확장·도로)**: the island grows from R3.2 to R5.2 (~2.6×
  the area) to make room for the expansion wishlist. The layout is zoned — NE house yard (+bowl),
  E rest area (sunbed), S hammock nook, SW pond, W fence lawn, four trees spread around — with the
  center kept as an open stone-tiled plaza (auto-leveled flat spot; hug point & monument land
  there next) and the N/NW meadows reserved for future features. A stone-dust loop road at
  mid-radius plus four spokes out of the plaza are terrain-hugging ribbon meshes with their own
  painted path/plaza-tile textures; six streetlamps now line the loop. Pets draw ~45% of their
  wander destinations from the road network (plaza/loop/spoke nodes) so movement between zones
  actually follows the paths; decorations (scaled up to ~380 tufts / 75 flowers / 46 pebbles)
  avoid the roads. Ocean inner radius/foam scale with the island; camera starts further out
  (zoom cap 15), shadow coverage widened, terrain grid densified to 34×96.
- **Animal Crossing-style texture pass (동숲 텍스처)**: the world graduates from flat colors to
  hand-painted-feel materials — all generated as tiling canvas textures at load, no asset files.
  The meadow wears the iconic staggered-triangle grass pattern (planar-mapped UVs on the terrain
  grid, near-white vertex tints for sunny/mossy patches), the cliff shows horizontal dirt strata
  with speckle, trunks/fence/hammock posts/door get wood grain, the house walls plaster and the
  roof scalloped shingles, the hammock becomes a striped awning and the sunbed towel striped pink,
  the pond rim sandy speckle. Tree crowns bake a vertical shade gradient into vertex colors (dark
  underside — the classic AC foliage read). Everything moves to MeshStandardMaterial with a subtle
  RoomEnvironment IBL (environmentIntensity 0.4) so materials get a soft studio sheen; emissive
  clouds/lamp globes stay Lambert. Pet models are untouched (they only pick up the gentle ambient).
- **AI pool days + adjustable streetlamps (물놀이·가로등)**: wandering pets now fancy a dip of
  their own — every few minutes (daytime only, cooldown) an idle pet walks to the pond edge or the
  cliff rim, wades in with a splash, cruises a few waypoints with the full swim stroke, then wades
  out (pond) or climbs the cliff back up (sea) — so both pets, player included, can end up swimming
  together. Dipping pets are excluded from duo partnering; sleep or possession ends a dip
  instantly. And the night got friendlier: four rim streetlamps (metal pole + glowing globe +
  warm point light) fade up through dusk, with a 💡 brightness slider (bottom-right, persisted
  across sessions) scaling both the light and the globe glow; moon/ambient night levels were also
  nudged up.
- **Translucent water + real swim strokes (물 투명도·수영 모션)**: the sea (opacity 0.85) and pond
  (0.68) are now glassy — the submerged cliff, the sandy pond basin and the swimmer's paddling legs
  all show through the surface. Swimming drops the land waddle for a dedicated procedural stroke,
  applied as a world-side overlay after the shared entity update (pet windows untouched): deep
  alternating leg kicks, rowing wing sweeps for the chick / trailing ears and a rudder-wag tail for
  the puppy, a stroke-synced body roll and bob with the head held out of the water, full strokes
  while moving vs. gentle treading when idle, and a little droplet wake kicked up behind while
  paddling. Blinking carries through; menu motions still take precedence.
- **Swimming + ⌘ interaction key (수영)**: the interaction key now accepts the Mac Command key
  alongside Ctrl everywhere (tuck-in, climb-out, hints show "Ctrl/⌘"). A possessed pet can now
  swim: wade into the pond or walk right off the cliff rim — it falls, lands with a puff of 3D
  water droplets, and switches to swim mode, floating half-submerged with a wave bob and a forward
  paddling lean (the waddle reads as a dog-paddle). Swimming is slower than trotting, Space does a
  splash-hop, and a leash keeps the swimmer from vanishing into the fog. Near the cliff the hint
  offers "Ctrl/⌘ 섬으로 올라가기" — an arced climb back onto the rim. Only the player-driven pet
  swims (wander AI still treats water as blocked), and releasing possession mid-swim returns the
  pet to solid ground so the AI never strands at sea. Splash droplets die on the sea surface
  beyond the rim (not the phantom terrain height).

### Changed (밥때 tuning)
- **Meals: 30-minute windows, random spots, huggable diners, 3D food**: the serving window shrank
  to 30 minutes; each pet now picks a random free spot around the bowl instead of a fixed side
  (retrying against colliders and the other diner's spot); and hugging an eating pet is allowed
  again — the meal is abandoned gracefully mid-bite (hijack-safe: the meal never stomps the hug
  and only releases the AI if it still owns the pet). Eating FX in the world are no longer emoji:
  each pet gets a real 3D ground prop (scattered golden grain patch for the chick, a little blue
  kibble bowl for the puppy) that is hidden when dining at the real bowl, and the nibble particles
  are tiny 3D morsels that pop from the mouth, arc under gravity and land in the grass.

### Added
- **Meal times at the bowl (밥때)**: at 8시·12시·18시 the pets trot over to the food bowl, take
  their own spot each (chick on one side, puppy on the other), turn to face the bowl and eat two
  helpings with the shared Eat motion before wandering off. Each serving window lasts 45 minutes
  and every pet eats once per serving (tracked per day); meals are skipped while possessed,
  sleeping, in bed or mid-choreography, and the duo partner filter now also excludes busy/goto pets
  so nobody gets pulled into a hug mid-bite. Preview with `?hour=12`.
- **Night auto-sleep + sunbed & hammock (자동취침·잠자리)**: two new furniture props join the island —
  a mint sunbed with a reclined backrest, towel and pillow, and a hammock with leaning posts and a
  sagging, edge-curled cloth. At 밤 10시 (22:00) the pets head to bed on their own — the chick takes
  the hammock (it rocks gently while she sleeps), the puppy the sunbed — walking to an approach
  point, hopping on with a little arc, tipping onto their backs (the lean lives on the mover so the
  shared sleep animation keeps breathing on top) and dozing until 6시, when they hop off with the
  sunrise. Waking them at night (click, chat, motion) makes them dismount; they drowsily climb back
  in ~90 seconds later. While driving a pet (🎮), walking near a free bed shows a "Ctrl 눕기" hint —
  pressing Ctrl sends it to climb in and lie down (possession hands back to the AI as it walks).
  Beds are blocking props, excluded from duo partnering, and `?hour=23` previews bedtime instantly.
- **Rippling ocean around the island (바다)**: the floating island now sits over an animated sea. A
  polar-grid ocean mesh (geometric ring spacing — dense where you look, sparse toward the horizon)
  runs four layered directional sine waves on its vertices every frame with recomputed normals, so
  the swells genuinely catch the sun/moon glints (Phong specular highlights that sweep with the
  day/night light). Wave amplitude fades toward the foggy horizon to avoid far shimmer, and the
  cliff now casts its shadow onto the water at low sun. Two foam rings lap against the cliff base,
  swelling outward and fading half a phase apart, with a gentle bob. Sea color follows the clock —
  pastel cyan by day, deep navy at night, a warm tint at golden hour — and the foam dims to a pale
  blue after dark.
- **World day/night cycle (밤낮)**: the world now follows the real clock — the sun rises at 6시 and
  sets at 18시, when the moon takes over the same east→west arc until morning; both slide along the
  sky as time passes (refreshed ~2×/min). The sky gradient blends between day pastels, a deep navy
  starfield night (240 stars fade in after dark), and a golden dawn/dusk glow that peaks exactly at
  6시/18시; fog and background follow the horizon color. The one shadow light plays the warm sun by
  day and a cool blue moon by night (hemisphere fill, cloud tint and cloud glow follow too), so
  shadows sweep across the island through the day. Preview any hour by opening
  `world.html?hour=21.5` in a browser.
- **코디 accessory system + santa hat**: pets can now wear outfit items. The shared entity module
  gains a `GLB_ACCESSORIES` registry + `setGlbPetAccessory(pet, id)` — items are built procedurally
  (no external assets), anchored to the normalized head-top via the new `pet.dims`, and parented to
  the motion wrap so every motion (nod, spin, sleep droop…) carries them. First item: 🎅 산타모자
  (white torus brim, red cone with a floppy tilted tip, pompom riding the tip). Wear/remove toggles
  live in all three pet menus — main pet, friend, and the world click menu (labels flip to "벗기"
  while worn). New items = one builder + one list entry.
- **World visual upgrade (퍼피레드 감성)**: the diorama got a pastel virtual-world glow-up.
  *Sky* — a gradient dome (zenith blue → warm pink horizon, painted on an inside-out sphere) with
  four puffy multi-lobe clouds drifting slowly around the island. *Terrain* — the flat disc became a
  gently rolling meadow: a 26×72 polar grid displaced by a `terrainHeight` function with two-tone
  vertex-color grass patches, flat pads auto-leveled under the house/pond, and a lathed faceted dirt
  cliff tapering to a rounded tip; `world.groundHeightAt` now returns the same function, so pets,
  props, the select ring and the catch ball all follow the hills. *Props* — trees grew fluffy
  sphere crowns (berries on the big one), the house got rounded walls, eaves, a chimney, a knobbed
  door, a framed window and a doorstep (RoundedBoxGeometry), the bowl gained a rim + kibbles, the
  fence turned to capped round posts, and a new pond (blocking) with sand rim, lily pad and stones
  joined the scene. *Set dressing* — ~170 instanced grass tufts, 34 color-varied flowers and 22
  pebbles scattered on unblocked ground. *Rendering* — ACES filmic tone mapping, warm sun, soft
  blurred shadows with normal-bias tuned for the curved terrain.
- **Camera buttons removed — mouse-only camera**: with drag-orbit damped and wheel zoom smoothed,
  the 📷 toggle and its button panel became redundant and are gone. Camera is now fully mouse-driven:
  drag = orbit, right-drag/two-finger = pan, wheel = smooth glide zoom (wheel deltas are normalized
  across devices — pixel/line/page modes — so trackpads and notched mice feel the same).
- **Camera panel folded into a 📷 toggle + silky zoom**: the camera buttons now live behind a single
  📷 button in the bottom-right (click to expand/collapse; the toggle glows blue while open). Mouse
  wheel zoom no longer uses OrbitControls' per-tick dolly steps — the wheel (and the ＋/－ buttons)
  steer a target distance the camera glides toward each frame (exponential ease-out), and drag
  damping/rotate speed were retuned (0.05 / 0.85), so zooming and orbiting feel smooth instead of
  notchy.
- **World camera buttons + pet keyboard control (카메라 조작·조종)**: a bottom-right button panel
  drives the camera — zoom in/out, a pan D-pad (island-clamped so the view can't drift off into the
  sky), and rotate/tilt angle buttons; tapping steps once, holding glides continuously. A pet's
  click-menu gains a pinned "🎮 조종하기" entry: the chosen pet parks its AI in a dedicated `player`
  state (excluded from duo partnering; mid-duo pets can't be grabbed), shows a golden select ring
  and a bottom-left hint, and is driven with the arrow keys (camera-relative, ↑ = away from you,
  brisker than wander pace, prop/rim collisions respected) plus Space to hop (simple gravity on the
  mover's Y so motion bobs stack cleanly). Esc or the menu releases it back to wandering; picking
  hug/play while possessed hands the pet back to its AI first. Typing in the chat bar never moves
  the pet. This is the first piece of the ③→② third-person track: the keyboard controller simply
  replaces the wander controller, exactly as the architecture planned.
- **World chat (채팅 이식)**: a bottom chat bar in the world window talks through the same backend as
  the pet windows — `/ws` (`set_user_input` → `trigger_send_message`) drives the main-UI agent, and
  the reply's `/ws/vrm` TTS chunks are re-sequenced and played in order with their text shown in a
  speech bubble anchored above the responder's head (silence chunks respected; omni streams show
  text only). The pet you name answers (병아리/삐약/chick · 강아지/멍멍/댕댕/puppy), defaulting to
  the chick; it ponders with the Think motion while the agent generates, stands still while
  speaking, and does a happy hop when the reply finishes. The world only consumes reply chunks for
  conversations it started, so chats typed in the main UI / pet windows don't echo into the world
  (the reverse still overlaps: with a pet window open, a world-initiated reply sounds in both).
  Enter respects Korean IME composition; the world window allows autoplay audio.
- **World motions + click interactions (모션 이식)**: clicking a pet in the world opens the same
  data-driven motion menu as the pet windows (clicking a sleeping pet wakes it; short unmoved press =
  click, otherwise it's an orbit drag). All 12 shared motions play per-entity; the emoji/💤💭/파이팅/
  food FX re-anchor to each pet's projected screen position and scale with its on-screen size
  (pet-window percentages are mapped so left:50/top:70 = "at the feet"). Hug and Play are
  re-choreographed in-scene with no window IPC: the two pets walk to meeting/catch spots via the
  shared steering (arrive-anyway guards prevent deadlocks), face each other, then play their synced
  halves — the catch ball is a real 3D sphere arcing between their "hands" 4 tosses with
  throw/catch/finish cues, and wander resumes afterward. Arriving pets sometimes do a happy/think
  flourish; duo directors are serialized by a `duoBusy` guard and menu input is ignored mid-choreography.
- **Pets move into the world + wander AI (입주/배회)**: the chick (0.4u) and puppy (0.5u) now load into
  the world scene as two independent entities of the shared module, greet with a wave, then live on a
  Sims-style loop — idle a few seconds, pick a reachable spot (`world.isBlocked` circle-collider +
  rim checks), turn along the shortest arc, waddle over with the existing walk animation, repeat.
  Each pet rides in a "mover" group carrying world position/heading while its wrap stays
  motion-local, so all 12 shared motions play unchanged on top; the wander controller is
  deliberately swappable (keyboard control in the 3rd-person phase, LLM planning later).
- **Pet world stage (무대)**: the world window now shows a floating grass island (grass disc + tapered
  dirt base) with primitive props — two trees, a house, a food bowl and a fence — driven by a data
  list (type/position/collider radius) so a low-poly asset kit can replace the builders later. An
  orbit camera (drag to circle, wheel to zoom, capped above the horizon) replaces the fixed view, and
  the `world.groundHeightAt(x,z)` / `world.isBlocked(x,z)` interface is in place — pets will only
  sense the ground through it, which is the swap point for heightmap (3rd-person) or voxel (sandbox)
  phases.
- **Pet world window (월드) skeleton**: the tray menu gains a "월드 열기" item (ko/en/zh) that opens a
  single normal, resizable window (re-opening focuses it) rendering the new `world.html`/`world.js`
  three.js scene — sky, hemisphere + shadow-casting sun light and the render loop. The diorama stage,
  pets, wandering and interactions land in the following world-mode steps. Desktop pet windows are
  untouched and can run alongside. Render loop pauses automatically while the window is hidden
  (Electron background throttling stays on).

### Changed
- **GLB pet code extracted into a shared entity module**: the pet loader, node discovery and all 12
  procedural motions moved from `vrm.js` into `static/js/glb-pet-entity.js` as per-entity functions
  (`createGlbPetEntity(url, {targetHeight, parent})` / `updateGlbPetEntity(pet, delta)` /
  `disposeGlbPetEntity`, plus the exported `GLB_MOTIONS` list and emoji particle helpers), so the
  upcoming world mode can run two pets in one scene. Model scale is now a `targetHeight` parameter
  (the pet window still derives it from the window height) and the 💤💭/파이팅/food overlays are
  per-entity hooks the window wires up. Step 1 of the world-mode plan — no pet-window behavior change.

### Added
- **GLB pet play motion (two pets)**: "놀이 (Play)" is a coordinated game of catch — the main process
  sets the two pet windows a catch-distance apart and tosses a ball (its own transparent window, a
  CSS-drawn sphere so it can't render as an emoji/charset artifact) back and forth ~4× in an arc, cueing
  each pet to throw/catch in sync (`vrm-play` / `vrm-play-start` / `vrm-play-cue`). The chick throws and
  catches with its wings, the puppy with its paws + a tail wag; the last catcher does a happy hop. Plays
  a solo bounce when alone. Generalizes the two-pet layer into shared `findDuoPartner` / `tweenBounds` /
  `duoFormation` helpers (Hug refactored onto them) for Heart to reuse.
- **Tray "summon desktop pet" item**: the menu-bar tray dropdown now has a "데스크탑 펫 소환" entry
  (between Show Window and Quit) that re-shows the pet if it is hidden or creates it if there is none
  (localized for ko/en/zh).
- **GLB pet hug motion (two pets)**: "포옹 (Hug)" is the first coordinated two-pet motion. Picking it
  asks the main process (new `vrm-hug` IPC) to slide the two pet windows together (approach → hold →
  part, via `setBounds` tweens), then signal both renderers (`vrm-hug-play`) to play their hug half in
  sync — the chick wraps its wings, the puppy reaches in on its paws and wags, both lean toward each
  other with 💕 hearts rising between them. Wander is blocked during the hug; plays a solo air-hug if
  there is no partner. Builds a reusable two-pet coordination layer (window pairing + synced IPC +
  role-based half-animation) that Play/Heart will reuse.
- **GLB pet eat motion**: "먹기 (Eat)" plays a ~3.2s head-down feeding loop (lean in → eat cycles → look
  up satisfied with an `outBack` pop). The chick does quick sharp ground pecks (head taps, beak opens,
  wings flick) over scattered grain; the puppy buries its head in a bowl with fast nibbles, tongue laps,
  a happy tail wag and ears flopped forward. A ground food prop (🌾 grain / 🥣 bowl, `setEat` toggle) and
  crumb/✨/❤️ particles sell it. Captures the `beak`/`tongue` nodes for the first time.
- **GLB pet celebrate motion**: "축하 (Celebrate)" does one big burst — an anticipation crouch, a leap — an anticipation crouch, a leap
  with a full spin, then a settle, with a confetti burst at the peak (new `spawnFloatEmojiBurst()`
  particle helper: many emoji fly out, fall under gravity, and fade).
- **GLB pet cheer motion**: "응원 (Cheer)" roots for you with rhythmic up-pumps, a bouncy beat and a
  forward lean while throwing ✊💪 emoji, and shows a "파이팅!" shout above the head. The shout has no
  bubble and picks a fresh random color each time the motion plays.
- **GLB pet motion menu**: a control-panel button (main pet, `fa-person-running`) opens a dropdown
  of on-demand motions to play. The list is data-driven from `GLB_MOTIONS` and `playGlbMotion(id)`
  is the play hook; Walk and Idle are default states and are intentionally excluded.
- **GLB pet sleep motion**: picking "수면 (Sleep)" from the motion menu puts the pet to sleep —
  eyes shut, slow deep breathing, head drooped with a gentle doze-bob, lazy tail, and a floating
  💤 above the head. It's a state, not a one-shot: the pet wakes when clicked or when it starts
  walking.
- **GLB pet dance motion**: "춤 (Dance)" does a beat-synced groove — on-beat bounce, side-to-side
  sway with a twist, limbs moving to the rhythm, and floating 🎵🎶 music notes (eases in/out at the
  start/end). Added a reusable `spawnFloatEmoji()` particle helper for future effect motions.
- **Motion dropdown scrolls**: the motion menu (main pet and friends) now shows ~3 items and scrolls
  for the rest (`max-height` + `overflow-y`), so the growing list stays compact.
- **GLB pet think motion**: "생각 (Think)" tilts the head side to side (pondering) with the chick
  scratching its head with a wing / the puppy lifting a paw to its chin, plus a 💭 thought bubble.
  Applies animation principles — anticipation (a still beat first), follow-through (settle with an
  `outBack` overshoot), and overlapping action (ears/tail lag behind the head). Added reusable
  easing helpers (`Ease.inOutSine/inOutQuad/outBack`).
- **GLB pet happy motion**: "기쁨 (Happy)" in the motion menu plays an excited reaction — the chick
  flaps both wings fast with bouncy hops, the puppy spins a full turn while hopping and wagging its
  tail hard (~1.8s one-shot, then back to idle).
- **Motion button on summoned friends**: friends now get the motion dropdown too (below the close
  button, revealed on hover), so you can play motions on a friend — previously they had only a close
  button.
- **GLB pet wave (greeting) motion**: the pet waves hello when it appears / is summoned, and from
  the motion menu ("인사"). It's the first timed one-shot (driven by `glbPet.action`, ~2.4s, then
  back to idle): plants the screen-left foot, leans, and waves the screen-right limb — the chick
  flaps a wing forward, the puppy waves a paw and wags its tail. (Left/right are picked by on-screen
  world position so the wrap's 180° flip doesn't mirror them.)
- **Auto-sleep**: the pet dozes off on its own after 10 min of no system-wide input (away), or
  after just 2 min at night (23:00–07:00), using Electron `powerMonitor.getSystemIdleTime`. It
  wakes instantly when input reaches the pet window, and within ~2s when you return from another
  app. A manual sleep (from the menu) is left alone — it only ends on a click. The pet won't wander
  while asleep.
- **GLB pet idle motion**: when not walking, chick/puppy now feel alive — subtle breathing bob and
  a slow sway (continuous), plus occasional eye blinks, a gentle head bow/nod, and chick wing
  flutter / puppy ear twitch. Each "occasional" action fires on its own randomized timer with a
  smooth ease-in/out pulse (not metronomic). Idle motions cross-fade out as the walk fades in;
  blinking continues while walking. All procedural on the separate GLB nodes (no rig).
- **Summon friend**: a button in the desktop pet's control panel (below the text-input button)
  spawns a second character beside the current one, loading the next model in the list as the
  friend (e.g. Chick → Puppy). New windows are staggered so they sit side by side instead of
  stacking. The friend window carries its model via `?model=...&friend=1`. Summoned friends are
  stationary, have no control panel (but the whole window is draggable), and don't grab the
  text-input hotkey. (GLB pet display size also reduced.)
- **GLB pet models** (non-VRM): the model picker now lists `.glb`/`.gltf` files from the model
  folder. Added two custom Blender characters — **Chick** and **Puppy** (extracted from
  `chick_and_puppy_cute.blend` into separate models). A lightweight loader (`loadGlbPet`) scales,
  grounds, and centers the model and skips the VRM pipeline. Since these have no humanoid rig or
  morphs, lip-sync/expressions are unavailable, but the speech bubble and wandering work.
  - **Procedural "waddle"** (`updateGlbPet`): no skeleton needed — the separate foot nodes swing
    in alternation, plus a body bob, side-to-side lean, and (puppy) tail wag, driven during idle
    wandering. Idle shows a gentle breathing bob.
- **Global memory** (`memorySettings.globalMemory`): a manual, always-injected note that is
  added as a system message on every request, regardless of the selected character or whether
  memory is on. Injected in both `generate_stream_response` and `generate_complete_response`,
  outside the character block. `{{user}}` is substituted; sub-agent requests are excluded.
- **VRM pet text input**: a bubble input box toggled by a configurable global hotkey
  (`VRMConfig.textInputHotkey`, default F13) that works even when the main window is hidden.
- **VRM show/hide global hotkeys** (`showPetHotkey` default F14 / `hidePetHotkey` default F15):
  summon or hide the desktop character even while the main window is hidden.
- **Idle remarks** (`VRMConfig.idleTalkEnabled`): the character spontaneously shows a random
  preset line in its speech bubble when idle. Configurable line list and interval (with jitter).
  Suppressed during chat/speech and while the bubble input box is open.
- **Idle wandering** (`VRMConfig.wanderEnabled`): the pet window occasionally slides to a nearby
  spot on screen when idle (plays a walk motion if one exists), clamped within the display work
  area. Configurable interval and range; suppressed during chat/speech/input.
- **Prompt caching for Claude via litellm** (`customAnthropic` path): an ephemeral
  `cache_control` breakpoint is attached to the system prefix so repeated requests read it at
  ~0.1x cost. Only the litellm path is touched; OpenAI and other providers are unaffected.

### Changed
- **Pet drag interaction is now move + rotate** (for the main pet and friends): a **left-drag** on
  the character moves the whole window across the desktop (window + character together, the standard
  desktop-pet behavior), and a **right-drag** rotates the character (rotation moved off the left
  button so the two coexist; the canvas right-click context menu is suppressed).
- **Pet windows shrunk to reduce the transparent area that blocks clicks**: capped to 280×240 (from
  540×960), roughly the character's on-screen footprint plus margin. Because the character's
  on-screen size is normalized to window height, the smaller window mostly trims empty margin rather
  than the character. To fit the short window, the main pet's control panel now wraps into columns
  (`flex-wrap`), and the friend's close (X) button moved to the top-right corner.
- **Summoned friends now behave like the main pet**: they wander on their own (previously
  stationary), and use the same drag interaction. The friend has no full control panel — just a
  close (X) button that appears on hover (top-right corner).
- **Scene lighting softened & widened**: directional key light intensity lowered (Math.PI → 2.0,
  then split to 1.4 key + 0.6 opposite-side fill light for ~30% broader coverage at the same total
  brightness), key direction nudged slightly lower (y 3 → 2.5), and ambient fill raised (0.1 →
  0.55) to reduce harsh contrast. Affects all models (VRM + GLB).
- **Context management** switched from rule-based selective pruning to a pure **sliding window**
  (keep all system messages + the most recent `max_rounds` rounds). The previous method is kept
  commented out for reference. Default `max_rounds=0` (no limit) is unchanged.
- **UI fully Korean-localized** via the `t()` locale method (ko-KR / en-US / zh-CN), including
  previously hardcoded/under-translated strings.
- **LLM-facing system prompts translated to English** (permission modes, VRM expression/motion
  tags, TTS voice/silence tags, the A2UI spec, character/memory injection, reasoner prompts),
  preserving all tags and placeholders.
- **Comments translated from Chinese to English**: 1,027 Python comments + 3,097 JS comments
  (4,124 total). Comment-only — code and string literals untouched.

### Removed
- China-only integrations and their dependencies (QQ/Feishu/WeChat/WeCom/DingTalk bots, bilibili
  live, modelscope, brotli, qrcode), without breaking core functionality. Bot support trimmed to
  Discord / Slack / Telegram.

### Fixed
- **Tray & native context menus showed Chinese**: the menu labels in `main.js` only had `zh-CN`
  and `en-US` locales, so when the renderer reported `ko-KR` the lookup failed and the menus fell
  back to the initial Chinese (the `zh-CN` default). Added a `ko-KR` locale block (tray "창 보이기/
  종료" plus the right-click menu: 잘라내기/복사/붙여넣기/이미지 저장/…) and set the default
  language to `ko-KR`.
- **Summoned friend could not be dragged upward** (could go left/right/down only). Root cause: the
  friend used the default 540×960 window, taller than the macOS work area, so it was pinned with
  its top at the menu bar (macOS clamps any visible window's top to `workArea.y`) — no room to move
  up. Friend windows are now capped to fit the work area with margin, leaving headroom.
- **Pet got clipped while being dragged**: macOS throttled the renderer during an OS window drag,
  freezing the transparent canvas mid-frame. Set `backgroundThrottling: false` on pet windows.
- **Main vs friend characters rendered at different on-screen sizes**: the same model looks bigger
  in a taller window (fixed camera FOV). GLB pet scale is now normalized to the window height, so
  the main pet and a shorter friend window show the character at the same on-screen size.
- **Tall dialogs could not be closed**: long dialogs (e.g. the "add behavior" dialog) grew past
  the viewport and their absolutely-positioned close (X) button scrolled out of view. Dialogs are
  now capped at 90vh with a scrollable body and pinned header/footer, so the X and footer buttons
  stay reachable. Applies to all non-fullscreen dialogs.

### Security
- CORS `allow_credentials=False` (was wildcard origins + credentials).
- SSRF: `sanitize_proxy_url` / `is_private_ip` now reject private/loopback/link-local/cloud-metadata
  targets (e.g. 169.254.169.254) instead of logging only.
